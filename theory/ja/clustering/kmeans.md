# k-means クラスタリング（Lloyd's アルゴリズム）

## 概要

k-means は、データを $k$ 個のクラスタに分割する最も基本的なクラスタリング手法。
各クラスタは「重心（centroid）」で代表され、各点は最も近い重心のクラスタに割り当てられる。
Tunny Dashboard では目的関数空間・変数空間・その複合空間を対象として実行できる。

---

## アルゴリズム

### 目的関数（最小化）

クラスタ内二乗和（WCSS: Within-Cluster Sum of Squares）を最小化する：

$$
\text{WCSS} = \sum_{k=1}^{K} \sum_{x_i \in C_k} \| x_i - \mu_k \|^2
$$

ここで $\mu_k$ はクラスタ $C_k$ の重心。

### 処理フロー

1. **初期化** — 選択した戦略（k-means++ または Deterministic）で初期重心を選択
2. **割り当て（Assignment）** — 各点を最近傍の重心クラスタに割り当て
3. **更新（Update）** — 各クラスタ内点の平均を新しい重心とする
4. **収束判定** — いずれの点のラベルも変化しなければ終了（最大 300 イテレーション）

### 初期化：2 種類の戦略

Lloyd's アルゴリズム本体（割り当て・更新・収束）は共通であり、初期重心の選び方だけが異なる。
どちらも固定シードで再現可能。

#### k-means++（デフォルト）

Arthur & Vassilvitskii（2007）が提案した初期化手法。
ランダム初期化と比べて局所最適解に陥りにくく、収束後の WCSS が平均的に低くなる。

**理論的背景**

ランダムに初期重心を選ぶと、複数の重心が同じクラスタ内に集中してしまう「偏り」が生じる。
k-means++ はこれを防ぐため、既存の重心から遠い点ほど次の重心に選ばれやすくなるよう確率を設計する。

$$
p(x_i) = \frac{D(x_i)^2}{\sum_j D(x_j)^2}
$$

$D(x_i)$ は点 $x_i$ から最近傍の既存重心までのユークリッド距離。
距離の**二乗**に比例させることで、近い点の確率を強く抑制し、遠い点を優先的に選ぶ。

**近似保証**

初期化のランダム性による最悪ケースを排除でき、期待 WCSS が最適解の $O(\log k)$ 倍以内に収まることが証明されている：

$$
\mathbb{E}[\text{WCSS}] \leq 8(\ln k + 2) \cdot \text{WCSS}_{\text{opt}}
$$

**本実装の詳細**

1. 最初の重心として $\lfloor N/2 \rfloor$ 番目の点を固定選択する
2. 各点 $x_i$ から最近傍既存重心への $D(x_i)^2$ を計算する
3. $p_i = D(x_i)^2 / \sum_j D(x_j)^2$ に従い次の重心を確率的にサンプリングする
4. $k$ 個揃うまで 2–3 を繰り返す

乱数生成には外部クレートを使わず **xorshift64 PRNG** を実装。
シードを $n$ と $k$ の組み合わせから導出することで、同じデータ・同じ設定では常に同一の結果が得られる。

#### Deterministic（決定論的スプレッド）

乱数を使わず、累積距離のしきい値で重心を等間隔に「スプレッド」させる手法。

**考え方**

全点の距離総和を「残り選択数 + 1」で割ったしきい値 $\theta$ を設定し、累積和が $\theta$ を超えた最初の点を選ぶ。
これにより、データ空間を等分割するように重心が選ばれる。

**本実装の詳細**

1. 最初の重心として $\lfloor N/2 \rfloor$ 番目の点を固定選択する
2. 各点 $x_i$ から最近傍既存重心までの距離 $d_i$ を計算する
3. しきい値 $\theta = \sum_i d_i \,/\, (\text{残り選択数} + 1)$ を設定する
4. データを先頭から走査し、累積和が $\theta$ に達した最初の点を次の重心とする
5. $k$ 個揃うまで 2–4 を繰り返す

**k-means++ との違い**

k-means++ が「確率的に遠い点を選ぶ」のに対し、こちらは「距離空間を均等分割する点を確定的に選ぶ」。
乱数を一切使わないため完全な決定論的再現性が得られる一方、データの偏りによっては初期点が局所的に集中することがある。

**比較まとめ**

|                | k-means++                | Deterministic            |
| -------------- | ------------------------ | ------------------------ |
| 選択方法       | D² 比例確率サンプリング  | 累積距離しきい値         |
| 乱数           | xorshift64（固定シード） | 不使用                   |
| 再現性         | 同シードで同一結果       | 常に同一結果             |
| 理論保証       | $O(\log k)$ 近似         | なし                     |
| 局所最適リスク | 低い                     | やや高い                 |
| 推奨場面       | 通常の用途（デフォルト） | 完全な決定論が必要な場合 |

### 割り当てステップの計算

各点 $x_i$ を最小二乗距離のクラスタに割り当てる：

$$
\text{label}(x_i) = \arg\min_{k} \| x_i - \mu_k \|^2
$$

二乗距離（平方根を取らない）で比較するため、比較コストが低い。

### 更新ステップの計算

各クラスタ $C_k$ の新しい重心：

$$
\mu_k^{\text{new}} = \frac{1}{|C_k|} \sum_{x_i \in C_k} x_i
$$

点が割り当てられていない空クラスタは、前ステップの重心をそのまま維持する。

---

## 収束保証と終了条件

| 条件               | 説明                                             |
| ------------------ | ------------------------------------------------ |
| ラベル変化なし     | いずれの点のクラスタも変化しなければ収束とみなす |
| 最大イテレーション | 収束しない場合は 300 回で強制終了                |
| WCSS の計算        | 収束時または強制終了時に最終 WCSS を算出する     |

Lloyd's アルゴリズムは WCSS が単調減少することが保証されているが、**大域的最適解**への収束は保証されない（局所最適に陥る可能性がある）。初期重心の選択がこの影響を左右する。

---

## 実装パラメータ

| パラメータ | 値                   | 意味                         |
| ---------- | -------------------- | ---------------------------- |
| `max_iter` | 300                  | 最大イテレーション数         |
| `sq_dist`  | $\sum (a_j - b_j)^2$ | 距離指標（二乗ユークリッド） |
| 空クラスタ | 前重心を維持         | 空クラスタ防止フォールバック |

---

## 特性と限界

### 強み

- 実装がシンプルで計算が高速
- 解釈が直感的（各点が最近傍の重心クラスタに属する）
- WCSS で解の品質を定量評価できる

### 弱み

- クラスタ数 $k$ を事前に指定する必要がある（エルボー法で自動推定可能）
- 凸状・球形のクラスタ形状を前提とする（三日月形などは苦手）
- 外れ値に感受性が高い（重心が外れ値に引っ張られる）
- 局所最適解に収束する可能性がある

---

## 入力空間の選択

Tunny Dashboard では 3 種類の入力空間を選択できる：

| 設定            | 使用するフィーチャー  | 適した用途                   |
| --------------- | --------------------- | ---------------------------- |
| Objective Space | 目的関数値のみ        | 性能空間でのクラスタ分析     |
| Variable Space  | パラメータ値のみ      | 設計変数空間でのクラスタ分析 |
| Combined        | 目的関数 + パラメータ | 両空間を統合した分析         |
