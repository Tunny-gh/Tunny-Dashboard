# Surface Plot

The surface plot visualizes the response surface of an objective function over two selected parameters using a surrogate model.

## Features

- Select two parameter axes to display the response surface.
- Choose between heatmap and contour display modes.
- Surrogate model interpolation generates a smooth response surface.
- R² score indicates how well the surrogate model fits the data.
