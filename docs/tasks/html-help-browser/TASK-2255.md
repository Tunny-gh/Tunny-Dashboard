# TASK-2255: layout.rs 言語切替メニュー追加

**タスクID**: TASK-2255
**タスクタイプ**: TDD
**推定工数**: 4h
**フェーズ**: Phase 3 - アプリ統合
**信頼性レベル**: 🟡 *ヒアリング結果・設計文書より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/html-help-browser/requirements.md) - REQ-030, REQ-031
- **設計文書**: [📐 architecture.md](../../design/html-help-browser/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/html-help-browser/dataflow.md) - フロー3: 言語切替

## タスク概要

`egui-app/src/ui/layout.rs` に言語切替メニューを追加する。設計文書によれば「⋯」メニュー内に `show_language_menu()` を実装し、`AppState.help_language` を切り替えられるようにする。

## 依存タスク

- **前提タスク**: [TASK-2254: AppState.help_language フィールド追加](TASK-2254.md)
- **後続タスク**: [TASK-2256: grid_canvas.rs 変更＋旧ファイル削除](TASK-2256.md)

## 完了条件

- [x] `layout.rs` に言語切替UIが実装されている
- [x] 「English」選択で `AppState.help_language` が `HelpLanguage::En` に設定される
- [x] 「日本語」選択で `AppState.help_language` が `HelpLanguage::Ja` に設定される
- [x] 現在選択中の言語がハイライト表示される
- [x] `cargo test` が通る

---

## 実装詳細

### 1. show_language_menu() 関数実装 🟡

**信頼性**: 🟡 *設計文書より（具体的な配置場所は既存 layout.rs を確認して決定）*

`layout.rs` に `show_language_menu()` 関数を追加する。`AppState.help_language` を参照・変更できるよう `&mut AppState` を引数に取る。

```rust
pub fn show_language_menu(ui: &mut egui::Ui, app_state: &mut AppState) {
    ui.menu_button("🌐 Help Language", |ui| {
        let current = app_state.help_language;
        if ui.selectable_label(current == HelpLanguage::En, "English").clicked() {
            app_state.help_language = HelpLanguage::En;
            ui.close_menu();
        }
        if ui.selectable_label(current == HelpLanguage::Ja, "日本語").clicked() {
            app_state.help_language = HelpLanguage::Ja;
            ui.close_menu();
        }
    });
}
```

**実装ファイル**: `egui-app/src/ui/layout.rs`

### 2. 既存の「⋯」メニューへの組み込み 🟡

**信頼性**: 🟡 *設計文書（「⋯」メニュー内配置）より・既存 layout.rs の構造を確認して配置*

既存の `layout.rs` に存在するメニューやサイドバーの適切な位置に `show_language_menu()` の呼び出しを追加する。具体的な配置は既存 `layout.rs` の実装を確認して決定する。

**実装ファイル**: `egui-app/src/ui/layout.rs`（呼び出し箇所）

### 3. 旧 show_help_modal 呼び出しの削除 🔵

**信頼性**: 🔵 *要件定義 REQ-020・設計文書より*

`layout.rs` に `show_help_modal()` への呼び出しが残っている場合は削除する（または TASK-2256 で対処）。

**実装ファイル**: `egui-app/src/ui/layout.rs`

---

## 単体テスト要件

### テストケース1: 言語切替で AppState が更新される 🟡

**信頼性**: 🟡 *標準的なUI状態テストから妥当な推測*

egui のコンテキストなしでロジック部分のみテスト可能な場合:

**Given**: `help_language = HelpLanguage::En` の `AppState`  
**When**: 日本語切替の処理を実行する  
**Then**: `help_language` が `HelpLanguage::Ja` に変わる

注: egui の描画テストは難しいため、ロジックの単体テストを優先する。実際の表示確認は TASK-2257 の手動テストで行う。

**テストファイル**: `egui-app/src/ui/layout.rs` の `#[cfg(test)]` セクション

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2255` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 具体的な配置位置は `egui-app/src/ui/layout.rs` の現在の実装を確認して決定すること
- egui のUI関数はほとんどテスト不可のため、単体テストは状態変更ロジックのみ対象とする
- `show_help_modal` の呼び出し削除は TASK-2256 と連携して実施する

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 2 | 0 | 3 |
| 単体テスト | 0 | 1 | 0 | 1 |

- **総項目数**: 4項目
- 🔵 **青信号**: 1項目 (25%)
- 🟡 **黄信号**: 3項目 (75%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ⚠️ 要改善 — 配置位置は既存 layout.rs 確認後に決定。egui UI テストは統合確認（TASK-2257）で補完する
