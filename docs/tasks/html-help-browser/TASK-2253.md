# TASK-2253: help_launcher.rs 新規実装（一時ファイル書き出し＋ブラウザ起動）

**タスクID**: TASK-2253
**タスクタイプ**: TDD
**推定工数**: 4h
**フェーズ**: Phase 2 - Rustランタイム実装
**信頼性レベル**: 🔵 *設計文書・要件定義より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/html-help-browser/requirements.md) - REQ-020〜023, EDGE-001, EDGE-002
- **設計文書**: [📐 architecture.md](../../design/html-help-browser/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/html-help-browser/dataflow.md)

## タスク概要

`egui-app/src/ui/help/help_launcher.rs` を新規作成する。`open_help(item, lang)` 関数を実装し、埋め込みHTMLを一時ファイルに書き出してOSデフォルトブラウザで開く。失敗時は `Err(String)` を返す。

## 依存タスク

- **前提タスク**: [TASK-2252: help_content.rs HTML参照ベース再実装](TASK-2252.md)
- **後続タスク**: [TASK-2256: grid_canvas.rs 変更＋旧ファイル削除](TASK-2256.md)

## 完了条件

- [x] `help_launcher.rs` が新規作成されている
- [x] `open_help(item: &PanelItem, lang: HelpLanguage) -> Result<(), String>` が実装されている
- [x] 一時ファイルのパスが `temp_dir() / "tunny-help-{widget_name}-{lang_code}.html"` となっている
- [x] `open::that(&path)` でブラウザが起動する
- [x] ファイル書き込み失敗時に `Err` が返る
- [x] ブラウザ起動失敗時に `Err` が返る
- [x] 単体テストがすべて通る

---

## 実装詳細

### 1. open_help() 関数実装 🔵

**信頼性**: 🔵 *設計文書 interfaces.rs・データフロー dataflow.md より*

```rust
use crate::state::layout_state::PanelItem;
use crate::ui::help::help_content::get_help_html;
use crate::ui::help::help_types::HelpLanguage;
use std::path::PathBuf;

pub fn open_help(item: &PanelItem, lang: HelpLanguage) -> Result<(), String> {
    let html = get_help_html(item, lang);
    let path = build_temp_path(item, lang);
    write_html_to_temp(&path, html)?;
    open_in_browser(&path)
}
```

**実装ファイル**: `egui-app/src/ui/help/help_launcher.rs`（新規）

### 2. 一時ファイルパス生成 🔵

**信頼性**: 🔵 *要件定義 REQ-023・設計文書より*

ファイル名はウィジェット名と言語コードを含め、同一ヘルプの連続クリックで同じファイルを上書きする（複数タブ防止）。

```rust
fn build_temp_path(item: &PanelItem, lang: HelpLanguage) -> PathBuf {
    let widget_name = get_widget_name_for_temp(item);
    let lang_code = lang.code();
    std::env::temp_dir().join(format!("tunny-help-{widget_name}-{lang_code}.html"))
}
```

ウィジェット名は ASCII 英小文字＋ハイフンのサニタイズ済み文字列を使用する（パストラバーサル防止）。

**実装ファイル**: `egui-app/src/ui/help/help_launcher.rs`

### 3. HTMLファイル書き出し 🔵

**信頼性**: 🔵 *要件定義 REQ-022・データフロー dataflow.md より*

```rust
fn write_html_to_temp(path: &PathBuf, html: &str) -> Result<(), String> {
    std::fs::write(path, html.as_bytes())
        .map_err(|e| format!("一時ファイルの書き出しに失敗: {e}"))
}
```

**実装ファイル**: `egui-app/src/ui/help/help_launcher.rs`

### 4. ブラウザ起動 🔵

**信頼性**: 🔵 *要件定義 REQ-021・設計文書より*

```rust
fn open_in_browser(path: &PathBuf) -> Result<(), String> {
    open::that(path).map_err(|e| format!("ブラウザの起動に失敗: {e}"))
}
```

`open` クレートは既存 Cargo.toml に含まれている（追加不要）。

**実装ファイル**: `egui-app/src/ui/help/help_launcher.rs`

---

## 単体テスト要件

### テストケース1: 一時ファイルパスが正しく生成される 🔵

**信頼性**: 🔵 *要件定義 REQ-023 より*

**Given**: `PanelItem::TrialTable` と `HelpLanguage::En`  
**When**: `build_temp_path()` を呼び出す  
**Then**: パスが `tunny-help-trial-table-en.html` で終わる

### テストケース2: 一時ファイルパスが JA で正しい 🔵

**信頼性**: 🔵 *要件定義 REQ-023 より*

**Given**: `PanelItem::TrialTable` と `HelpLanguage::Ja`  
**When**: `build_temp_path()` を呼び出す  
**Then**: パスが `tunny-help-trial-table-ja.html` で終わる

### テストケース3: write_html_to_temp が実際にファイルを作成する 🟡

**信頼性**: 🟡 *標準的なファイルI/Oテストから妥当な推測*

**Given**: 有効な一時ディレクトリパスとHTML文字列  
**When**: `write_html_to_temp()` を呼び出す  
**Then**: ファイルが作成され、内容が一致する（`tempfile` クレートを使ったテスト用一時パスを使用）

### テストケース4: open_help がブラウザなし環境でも Err を返す 🟡

**信頼性**: 🟡 *EDGE-001 から妥当な推測（CI環境でのテスト）*

**Given**: ブラウザが存在しない環境（CI環境）  
**When**: `open_help()` を呼び出す  
**Then**: `Err(String)` が返る（パニックしない）  

注: このテストは `#[ignore]` でマークしてCI環境では明示的に除外するか、`open::that` をモック可能な設計にする。

**テストファイル**: `egui-app/src/ui/help/help_launcher.rs` の `#[cfg(test)]` セクション

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2253` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `open` クレートは `egui-app/Cargo.toml` の既存依存に含まれているため追加不要
- 一時ファイルのウィジェット名には ASCII英小文字とハイフンのみ使用（パストラバーサル防止）
- `std::fs::write` は同名ファイルを上書きするため、同一ヘルプの連続クリックで複数タブは開かない
- ブラウザ起動テストは統合テスト（TASK-2257）で手動確認する

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 4 | 0 | 0 | 4 |
| 単体テスト | 2 | 2 | 0 | 4 |

- **総項目数**: 8項目
- 🔵 **青信号**: 6項目 (75%)
- 🟡 **黄信号**: 2項目 (25%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
