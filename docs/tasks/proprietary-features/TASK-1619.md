# TASK-1619: wasmLoader.ts computeTopsisバインド追加・テスト ✅ **完了** (TDD開発完了 - 10テストケース全通過)

**タスクID**: TASK-1619
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - TypeScript型宣言・WasmLoader（TOPSIS）
**信頼性レベル**: 🔵 *architecture.md・既存wasmLoader.tsパターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/mode-frontier-features/architecture.md)
- **型定義**: [📝 interfaces.ts](../../design/mode-frontier-features/interfaces.ts)

## タスク概要

`frontend/src/wasm/wasmLoader.ts` の `WasmModule` インターフェースに `computeTopsis` プロパティを追加し、`_initialize()` でバインドを追加する。既存の `computeHvHistory` バインドと同パターンで実装する。vitest による単体テストも追加する。

## 依存タスク

- **前提タスク**: [TASK-1618: tunny_core.d.ts更新](TASK-1618.md)
- **後続タスク**: [TASK-1621: mcdmStore.ts作成](TASK-1621.md)

## 完了条件

- [x] `WasmLoader` クラスに `computeTopsis` メソッドが追加されている
- [x] `_initialize()` に `computeTopsis` バインドが追加されている
- [x] `wasmLoader.test.ts` に `computeTopsis` のテストケースが追加されている（TC-1619-01）
- [x] `npm test` がパスする（10件全通過）

---

## 実装詳細

### 1. WasmModule インターフェース追加 🔵

**信頼性**: 🔵 *既存wasmLoader.tsの `computeHvHistory` 追加パターンより*

**実装ファイル**: `frontend/src/wasm/wasmLoader.ts`

```typescript
// WasmModule インターフェースに追加
computeTopsis: (
  values: Float64Array,
  nTrials: number,
  nObjectives: number,
  weights: Float64Array,
  isMinimize: boolean[]
) => TopsisWasmResult
```

### 2. _initialize() バインド追加 🔵

**信頼性**: 🔵 *既存 _initialize() パターンより*

```typescript
// _initialize() 内に追加
this.computeTopsis = (values, nTrials, nObjectives, weights, isMinimize) =>
  wasmComputeTopsis(values, nTrials, nObjectives, weights, isMinimize) as TopsisWasmResult;
```

---

## 単体テスト要件

### テストケース1: computeTopsis呼び出し確認 🔵

**信頼性**: 🔵 *既存wasmLoader.test.tsパターンより*

**テストファイル**: `frontend/src/wasm/wasmLoader.test.ts`

WASMモジュールモックを使用して `computeTopsis` が正しい引数で呼ばれることを確認する。

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements TASK-1619`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
