# TASK-1625: `tunny_core.d.ts` Pdp2dResult・computePdp2d追加 ✅ **完了** (DIRECT実装完了 - TSコンパイルOK)

**タスクID**: TASK-1625
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 4 - 3D曲面プロット WASM層
**信頼性レベル**: 🔵 *wasm-api.md compute_pdp_2d()定義・interfaces.ts Pdp2dResult型より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **型定義設計**: [📝 interfaces.ts](../../design/mode-frontier-features/interfaces.ts)
- **WASM API仕様**: [📐 wasm-api.md](../../design/tunny-dashboard/wasm-api.md)

## タスク概要

TASK-1624で追加した `wasm_compute_pdp_2d()` に対応するTypeScript型宣言を `frontend/src/wasm/pkg/tunny_core.d.ts` に追加する。`Pdp2dResult` インターフェースと `computePdp2d()` 関数シグネチャを手動で追加する。

TASK-1618（TopsisWasmResult追加）と同じ手順で実施。

## 依存タスク

- **前提タスク**: [TASK-1624: lib.rs wasm_compute_pdp_2d追加](TASK-1624.md)
- **後続タスク**: [TASK-1626: wasmLoader.ts computePdp2dバインド追加](TASK-1626.md)

## 完了条件

- [x] `frontend/src/wasm/wasmLoader.ts` に `Pdp2dWasmResult` インターフェースが追加されている
- [x] `computePdp2d()` メソッドがWasmLoaderに追加されている
- [x] TypeScriptコンパイルエラーが発生しない

---

## 実装詳細

### 1. Pdp2dResult インターフェース追加 🔵

**信頼性**: 🔵 *wasm-api.md compute_pdp_2d()・interfaces.ts Pdp2dResult型より*

**実装ファイル**: `frontend/src/wasm/pkg/tunny_core.d.ts`

```typescript
export interface Pdp2dResult {
  grid1: Float64Array;      // param1のグリッド点（n_grid点）
  grid2: Float64Array;      // param2のグリッド点（n_grid点）
  heatmap: Float64Array;    // [n_grid × n_grid] flattened Z値（行major）
  durationMs: number;       // 計算時間（ms）
}
```

### 2. computePdp2d 関数シグネチャ追加 🔵

**信頼性**: 🔵 *wasm-api.md compute_pdp_2d()シグネチャより*

```typescript
/** 2変数PDP（3D曲面グリッドデータ）計算 */
export function computePdp2d(
  param1: string,
  param2: string,
  objective: string,
  n_grid: number
): Pdp2dResult;
```

---

## 信頼性レベルサマリー

- 🔵 青信号: 2件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質
