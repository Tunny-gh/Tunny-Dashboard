# TASK-1620: types/index.ts ChartId拡張・TopsisRankingResult・McdmStore型追加 ✅ **完了** (DIRECT実装完了 - TSコンパイルOK)

**タスクID**: TASK-1620
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 2 - TypeScript型宣言・WasmLoader（TOPSIS）
**信頼性レベル**: 🔵 *architecture.md・interfaces.ts設計・既存types/index.tsパターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/mode-frontier-features/architecture.md)
- **型定義**: [📝 interfaces.ts](../../design/mode-frontier-features/interfaces.ts)

## タスク概要

`frontend/src/types/index.ts` に以下を追加する：
1. `ChartId` union型に `'surface3d'` と `'topsis-ranking'` を追加
2. `TopsisRankingResult` インターフェースを追加
3. `McdmStore` インターフェースを追加
4. `SurrogateModelType` 型を追加（Phase 4以降で使用）

## 依存タスク

- **前提タスク**: [TASK-1619: wasmLoader.ts バインド](TASK-1619.md)
- **後続タスク**: [TASK-1621: mcdmStore.ts作成](TASK-1621.md), [TASK-1627: analysisStore.ts拡張](TASK-1627.md)

## 完了条件

- [x] `ChartId` に `'surface3d'` と `'topsis-ranking'` が追加されている
- [x] `TopsisRankingResult` インターフェースが定義されている
- [x] `McdmStore` インターフェースが定義されている
- [x] `SurrogateModelType` 型が定義されている
- [x] TypeScript コンパイルエラーがない

---

## 実装詳細

### 1. ChartId 拡張 🔵

**信頼性**: 🔵 *ユーザヒアリング・既存ChartId定義パターンより*

**実装ファイル**: `frontend/src/types/index.ts`

```typescript
// 既存の ChartId union に追加
export type ChartId =
  | 'pareto-front'
  | 'parallel-coords'
  | 'scatter-matrix'
  | 'importance'
  | 'history'
  | 'hypervolume'
  | 'objective-pair-matrix'
  | 'pdp'
  | 'sensitivity-heatmap'
  | 'cluster-view'
  | 'umap'
  | 'slice'
  | 'edf'
  | 'contour'
  | 'surface3d'       // 新規: 3D応答曲面プロット
  | 'topsis-ranking'  // 新規: TOPSISランキング
```

### 2. TopsisRankingResult 型追加 🔵

**信頼性**: 🔵 *architecture.md・interfaces.ts設計より*

```typescript
export interface TopsisRankingResult {
  scores: number[]
  rankedIndices: number[]
  positiveIdeal: number[]
  negativeIdeal: number[]
  durationMs: number
}
```

### 3. McdmStore インターフェース追加 🔵

**信頼性**: 🔵 *architecture.md McdmStore設計より*

```typescript
export interface McdmStore {
  topsisWeights: number[]
  topsisResult: TopsisRankingResult | null
  isComputing: boolean
  topN: number
  topsisError: string | null
  setTopsisWeights: (weights: number[]) => void
  computeTopsis: () => Promise<void>
  setTopN: (n: number) => void
  reset: () => void
}
```

### 4. SurrogateModelType 追加 🔵

**信頼性**: 🔵 *architecture.md・ユーザヒアリングより*

```typescript
export type SurrogateModelType = 'ridge' | 'random_forest' | 'kriging'
```

---

## 実装手順（DIRECT）

1. `/tsumiki:direct-setup` - types/index.ts 追記
2. `/tsumiki:direct-verify` - TypeScript コンパイル・テスト確認

---

## 注意事項

- `ChartId` の変更は `FreeLayoutCanvas.tsx` の switch 文・`ChartCatalogPanel` に影響する（後続タスクで対応）
- Tailwind CSS は使用しない（既存制約）

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
