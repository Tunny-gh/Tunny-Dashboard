# TASK-1604: clusterStore 実装 + テスト

## メタデータ

| 項目 | 値 |
|------|-----|
| タスクID | TASK-1604 |
| タスクタイプ | TDD |
| 推定工数 | 6時間 |
| フェーズ | Phase 2 - Zustand ストア |
| 信頼性レベル | 🔵 (REQ-VE-040〜045 / 設計ヒアリング subscribe パターンより) |
| 前提タスク | TASK-1602 |
| 後続タスク | TASK-1606, TASK-1607, TASK-1608, TASK-1609 |

## タスク概要

`frontend/src/stores/clusterStore.ts` を新設し、PCA・k-means・cluster stats の順次実行、Elbow 法 k 推定、Study 変更時リセットを実装する。Vitest でストアの単体テストを作成する。

## 完了条件

- [ ] `clusterStore.ts` が `useClusterStore` フックとして export されている
- [ ] 8つの状態（pcaProjections, clusterLabels, clusterStats, elbowResult, isRunning, clusterError, clusterSpace, k）が実装されている
- [ ] `runClustering(space, k)` が PCA → k-means → clusterStats の順で WASM を呼び出す
- [ ] `estimateK(space)` が PCA → estimateKElbow の順で WASM を呼び出す
- [ ] `useStudyStore.subscribe` で Study 変更時に全クラスタ状態が null にリセットされる
- [ ] `clusterStore.test.ts` でストアの主要アクションがテストされている
- [ ] 既存テスト159件が全て合格している

## 実装詳細

### 1. clusterStore.ts 作成 🔵
*REQ-VE-040〜045 / architecture.md runClustering実行順序より*

実装ファイル: `frontend/src/stores/clusterStore.ts`

```typescript
import { create } from 'zustand';
import { subscribeWithSelector } from 'zustand/middleware';
import { useStudyStore } from './studyStore';
import { WasmLoader } from '../wasm/wasmLoader';
import type { ClusterStatsWasmResult, ElbowWasmResult } from '../wasm/pkg/tunny_core';

type ClusterSpace = 'param' | 'objective' | 'all';

interface ClusterState {
  pcaProjections: number[][] | null;
  clusterLabels: number[] | null;
  clusterStats: ClusterStatsWasmResult | null;
  elbowResult: ElbowWasmResult | null;
  isRunning: boolean;
  clusterError: string | null;
  clusterSpace: ClusterSpace;
  k: number;
  runClustering: (space: ClusterSpace, k: number) => Promise<void>;
  estimateK: (space: ClusterSpace) => Promise<void>;
}

export const useClusterStore = create<ClusterState>()(
  subscribeWithSelector((set) => ({
    pcaProjections: null,
    clusterLabels: null,
    clusterStats: null,
    elbowResult: null,
    isRunning: false,
    clusterError: null,
    clusterSpace: 'param',
    k: 3,

    runClustering: async (space: ClusterSpace, k: number) => {
      set({ isRunning: true, clusterError: null, clusterSpace: space, k });
      try {
        const wasm = await WasmLoader.getInstance();

        // Step 1: PCA
        const pcaResult = wasm.runPca(2, space);
        set({ pcaProjections: pcaResult.projections });

        // Step 2: Flatten projections for k-means
        const flat = new Float64Array(pcaResult.projections.flatMap(row => row));

        // Step 3: k-means
        const kmeansResult = wasm.runKmeans(k, flat, 2);
        set({ clusterLabels: kmeansResult.labels });

        // Step 4: Cluster stats
        const labelsInt32 = new Int32Array(kmeansResult.labels);
        const statsResult = wasm.computeClusterStats(labelsInt32);
        set({ clusterStats: statsResult, isRunning: false });
      } catch (e) {
        set({
          clusterError: e instanceof Error ? e.message : String(e),
          isRunning: false,
        });
      }
    },

    estimateK: async (space: ClusterSpace) => {
      set({ isRunning: true, clusterError: null });
      try {
        const wasm = await WasmLoader.getInstance();

        // Step 1: PCA
        const pcaResult = wasm.runPca(2, space);

        // Step 2: Flatten
        const flat = new Float64Array(pcaResult.projections.flatMap(row => row));

        // Step 3: Elbow
        const elbowResult = wasm.estimateKElbow(flat, 2, 10);
        set({ elbowResult, isRunning: false });
      } catch (e) {
        set({
          clusterError: e instanceof Error ? e.message : String(e),
          isRunning: false,
        });
      }
    },
  }))
);

// Study 変更時にクラスタ状態をリセット
useStudyStore.subscribe(
  (state) => state.currentStudy,
  (_newStudy) => {
    useClusterStore.setState({
      pcaProjections: null,
      clusterLabels: null,
      clusterStats: null,
      elbowResult: null,
      clusterError: null,
      isRunning: false,
    });
  }
);
```

### 2. clusterStore.test.ts 作成 🔵
*REQ-VE-042〜045 / Vitest テストパターンより*

実装ファイル: `frontend/src/stores/clusterStore.test.ts`

#### テストケース

**TC-1604-01: 初期状態確認** 🔵
- Given: ストアの初期状態
- When: ストアを参照
- Then: pcaProjections=null, clusterLabels=null, clusterStats=null, elbowResult=null, isRunning=false, k=3, clusterSpace='param'

**TC-1604-02: runClustering 成功（PCA→kmeans→stats 順次実行）** 🔵
- Given: WasmLoader が PcaResult・KmeansResult・ClusterStatsResult を順に返すモック
- When: runClustering('param', 3) を呼び出す
- Then: pcaProjections・clusterLabels・clusterStats がセットされ、isRunning=false

**TC-1604-03: runClustering 実行中フラグ** 🔵
- Given: WasmLoader が非同期で遅延するモック
- When: runClustering 呼び出し直後
- Then: isRunning=true

**TC-1604-04: runClustering エラー（例: Insufficient data for PCA）** 🔵
- Given: wasm.runPca が "Insufficient data for PCA" を throw するモック
- When: runClustering('all', 3) を呼び出す
- Then: clusterError がセットされ、isRunning=false, pcaProjections=null

**TC-1604-05: estimateK 成功** 🔵
- Given: WasmLoader が PcaResult・ElbowResult を返すモック
- When: estimateK('param') を呼び出す
- Then: elbowResult がセットされ、isRunning=false

**TC-1604-06: runClustering の flatMap 変換確認（Float64Array生成）** 🔵
- Given: pcaProjections = [[1.0, 2.0], [3.0, 4.0]]
- When: runKmeans が呼び出されるとき
- Then: Float64Array([1.0, 2.0, 3.0, 4.0]) が runKmeans に渡される

**TC-1604-07: Study 変更時リセット** 🔵
- Given: pcaProjections・clusterLabels・clusterStats がセットされている状態
- When: useStudyStore の currentStudy が変更される
- Then: pcaProjections=null, clusterLabels=null, clusterStats=null, elbowResult=null

## 実装手順 (TDDタスク)

1. `/tsumiki:tdd-requirements TASK-1604` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成（clusterStore.test.ts）
3. `/tsumiki:tdd-red` - テスト実装（失敗状態確認）
4. `/tsumiki:tdd-green` - clusterStore.ts 実装
5. `/tsumiki:tdd-refactor` - PCA→kmeans→stats の順次実行最適化
6. `/tsumiki:tdd-verify-complete` - 既存159件 + 新規テスト合格確認

## 注意事項

- `Float64Array` の `flatMap` はブラウザ互換性に注意。`Array.from(projections.flat())` で代替可能
- `Int32Array(labels)` 変換時、labels は 0-indexed の整数配列なので直接変換可能
- `useStudyStore.subscribe` のモジュールレベル実行はテスト間でリセットが必要な場合がある（beforeEach でストア状態をリセット）
- 既存の `types/index.ts` に古い `ClusterStore` 型定義があるが、本タスクの `clusterStore.ts` がそれを上書き/独立して実装する

## 信頼性レベルサマリー

| カテゴリ | 件数 | 信頼性 |
|----------|------|--------|
| 実装詳細 | 2件 | 🔵 (100%) |
| 単体テスト | 7件 | 🔵 (100%) |
| 総項目 | 9件 | 🔵 (100%) |

**品質評価**: 高品質
