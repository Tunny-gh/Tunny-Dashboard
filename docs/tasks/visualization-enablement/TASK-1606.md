# TASK-1606: ClusterScatter コンポーネント実装 + テスト

**タスクID**: TASK-1606
**タスクタイプ**: TDD
**推定工数**: 5時間
**フェーズ**: Phase 3 - チャートコンポーネント
**信頼性レベル**: 🔵 *REQ-VE-070〜077 / ClusterList.tsx getClusterColor より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/visualization-enablement/requirements.md) - REQ-VE-070〜077
- **設計文書**: [📐 architecture.md](../../design/visualization-enablement/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/visualization-enablement/dataflow.md)

## タスク概要

`frontend/src/components/charts/ClusterScatter.tsx` を新設し、`clusterStore` の PCA 2D 投影データをクラスタラベルで色分けした ECharts 散布図で表示する。

## 依存タスク

- **前提タスク**: [TASK-1604: clusterStore 実装 + テスト](TASK-1604.md)
- **後続タスク**: [TASK-1608: FreeLayoutCanvas 4ケース配線](TASK-1608.md)

## 完了条件

- [ ] `ClusterScatter.tsx` が `ClusterScatter` コンポーネントとして export されている
- [ ] `clusterStore.pcaProjections` が null の場合に EmptyState を表示する
- [ ] `clusterStore.isRunning` が true の間ローディングを表示する
- [ ] `clusterStore.clusterError` がある場合に EmptyState を表示する
- [ ] PCA 投影データをクラスタラベルで色分けした ECharts scatter を表示する
- [ ] X 軸ラベル "PC1"・Y 軸ラベル "PC2"・凡例 "Cluster {n}" を表示する
- [ ] `getClusterColor(clusterId)` を ClusterList.tsx からインポートして使用する
- [ ] インラインスタイルのみ使用（Tailwind CSS なし）
- [ ] `ClusterScatter.test.tsx` で主要な表示パターンがテストされている
- [ ] 既存テストが全て合格している

---

## 実装詳細

### 1. ClusterScatter.tsx 作成 🔵

**信頼性**: 🔵 *REQ-VE-070〜077 / dataflow.md フロー3b より*

実装ファイル: `frontend/src/components/charts/ClusterScatter.tsx`

```tsx
import ReactECharts from 'echarts-for-react';
import { useClusterStore } from '../../stores/clusterStore';
import { getClusterColor } from '../panels/ClusterList';

export function ClusterScatter() {
  const { pcaProjections, clusterLabels, isRunning, clusterError } = useClusterStore();

  if (clusterError) {
    return <EmptyState message={clusterError} />;
  }
  if (isRunning) {
    return (
      <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100%' }}>
        Loading...
      </div>
    );
  }
  if (!pcaProjections) {
    return <EmptyState message="Run clustering in the left panel first" />;
  }

  // クラスタ別にデータを分類
  const k = clusterLabels ? Math.max(...clusterLabels) + 1 : 1;
  const series = clusterLabels
    ? Array.from({ length: k }, (_, ci) => ({
        name: `Cluster ${ci}`,
        type: 'scatter' as const,
        data: pcaProjections
          .filter((_, i) => clusterLabels[i] === ci)
          .map(([x, y]) => [x, y]),
        itemStyle: { color: getClusterColor(ci) },
      }))
    : [
        {
          name: 'Data',
          type: 'scatter' as const,
          data: pcaProjections.map(([x, y]) => [x, y]),
        },
      ];

  const option = {
    xAxis: { name: 'PC1', type: 'value' },
    yAxis: { name: 'PC2', type: 'value' },
    legend: { show: !!clusterLabels },
    series,
    tooltip: { trigger: 'item' },
  };

  return (
    <ReactECharts
      option={option}
      style={{ width: '100%', height: '100%' }}
    />
  );
}
```

### 2. getClusterColor の利用 🔵

**信頼性**: 🔵 *REQ-VE-073 / TASK-902 memo ClusterList.tsx より*

`getClusterColor` は `frontend/src/components/panels/ClusterList.tsx` から named export されている関数を使用。クラスタ ID（0-indexed）を渡すとカラー文字列を返す。

---

## 単体テスト要件

### TC-1606-01: pcaProjections が null の場合 EmptyState 表示 🔵

**信頼性**: 🔵 *REQ-VE-071 より*

**Given**: clusterStore.pcaProjections = null, isRunning = false
**When**: ClusterScatter がレンダリングされる
**Then**: "Run clustering in the left panel first" が表示される

**テストファイル**: `frontend/src/components/charts/ClusterScatter.test.tsx`

### TC-1606-02: isRunning=true でローディング表示 🔵

**信頼性**: 🔵 *REQ-VE-074 より*

**Given**: clusterStore.isRunning = true
**When**: ClusterScatter がレンダリングされる
**Then**: ローディング表示が見える

### TC-1606-03: clusterError がある場合 EmptyState 表示 🔵

**信頼性**: 🔵 *REQ-VE-077 より*

**Given**: clusterStore.clusterError = "Insufficient data"
**When**: ClusterScatter がレンダリングされる
**Then**: エラーメッセージが表示される

### TC-1606-04: クラスタラベルなしでプロット（単色）🟡

**信頼性**: 🟡 *REQ-VE-075 推測・実装上の簡略化として妥当*

**Given**: pcaProjections = [[0, 1], [2, 3]], clusterLabels = null
**When**: ClusterScatter がレンダリングされる
**Then**: チャートがレンダリングされクラッシュしない

### TC-1606-05: クラスタラベルありで凡例に "Cluster {n}" が含まれる 🔵

**信頼性**: 🔵 *REQ-VE-076 より*

**Given**: pcaProjections = [[0,1],[2,3],[4,5]], clusterLabels = [0, 1, 0]
**When**: ClusterScatter がレンダリングされる
**Then**: ECharts オプションの凡例に "Cluster 0"・"Cluster 1" が含まれる

---

## 統合テスト要件

### 統合テスト1: clusterStore との連携 🔵

**信頼性**: 🔵 *dataflow.md フロー3b より*

**テスト内容**: clusterStore.runClustering 完了後 → ClusterScatter が散布図を表示
**期待結果**: pcaProjections と clusterLabels が反映されたチャートが描画される

---

## UI/UX要件

### ローディング状態 🔵

**信頼性**: 🔵 *REQ-VE-074 より*

- isRunning=true の間はローディング表示を行う
- インラインスタイルで中央配置

### エラー表示 🔵

**信頼性**: 🔵 *REQ-VE-077 / NFR-VE-020 より*

- clusterError がある場合は EmptyState でエラーメッセージを表示
- フロントエンドがクラッシュしないこと

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-1606` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成（ClusterScatter.test.tsx）
3. `/tsumiki:tdd-red` - テスト実装（失敗状態確認）
4. `/tsumiki:tdd-green` - ClusterScatter.tsx 実装
5. `/tsumiki:tdd-refactor` - クラスタ分類ロジック整理
6. `/tsumiki:tdd-verify-complete` - 既存テスト + 新規テスト合格確認

---

## 注意事項

- `getClusterColor` は `frontend/src/components/panels/ClusterList.tsx` から import すること
- インラインスタイルのみ使用（Tailwind CSS は禁止、NFR-VE-010）
- clusterLabels が null（クラスタリング未実行）だが pcaProjections がある場合は単色でプロット可（REQ-VE-075）

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 4 | 1 | 0 | 5 |
| 統合テスト | 1 | 0 | 0 | 1 |
| UI/UX要件 | 2 | 0 | 0 | 2 |

### 全体評価

- **総項目数**: 10項目
- 🔵 **青信号**: 9項目 (90%)
- 🟡 **黄信号**: 1項目 (10%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
