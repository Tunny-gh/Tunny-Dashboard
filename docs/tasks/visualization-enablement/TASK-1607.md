# TASK-1607: DimReductionScatter コンポーネント実装 + テスト

**タスクID**: TASK-1607
**タスクタイプ**: TDD
**推定工数**: 5時間
**フェーズ**: Phase 3 - チャートコンポーネント
**信頼性レベル**: 🔵 *REQ-VE-080〜087 / dataflow.md フロー4より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/visualization-enablement/requirements.md) - REQ-VE-080〜087
- **設計文書**: [📐 architecture.md](../../design/visualization-enablement/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/visualization-enablement/dataflow.md)
- **インターフェース**: [📝 interfaces.ts](../../design/visualization-enablement/interfaces.ts)

## タスク概要

`frontend/src/components/charts/DimReductionScatter.tsx` を新設し、PCA 2D 散布図（UMAP チャート）を実装する。`clusterStore.pcaProjections` を優先再利用し、なければ `wasm.runPca(2, 'all')` を直接呼び出す。次元削減方式セレクター（PCA / UMAP Coming Soon）を持つ。

## 依存タスク

- **前提タスク**: [TASK-1604: clusterStore 実装 + テスト](TASK-1604.md)
- **後続タスク**: [TASK-1608: FreeLayoutCanvas 4ケース配線](TASK-1608.md)

## 完了条件

- [ ] `DimReductionScatter.tsx` が `DimReductionScatter` コンポーネントとして export されている
- [ ] 次元削減方式セレクター（PCA / UMAP disabled）が実装されている
- [ ] `clusterStore.pcaProjections` があれば再利用し、なければ `wasm.runPca(2, 'all')` を呼び出す
- [ ] `selectionStore.colorMode` によるカラーマッピングで点を色付けする
- [ ] チャートタイトル "Dimensionality Reduction (PCA)" を表示する
- [ ] `clusterStore.isRunning` または内部ローディング中はローディング表示
- [ ] PCA データが取得できない場合のみ EmptyState を表示する
- [ ] インラインスタイルのみ使用（Tailwind CSS なし）
- [ ] `DimReductionScatter.test.tsx` で主要な表示パターンがテストされている
- [ ] 既存テストが全て合格している

---

## 実装詳細

### 1. DimReductionScatter.tsx 作成 🔵

**信頼性**: 🔵 *REQ-VE-080〜087 / dataflow.md フロー4 より*

実装ファイル: `frontend/src/components/charts/DimReductionScatter.tsx`

```tsx
import { useEffect, useState } from 'react';
import ReactECharts from 'echarts-for-react';
import { useClusterStore } from '../../stores/clusterStore';
import { useSelectionStore } from '../../stores/selectionStore';
import { WasmLoader } from '../../wasm/wasmLoader';

export function DimReductionScatter() {
  const [method, setMethod] = useState<'pca' | 'umap'>('pca');
  const [localProjections, setLocalProjections] = useState<number[][] | null>(null);
  const [isLocalLoading, setIsLocalLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const pcaProjections = useClusterStore(s => s.pcaProjections);
  const isRunning = useClusterStore(s => s.isRunning);
  const colorMode = useSelectionStore(s => s.colorMode);

  const projections = pcaProjections ?? localProjections;

  useEffect(() => {
    if (!pcaProjections && !isLocalLoading && !localProjections) {
      setIsLocalLoading(true);
      WasmLoader.getInstance()
        .then(wasm => {
          const result = wasm.runPca(2, 'all');
          setLocalProjections(result.projections);
        })
        .catch(e => setError(e instanceof Error ? e.message : String(e)))
        .finally(() => setIsLocalLoading(false));
    }
  }, [pcaProjections, isLocalLoading, localProjections]);

  if (error) return <EmptyState message={error} />;
  if (isRunning || isLocalLoading) {
    return (
      <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100%' }}>
        Loading...
      </div>
    );
  }
  if (!projections) return null;

  const option = {
    title: { text: 'Dimensionality Reduction (PCA)' },
    xAxis: { type: 'value', name: 'PC1' },
    yAxis: { type: 'value', name: 'PC2' },
    tooltip: { trigger: 'item' },
    series: [
      {
        type: 'scatter',
        data: projections.map(([x, y]) => [x, y]),
        // colorMode に応じた色付けは selectionStore の colorMap を参照
      },
    ],
  };

  return (
    <div style={{ width: '100%', height: '100%', display: 'flex', flexDirection: 'column' }}>
      <div style={{ padding: '8px' }}>
        <select
          value={method}
          onChange={e => setMethod(e.target.value as 'pca' | 'umap')}
          style={{ padding: '4px 8px', borderRadius: '4px' }}
        >
          <option value="pca">PCA</option>
          <option value="umap" disabled>UMAP (Coming Soon)</option>
        </select>
      </div>
      <ReactECharts option={option} style={{ flex: 1 }} />
    </div>
  );
}
```

### 2. PCA データソースの優先順位 🔵

**信頼性**: 🔵 *REQ-VE-082 / dataflow.md フロー4 より*

- `clusterStore.pcaProjections` が利用可能な場合はそれを再利用（WASM 呼び出しなし）
- ない場合は `wasm.runPca(2, 'all')` を直接呼び出してローカル state に保存
- clusterStore の状態を汚染しない（clusterStore 経由ではなく WasmLoader を直接呼び出す）

---

## 単体テスト要件

### TC-1607-01: clusterStore.pcaProjections がある場合に再利用する 🔵

**信頼性**: 🔵 *REQ-VE-082 より*

**Given**: clusterStore.pcaProjections = [[1,2], [3,4]]
**When**: DimReductionScatter がレンダリングされる
**Then**: WasmLoader.getInstance は呼び出されない、チャートが表示される

**テストファイル**: `frontend/src/components/charts/DimReductionScatter.test.tsx`

### TC-1607-02: pcaProjections が null の場合に wasm.runPca を呼び出す 🔵

**信頼性**: 🔵 *REQ-VE-082 より*

**Given**: clusterStore.pcaProjections = null, WasmLoader.getInstance が projections を返すモック
**When**: DimReductionScatter がマウントされる
**Then**: wasm.runPca(2, 'all') が呼び出される

### TC-1607-03: ローディング表示 🔵

**信頼性**: 🔵 *REQ-VE-086 より*

**Given**: pcaProjections = null, WasmLoader が pending の Promise
**When**: DimReductionScatter がレンダリングされる
**Then**: ローディング表示が見える

### TC-1607-04: エラー時 EmptyState 表示 🔵

**信頼性**: 🔵 *REQ-VE-087 / NFR-VE-020 より*

**Given**: wasm.runPca がエラーを throw
**When**: DimReductionScatter がレンダリングされる
**Then**: EmptyState が表示される

### TC-1607-05: UMAP オプションが disabled 🔵

**信頼性**: 🔵 *REQ-VE-081 / EDGE-VE-040 より*

**Given**: セレクターが表示されている
**Then**: "UMAP (Coming Soon)" オプションが disabled 属性を持つ

### TC-1607-06: チャートタイトルが正しい 🔵

**信頼性**: 🔵 *REQ-VE-085 より*

**Given**: 有効な projections が存在する
**Then**: ECharts オプションに "Dimensionality Reduction (PCA)" が含まれる

### TC-1607-07: isRunning=true でローディング表示 🔵

**信頼性**: 🔵 *REQ-VE-086 より*

**Given**: clusterStore.isRunning = true
**When**: DimReductionScatter がレンダリングされる
**Then**: ローディング表示が見える

---

## 統合テスト要件

### 統合テスト1: clusterStore 連携 + PCA データ再利用 🔵

**信頼性**: 🔵 *dataflow.md フロー4 より*

**テスト内容**: clusterStore.runClustering 後 → DimReductionScatter が pcaProjections を再利用して表示
**期待結果**: WASM の追加呼び出しなしでチャートが描画される

---

## UI/UX要件

### ローディング状態 🔵

**信頼性**: 🔵 *REQ-VE-086 より*

- isRunning または isLocalLoading の間はローディング表示
- インラインスタイルで中央配置

### エラー表示 🔵

**信頼性**: 🔵 *REQ-VE-087 / NFR-VE-020 より*

- PCA データ取得失敗時のみ EmptyState を表示
- フロントエンドがクラッシュしないこと

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-1607` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成（DimReductionScatter.test.tsx）
3. `/tsumiki:tdd-red` - テスト実装（失敗状態確認）
4. `/tsumiki:tdd-green` - DimReductionScatter.tsx 実装
5. `/tsumiki:tdd-refactor` - PCA データソース選択ロジック整理
6. `/tsumiki:tdd-verify-complete` - 既存テスト + 新規テスト合格確認

---

## 注意事項

- `selectionStore.colorMode` との連携詳細は `colormaps.ts` の `getColorForValue` 等を参照して実装すること
- `clusterStore` 経由ではなく `WasmLoader` を直接呼び出すことで clusterStore の状態を汚染しない（設計ヒアリング決定）
- インラインスタイルのみ使用（Tailwind CSS は禁止、NFR-VE-010）
- `useEffect` の依存配列に `localProjections` を含めて無限ループを防ぐこと

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 7 | 0 | 0 | 7 |
| 統合テスト | 1 | 0 | 0 | 1 |
| UI/UX要件 | 2 | 0 | 0 | 2 |

### 全体評価

- **総項目数**: 12項目
- 🔵 **青信号**: 12項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
