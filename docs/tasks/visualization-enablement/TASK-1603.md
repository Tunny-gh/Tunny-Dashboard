# TASK-1603: analysisStore 実装 + テスト

## メタデータ

| 項目 | 値 |
|------|-----|
| タスクID | TASK-1603 |
| タスクタイプ | TDD |
| 推定工数 | 5時間 |
| フェーズ | Phase 2 - Zustand ストア |
| 信頼性レベル | 🔵 (REQ-VE-030〜034 / 設計ヒアリング subscribe パターンより) |
| 前提タスク | TASK-1602 |
| 後続タスク | TASK-1605, TASK-1608 |

## タスク概要

`frontend/src/stores/analysisStore.ts` を新設し、感度分析結果の保持・WASM 呼び出し・Study 変更時リセットを実装する。Vitest でストアの単体テストを作成する。

## 完了条件

- [ ] `analysisStore.ts` が `useAnalysisStore` フックとして export されている
- [ ] `sensitivityResult`・`isComputingSensitivity`・`sensitivityError` の3状態が実装されている
- [ ] `computeSensitivity()` が `WasmLoader.getInstance()` 経由で WASM を呼び出し結果をセットする
- [ ] `computeSensitivitySelected(indices: Uint32Array)` が実装されている
- [ ] `useStudyStore.subscribe` で Study 変更時に sensitivityResult が null にリセットされる
- [ ] `analysisStore.test.ts` でストアの主要アクションがテストされている
- [ ] 既存テスト159件が全て合格している

## 実装詳細

### 1. analysisStore.ts 作成 🔵
*REQ-VE-030〜034 / studyStore.ts・selectionStore.ts 既存パターンより*

実装ファイル: `frontend/src/stores/analysisStore.ts`

```typescript
import { create } from 'zustand';
import { subscribeWithSelector } from 'zustand/middleware';
import { useStudyStore } from './studyStore';
import { WasmLoader } from '../wasm/wasmLoader';
import type { SensitivityWasmResult } from '../wasm/pkg/tunny_core';

interface AnalysisState {
  sensitivityResult: SensitivityWasmResult | null;
  isComputingSensitivity: boolean;
  sensitivityError: string | null;
  computeSensitivity: () => Promise<void>;
  computeSensitivitySelected: (indices: Uint32Array) => Promise<void>;
}

export const useAnalysisStore = create<AnalysisState>()(
  subscribeWithSelector((set) => ({
    sensitivityResult: null,
    isComputingSensitivity: false,
    sensitivityError: null,

    computeSensitivity: async () => {
      set({ isComputingSensitivity: true, sensitivityError: null });
      try {
        const wasm = await WasmLoader.getInstance();
        const result = wasm.computeSensitivity();
        set({ sensitivityResult: result, isComputingSensitivity: false });
      } catch (e) {
        set({
          sensitivityError: e instanceof Error ? e.message : String(e),
          isComputingSensitivity: false,
        });
      }
    },

    computeSensitivitySelected: async (indices: Uint32Array) => {
      set({ isComputingSensitivity: true, sensitivityError: null });
      try {
        const wasm = await WasmLoader.getInstance();
        const result = wasm.computeSensitivitySelected(indices);
        set({ sensitivityResult: result, isComputingSensitivity: false });
      } catch (e) {
        set({
          sensitivityError: e instanceof Error ? e.message : String(e),
          isComputingSensitivity: false,
        });
      }
    },
  }))
);

// Study 変更時に sensitivityResult をリセット
useStudyStore.subscribe(
  (state) => state.currentStudy,
  (_newStudy) => {
    useAnalysisStore.setState({
      sensitivityResult: null,
      sensitivityError: null,
      isComputingSensitivity: false,
    });
  }
);
```

### 2. analysisStore.test.ts 作成 🔵
*REQ-VE-032〜034 / Vitest + Zustand テストパターンより*

実装ファイル: `frontend/src/stores/analysisStore.test.ts`

#### テストケース

**TC-1603-01: 初期状態確認** 🔵
- Given: ストアの初期状態
- When: ストアを参照
- Then: sensitivityResult=null, isComputingSensitivity=false, sensitivityError=null

**TC-1603-02: computeSensitivity 成功** 🔵
- Given: WasmLoader.getInstance が SensitivityWasmResult を返すモック
- When: computeSensitivity() を呼び出す
- Then: sensitivityResult に結果がセットされ、isComputingSensitivity=false

**TC-1603-03: computeSensitivity 計算中フラグ** 🔵
- Given: WasmLoader.getInstance が遅延を持つモック
- When: computeSensitivity() を呼び出した直後
- Then: isComputingSensitivity=true

**TC-1603-04: computeSensitivity エラー** 🔵
- Given: WasmLoader.getInstance が "No active study" エラーを throw するモック
- When: computeSensitivity() を呼び出す
- Then: sensitivityError="No active study", isComputingSensitivity=false, sensitivityResult=null

**TC-1603-05: computeSensitivitySelected 成功** 🔵
- Given: WasmLoader が SensitivityWasmResult を返すモック + indices=[0,1,2]
- When: computeSensitivitySelected(indices) を呼び出す
- Then: sensitivityResult に結果がセットされる

**TC-1603-06: Study 変更時リセット** 🔵
- Given: sensitivityResult に結果がセットされている状態
- When: useStudyStore の currentStudy が変更される
- Then: sensitivityResult=null, sensitivityError=null

#### 単体テスト実装例

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { act } from 'react';
import { useAnalysisStore } from './analysisStore';
import { WasmLoader } from '../wasm/wasmLoader';

vi.mock('../wasm/wasmLoader', () => ({
  WasmLoader: {
    getInstance: vi.fn(),
  },
}));

describe('analysisStore', () => {
  beforeEach(() => {
    useAnalysisStore.setState({
      sensitivityResult: null,
      isComputingSensitivity: false,
      sensitivityError: null,
    });
  });

  it('初期状態が正しい', () => {
    const state = useAnalysisStore.getState();
    expect(state.sensitivityResult).toBeNull();
    expect(state.isComputingSensitivity).toBe(false);
    expect(state.sensitivityError).toBeNull();
  });

  it('computeSensitivity 成功時に sensitivityResult をセットする', async () => {
    const mockResult = { spearman: [[0.5]], ridge: [{ beta: [0.3], rSquared: 0.8 }], paramNames: ['x'], objectiveNames: ['y'], durationMs: 10 };
    vi.mocked(WasmLoader.getInstance).mockResolvedValue({ computeSensitivity: () => mockResult } as any);

    await act(async () => {
      await useAnalysisStore.getState().computeSensitivity();
    });

    expect(useAnalysisStore.getState().sensitivityResult).toEqual(mockResult);
    expect(useAnalysisStore.getState().isComputingSensitivity).toBe(false);
  });

  it('computeSensitivity エラー時に sensitivityError をセットする', async () => {
    vi.mocked(WasmLoader.getInstance).mockResolvedValue({ computeSensitivity: () => { throw new Error('No active study'); } } as any);

    await act(async () => {
      await useAnalysisStore.getState().computeSensitivity();
    });

    expect(useAnalysisStore.getState().sensitivityError).toBe('No active study');
    expect(useAnalysisStore.getState().isComputingSensitivity).toBe(false);
  });
});
```

## 実装手順 (TDDタスク)

1. `/tsumiki:tdd-requirements TASK-1603` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成（analysisStore.test.ts）
3. `/tsumiki:tdd-red` - テスト実装（失敗状態確認）
4. `/tsumiki:tdd-green` - analysisStore.ts 実装
5. `/tsumiki:tdd-refactor` - リファクタリング（subscribeパターン最適化）
6. `/tsumiki:tdd-verify-complete` - 既存159件 + 新規テスト合格確認

## 注意事項

- `subscribeWithSelector` ミドルウェアが必要（studyStore.ts は使っていないが comparisonStore.ts が使っているパターンに合わせる）
- `useStudyStore.subscribe` の呼び出しはモジュールレベルに記述（ストア生成後）
- テストでは WasmLoader をモックして WASM 呼び出しを分離すること

## 信頼性レベルサマリー

| カテゴリ | 件数 | 信頼性 |
|----------|------|--------|
| 実装詳細 | 2件 | 🔵 (100%) |
| 単体テスト | 6件 | 🔵 (100%) |
| 総項目 | 8件 | 🔵 (100%) |

**品質評価**: 高品質
