# TASK-1605: ImportanceChart コンポーネント実装 + テスト

**タスクID**: TASK-1605
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 3 - チャートコンポーネント
**信頼性レベル**: 🔵 *REQ-VE-060〜065 / SensitivityHeatmap.tsx 同パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/visualization-enablement/requirements.md) - REQ-VE-060〜065
- **設計文書**: [📐 architecture.md](../../design/visualization-enablement/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/visualization-enablement/dataflow.md)
- **インターフェース**: [📝 interfaces.ts](../../design/visualization-enablement/interfaces.ts)

## タスク概要

`frontend/src/components/charts/ImportanceChart.tsx` を新設し、analysisStore の感度分析結果から重要度指標（Spearman |ρ| / Ridge |β|）を選択して降順ソートした水平バーチャートを表示する。

## 依存タスク

- **前提タスク**: [TASK-1603: analysisStore 実装 + テスト](TASK-1603.md)
- **後続タスク**: [TASK-1608: FreeLayoutCanvas 4ケース配線](TASK-1608.md)

## 完了条件

- [ ] `ImportanceChart.tsx` が `ImportanceChart` コンポーネントとして export されている
- [ ] Spearman |ρ| / Ridge |β| 切り替えドロップダウンが実装されている
- [ ] 選択された指標に基づき降順ソートされた水平バーチャート（ECharts）が表示される
- [ ] `analysisStore.sensitivityResult` が null の場合に `computeSensitivity()` を自動実行する
- [ ] ローディング中はローディング表示を行う
- [ ] エラー時・パラメータなしの場合は `EmptyState` を表示する
- [ ] インラインスタイルのみ使用（Tailwind CSS なし）
- [ ] `ImportanceChart.test.tsx` で主要な表示パターンがテストされている
- [ ] 既存テストが全て合格している

---

## 実装詳細

### 1. ImportanceChart.tsx 作成 🔵

**信頼性**: 🔵 *REQ-VE-060〜065 / SensitivityHeatmap.tsx 同パターンより*

実装ファイル: `frontend/src/components/charts/ImportanceChart.tsx`

```tsx
import { useEffect, useState } from 'react';
import ReactECharts from 'echarts-for-react';
import { useAnalysisStore } from '../../stores/analysisStore';

type ImportanceMetric = 'spearman' | 'beta';

export function ImportanceChart() {
  const [metric, setMetric] = useState<ImportanceMetric>('spearman');
  const {
    sensitivityResult,
    isComputingSensitivity,
    sensitivityError,
    computeSensitivity,
  } = useAnalysisStore();

  useEffect(() => {
    if (!sensitivityResult && !isComputingSensitivity) {
      computeSensitivity();
    }
  }, [sensitivityResult, isComputingSensitivity, computeSensitivity]);

  if (sensitivityError) {
    return <EmptyState message={sensitivityError} />;
  }
  if (isComputingSensitivity || !sensitivityResult) {
    return (
      <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100%' }}>
        Loading...
      </div>
    );
  }
  if (sensitivityResult.paramNames.length === 0) {
    return <EmptyState message="No parameters" />;
  }

  // 重要度計算
  const nObj = sensitivityResult.objectiveNames.length;
  const importances = sensitivityResult.paramNames.map((name, pi) => {
    let score = 0;
    if (metric === 'spearman') {
      score = sensitivityResult.spearman[pi].reduce((sum, v) => sum + Math.abs(v), 0) / nObj;
    } else {
      score = sensitivityResult.ridge.reduce((sum, r) => sum + Math.abs(r.beta[pi]), 0) / nObj;
    }
    return { name, score };
  });

  // ECharts の yAxis は下から上なので昇順ソート（表示は降順）
  importances.sort((a, b) => a.score - b.score);

  const metricLabel = metric === 'spearman' ? 'Spearman |ρ|' : 'Ridge |β|';

  const option = {
    title: { text: `Parameter Importance (${metricLabel})` },
    tooltip: {},
    xAxis: { type: 'value', min: 0 },
    yAxis: { type: 'category', data: importances.map(i => i.name) },
    series: [{ type: 'bar', data: importances.map(i => i.score) }],
  };

  return (
    <div style={{ width: '100%', height: '100%', display: 'flex', flexDirection: 'column' }}>
      <div style={{ padding: '8px' }}>
        <select
          value={metric}
          onChange={e => setMetric(e.target.value as ImportanceMetric)}
          style={{ padding: '4px 8px', borderRadius: '4px' }}
        >
          <option value="spearman">Spearman |ρ|</option>
          <option value="beta">Ridge |β|</option>
        </select>
      </div>
      <ReactECharts option={option} style={{ flex: 1 }} />
    </div>
  );
}
```

### 2. 重要度計算ロジック 🔵

**信頼性**: 🔵 *REQ-VE-062 より*

- `spearman` 指標: `sensitivityResult.spearman[paramIdx][objIdx]` の絶対値を全目的関数で平均
- `beta` 指標: `sensitivityResult.ridge[objIdx].beta[paramIdx]` の絶対値を全目的関数で平均
- 単目的の場合（nObj=1）: 平均をとらずその1目的分をそのまま使用（結果は同一）

---

## 単体テスト要件

### TC-1605-01: sensitivityResult が null の場合に computeSensitivity を呼び出す 🔵

**信頼性**: 🔵 *REQ-VE-064 より*

**Given**: analysisStore.sensitivityResult = null, isComputingSensitivity = false
**When**: ImportanceChart がマウントされる
**Then**: computeSensitivity が呼び出される

**テストファイル**: `frontend/src/components/charts/ImportanceChart.test.tsx`

### TC-1605-02: ローディング状態の表示 🔵

**信頼性**: 🔵 *REQ-VE-064 / NFR-VE-020 より*

**Given**: isComputingSensitivity = true
**When**: ImportanceChart がレンダリングされる
**Then**: ローディング表示（"Loading..."）が見える

### TC-1605-03: エラー状態の EmptyState 表示 🔵

**信頼性**: 🔵 *REQ-VE-054 / NFR-VE-020 より*

**Given**: sensitivityError = "No active study"
**When**: ImportanceChart がレンダリングされる
**Then**: エラーメッセージが表示される

### TC-1605-04: パラメータなしの EmptyState 表示 🔵

**信頼性**: 🔵 *REQ-VE-065 より*

**Given**: sensitivityResult.paramNames.length === 0
**When**: ImportanceChart がレンダリングされる
**Then**: "No parameters" が表示される

### TC-1605-05: Spearman 指標でタイトルが正しい 🔵

**信頼性**: 🔵 *REQ-VE-063 より*

**Given**: sensitivityResult が有効なデータを持つ、metric = 'spearman'
**When**: ImportanceChart がレンダリングされる
**Then**: "Parameter Importance (Spearman |ρ|)" が ECharts の設定に含まれる

### TC-1605-06: Ridge β 指標でタイトルが正しい 🔵

**信頼性**: 🔵 *REQ-VE-063 より*

**Given**: sensitivityResult が有効なデータを持つ
**When**: ドロップダウンで 'beta' を選択する
**Then**: "Parameter Importance (Ridge |β|)" が表示される

---

## 統合テスト要件

### 統合テスト1: analysisStore との連携 🔵

**信頼性**: 🔵 *dataflow.md フロー2 より*

**テスト内容**: analysisStore.sensitivityResult が null → computeSensitivity 呼び出し → 結果セット → チャート表示
**期待結果**: 一連のフローが正しく動作する

---

## UI/UX要件

### ローディング状態 🔵

**信頼性**: 🔵 *REQ-VE-064 / NFR-VE-020 より*

- isComputingSensitivity=true または sensitivityResult=null（計算待ち）の間はローディング表示
- インラインスタイルで中央配置

### エラー表示 🔵

**信頼性**: 🔵 *REQ-VE-054 より*

- sensitivityError がある場合は EmptyState コンポーネントでエラーメッセージを表示
- フロントエンドがクラッシュしないこと（NFR-VE-020）

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-1605` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成（ImportanceChart.test.tsx）
3. `/tsumiki:tdd-red` - テスト実装（失敗状態確認）
4. `/tsumiki:tdd-green` - ImportanceChart.tsx 実装
5. `/tsumiki:tdd-refactor` - 重要度計算ロジック整理
6. `/tsumiki:tdd-verify-complete` - 既存テスト + 新規テスト合格確認

---

## 注意事項

- `EmptyState` コンポーネントは既存のものを import して使用すること
- ECharts は `echarts-for-react` の `ReactECharts` を使用（既存 SensitivityHeatmap.tsx と同パターン）
- インラインスタイルのみ使用（Tailwind CSS は禁止、NFR-VE-010）
- `useEffect` の依存配列に注意（computeSensitivity は安定した参照を使うこと）

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 6 | 0 | 0 | 6 |
| 統合テスト | 1 | 0 | 0 | 1 |
| UI/UX要件 | 2 | 0 | 0 | 2 |

### 全体評価

- **総項目数**: 11項目
- 🔵 **青信号**: 11項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
