# TASK-1601: WASM バインディング追加

**タスクID**: TASK-1601
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - WASM インフラ
**信頼性レベル**: 🔵 *REQ-VE-001〜006 / architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/visualization-enablement/requirements.md) - REQ-VE-001〜006
- **設計文書**: [📐 architecture.md](../../design/visualization-enablement/architecture.md)

## タスク概要

`rust_core/src/lib.rs` に `#[wasm_bindgen]` 関数6本を追加し、`wasm-pack build` でビルド後にパッケージを更新する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-1602: TypeScript型宣言 + WasmLoaderバインディング追加](TASK-1602.md)

## 完了条件

- [ ] `computeSensitivity`・`computeSensitivitySelected`・`runPca`・`runKmeans`・`estimateKElbow`・`computeClusterStats` の 6 関数が `lib.rs` に追加されている
- [ ] 各関数が `serde_wasm_bindgen::Serializer::json_compatible()` でシリアライズして返す
- [ ] `wasm-pack build --target web --out-dir ../frontend/src/wasm/pkg` が成功する
- [ ] `frontend/src/wasm/pkg/tunny_core_bg.wasm` が更新されている

---

## 実装詳細

### 1. `computeSensitivity` バインディング 🔵
*REQ-VE-001 / sensitivity.rs より*

実装ファイル: `rust_core/src/lib.rs`

```rust
#[wasm_bindgen(js_name = "computeSensitivity")]
pub fn wasm_compute_sensitivity() -> Result<JsValue, JsValue> {
    let start = js_sys::Date::now();
    match sensitivity::compute_sensitivity() {
        Some(result) => {
            let duration_ms = js_sys::Date::now() - start;
            let output = serde_json::json!({
                "spearman": result.spearman,
                "ridge": result.ridge.iter().map(|r| serde_json::json!({"beta": r.beta, "rSquared": r.r_squared})).collect::<Vec<_>>(),
                "paramNames": result.param_names,
                "objectiveNames": result.objective_names,
                "durationMs": duration_ms,
            });
            let serializer = serde_wasm_bindgen::Serializer::json_compatible();
            output.serialize(&serializer).map_err(|e| JsValue::from_str(&e.to_string()))
        },
        None => Err(JsValue::from_str("No active study")),
    }
}
```

### 2. `computeSensitivitySelected` バインディング 🔵
*REQ-VE-002 / sensitivity.rs より*

実装ファイル: `rust_core/src/lib.rs`

```rust
#[wasm_bindgen(js_name = "computeSensitivitySelected")]
pub fn wasm_compute_sensitivity_selected(indices: js_sys::Uint32Array) -> Result<JsValue, JsValue> {
    if indices.length() == 0 {
        return Err(JsValue::from_str("Empty selection"));
    }
    let idx_vec: Vec<u32> = indices.to_vec();
    let start = js_sys::Date::now();
    match sensitivity::compute_sensitivity_selected(&idx_vec) {
        Some(result) => {
            let duration_ms = js_sys::Date::now() - start;
            let output = serde_json::json!({
                "spearman": result.spearman,
                "ridge": result.ridge.iter().map(|r| serde_json::json!({"beta": r.beta, "rSquared": r.r_squared})).collect::<Vec<_>>(),
                "paramNames": result.param_names,
                "objectiveNames": result.objective_names,
                "durationMs": duration_ms,
            });
            let serializer = serde_wasm_bindgen::Serializer::json_compatible();
            output.serialize(&serializer).map_err(|e| JsValue::from_str(&e.to_string()))
        },
        None => Err(JsValue::from_str("No active study")),
    }
}
```

### 3. `runPca` バインディング 🔵
*REQ-VE-003 / clustering.rs より*

実装ファイル: `rust_core/src/lib.rs`

```rust
#[wasm_bindgen(js_name = "runPca")]
pub fn wasm_run_pca(n_components: u32, space: &str) -> Result<JsValue, JsValue> {
    let pca_space = match space {
        "param" => clustering::PcaSpace::Param,
        "objective" => clustering::PcaSpace::Objective,
        "all" => clustering::PcaSpace::All,
        _ => return Err(JsValue::from_str("Invalid space")),
    };
    let start = js_sys::Date::now();
    match clustering::run_pca(n_components as usize, pca_space) {
        Some(result) => {
            let duration_ms = js_sys::Date::now() - start;
            let output = serde_json::json!({
                "projections": result.projections,
                "explainedVariance": result.explained_variance,
                "featureNames": result.feature_names,
                "durationMs": duration_ms,
            });
            let serializer = serde_wasm_bindgen::Serializer::json_compatible();
            output.serialize(&serializer).map_err(|e| JsValue::from_str(&e.to_string()))
        },
        None => Err(JsValue::from_str("Insufficient data for PCA")),
    }
}
```

### 4. `runKmeans` バインディング 🔵
*REQ-VE-004 / clustering.rs より*

実装ファイル: `rust_core/src/lib.rs`

```rust
#[wasm_bindgen(js_name = "runKmeans")]
pub fn wasm_run_kmeans(k: u32, data: js_sys::Float64Array, n_cols: u32) -> Result<JsValue, JsValue> {
    let data_vec: Vec<f64> = data.to_vec();
    let start = js_sys::Date::now();
    let result = clustering::run_kmeans(k as usize, &data_vec, n_cols as usize);
    let duration_ms = js_sys::Date::now() - start;
    let output = serde_json::json!({
        "labels": result.labels,
        "centroids": result.centroids,
        "wcss": result.wcss,
        "durationMs": duration_ms,
    });
    let serializer = serde_wasm_bindgen::Serializer::json_compatible();
    output.serialize(&serializer).map_err(|e| JsValue::from_str(&e.to_string()))
}
```

### 5. `estimateKElbow` バインディング 🔵
*REQ-VE-005 / clustering.rs より*

実装ファイル: `rust_core/src/lib.rs`

```rust
#[wasm_bindgen(js_name = "estimateKElbow")]
pub fn wasm_estimate_k_elbow(data: js_sys::Float64Array, n_cols: u32, max_k: u32) -> Result<JsValue, JsValue> {
    let data_vec: Vec<f64> = data.to_vec();
    let start = js_sys::Date::now();
    let result = clustering::estimate_k_elbow(&data_vec, n_cols as usize, max_k as usize);
    let duration_ms = js_sys::Date::now() - start;
    let output = serde_json::json!({
        "wcssPerK": result.wcss_per_k,
        "recommendedK": result.recommended_k,
        "durationMs": duration_ms,
    });
    let serializer = serde_wasm_bindgen::Serializer::json_compatible();
    output.serialize(&serializer).map_err(|e| JsValue::from_str(&e.to_string()))
}
```

### 6. `computeClusterStats` バインディング 🔵
*REQ-VE-006 / clustering.rs / note.md Int32Array変換注記より*

**注意**: `i32 as usize` 変換は k-means ラベル（常に ≥ 0）では安全。HDBSCAN 追加時は負値フィルタが必要。

実装ファイル: `rust_core/src/lib.rs`

```rust
#[wasm_bindgen(js_name = "computeClusterStats")]
pub fn wasm_compute_cluster_stats(labels: js_sys::Int32Array) -> Result<JsValue, JsValue> {
    let labels_vec: Vec<usize> = labels.to_vec().into_iter().map(|l| l as usize).collect();
    let start = js_sys::Date::now();
    let stats = clustering::compute_cluster_stats(&labels_vec);
    let duration_ms = js_sys::Date::now() - start;
    let output = serde_json::json!({
        "stats": stats.iter().map(|s| serde_json::json!({
            "clusterId": s.cluster_id,
            "size": s.size,
            "centroid": s.centroid,
            "std": s.std,
            "significantDiffs": s.significant_diffs,
        })).collect::<Vec<_>>(),
        "durationMs": duration_ms,
    });
    let serializer = serde_wasm_bindgen::Serializer::json_compatible();
    output.serialize(&serializer).map_err(|e| JsValue::from_str(&e.to_string()))
}
```

### 7. wasm-pack ビルド実行 🔵
*note.md wasm-pack build 手順より*

```bash
cd rust_core
wasm-pack build --target web --out-dir ../frontend/src/wasm/pkg
```

ビルド後 `tunny_core.d.ts` が自動更新されるが、型定義（interfaces）は手動追加が必要（TASK-1602）。

---

## 単体テスト要件

WASM バインディング自体の単体テストは Rust 側テスト（sensitivity.rs・clustering.rs）で対応済み。lib.rs バインディング関数のテストはビルド＋統合テストで確認する。

---

## 統合テスト要件

- TASK-1608 の FreeLayoutCanvas 配線テスト実行時に WasmLoader モックを通じて間接的に確認
- wasm-pack ビルド成功 + 型エラーなしでコンパイル通過を確認

---

## 実装手順

1. `/tsumiki:direct-setup TASK-1601` - lib.rs に 6 関数追加 + wasm-pack ビルド
2. `/tsumiki:direct-verify` - ビルド成功確認・TypeScript コンパイルエラーなし確認

---

## 注意事項

- 既存バインディング（`parseJournal`・`computeHvHistory` 等）のコードを壊さないこと
- `sensitivity` と `clustering` モジュールが `lib.rs` でインポート済みであることを確認すること
- `use serde::Serialize;` が必要

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 6 | 0 | 0 | 6 |
| 単体テスト | 1 | 0 | 0 | 1 |
| 統合テスト | 1 | 0 | 0 | 1 |
| UI/UX要件 | - | - | - | - |

### 全体評価

- **総項目数**: 8項目
- 🔵 **青信号**: 8項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
