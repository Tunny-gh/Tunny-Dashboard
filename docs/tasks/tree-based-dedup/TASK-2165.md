# TASK-2165: importance_chart.rs extract_tree_importance 導入 + match arm 統合

**タスクID**: TASK-2165
**タスクタイプ**: DIRECT
**推定工数**: 1時間
**フェーズ**: Phase 5 - UI match arm 統合
**信頼性レベル**: 🔵 *要件定義書 REQ-601〜603・既存 importance_chart.rs 実装より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/tree-based-dedup/requirements.md) - REQ-601〜603
- **設計文書**: [📐 architecture.md](../../design/tree-based-dedup/architecture.md) - §9

## タスク概要

`egui-app/src/ui/widgets/importance_chart.rs` に `extract_tree_importance` ヘルパー関数を追加し、`compute_sorted_importance` 内の RfAnova / Mdi / Shap / Permutation の4 match arm を1つの arm に統合する。Spearman / Ridge / Sobol の arm は変更しない。

## 依存タスク

- **前提タスク**: [TASK-2164](TASK-2164.md)
- **後続タスク**: [TASK-2166](TASK-2166.md)

## 完了条件

- [x] `importance_chart.rs` に `fn extract_tree_importance` が追加されている
- [x] `compute_sorted_importance` の RfAnova, Mdi, Shap, Permutation の4 arm が1つの arm にまとめられている
- [x] Spearman / Ridge / Sobol の arm は変更されていない
- [x] `cargo test` が全て通過する

---

## 主要実装項目

### 1. extract_tree_importance ヘルパー関数の追加 🔵

**信頼性**: 🔵 *要件 REQ-601・設計文書 §9 より*

`egui-app/src/ui/widgets/importance_chart.rs` に以下の関数を追加する（`compute_sorted_importance` 関数の外、同一ファイル内）:

```rust
fn extract_tree_importance(
    result: &SensitivityResult,
    metric: &ImportanceMetric,
    obj_idx: usize,
) -> Option<Vec<f64>> {
    let importances = match metric {
        ImportanceMetric::RfAnova => result.rf_anova.as_ref()?.importances.clone(),
        ImportanceMetric::Mdi => result.mdi.as_ref()?.importances.clone(),
        ImportanceMetric::Shap => result.shap.as_ref()?.importances.clone(),
        ImportanceMetric::Permutation => result.permutation.as_ref()?.importances.clone(),
        _ => return None,
    };
    Some(
        importances
            .iter()
            .map(|param_imp| param_imp.get(obj_idx).copied().unwrap_or(0.0).abs())
            .collect(),
    )
}
```

### 2. compute_sorted_importance の match arm 置換 🔵

**信頼性**: 🔵 *要件 REQ-602〜603・設計文書 §9 より*

**ノート**: 対象箇所はコンテキストノートによると `compute_sorted_importance` 内の行297-332 付近の RfAnova / Mdi / Shap / Permutation の4 arm。

置換パターン:

```diff
  match metric {
      ImportanceMetric::Spearman => { /* 変更なし */ }
      ImportanceMetric::Ridge => { /* 変更なし */ }
-     ImportanceMetric::RfAnova => {
-         let Some(ref rf) = result.rf_anova else { return vec![]; };
-         rf.importances.iter()
-             .map(|param_imp| param_imp.get(obj_idx).copied().unwrap_or(0.0).abs())
-             .collect()
-     }
-     ImportanceMetric::Mdi => {
-         /* 同様の重複ロジック */
-     }
-     ImportanceMetric::Shap => {
-         /* 同様の重複ロジック */
-     }
-     ImportanceMetric::Permutation => {
-         /* 同様の重複ロジック */
-     }
+     ImportanceMetric::RfAnova
+     | ImportanceMetric::Mdi
+     | ImportanceMetric::Shap
+     | ImportanceMetric::Permutation => {
+         extract_tree_importance(result, metric, obj_idx).unwrap_or_default()
+     }
      ImportanceMetric::SobolFirst | ImportanceMetric::SobolTotal => return vec![],
  };
```

**注意**: Sobol / Spearman / Ridge の arm はスコープ外のため変更しない（REQ-603）。
