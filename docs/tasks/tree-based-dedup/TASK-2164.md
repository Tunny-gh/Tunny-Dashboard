# TASK-2164: egui-app results.rs TreeImportanceResult 導入

**タスクID**: TASK-2164
**タスクタイプ**: DIRECT
**推定工数**: 0.5時間
**フェーズ**: Phase 4 - Result 型統合（egui-app）
**信頼性レベル**: 🔵 *要件定義書 REQ-701〜702・既存 results.rs 実装より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/tree-based-dedup/requirements.md) - REQ-701〜702
- **設計文書**: [📐 architecture.md](../../design/tree-based-dedup/architecture.md) - §8

## タスク概要

`egui-app/src/state/results.rs` に `TreeImportanceResult` 構造体を導入し、`MdiResult`・`RfAnovaResult`・`ShapResult`・`PermutationResult` を型エイリアスに変更する。`SensitivityResult` の各フィールドの型を `Option<TreeImportanceResult>` に統一する。

## 依存タスク

- **前提タスク**: [TASK-2163](TASK-2163.md)
- **後続タスク**: [TASK-2165](TASK-2165.md)

## 完了条件

- [x] `egui-app/src/state/results.rs` に `pub struct TreeImportanceResult` が追加されている
- [x] `MdiResult`, `RfAnovaResult`, `ShapResult`, `PermutationResult` が `pub type` エイリアスになっている
- [x] `SensitivityResult` の `rf_anova`, `mdi`, `shap`, `permutation` フィールドが `Option<TreeImportanceResult>` 型になっている
- [x] `cargo build` が通る

---

## 主要実装項目

### 1. results.rs の型統合 🔵

**信頼性**: 🔵 *要件 REQ-701〜702・設計文書 §8 より*

**注意**: egui-app 側の `results.rs` は `rust_core` とは独立した型定義を持つため、別途同様の変更が必要（コンテキストノート「影響範囲」3 参照）。

`egui-app/src/state/results.rs` の既存4型定義を以下に置換:

```rust
#[derive(Debug, Clone)]
pub struct TreeImportanceResult {
    pub importances: Vec<Vec<f64>>,
    pub r_squared: Vec<f64>,
}

pub type MdiResult = TreeImportanceResult;
pub type RfAnovaResult = TreeImportanceResult;
pub type ShapResult = TreeImportanceResult;
pub type PermutationResult = TreeImportanceResult;
```

### 2. SensitivityResult フィールド型の更新 🟡

**信頼性**: 🟡 *REQ-702・results.rs 既存構造から妥当な推測*

`SensitivityResult` の該当フィールドを型エイリアス名から `TreeImportanceResult` に統一:

```rust
pub struct SensitivityResult {
    pub param_names: Vec<String>,
    pub objective_names: Vec<String>,
    pub spearman: Vec<Vec<f64>>,
    pub ridge: Vec<RidgeResult>,
    pub rf_anova: Option<TreeImportanceResult>,
    pub mdi: Option<TreeImportanceResult>,
    pub shap: Option<TreeImportanceResult>,
    pub permutation: Option<TreeImportanceResult>,
}
```

**注意**: 型エイリアス経由でも同一動作するが、フィールド定義は `TreeImportanceResult` で統一することで意図を明確にする。
