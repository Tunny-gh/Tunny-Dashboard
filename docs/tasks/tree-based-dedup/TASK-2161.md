# TASK-2161: tree_common.rs 新規作成 + rf_anova/permutation の import 切り替え

**タスクID**: TASK-2161
**タスクタイプ**: DIRECT
**推定工数**: 1時間
**フェーズ**: Phase 1 - rust_core ヘルパー共通化
**信頼性レベル**: 🔵 *要件定義書 REQ-401〜406、REQ-501〜502・既存実装より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/tree-based-dedup/requirements.md) - REQ-401〜406, REQ-501〜502
- **設計文書**: [📐 architecture.md](../../design/tree-based-dedup/architecture.md) - §1, §4, §5
- **コンテキストノート**: [📝 note.md](../../spec/tree-based-dedup/note.md)

## タスク概要

`rust_core/src/sensitivity/tree_common.rs` を新規作成し、`rf_anova.rs` と `permutation.rs` に重複している `permute_single_column` / `normalize` 関数を集約する。rf_anova.rs では R² 計算も `mse_to_r_squared()` 呼び出しに統一する。`mod.rs` に `mod tree_common;` を追加する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-2162](TASK-2162.md)

## 完了条件

- [x] `rust_core/src/sensitivity/tree_common.rs` が存在し `pub(crate) fn permute_single_column` と `pub(crate) fn normalize` を含む
- [x] `rust_core/src/sensitivity/mod.rs` に `mod tree_common;` が追加されている
- [x] `rf_anova.rs` のローカル `permute_single_column`・`normalize` 定義が削除されている
- [x] `rf_anova.rs` に `use super::tree_common::{normalize, permute_single_column};` が追加されている
- [x] `rf_anova.rs` の R² 計算が `mse_to_r_squared()` 呼び出しに統一されている
- [x] `permutation.rs` のローカル `permute_single_column`・`normalize` 定義が削除されている
- [x] `permutation.rs` に `use super::tree_common::{normalize, permute_single_column};` が追加されている
- [x] `cargo test` が全て通過する

---

## 主要実装項目

### 1. tree_common.rs の新規作成 🔵

**信頼性**: 🔵 *要件 REQ-401〜404・設計文書 §1 より*

`rust_core/src/sensitivity/tree_common.rs` を新規作成:

```rust
use crate::core::random_forest::Lcg;

/// 指定列を Fisher-Yates シャッフルで並び替えた行列を返す
pub(crate) fn permute_single_column(
    x_matrix: &[Vec<f64>],
    feature_idx: usize,
    seed: u64,
) -> Option<Vec<Vec<f64>>> {
    let n = x_matrix.len();
    if n == 0 {
        return None;
    }

    let mut column_values: Vec<f64> = x_matrix.iter().map(|row| row[feature_idx]).collect();

    let mut rng = Lcg::new(seed);
    for i in (1..n).rev() {
        let j = rng.next_usize(i + 1);
        column_values.swap(i, j);
    }

    let mut permuted = x_matrix.to_vec();
    for (i, row) in permuted.iter_mut().enumerate() {
        row[feature_idx] = column_values[i];
    }
    Some(permuted)
}

/// 値を合計で正規化する。合計が 0 以下の場合は全要素を 0.0 にする
pub(crate) fn normalize(values: &mut [f64]) {
    let sum = values.iter().sum::<f64>();
    if sum < f64::EPSILON {
        for v in values.iter_mut() {
            *v = 0.0;
        }
        return;
    }
    for v in values.iter_mut() {
        *v /= sum;
    }
}
```

**注意**: `normalize` は for ループスタイルで実装する（ヒアリング確認済み）

### 2. mod.rs への追加 🔵

**信頼性**: 🔵 *要件 REQ-406・設計文書 §3 より*

`rust_core/src/sensitivity/mod.rs` に `mod tree_common;` を追加する（既存の `mod` 宣言群と同じ箇所に配置）。

### 3. rf_anova.rs の修正 🔵

**信頼性**: 🔵 *要件 REQ-405, REQ-501, REQ-502・設計文書 §4 より*

**ノート**: `rf_anova.rs` の重複箇所はコンテキストノートによると行131-167（`permute_single_column` 行131-154、`normalize` 行156-167）。

変更内容:
1. `use super::tree_common::{normalize, permute_single_column};` を import 節に追加
2. `use crate::core::lgbm::{lgbm_mse, mse_to_r_squared, ...}` に `mse_to_r_squared` が含まれていなければ追加
3. ローカルの `permute_single_column` 関数定義を削除（行131-154）
4. ローカルの `normalize` 関数定義を削除（行156-167）
5. R² 計算の独自実装（`ss_res`, `ss_tot` ベースの計算）を `mse_to_r_squared(baseline_mse, y)` 呼び出しに変更

### 4. permutation.rs の修正 🔵

**信頼性**: 🔵 *要件 REQ-405・設計文書 §5 より*

**ノート**: `permutation.rs` の重複箇所はコンテキストノートによると行126-155（`permute_single_column` 行126-146、`normalize` 行148-155）。

変更内容:
1. `use super::tree_common::{normalize, permute_single_column};` を import 節に追加
2. ローカルの `permute_single_column` 関数定義を削除（行126-146）
3. ローカルの `normalize` 関数定義を削除（行148-155）
4. `permutation.rs` はすでに `mse_to_r_squared` を使用しているため R² 計算は変更不要

## エッジケース対応

- EDGE-001: `permute_single_column` に空行列が渡された場合 `None` を返す → `if n == 0 { return None; }` で対応
- EDGE-002: `normalize` に合計値ゼロ以下の配列が渡された場合、全要素を 0.0 に設定 → `if sum < f64::EPSILON` で対応
