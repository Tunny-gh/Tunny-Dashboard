# TASK-2226: チャート色定数の更新（chart_colors.rs）

**タスクID**: TASK-2226
**タスクタイプ**: DIRECT
**推定工数**: 1時間
**フェーズ**: Phase 1 - 色定数更新
**信頼性レベル**: 🔵 *設計文書 architecture.md の新旧マッピングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/color-theme-refinement/requirements.md) - REQ-031〜REQ-073
- **設計文書**: [📐 architecture.md](../../design/color-theme-refinement/architecture.md)

## タスク概要

`egui-app/src/theme/chart_colors.rs` の全チャート色定数を、設計文書の新旧マッピングに従って
Google Material Design系パレットに更新する。3Dビューのライト化とハイライト色の変更も含む。

## 依存タスク

- **前提タスク**: なし（TASK-2225と並行可能）
- **後続タスク**: [TASK-2227: ビルド・テスト・視覚検証](TASK-2227.md)

## 完了条件

- [ ] chart_colors.rs の全定数が設計文書の新RGB値に更新されている
- [ ] premultiplied alpha値が新しいベース色から正しく再計算されている
- [ ] `cargo build` が警告なしで成功する
- [ ] `cargo test` が全テスト通過する

---

## 実装詳細

### 1. Pareto系の更新 🔵

**信頼性**: 🔵 *設計文書 architecture.md Pareto系マッピングより*

**実装ファイル**: `egui-app/src/theme/chart_colors.rs`

```rust
pub const COLOR_PARETO: Color32 = Color32::from_rgb(234, 67, 53);
pub const COLOR_NON_PARETO: Color32 = Color32::from_rgb(66, 133, 244);
pub const COLOR_PARETO_DIM: Color32 = Color32::from_rgba_premultiplied(55, 16, 12, 60);
pub const COLOR_NON_PARETO_DIM: Color32 = Color32::from_rgba_premultiplied(16, 31, 57, 60);
```

### 2. 3D軸色のパステル化 🔵

**信頼性**: 🔵 *設計ヒアリング「パステル系に変更」・設計文書より*

```rust
pub const COLOR_AXIS_X: Color32 = Color32::from_rgb(210, 100, 100); // was (220, 80, 80)
pub const COLOR_AXIS_Y: Color32 = Color32::from_rgb(80, 170, 80);   // was (80, 220, 80)
pub const COLOR_AXIS_Z: Color32 = Color32::from_rgb(100, 100, 200); // was (80, 80, 220)
```

### 3. MCDMスコア色の更新 🔵

**信頼性**: 🔵 *設計文書 architecture.md MCDM系マッピングより*

```rust
pub const COLOR_MCDM_HIGH: Color32 = Color32::from_rgb(234, 67, 53);
pub const COLOR_MCDM_MID: Color32 = Color32::from_rgb(251, 188, 4);
pub const COLOR_MCDM_LOW: Color32 = Color32::from_rgb(52, 168, 83);
pub const COLOR_MCDM_NONE: Color32 = Color32::from_rgb(200, 200, 200); // 変更なし
```

### 4. バー・チャート系の更新 🔵

**信頼性**: 🔵 *設計文書 architecture.md バー系マッピングより*

```rust
pub const COLOR_BAR_PRIMARY: Color32 = Color32::from_rgb(66, 133, 244);
pub const COLOR_BAR_NEGATIVE: Color32 = Color32::from_rgb(234, 67, 53);
pub const COLOR_BAR_ACCENT: Color32 = Color32::from_rgb(251, 188, 4);
pub const COLOR_IMPORTANCE_BAR: Color32 = Color32::from_rgb(30, 60, 114);
```

### 5. 最適化履歴系の更新 🔵

**信頼性**: 🔵 *設計文書 architecture.md 履歴系マッピングより*

```rust
pub const COLOR_OPT_TRIAL: Color32 = Color32::from_rgb(66, 133, 244);
pub const COLOR_OPT_PRUNED: Color32 = Color32::from_rgb(234, 67, 53);
pub const COLOR_OPT_RUNNING: Color32 = Color32::from_rgb(52, 168, 83);
pub const COLOR_OPT_BEST: Color32 = Color32::from_rgb(251, 188, 4); // was GOLD
```

### 6. HV・フィット品質系の更新 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

```rust
pub const COLOR_HV_LINE: Color32 = Color32::from_rgb(52, 168, 83);
pub const COLOR_FIT_LOW: Color32 = Color32::from_rgb(234, 67, 53);
pub const COLOR_FIT_MID: Color32 = Color32::from_rgb(251, 188, 4);
pub const COLOR_FIT_HIGH: Color32 = Color32::from_rgb(52, 168, 83);
```

### 7. PDP系の更新 🔵

**信頼性**: 🔵 *設計文書 architecture.md PDP系マッピングより*

```rust
pub const COLOR_PDP_LINE: Color32 = Color32::from_rgb(66, 133, 244);
pub const COLOR_PDP_CI: Color32 = Color32::from_rgba_premultiplied(13, 26, 48, 50);
pub const COLOR_ICE_LINE: Color32 = Color32::from_rgba_premultiplied(35, 35, 35, 60); // 変更なし
pub const COLOR_CONTOUR: Color32 = Color32::from_rgb(124, 77, 255); // was YELLOW → Deep Purple
```

### 8. スキャッタ・選択ハイライト系の更新 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

```rust
pub const COLOR_SCATTER_DOT: Color32 = Color32::from_rgb(66, 133, 244);
pub const COLOR_SELECTION_HIGHLIGHT: Color32 = Color32::from_rgba_premultiplied(10, 21, 38, 40);
pub const COLOR_CELL_HIGHLIGHT: Color32 = Color32::from_rgba_premultiplied(21, 42, 77, 80);
pub const COLOR_LINK: Color32 = Color32::from_rgb(66, 133, 244);
```

### 9. 3Dビューのライト化 🔵

**信頼性**: 🔵 *設計文書 architecture.md 3D系マッピングより*

```rust
pub const COLOR_3D_BG: Color32 = Color32::from_rgb(240, 242, 245);    // was (20, 20, 30)
pub const COLOR_3D_GRID: Color32 = Color32::from_rgba_premultiplied(33, 33, 36, 70);
```

### 10. ハイライト点の色変更 🔵

**信頼性**: 🔵 *設計ヒアリング「別色に変更」・設計文書より*

```rust
pub const COLOR_HIGHLIGHT_PT: Color32 = Color32::from_rgb(124, 77, 255); // was YELLOW → Deep Purple
```

### 11. パラレルコーディネート系の更新 🟡

**信頼性**: 🟡 *設計文書から妥当な推測*

```rust
pub const COLOR_PARALLEL_TICK: Color32 = Color32::from_rgb(95, 99, 104);
pub const COLOR_PARALLEL_LINE_DEFAULT: Color32 = Color32::from_rgb(66, 133, 244);
pub const COLOR_PARALLEL_AXIS: Color32 = Color32::from_rgb(218, 220, 224);
```

### 12. PDP CI凡例・AHP・チャート汎用色の更新 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

```rust
pub const COLOR_PDP_CI_LEGEND: Color32 = Color32::from_rgba_premultiplied(31, 63, 115, 120);
pub const COLOR_CR_OK: Color32 = Color32::from_rgb(52, 168, 83);
pub const COLOR_CHART_TEXT: Color32 = Color32::from_rgb(32, 33, 36);
pub const COLOR_EMPTY_STATE: Color32 = Color32::from_rgb(95, 99, 104);
pub const COLOR_GRID_STROKE: Color32 = Color32::from_rgb(218, 220, 224);
```

---

## テスト要件

### ビルド・テスト確認 🔵

- `cargo build` が警告なしで成功すること
- `cargo test` が全テスト通過すること（colormap.rsのテストを含む）

### premultiplied alpha整合性 🔵

- 全 premultiplied alpha 色のRGB成分がベース色から正しく計算されていること
- 計算式: `r_premul = r_base * alpha / 255`（四捨五入）

---

## 実装手順

1. `/tsumiki:direct-setup` - chart_colors.rs の全定数を新値に更新
2. `/tsumiki:direct-verify` - cargo build / cargo test で確認

---

## 注意事項

- `COLOR_OPT_BEST` は `Color32::GOLD` から `Color32::from_rgb(251, 188, 4)` に変更
- `COLOR_CONTOUR` と `COLOR_HIGHLIGHT_PT` は `Color32::YELLOW` から `Color32::from_rgb(124, 77, 255)` に変更
- `COLOR_CR_OK` は `Color32::GREEN` から `Color32::from_rgb(52, 168, 83)` に変更
- `COLOR_CHART_TEXT` は `Color32::BLACK` から `Color32::from_rgb(32, 33, 36)` に変更
- `COLOR_EMPTY_STATE` は `Color32::GRAY` から `Color32::from_rgb(95, 99, 104)` に変更
- `COLOR_GRID_STROKE` は `Color32::GRAY` から `Color32::from_rgb(218, 220, 224)` に変更

---

## 信頼性レベルサマリー

- **総項目数**: 13項目
- 🔵 **青信号**: 12項目 (92%)
- 🟡 **黄信号**: 1項目 (8%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
