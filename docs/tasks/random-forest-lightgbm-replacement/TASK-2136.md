# TASK-2136: 統合テスト・最終検証

**タスクID**: TASK-2136
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 4 - 統合検証
**信頼性レベル**: 🔵 *要件定義書受け入れ基準 + architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/random-forest-lightgbm-replacement/requirements.md) - REQ-001〜REQ-104, REQ-003, REQ-401
- **設計文書**: [📐 architecture.md](../../design/random-forest-lightgbm-replacement/architecture.md)
- **受け入れ基準**: [📋 acceptance-criteria.md](../../spec/random-forest-lightgbm-replacement/acceptance-criteria.md)

## タスク概要

全モジュール（2D PDP、SHAP、MDI、RF-ANOVA）の LightGBM 置き換えが完了した後、統合テストを実施して全機能が正常動作することを確認する。また `Lcg` を使う Kriging モジュールへの影響がないこと、egui-app 全体がビルド可能なことを検証する。

## 依存タスク

- **前提タスク**: [TASK-2132: 2D PDP 置き換え](TASK-2132.md), [TASK-2133: RF-ANOVA 置き換え](TASK-2133.md), [TASK-2134: MDI 置き換え](TASK-2134.md), [TASK-2135: SHAP 完全置き換え](TASK-2135.md)
- **後続タスク**: なし

## 完了条件

- [x] `cargo test -p tunny-core` が全テスト通過する
- [x] `cargo build -p egui-app` がエラーなく完了する
- [x] Kriging モジュールのテストが全て通る（Lcg 変更なし確認）
- [x] 2D PDP が `Some(PdpResult2d)` を返す
- [x] SHAP importances の合計が 1.0（または全ゼロ）
- [x] MDI importances の合計が 1.0（または全ゼロ）
- [x] RF-ANOVA importances の合計が 1.0（または全ゼロ）
- [x] `lightgbm` クレートのバージョンが `Cargo.toml` に固定されている（`*` でない）

---

## 実装詳細

### 1. 全テスト実行 🔵

**信頼性**: 🔵 *受け入れ基準より*

**実装ファイル**: 新規テストファイル `rust_core/src/tests/integration.rs`（または各モジュールのテスト内）

```bash
rtk cargo test -p tunny-core
```

失敗があればエラーを解析し、対応するタスク（TASK-2132〜2135）の修正を行う。

### 2. egui-app ビルド確認 🔵

**信頼性**: 🔵 *REQ-401 + NFR-201 より*

```bash
rtk cargo build -p egui-app
```

`tunny-core` が `egui-app` に正しくリンクされていることを確認する。

### 3. Lcg 疎通確認（Kriging 影響なし） 🔵

**信頼性**: 🔵 *REQ-003 + REQ-402 より*

```bash
grep -r "random_forest::Lcg" rust_core/src/core/kriging/
rtk cargo test -p tunny-core kriging
```

Kriging モジュールのテストが全て通ることで、`Lcg` のパス変更がないことを確認する。

### 4. `lightgbm` クレートバージョン固定 🟡

**信頼性**: 🟡 *設計文書残課題より（ビルド確認後に固定）*

`Cargo.lock` に記録された実際のバージョンを確認し、`rust_core/Cargo.toml` の `lightgbm = "*"` を固定バージョンに変更する:

```bash
grep "^name = \"lightgbm\"" -A 1 Cargo.lock
```

例: `lightgbm = "0.3.x"` のように固定する。

### 5. 旧コードの残存確認 🔵

**信頼性**: 🔵 *architecture.md 削除一覧より*

削除済みであるべきシンボルが残存していないことを確認:

```bash
grep -r "ShapNode\|PathElement\|MdiNode\|train_rf_on_columns\|compute_pdp_2d_rf" rust_core/src/
```

上記コマンドの出力が空であることを確認する。

---

## 単体テスト要件

### テストケース1: 全感度分析モジュールの統合テスト 🔵

**信頼性**: 🔵 *受け入れ基準 TC-001〜TC-004 より*

**テストファイル**: `rust_core/src/tests/integration.rs`（新規）

```rust
#[test]
fn test_shap_importances_sum_to_one() {
    let x = vec![vec![1.0, 2.0], vec![3.0, 4.0], /* ... 20サンプル以上 */];
    let y = vec![1.0, 2.0, /* ... */];
    let (importances, _r2) = compute_shap_importances(&x, &y);
    let sum: f64 = importances.iter().sum();
    assert!((sum - 1.0).abs() < 1e-6 || sum == 0.0);
}

#[test]
fn test_mdi_importances_sum_to_one() { /* 同様 */ }

#[test]
fn test_rf_anova_importances_sum_to_one() { /* 同様 */ }

#[test]
fn test_pdp_2d_returns_result() {
    // compute_pdp_2d が "random_forest" で Some を返すこと
}
```

### テストケース2: Kriging モジュール通過確認 🔵

**信頼性**: 🔵 *REQ-003 より*

**Given**: `random_forest/mod.rs` が `rng.rs` のみ残す形に更新済み
**When**: Kriging 関連テストを実行
**Then**: 全テストが通る

---

## 統合テスト要件

### 統合テスト1: egui-app フルビルド 🔵

**信頼性**: 🔵 *REQ-401 + NFR-201 より*

**テスト内容**: `cargo build -p egui-app` がエラーなく完了すること
**期待結果**: バイナリが生成される

### 統合テスト2: 旧シンボルの残存なし 🔵

**信頼性**: 🔵 *architecture.md 削除ファイル一覧より*

**テスト内容**: `grep` で削除対象シンボルが `rust_core/src/` に存在しないこと
**期待結果**: grep の出力が空

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-2136` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装（統合テスト記述）
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- このタスクは Phase 3 の全タスク（TASK-2132〜2135）が完了してから実施する
- テスト失敗があった場合は該当タスク（TASK-2132〜2135）に戻って修正する
- `lightgbm` バージョン固定は忘れずに実施すること（運用上重要）
- Windows 環境では `lib_lightgbm.dll` が実行時パスに存在することも確認する

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 4 | 1 | 0 | 5 |
| 単体テスト | 2 | 0 | 0 | 2 |
| 統合テスト | 2 | 0 | 0 | 2 |

### 全体評価

- **総項目数**: 9項目
- 🔵 **青信号**: 8項目 (89%)
- 🟡 **黄信号**: 1項目 (11%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質

**推測が多い項目**: lightgbm クレートバージョン（ビルド後に固定）
