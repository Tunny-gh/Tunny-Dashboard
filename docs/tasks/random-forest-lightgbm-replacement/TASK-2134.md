# TASK-2134: MDI の LightGBM 完全置き換え

**タスクID**: TASK-2134
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 3 - 各モジュール置き換え
**信頼性レベル**: 🔵 *REQ-103 + ユーザヒアリング（MDI互換性許容）+ dataflow.md 5. MDI データフローより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/random-forest-lightgbm-replacement/requirements.md) - REQ-103
- **設計文書**: [📐 architecture.md](../../design/random-forest-lightgbm-replacement/architecture.md)
- **型定義**: [📝 interfaces.rs](../../design/random-forest-lightgbm-replacement/interfaces.rs)
- **データフロー図**: [🔄 dataflow.md](../../design/random-forest-lightgbm-replacement/dataflow.md)

## タスク概要

`sensitivity/mdi.rs` の独自 `MdiNode` 木構造とゲイン集計実装を全廃し、LightGBM の `feature_importance(Gain)` と `train_lgbm_rf` に完全置き換えする。`compute_mdi_importances` の入出力シグネチャは変えない。

## 依存タスク

- **前提タスク**: [TASK-2131: core::lgbm SHAP/MDI関数実装 + random_forest整理](TASK-2131.md)
- **後続タスク**: [TASK-2136: 統合テスト・最終検証](TASK-2136.md)

## 完了条件

- [x] `MdiNode` enum が削除されている
- [x] `find_best_split_with_gain_idx`, `build_mdi_tree_idx`, `accumulate_gains` 関数が削除されている
- [x] `train_rf_on_columns`, `mse_on_dataset` への参照が削除されている
- [x] `compute_mdi_importances` が `train_lgbm_rf` + `lgbm_feature_importance` + `lgbm_mse` を使うように更新されている
- [x] 戻り値 `(Vec<f64>, f64)` のシグネチャは変更されていない
- [x] `cargo test -p tunny-core` の mdi テストが通る

---

## 実装詳細

### 1. 削除対象コードの特定 🔵

**信頼性**: 🔵 *architecture.md MDI完全置き換え設計 + コードベース調査より*

`sensitivity/mdi.rs` から以下を削除:
- `MdiNode` enum（`Leaf`, `Internal` バリアント）
- `find_best_split_with_gain_idx` 関数
- `build_mdi_tree_idx` 関数
- `accumulate_gains` 関数
- `train_rf_on_columns`, `mse_on_dataset` の use 文

### 2. `compute_mdi_importances` の実装置き換え 🔵

**信頼性**: 🔵 *REQ-103 + dataflow.md 5. MDI データフローより*

**実装ファイル**: `rust_core/src/sensitivity/mdi.rs`

**新しい実装の流れ**（dataflow.md のフロー通り）:
```rust
pub fn compute_mdi_importances(x_matrix: &[Vec<f64>], y: &[f64]) -> (Vec<f64>, f64) {
    let p = if x_matrix.is_empty() { 0 } else { x_matrix[0].len() };

    // ① データクリーニング（非有限値除去）— 既存ロジック流用
    // ② ダウンサンプリング（max MDI_MAX_ROWS=1000 行）— 既存ロジック流用
    // ③ Lcg でシャッフル → 80/20 ホールドアウト分割 — 既存ロジック流用
    let (x_train, y_train, x_eval, y_eval) = split_holdout(...);

    // ④ LightGBM RF 訓練（64木, 深度64 — MDI用設定）
    let config = LgbmRfConfig {
        num_iterations: RF_TREES,      // 64
        max_depth: RF_MAX_DEPTH,       // 64
        min_data_in_leaf: RF_MIN_SAMPLES_LEAF as i32,
        seed: RF_SEED as i32,
        ..Default::default()
    };
    let Some(booster) = train_lgbm_rf(&x_train, &y_train, &config) else {
        return (vec![0.0; p], 0.0);
    };

    // ⑤ lgbm_feature_importance(Gain) → MDI importances
    let importances = lgbm_feature_importance(&booster, p);

    // ⑥ lgbm_mse → R²
    let mse = lgbm_mse(&booster, &x_eval, &y_eval).unwrap_or(f64::INFINITY);
    let r_squared = mse_to_r_squared(mse, &y_eval);

    (importances, r_squared)
}
```

### 3. 定数の確認 🔵

**信頼性**: 🔵 *spec/note.md ハイパーパラメータ対応表より*

```rust
const RF_TREES: usize = 64;
const RF_MAX_DEPTH: usize = 64;   // MDI は深度64（SHAP と異なる）
const RF_MIN_SAMPLES_LEAF: usize = 2;
const RF_SEED: u64 = 42;
const MDI_MAX_ROWS: usize = 1000;
```

### 4. フォールバック処理 🟡

**信頼性**: 🟡 *dataflow.md 8. エラーハンドリングフロー + EDGE-001 から妥当な推測*

`train_lgbm_rf` が `None` を返した場合（データ不足等）は `(vec![0.0; p], 0.0)` を返す。

---

## 単体テスト要件

### テストケース1: compute_mdi_importances 基本動作 🔵

**信頼性**: 🔵 *REQ-103 より*

**Given**: 20サンプル3特徴量の合成データ
**When**: `compute_mdi_importances` を呼び出す
**Then**: importances の長さが 3、合計が 1.0（または全ゼロ）、r_squared が有限値

### テストケース2: データ不足時のフォールバック 🟡

**信頼性**: 🟡 *EDGE-001 から妥当な推測*

**Given**: 2サンプル以下の極小データ
**When**: `compute_mdi_importances` を呼び出す
**Then**: `(vec![0.0; p], 0.0)` が返る（パニックしない）

---

## 統合テスト要件

### 統合テスト1: MDI 結果が正規化されていること 🔵

**信頼性**: 🔵 *REQ-103 より*

**テスト内容**: `compute_mdi_importances` の戻り値 `importances` の合計が 1.0 以下
**期待結果**: `importances.iter().sum::<f64>() <= 1.0 + 1e-9`

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2134` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装（MdiNode削除 + LightGBM置き換え）
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- MDI の計算結果は変わる（ユーザヒアリングで許容済み）
- `max_depth = 64` は MDI 固有の設定（SHAP や RF-ANOVA の `10` とは異なる）
- `Lcg` シャッフルは既存ロジックを流用（REQ-003）

---

## 信頼性レベルサマリー

### 全体評価

- **総項目数**: 7項目
- 🔵 **青信号**: 5項目 (71%)
- 🟡 **黄信号**: 2項目 (29%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質

**推測が多い項目**: フォールバック処理、データ不足時の挙動
