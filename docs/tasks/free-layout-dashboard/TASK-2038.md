# TASK-2038: right_panel.rs 新規作成（ウィジェット一覧・ハンバーガーボタン）

**タスクID**: TASK-2038
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - 右パネル実装
**信頼性レベル**: 🔵 *設計文書 architecture.md・ユーザーヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/free-layout-dashboard/architecture.md)

## タスク概要

`egui-app/src/ui/right_panel.rs` を新規作成する。
右パネルは `PanelItem::all()` のリストを表示し、グリッドに配置済みのアイテムはグレーアウトする。
ハンバーガーボタン（≡）で開閉切り替えができる。

## 依存タスク

- **前提タスク**: [TASK-2035](TASK-2035.md), [TASK-2037](TASK-2037.md)
- **後続タスク**: [TASK-2039](TASK-2039.md)

## 完了条件

- [x] `right_panel.rs` が作成され `show_right_panel(ui, app_state, layout)` が公開されている
- [x] `PanelItem::all()` のリストが表示される
- [x] グリッドに配置済みのアイテムはグレーアウトで表示される
- [x] ハンバーガーボタンで `layout.right_panel.is_open` が切り替わる
- [x] `ui/mod.rs` に `right_panel` モジュールが追加されている

---

## 実装詳細

### 1. show_right_panel() の実装 🔵

**信頼性**: 🔵 *ユーザーヒアリング（ハンバーガーメニュー）より*

**実装ファイル**: `egui-app/src/ui/right_panel.rs`

```rust
use crate::state::app_state::AppState;
use crate::state::layout_state::{LayoutState, PanelItem};

pub fn show_right_panel(ui: &mut egui::Ui, app_state: &AppState, layout: &mut LayoutState) {
    // ≡ ボタンで開閉
    if ui.button("≡").clicked() {
        layout.right_panel.is_open = !layout.right_panel.is_open;
    }
    if !layout.right_panel.is_open {
        return;
    }
    ui.separator();
    ui.heading("Widgets");

    let placed: Vec<&PanelItem> = layout.grid.placed_items();

    for item in PanelItem::all() {
        let is_placed = placed.contains(&&item);
        if is_placed {
            ui.add_enabled(false, egui::Label::new(item.label()));
        } else {
            // D&D ソースとして登録（TASK-2042 で実装）
            ui.label(item.label());
        }
    }
}
```

### 2. ui/mod.rs への追加 🔵

**信頼性**: 🔵 *既存パターンに合わせる*

```rust
pub mod right_panel;
```

---

## 単体テスト要件

### テストケース1: is_open 切り替えロジック 🔵

**Given**: `layout.right_panel.is_open = true`
**When**: ≡ ボタンクリックをシミュレート（`is_open = !is_open`）
**Then**: `layout.right_panel.is_open = false`

---

## UI/UX要件

### グレーアウト表示 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

- 配置済みアイテムは `add_enabled(false, ...)` でグレーアウト表示
- 未配置アイテムは通常のラベル/ボタンとして表示

### 開閉状態 🔵

- パネルを閉じると一覧が非表示（is_open = false のとき早期リターン）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2038` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 7件 (87.5%)
- 🟡 黄信号: 1件 (12.5%)
- 🔴 赤信号: 0件

**品質評価**: ✅ 高品質
