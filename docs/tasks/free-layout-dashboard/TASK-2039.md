# TASK-2039: layout.rs に右パネル SidePanel::right を追加

**タスクID**: TASK-2039
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - 右パネル実装
**信頼性レベル**: 🔵 *設計文書 architecture.md・ユーザーヒアリングより*

## 関連文書
- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/free-layout-dashboard/architecture.md)

## タスク概要
`egui-app/src/ui/layout.rs` の `show_layout()` に `egui::SidePanel::right("right_panel")` を追加し、`show_right_panel()` を呼び出す。既存の4エリアレイアウト（top/left/central）に右パネルを追加する5エリアレイアウトにする。

## 依存タスク
- **前提タスク**: [TASK-2038](TASK-2038.md)
- **後続タスク**: [TASK-2040](TASK-2040.md)

## 完了条件
- [x] `layout.rs` に `egui::SidePanel::right("right_panel")` が追加されている
- [x] 右パネルの幅が `layout.right_panel.width` でリサイズ可能
- [x] アプリを起動すると右側にパネルが表示される
- [x] `cargo check` が通る

## 実装詳細

### layout.rs の変更 🔵
**実装ファイル**: `egui-app/src/ui/layout.rs`

```rust
egui::SidePanel::right("right_panel")
    .resizable(true)
    .default_width(app.layout.right_panel.width)
    .width_range(100.0..=400.0)
    .show(ctx, |ui| {
        show_right_panel(ui, &app.app_state, &mut app.layout);
    });
```

SidePanel は bottom_panel 削除後の Central Panel より前に宣言する。

## 単体テスト要件
### テストケース1: validate_panel_constraints 更新 🟡
既存の `validate_panel_constraints()` は `bottom_height` を使っているが不要になるため、関数シグネチャを更新する（または削除）。

## 実装手順
1. `/tsumiki:tdd-requirements TASK-2039`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

## 信頼性レベルサマリー
- 🔵 青信号: 4件 (80%)
- 🟡 黄信号: 1件 (20%)
- 🔴 赤信号: 0件
**品質評価**: ✅ 高品質
