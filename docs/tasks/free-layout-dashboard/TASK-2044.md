# TASK-2044: main_canvas.rs を grid_canvas.rs への委譲に変更

**タスクID**: TASK-2044
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 5 - 統合
**信頼性レベル**: 🔵 *設計文書 architecture.md より*

## 関連文書
- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/free-layout-dashboard/architecture.md)

## タスク概要
既存の `egui-app/src/ui/main_canvas.rs` の内容を `grid_canvas.rs` に委譲するよう書き換える。現在の固定2×2グリッドロジックを削除し、`show_grid_canvas()` を呼び出すだけにする。`show_main_canvas()` 関数自体は残し、引数シグネチャは維持する。

## 依存タスク
- **前提タスク**: [TASK-2043](TASK-2043.md)
- **後続タスク**: [TASK-2045](TASK-2045.md)

## 完了条件
- [ ] `main_canvas.rs` が `show_grid_canvas()` に委譲している
- [ ] 固定2×2グリッドのコードが削除されている
- [ ] `show_panel_chart()` / `show_grid_row()` / `show_empty_panel()` が `grid_canvas.rs` へ移動または削除されている
- [ ] `cargo test` がすべて通る

## 実装詳細

### main_canvas.rs の簡素化 🔵
```rust
pub fn show_main_canvas(
    ui: &mut egui::Ui,
    app_state: &mut AppState,
    layout: &mut LayoutState,
    widgets: &mut WidgetStates,
) {
    if app_state.current_study.is_none() {
        ui.centered_and_justified(|ui| {
            if app_state.all_studies.is_empty() {
                ui.label("Open a journal file to start.");
            } else {
                ui.label("Select a study from the toolbar.");
            }
        });
        return;
    }
    crate::ui::grid_canvas::show_grid_canvas(ui, app_state, layout, widgets);
}
```

## 単体テスト要件
### テストケース1: スタディ未選択時のガイダンス表示 🔵
既存テスト（スタディ未選択→ガイダンス）が引き続き通ることを確認。

## 実装手順
1. `/tsumiki:tdd-requirements TASK-2044`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

## 信頼性レベルサマリー
- 🔵 青信号: 5件 (100%)
- 🟡 黄信号: 0件
- 🔴 赤信号: 0件
**品質評価**: ✅ 高品質
