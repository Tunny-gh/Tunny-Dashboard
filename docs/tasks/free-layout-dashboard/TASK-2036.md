# TASK-2036: TrialTable を widgets/trial_table.rs に移植

**タスクID**: TASK-2036
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - TrialTable独立化・左パネル整理
**信頼性レベル**: 🔵 *設計文書 architecture.md・ユーザーヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/free-layout-dashboard/architecture.md)

## タスク概要

`egui-app/src/ui/bottom_panel.rs` のトライアルテーブル描画ロジックを
`egui-app/src/ui/widgets/trial_table.rs` に移植する。
`TrialTableWidget` 構造体として公開し、`show(ui, app_state)` メソッドで描画できるようにする。
`PanelItem::TrialTable` としてグリッドキャンバスから呼び出せる形にする。

## 依存タスク

- **前提タスク**: [TASK-2033](TASK-2033.md)
- **後続タスク**: [TASK-2037](TASK-2037.md), [TASK-2041](TASK-2041.md)

## 完了条件

- [x] `egui-app/src/ui/widgets/trial_table.rs` が作成されている
- [x] `TrialTableWidget` 構造体が `show(ui, app_state)` メソッドを持つ
- [x] `widgets/mod.rs` に `trial_table` モジュールが追加されている
- [x] `get_display_rows()` ロジックが移植されている
- [x] 既存 `bottom_panel.rs` のテストが `trial_table.rs` 側に移植・通過する

---

## 実装詳細

### 1. widgets/trial_table.rs の新規作成 🔵

**信頼性**: 🔵 *bottom_panel.rs の移植*

**実装ファイル**: `egui-app/src/ui/widgets/trial_table.rs`

```rust
use crate::state::app_state::AppState;

#[derive(Default)]
pub struct TrialTableWidget;

impl TrialTableWidget {
    pub fn show(&mut self, ui: &mut egui::Ui, app_state: &mut AppState) {
        // bottom_panel.rs の show_bottom_panel ロジックをそのまま移植
    }
}
```

### 2. get_display_rows() の移植 🔵

**信頼性**: 🔵 *bottom_panel.rs から移植*

`get_display_rows()` 関数を `trial_table.rs` に移動し、pub fn として公開する。

### 3. widgets/mod.rs への追加 🔵

**信頼性**: 🔵 *既存パターンに合わせる*

```rust
pub mod trial_table;
```

---

## 単体テスト要件

### テストケース1: get_display_rows() が全件返す（selected_indices 空） 🔵

既存テストを移植する。

### テストケース2: get_display_rows() がフィルタリングする 🔵

既存テストを移植する。

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2036` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 8件 (100%)
- 🟡 黄信号: 0件
- 🔴 赤信号: 0件

**品質評価**: ✅ 高品質
