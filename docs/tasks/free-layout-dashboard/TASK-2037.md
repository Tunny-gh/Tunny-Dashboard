# TASK-2037: BottomPanel 廃止・LeftPanel チャート選択削除

**タスクID**: TASK-2037
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 2 - TrialTable独立化・左パネル整理
**信頼性レベル**: 🔵 *設計文書 architecture.md・ユーザーヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/free-layout-dashboard/architecture.md)

## タスク概要

1. `egui-app/src/ui/layout.rs` から `egui::TopBottomPanel::bottom("bottom_panel")` の呼び出しを削除する。
2. `egui-app/src/ui/left_panel.rs` から `show_chart_selection()` 関数と呼び出し箇所を削除する。
   左パネルは Study Info / Filters / Color Mode のみに整理する。
3. `egui-app/src/ui/bottom_panel.rs` は将来的に削除するが、このタスクでは参照を外すだけでよい。

## 依存タスク

- **前提タスク**: [TASK-2036](TASK-2036.md), [TASK-2035](TASK-2035.md)
- **後続タスク**: [TASK-2038](TASK-2038.md)

## 完了条件

- [x] `layout.rs` から `show_bottom_panel` の呼び出しが削除されている
- [x] `left_panel.rs` から `show_chart_selection` が削除されている
- [x] `cargo check` がエラーなく通る
- [x] アプリが起動してツールバー・左パネル（フィルター）・中央パネルが表示される

---

## 実装詳細

### 1. layout.rs の変更 🔵

**信頼性**: 🔵 *ユーザーヒアリング（BottomPanel廃止）より*

`egui::TopBottomPanel::bottom("bottom_panel")` ブロックを削除する。
`layout.bottom_panel_height` のフィールドも不要になるが、
`LayoutState` への変更は最小限にとどめ、後続タスクで整理する（🟡）。

### 2. left_panel.rs の変更 🔵

**信頼性**: 🔵 *ユーザーヒアリング（左パネルをフィルター専用化）より*

- `show_chart_selection()` 関数を削除
- `show_left_panel()` 内の `show_chart_selection(ui, layout)` の呼び出しを削除
- `ChartId` のインポートも不要なら削除

---

## 実装手順

1. `/tsumiki:direct-setup` - 変更実施
2. `/tsumiki:direct-verify` - `cargo check` で確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 5件 (83%)
- 🟡 黄信号: 1件 (17%)
- 🔴 赤信号: 0件

**品質評価**: ✅ 高品質
