# TASK-2003: AppState + 型定義実装

**タスクID**: TASK-2003
**タスクタイプ**: TDD
**推定工数**: 4h
**フェーズ**: Phase 2 - 状態管理
**信頼性レベル**: 🔵 *設計文書・ユーザーヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **アーキテクチャ**: [📐 architecture.md](../../design/egui-migration/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/egui-migration/dataflow.md)
- **型定義**: [📝 interfaces.rs](../../design/egui-migration/interfaces.rs)

## タスク概要

`AppState`・`StudyMeta`・`TrialRow`・`GpuBufferData` 等の型定義と AppState の基本操作を実装する。interfaces.rs の設計に従い、egui-app クレートの状態管理基盤を構築する。

## 依存タスク

- **前提タスク**: [TASK-2002](TASK-2002.md)
- **後続タスク**: [TASK-2004](TASK-2004.md), [TASK-2006](TASK-2006.md)

## 完了条件

- [x] AppState 構造体の全フィールドが定義される
- [x] 型定義が interfaces.rs の設計通りに実装される
- [x] `cargo test -p tunny-desktop` で AppState のテストが通る

---

## 実装詳細

### 1. AppState struct 実装 🔵

**信頼性**: 🔵 *設計文書より*

`egui-app/src/state/app_state.rs` に AppState を実装する。

```rust
pub struct AppState {
    pub all_studies: Vec<StudyMeta>,
    pub current_study: Option<StudyContext>,
    pub selected_indices: Vec<u32>,
    pub filter_ranges: HashMap<String, (f64, f64)>,
    pub highlighted_trial: Option<u32>,
    pub color_mode: ColorMode,
    pub sensitivity_result: Option<SensitivityResult>,
    pub cluster_result: Option<ClusterResult>,
    pub downsample_cache: DownsampleCache,
    pub live_update: LiveUpdateState,
}
```

### 2. 基本型定義 🔵

**信頼性**: 🔵 *設計文書より*

以下の型を定義する。

- `StudyMeta`: study_id, study_name, n_trials, n_objectives, n_params, directions
- `StudyContext`: meta, trial_rows, gpu_data, pareto_indices
- `TrialRow`: trial_id, params (HashMap), values (Vec<f64>), state, datetime_start
- `Direction`: enum (Minimize, Maximize)
- `TrialState`: enum (Complete, Pruned, Running, Waiting, Failed)

### 3. ColorMode enum 🔵

**信頼性**: 🔵 *設計文書より*

```rust
pub enum ColorMode {
    ParetoRank,
    ObjectiveValue(String),
    TrialNumber,
    ClusterId,
}
```

### 4. GpuBufferData struct 🔵

**信頼性**: 🔵 *設計文書より*

```rust
pub struct GpuBufferData {
    pub positions: Vec<f32>,
    pub colors: Vec<f32>,
    pub sizes: Vec<f32>,
}
```

### 5. DownsampleCache struct 🔵

**信頼性**: 🔵 *設計文書より*

```rust
pub struct DownsampleCache {
    pub scatter: Option<Vec<u32>>,
    pub pcp: Option<Vec<u32>>,
    pub thumbnail: Option<Vec<u32>>,
}
```

### 6. AppState コンストラクタ・メソッド 🔵

**信頼性**: 🔵 *設計文書より*

- `AppState::new()`: 全フィールドをデフォルト値で初期化
- `AppState::clear()`: Study切り替え時に selected_indices・filter_ranges・highlighted_trial・sensitivity_result・cluster_result・downsample_cache をリセット

---

## テスト要件

### AppState::new() の初期状態確認 🔵

**信頼性**: 🔵 *設計より*

- `all_studies` が空の Vec であることを確認する
- `current_study` が None であることを確認する
- `selected_indices` が空の Vec であることを確認する
- `filter_ranges` が空の HashMap であることを確認する

### AppState::clear() 動作確認 🔵

**信頼性**: 🔵 *設計より*

- clear() 呼び出し後に selected_indices が空になることを確認する
- clear() 呼び出し後に filter_ranges が空になることを確認する
- clear() 呼び出し後に highlighted_trial が None になることを確認する

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-2003` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 7件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: ✅ 高品質
