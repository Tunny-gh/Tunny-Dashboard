# TASK-2155: 既存インポートの切り替えと重複削除

**タスクID**: TASK-2155
**タスクタイプ**: DIRECT
**推定工数**: 0.5時間
**フェーズ**: Phase 1 - linspace 共通化
**信頼性レベル**: 🔵 *設計文書 architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/pdp-linspace-dedup/requirements.md) - REQ-002, REQ-003, REQ-004
- **設計文書**: [📐 architecture.md](../../design/pdp-linspace-dedup/architecture.md)

## タスク概要

既存の `pdp_linspace`（lgbm.rs）と `linspace`（pdp/utils.rs）を削除し、全呼び出し元を `core::math::grid::linspace` に切り替える。

## 依存タスク

- **前提タスク**: [TASK-2154: core::math::grid モジュール新規作成](TASK-2154.md)
- **後続タスク**: なし（最終タスク）

## 完了条件

- [ ] `core/lgbm.rs` から `pdp_linspace` 定義が削除されている
- [ ] `pdp/utils.rs` から `linspace` 定義が削除されている
- [ ] `pdp/ridge_core.rs` の import が `crate::core::math::grid::linspace` に変更されている
- [ ] `pdp/kriging_core.rs` の import が `crate::core::math::grid::linspace` に変更されている
- [ ] `cargo test --workspace` が全テスト通過（704 passed, 4 ignored）

---

## 主要実装項目

### lgbm.rs の修正 🔵

**信頼性**: 🔵 *設計文書 architecture.md 詳細設計2より*

- `use crate::core::math::grid::linspace;` を追加
- `fn pdp_linspace(...)` 定義（行363-373）を削除
- `pdp_linspace` → `linspace` に呼び出し置換（3箇所）

**実装ファイル**: `rust_core/src/core/lgbm.rs`

### ridge_core.rs の import 分離 🔵

**信頼性**: 🔵 *設計文書 architecture.md 呼び出し元への影響分析より*

```rust
// 変更前
use super::utils::{col_mean_std, linspace};
// 変更後
use super::utils::col_mean_std;
use crate::core::math::grid::linspace;
```

**実装ファイル**: `rust_core/src/pdp/ridge_core.rs`

### kriging_core.rs の import 変更 🔵

**信頼性**: 🔵 *設計文書 architecture.md 呼び出し元への影響分析より*

```rust
// 変更前
use super::utils::linspace;
// 変更後
use crate::core::math::grid::linspace;
```

**実装ファイル**: `rust_core/src/pdp/kriging_core.rs`

### pdp/utils.rs の linspace 削除 🔵

**信頼性**: 🔵 *設計文書 architecture.md 詳細設計3より*

`pub(super) fn linspace(...)` 定義を削除。`col_mean_std` は残存。

**実装ファイル**: `rust_core/src/pdp/utils.rs`

---

## 基本的なテスト要件

### 全テストスイート通過 🔵

**信頼性**: 🔵 *要件定義書 REQ-004 より*

- `cargo test --workspace` が 704 passed, 4 ignored
- `cargo clippy --workspace` が No issues found

---

## 実装手順

1. `/tsumiki:direct-setup` - 4ファイルの import 切り替え・重複削除
2. `/tsumiki:direct-verify` - cargo test + clippy で回帰確認

---

## 信頼性レベルサマリー

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
