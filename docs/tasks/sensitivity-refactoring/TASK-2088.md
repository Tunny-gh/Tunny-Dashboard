# TASK-2088: mdi.rs / shap.rs を prepare_training_data に統一

**タスクID**: TASK-2088
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - 重複排除
**信頼性レベル**: 🔵 *ユーザヒアリング: prepare_training_data に統一・コード分析より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/sensitivity-refactoring/requirements.md) - REQ-004, NFR-003
- **設計文書**: [📐 architecture.md](../../design/sensitivity-refactoring/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/sensitivity-refactoring/dataflow.md)

## タスク概要

`mdi.rs` と `shap.rs` は現在、NaN/Inf フィルタリング・ダウンサンプリング・80/20ホールドアウト分割・Fisher-Yatesシャッフルを独自実装している。これらを `tree_common::prepare_training_data` に置き換え、コードの重複を排除する（約100〜120行削減）。また `constants.rs` からの定数インポートに切り替える。

## 依存タスク

- **前提タスク**:
  - [TASK-2085: sensitivity/constants.rs の新規作成](TASK-2085.md)
  - [TASK-2086: sensitivity/types.rs Newtypeパターンへの変更](TASK-2086.md)
- **後続タスク**:
  - [TASK-2087: sensitivity/metrics.rs の新規作成](TASK-2087.md)（並行可）
  - [TASK-2092: 全テスト・検証](TASK-2092.md)

## 完了条件

- [ ] `mdi.rs` の NaN/Inf フィルタリング・ダウンサンプリング・80/20分割・シャッフルのインライン実装が削除されている
- [ ] `mdi.rs` が `tree_common::prepare_training_data` を使用している
- [ ] `shap.rs` の同様のインライン実装が削除されている
- [ ] `shap.rs` が `tree_common::prepare_training_data` を使用している
- [ ] `mdi.rs` と `shap.rs` が `constants.rs` から定数をインポートしている（ローカルの `const` 定義が削除されている）
- [ ] `cargo test -p rust_core` で既存の mdi/shap 関連テスト（29件中）が全てパスする

---

## 実装詳細

### 1. mdi.rs のリファクタリング 🔵

**信頼性**: 🔵 *コード分析・ユーザヒアリングより*

**実装ファイル**: `rust_core/src/sensitivity/mdi.rs`

**削除するコード**（約55行）:
- `const RF_TREES`, `RF_MAX_DEPTH`, `RF_MIN_SAMPLES_LEAF`, `RF_SEED`, `MDI_MAX_ROWS` の定義
- NaN/Inf フィルタリング: `valid_indices: Vec<usize>` の計算
- フィルタ後データの clone + ダウンサンプリング判定
- 80/20 split 計算（`MIN_EVAL`, `MIN_TRAIN`, `use_holdout`, `split_idx`）
- Fisher-Yates シャッフル（`shuffle_idx`, `rng_split`）
- `x_shuffled`, `y_shuffled` の組み立て

**追加するコード**:
```rust
use super::constants::*;
use super::tree_common::prepare_training_data;

pub fn compute_mdi_importances(x_matrix: &[Vec<f64>], y: &[f64]) -> (Vec<f64>, f64) {
    // ... 既存の入力検証（n < 2, p == 0）

    let Some(data) = prepare_training_data(
        x_matrix, y,
        MDI_MAX_ROWS,
        RF_SEED,
        RF_SEED.wrapping_add(1),
    ) else {
        return (vec![0.0; x_matrix[0].len()], 0.0);
    };

    let p = x_matrix[0].len();
    let (x_train, y_train, x_eval, y_eval) = data.split();

    // LightGBM 訓練と重要度計算（既存コード維持）
    let config = LgbmRfConfig { ... };
    // ...
}
```

`PreparedData` に `split()` メソッドが必要な場合は `tree_common.rs` に追加する（または split_idx を使って直接スライス）。

### 2. shap.rs のリファクタリング 🔵

**信頼性**: 🔵 *コード分析・ユーザヒアリングより*

**実装ファイル**: `rust_core/src/sensitivity/shap.rs`

mdi.rs と同様のパターンで、`prepare_training_data` を使う形にリファクタリングする。
削除されるコード量は同程度（約55行）。

### 3. 定数インポートへの切り替え 🔵

**信頼性**: 🔵 *REQ-005-1 より*

`mdi.rs` と `shap.rs` のローカル `const` 定義を削除し、以下のインポートに置き換える:
```rust
use super::constants::{
    RF_TREES, RF_MAX_DEPTH, RF_MIN_SAMPLES_LEAF,
    RF_SEED, MDI_MAX_ROWS, SHAP_MAX_ROWS,
};
```

---

## 単体テスト要件

### テストケース1: MDI 正常系（既存テスト維持） 🔵

**信頼性**: 🔵 *REQ-006-1 より*

`mdi.rs` 内の既存テスト:
- `importances_sum_to_one`: リファクタリング後も重要度の合計が 1.0
- `dominant_feature_ranks_first`: 支配的な特徴量が最高重要度

これらが変更なくパスすることを確認。

### テストケース2: SHAP 正常系（既存テスト維持） 🔵

**信頼性**: 🔵 *REQ-006-1 より*

`shap.rs` 内の既存テスト（あれば）が変更なくパスすること。

### テストケース3: 統合テスト（TC-801-10〜14） 🔵

**信頼性**: 🔵 *REQ-006-1 より*

`tests.rs` の統合テストがパスすること。特に MDI / SHAP を含む計算が含まれるテスト。

---

## 統合テスト要件

### パフォーマンステスト 🔵

**信頼性**: 🔵 *REQ-006-2: パフォーマンス制約より*

`cargo test --release` で TC-801-P01〜P03 がパスすること。
`prepare_training_data` に統一することでクローン削減の効果が期待される（パフォーマンス改善または維持）。

---

## 実装手順

1. `/tsumiki:tdd-red` - テスト実行して現状を確認
2. `/tsumiki:tdd-green` - mdi.rs を prepare_training_data を使う形にリファクタリング → テストパス確認
3. `/tsumiki:tdd-green` - shap.rs を同様にリファクタリング → テストパス確認
4. `/tsumiki:tdd-refactor` - コードの整理・不要な import 削除
5. `/tsumiki:tdd-verify-complete` - 全テストパス・コード削減量確認

---

## 注意事項

- `prepare_training_data` の引数 `split_seed` には既存の `RF_SEED.wrapping_add(1)` を使う（再現性維持）
- `PreparedData` の `split_idx` を使ってトレーニング/評価データに分割する
- LightGBM の訓練ロジック自体（`train_lgbm_rf`, `lgbm_feature_importance` 等）は変更しない
- `mdi.rs` の `compute_mdi_importances` 関数のシグネチャ（公開API）は変更しない

---

## 信頼性レベルサマリー

- **総項目数**: 8項目
- 🔵 **青信号**: 8項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
