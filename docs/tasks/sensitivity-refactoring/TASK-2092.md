# TASK-2092: 全テスト実行・パフォーマンステスト検証・API維持確認

**タスクID**: TASK-2092
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 4 - 検証
**信頼性レベル**: 🔵 *REQ-006・REQ-007・NFR-001〜002 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定数**: [📋 requirements.md](../../spec/sensitivity-refactoring/requirements.md) - REQ-006, REQ-007, NFR-001, NFR-002
- **受け入れ基準**: [✅ acceptance-criteria.md](../../spec/sensitivity-refactoring/acceptance-criteria.md)

## タスク概要

全タスク完了後の最終検証タスク。既存29件のテストパス確認、パフォーマンステストの回帰確認、パブリックAPIの維持確認、コードの行数削減確認を行う。

## 依存タスク

- **前提タスク**: 全タスク (TASK-2084〜TASK-2091)
- **後続タスク**: なし（最終タスク）

## 完了条件

- [ ] `cargo test -p rust_core` で29件全てパスする
- [ ] `cargo test -p rust_core --release` でパフォーマンステストがパスする
- [ ] `cargo build -p rust_core --release` がエラーなく完了する
- [ ] `sensitivity/mod.rs` の `pub use` シンボル一覧が変更前と一致する
- [ ] `pdp/mod.rs` の `pub use` シンボル一覧が変更前と一致する
- [ ] `sensitivity/` + `pdp/` の合計行数がリファクタリング前より削減されている

---

## 実装詳細

### 1. テストスイートの実行 🔵

**信頼性**: 🔵 *REQ-006-1 より*

```bash
# debug ビルドで全テスト実行
cargo test -p rust_core

# release ビルドでパフォーマンステスト実行
cargo test -p rust_core --release
```

期待結果: 全29件パス（うちパフォーマンステスト3件は release のみ）

### 2. パフォーマンステストの確認 🔵

**信頼性**: 🔵 *REQ-006-2 より*

パフォーマンス制約（50k×30×4 行のデータ）:

| テスト | 制約 | テストID |
|--------|------|---------|
| Spearman | ≤500ms | TC-801-P01 |
| Ridge | ≤300ms | TC-801-P02 |
| Selected | ≤50ms | TC-801-P03 |

リファクタリング前と比較して 5% 以上の劣化がないこと（NFR-001）。

### 3. パブリックAPIの維持確認 🔵

**信頼性**: 🔵 *REQ-007-1, REQ-007-2 より*

`sensitivity/mod.rs` の `pub use` シンボル確認:

```rust
// 変更不可シンボル一覧
pub use types::{TreeImportanceResult, SensitivityResult, SensitivityMetric,
               RidgeResult, SobolResult,
               RfAnovaResult, MdiResult, ShapResult, PermutationResult};
pub use {compute_sensitivity, compute_sensitivity_all, compute_sensitivity_selected,
         compute_sensitivity_single_obj, compute_sensitivity_without_mdi,
         compute_mdi_importances, compute_permutation_importances,
         compute_rf_anova_importances, compute_ridge, compute_shap_importances,
         compute_sobol, compute_sobol_from_df, compute_spearman};
```

`pdp/mod.rs` の `pub use` シンボル確認も同様に実施。

### 4. コード行数の確認 🟡

**信頼性**: 🟡 *NFR-003 から妥当な推測*

```bash
# 行数カウント
find rust_core/src/sensitivity -name "*.rs" | xargs wc -l
find rust_core/src/pdp -name "*.rs" | xargs wc -l
```

目標: リファクタリング前（約2,650行）より 10% 以上削減（約2,385行以下）。

---

## 単体テスト要件

### テストケース1: 全29テストパス確認 🔵

**信頼性**: 🔵 *REQ-006-1 より*

`cargo test -p rust_core` と `cargo test -p rust_core --release` の両方でパスすること。

### テストケース2: API シンボルの一致確認 🔵

**信頼性**: 🔵 *REQ-007-1 より*

`sensitivity/mod.rs` の `pub use` が以下のシンボルをエクスポートしていること:
- `compute_sensitivity`, `compute_sensitivity_all`, `compute_sensitivity_selected`
- `compute_sensitivity_single_obj`, `compute_sensitivity_without_mdi`
- `compute_mdi_importances`, `compute_permutation_importances`
- `compute_rf_anova_importances`, `compute_ridge`, `compute_shap_importances`
- `compute_sobol`, `compute_sobol_from_df`, `compute_spearman`
- `MdiResult`, `PermutationResult`, `RfAnovaResult`, `RidgeResult`
- `SensitivityMetric`, `SensitivityResult`, `ShapResult`, `SobolResult`, `TreeImportanceResult`

---

## 統合テスト要件

### 受け入れ基準の全項目確認 🔵

**信頼性**: 🔵 *acceptance-criteria.md より*

22件のテストケース（TC-REF-001-01 〜 TC-REF-007-02）が全てパスすること。

---

## 実装手順

1. `/tsumiki:direct-setup` - 確認スクリプトの準備（必要に応じて）
2. `/tsumiki:direct-verify` - 以下の検証を実行:
   - `cargo test -p rust_core` の実行
   - `cargo test -p rust_core --release` の実行
   - `sensitivity/mod.rs` の `pub use` 確認
   - `pdp/mod.rs` の `pub use` 確認
   - 行数カウントの実行と比較

---

## 注意事項

- このタスクは検証のみであり、新規コードの実装は行わない
- テストが失敗した場合は、該当する前段タスクに戻って修正する
- パフォーマンステストの数値は release ビルドでのみ評価する（debug は参照値）
- 行数削減の確認には `core/math/stats.rs` と `sensitivity/constants.rs` の新規ファイル分を含めて計算する

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 1 | 0 | 4 |
| 単体テスト | 2 | 0 | 0 | 2 |
| 統合テスト | 1 | 0 | 0 | 1 |

### 全体評価

- **総項目数**: 7項目
- 🔵 **青信号**: 6項目 (86%)
- 🟡 **黄信号**: 1項目 (14%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
