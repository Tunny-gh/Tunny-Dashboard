# TASK-1638: `wasmLoader.ts` computePdp2d modelType 追加 + テスト

**タスクID**: TASK-1638
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 3 - WASM + TypeScript 更新
**信頼性レベル**: 🔵 *architecture.md「wasmLoader.ts computePdp2d modelType 引数追加」・既存 wasmLoader.ts パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/surface-plot-surrogate-models/architecture.md)
- **型定義差分**: [📝 interfaces.ts](../../design/surface-plot-surrogate-models/interfaces.ts)

## タスク概要

`frontend/src/wasm/wasmLoader.ts` の `computePdp2d` メソッド定義と `_initialize()` のバインドに `modelType: string` 引数を追加する。TASK-1626（computePdp2d バインド追加）と同じパターンで修正する。

## 依存タスク

- **前提タスク**: [TASK-1637: lib.rs / pdp.rs model_type 引数追加 + WASM リビルド](TASK-1637.md)
- **後続タスク**: [TASK-1639: analysisStore.ts surrogateModelType を WASM に渡す修正](TASK-1639.md)

## 完了条件

- [ ] `WasmLoader.computePdp2d` メソッドの型定義に `modelType: string` が追加されている
- [ ] `_initialize()` のバインドが `modelType` を渡すよう更新されている
- [ ] TypeScript コンパイルエラーが発生しない
- [ ] `npm test` でテストが通る

---

## 実装詳細

### 1. WasmLoader メソッド定義更新 🔵

**信頼性**: 🔵 *architecture.md「wasmLoader.ts computePdp2d に modelType 追加」・既存パターン（wasmLoader.ts:212-217）より*

**実装ファイル**: `frontend/src/wasm/wasmLoader.ts`

```typescript
// 変更前（wasmLoader.ts:212-217）
computePdp2d!: (
    param1Name: string,
    param2Name: string,
    objectiveName: string,
    nGrid: number,
) => Pdp2dWasmResult

// 変更後
computePdp2d!: (
    param1Name: string,
    param2Name: string,
    objectiveName: string,
    nGrid: number,
    modelType: string,  // 追加
) => Pdp2dWasmResult
```

### 2. _initialize() バインド更新 🔵

**信頼性**: 🔵 *既存 wasmLoader.ts:282-283 のバインドパターンより*

**実装ファイル**: `frontend/src/wasm/wasmLoader.ts`

```typescript
// 変更前（wasmLoader.ts:282-283）
loader.computePdp2d = (param1Name, param2Name, objectiveName, nGrid) =>
    wasmComputePdp2d(param1Name, param2Name, objectiveName, nGrid) as Pdp2dWasmResult

// 変更後
loader.computePdp2d = (param1Name, param2Name, objectiveName, nGrid, modelType) =>
    wasmComputePdp2d(param1Name, param2Name, objectiveName, nGrid, modelType) as Pdp2dWasmResult
```

---

## 単体テスト要件

### テストケース1: computePdp2d が modelType を受け取る 🔵

**信頼性**: 🔵 *既存 wasmLoader.test.ts TC-1626 パターンより*

**Given**: WasmLoader インスタンス（モック済み）
**When**: `wasm.computePdp2d('x1', 'x2', 'y', 10, 'random_forest')` 呼び出し
**Then**: 呼び出しがエラーなく完了し `Pdp2dWasmResult` 型の値が返る

**テストファイル**: `frontend/src/wasm/wasmLoader.test.ts`（既存テストに追加）

### テストケース2: 'ridge' / 'random_forest' / 'kriging' の各 modelType で呼び出せる 🟡

**信頼性**: 🟡 *既存テストパターンから妥当な推測*

**Given**: モック WASM
**When**: 各 modelType で `computePdp2d` を呼び出す
**Then**: いずれもエラーなく完了する

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-1638` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 3件 (75%)
- 🟡 黄信号: 1件 (25%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質
