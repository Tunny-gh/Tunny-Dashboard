# TASK-1636: `pdp.rs` compute_pdp_2d_kriging() 実装 + Rust テスト

**タスクID**: TASK-1636
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - Kriging Rust実装
**信頼性レベル**: 🔵 *architecture.md 2D部分空間射影・dataflow.md Kriging PDP計算フローより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/surface-plot-surrogate-models/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/surface-plot-surrogate-models/dataflow.md)

## タスク概要

`rust_core/src/pdp.rs` に `compute_pdp_2d_kriging()` 関数を追加し、`compute_pdp_2d()` のディスパッチに `"kriging"` 分岐を追加する。2D 部分空間から `train_gp()` で Kriging モデルを学習し、50×50 グリッド上で `predict_mean()` を呼び出して予測する。

## 依存タスク

- **前提タスク**: [TASK-1635: kriging.rs L-BFGS + ハイパーパラメータ最適化統合](TASK-1635.md)
- **後続タスク**: [TASK-1637: lib.rs / pdp.rs model_type 引数追加 + WASM リビルド](TASK-1637.md)

## 完了条件

- [ ] `compute_pdp_2d_kriging()` 関数が `pdp.rs` に追加されている
- [ ] `compute_pdp_2d()` に `"kriging"` ディスパッチが追加されている（stub の None を削除）
- [ ] Kriging PDP が正しいグリッド形状を返す
- [ ] `cargo test` で全 Rust テストが通る（RF + Kriging を含む）

---

## 実装詳細

### 1. compute_pdp_2d_kriging() 実装 🔵

**信頼性**: 🔵 *architecture.md 2D 部分空間射影・dataflow.md Kriging PDP フローより*

**実装ファイル**: `rust_core/src/pdp.rs`

```rust
use crate::kriging;

pub(crate) fn compute_pdp_2d_kriging(
    x_matrix: &[Vec<f64>],
    y: &[f64],
    param_names: &[String],
    objective_name: &str,
    param1_idx: usize,
    param2_idx: usize,
    n_grid: usize,
) -> PdpResult2d {
    let p1_name = param_names.get(param1_idx).cloned().unwrap_or_default();
    let p2_name = param_names.get(param2_idx).cloned().unwrap_or_default();
    let empty = PdpResult2d { /* ... */ };

    let n = y.len();
    if n < 3 || n_grid == 0 { return empty; }  // GP には最低 3 点必要

    // 2D 部分空間射影
    let x_2d: Vec<Vec<f64>> = x_matrix.iter().map(|row| {
        vec![
            row.get(param1_idx).copied().unwrap_or(0.0),
            row.get(param2_idx).copied().unwrap_or(0.0),
        ]
    }).collect();

    // GP 学習（n > 1000 は 1000 点にサブサンプリング）
    let model = match kriging::train_gp(x_2d.clone(), y.to_vec(), 1000, 42) {
        Some(m) => m,
        None => return empty,
    };

    // グリッド構築
    let col1: Vec<f64> = x_2d.iter().map(|r| r[0]).collect();
    let col2: Vec<f64> = x_2d.iter().map(|r| r[1]).collect();
    let min1 = col1.iter().cloned().fold(f64::INFINITY, f64::min);
    let max1 = col1.iter().cloned().fold(f64::NEG_INFINITY, f64::max);
    let min2 = col2.iter().cloned().fold(f64::INFINITY, f64::min);
    let max2 = col2.iter().cloned().fold(f64::NEG_INFINITY, f64::max);
    let grid1 = linspace(min1, max1, n_grid);
    let grid2 = linspace(min2, max2, n_grid);

    // グリッド予測
    let values: Vec<Vec<f64>> = grid1.iter().map(|&v1| {
        grid2.iter().map(|&v2| {
            kriging::predict_mean(&model, &[v1, v2])
        }).collect()
    }).collect();

    // R² 計算（訓練データ上）
    let y_mean = y.iter().sum::<f64>() / n as f64;
    let ss_tot: f64 = y.iter().map(|&v| (v - y_mean).powi(2)).sum();
    let ss_res: f64 = x_2d.iter().zip(y).map(|(xi, &yi)| {
        let pred = kriging::predict_mean(&model, xi);
        (yi - pred).powi(2)
    }).sum();
    let r_squared = if ss_tot < f64::EPSILON { 1.0 } else { 1.0 - ss_res / ss_tot };

    PdpResult2d {
        param1_name: p1_name,
        param2_name: p2_name,
        objective_name: objective_name.to_string(),
        grid1,
        grid2,
        values,
        r_squared,
    }
}
```

### 2. "kriging" ディスパッチ追加 🔵

**信頼性**: 🔵 *architecture.md model_type ディスパッチ設計より*

**実装ファイル**: `rust_core/src/pdp.rs`

TASK-1632 で追加した match 文の `"kriging"` 分岐を stub (None) から実際の呼び出しに変更:

```rust
"kriging" => Some(compute_pdp_2d_kriging(&x_matrix, &y, &param_names, objective_name, p1_idx, p2_idx, n_grid)),
```

---

## 単体テスト要件

### テストケース1: Kriging PDP がグリッド形状を正しく返す 🔵

**Given**: 2 パラメータ・30 試行のデータ, n_grid=5
**When**: `compute_pdp_2d_kriging()` 呼び出し
**Then**: `values.len() == 5`, `values[0].len() == 5`

### テストケース2: "kriging" ディスパッチが動作する 🔵

**Given**: アクティブな Study が設定されている
**When**: `compute_pdp_2d("x1", "x2", "y", 5, "kriging")` 呼び出し
**Then**: `Some(PdpResult2d)` が返る（None ではない）

### テストケース3: 全 Rust テストが通る 🔵

**Given**: `cargo test --manifest-path rust_core/Cargo.toml`
**When**: 全テスト実行
**Then**: RF / Kriging 含む全テストが通る

---

## 注意事項

- Kriging は学習に時間がかかるため、テストは n_grid=5 などの小さいグリッドで実施
- `cargo test` の実行時間が長い場合は `#[ignore]` フラグで遅いテストを除外

---

## 信頼性レベルサマリー

- 🔵 青信号: 6件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質
