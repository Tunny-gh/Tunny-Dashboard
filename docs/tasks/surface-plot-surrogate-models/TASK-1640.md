# TASK-1640: `SurfacePlot3D.tsx` RF/Kriging disabled 解除 + チャートカタログ更新

**タスクID**: TASK-1640
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 3 - WASM + TypeScript 更新
**信頼性レベル**: 🔵 *architecture.md「SurfacePlot3D.tsx RF/Kriging disabled 解除」より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/surface-plot-surrogate-models/architecture.md)

## タスク概要

`frontend/src/components/charts/SurfacePlot3D.tsx` の `MODEL_OPTIONS` から RF / Kriging オプションの `disabled: true` フラグを削除し、ラベルを更新する。あわせてチャートカタログの `surface3d` エントリの description を更新する。

## 依存タスク

- **前提タスク**: [TASK-1639: analysisStore.ts surrogateModelType を WASM に渡す修正](TASK-1639.md)
- **後続タスク**: [TASK-1641: E2E 動作確認・全テスト通過確認](TASK-1641.md)

## 完了条件

- [ ] `MODEL_OPTIONS` の `random_forest` エントリから `disabled: true` が削除されている
- [ ] `MODEL_OPTIONS` の `kriging` エントリから `disabled: true` が削除されている
- [ ] ラベルが `'Random Forest (coming soon)'` → `'Random Forest'`、`'Kriging (coming soon)'` → `'Kriging'` に更新されている
- [ ] チャートカタログの `surface3d` の description が更新されている
- [ ] `SurfacePlot3D.test.tsx` で RF/Kriging オプションが enabled であることを確認するテストが追加・更新されている
- [ ] `npm test` でテストが通る

---

## 実装詳細

### 1. MODEL_OPTIONS の disabled 解除 🔵

**信頼性**: 🔵 *architecture.md「SurfacePlot3D MODEL_OPTIONS disabled 解除」より*

**実装ファイル**: `frontend/src/components/charts/SurfacePlot3D.tsx`

```typescript
// 変更前
const MODEL_OPTIONS = [
  { value: 'ridge', label: 'Ridge Regression' },
  { value: 'random_forest', label: 'Random Forest (coming soon)', disabled: true },
  { value: 'kriging', label: 'Kriging (coming soon)', disabled: true },
]

// 変更後
const MODEL_OPTIONS = [
  { value: 'ridge', label: 'Ridge Regression' },
  { value: 'random_forest', label: 'Random Forest' },
  { value: 'kriging', label: 'Kriging' },
]
```

### 2. チャートカタログ description 更新 🔵

**信頼性**: 🔵 *architecture.md「ChartCatalog surface3d エントリ更新」より*

**実装ファイル**: `frontend/src/components/ChartCatalogPanel.tsx`（または chart catalog 定義ファイル）

`surface3d` エントリの description を更新:

```typescript
// 変更前
description: 'サロゲートモデルによる3次元応答曲面プロット（Ridge回帰）'

// 変更後
description: 'サロゲートモデル（Ridge / Random Forest / Kriging）による3次元応答曲面プロット'
```

---

## 単体テスト要件

### テストケース1: RF オプションが enabled 🔵

**信頼性**: 🔵 *architecture.md RF/Kriging 有効化設計より*

**Given**: SurfacePlot3D コンポーネントをレンダリング
**When**: モデル選択ドロップダウンを確認
**Then**: 'Random Forest' オプションが `disabled` 属性を持たない

**テストファイル**: `frontend/src/components/charts/SurfacePlot3D.test.tsx`

### テストケース2: Kriging オプションが enabled 🔵

**Given**: SurfacePlot3D コンポーネントをレンダリング
**When**: モデル選択ドロップダウンを確認
**Then**: 'Kriging' オプションが `disabled` 属性を持たない

### テストケース3: '(coming soon)' ラベルが存在しない 🔵

**Given**: SurfacePlot3D コンポーネントをレンダリング
**When**: コンポーネントのテキストを確認
**Then**: '(coming soon)' の文字列が画面に存在しない

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-1640` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 5件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質
