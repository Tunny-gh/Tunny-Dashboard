# TASK-1641: E2E 動作確認・全テスト通過確認

**タスクID**: TASK-1641
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 3 - WASM + TypeScript 更新
**信頼性レベル**: 🔵 *既存テスト通過確認プロセス・npm test / cargo test より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/surface-plot-surrogate-models/architecture.md)

## タスク概要

全フェーズの実装完了後、以下を確認する最終タスク：
1. `npm test` で全フロントエンドテストが通ること
2. `cargo test` で全 Rust テストが通ること
3. `npm run build` でビルドエラーがないこと
4. ブラウザで RF / Kriging モデルを選択して 3D 曲面が表示されることを手動確認

## 依存タスク

- **前提タスク**: [TASK-1640: SurfacePlot3D.tsx RF/Kriging disabled 解除](TASK-1640.md)
- **後続タスク**: なし（最終タスク）

## 完了条件

- [ ] `npm test` で全フロントエンドテストが通る（0 失敗）
- [ ] `cargo test --manifest-path rust_core/Cargo.toml` で全 Rust テストが通る
- [ ] `npm run build` でビルドエラーが発生しない
- [ ] ブラウザで fish.log 等のサンプルデータを読み込み、Ridge / RF / Kriging を切り替えて 3D 曲面が表示されることを確認

---

## 実装詳細

### 1. Rust テスト全通過確認 🔵

**信頼性**: 🔵 *cargo test 実行による確認*

```bash
cargo test --manifest-path rust_core/Cargo.toml
```

期待結果: TASK-1630〜1636 で追加した RF / Kriging テストを含む全テストが通る

### 2. フロントエンドテスト全通過確認 🔵

**信頼性**: 🔵 *npm test 実行による確認*

```bash
cd frontend && npm test
```

期待結果: TASK-1638〜1640 で追加・更新したテストを含む全テストが通る

### 3. ビルド確認 🔵

**信頼性**: 🔵 *npm run build 実行による確認*

```bash
cd frontend && npm run build
```

期待結果: TypeScript コンパイルエラーなし

### 4. ブラウザ手動確認 🟡

**信頼性**: 🟡 *E2E 動作確認の標準プロセスから妥当な推測*

1. `npm run dev` で開発サーバーを起動
2. サンプルの `.log` ファイルを読み込む（fish.log 等）
3. SurfacePlot3D チャートを開く
4. モデルドロップダウンで "Random Forest" を選択 → 3D 曲面が表示される
5. モデルドロップダウンで "Kriging" を選択 → ローディング後に 3D 曲面が表示される
6. モデルを Ridge に戻す → 即座に（キャッシュから）表示される

---

## 実装手順

1. `/tsumiki:direct-setup` - 直接確認作業実行
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- Kriging は計算に時間がかかるため（最大 3000ms）、ローディングスピナーが表示されることを確認する
- RF は Ridge より若干遅い（100〜500ms）ことを確認する
- テスト失敗時は各フェーズのタスクに戻って修正する

---

## 信頼性レベルサマリー

- 🔵 青信号: 3件 (75%)
- 🟡 黄信号: 1件 (25%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質
