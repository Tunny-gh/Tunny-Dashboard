# TASK-2149: compute_pdp_1d_lgbm() 実装（lgbm.rs）

**タスクID**: TASK-2149
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 1 - LightGBM PDP 実装
**信頼性レベル**: 🔵 *要件定義書 REQ-001 / 実装ガイド Step 1 / 設計ヒアリング Q2・Q3 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/lightgbm-surface-plot/requirements.md) - REQ-001
- **設計文書**: [📐 architecture.md](../../design/lightgbm-surface-plot/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/lightgbm-surface-plot/dataflow.md)
- **実装ガイド**: [🔧 implementation-guide.md](../../design/lightgbm-surface-plot/implementation-guide.md)

## タスク概要

`rust_core/src/core/lgbm.rs` に `compute_pdp_1d_lgbm()` 関数を追加する。既存の `compute_pdp_2d_lgbm()` の直後に配置し、同一パターンで実装する。全特徴量で LightGBM RandomForest を学習し、各グリッド点で対象パラメータを固定して全行を周辺化することで真の 1D PDP を計算する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-2150: "random_forest" ディスパッチ追加](TASK-2150.md)

## 完了条件

- [ ] `compute_pdp_1d_lgbm()` が `rust_core/src/core/lgbm.rs` の `compute_pdp_2d_lgbm()` 直後に追加済み
- [ ] 戻り値型 `Pdp1dResult = Option<(Vec<f64>, Vec<f64>, f64)>` が定義済み
- [ ] `pdp_1d_lgbm_shape` テストが通過
- [ ] `pdp_1d_lgbm_returns_none_for_invalid_input` テストが通過
- [ ] `pdp_1d_lgbm_monotone_for_linear_data` テストが通過
- [ ] `cargo test -p tunny-core pdp_1d_lgbm` がすべて通過
- [ ] `cargo clippy` 警告 0 件

---

## 実装詳細

### 1. 型エイリアス定義 🔵

**信頼性**: 🔵 *アーキテクチャ設計「循環依存の回避」/ 既存 compute_pdp_2d_lgbm パターンより*

**実装ファイル**: `rust_core/src/core/lgbm.rs`

`compute_pdp_2d_lgbm()` 直後に以下を追加する。`PdpResult1d` を直接参照すると `pdp::types` との循環依存が発生するため、タプル型で返す。

```rust
// ── 1D PDP ────────────────────────────────────────────────────────────────────

type Pdp1dResult = Option<(Vec<f64>, Vec<f64>, f64)>;
```

### 2. compute_pdp_1d_lgbm() 実装 🔵

**信頼性**: 🔵 *REQ-001 / 設計ヒアリング Q2（全特徴量学習）・Q3（num_iterations=100）より*

**実装ファイル**: `rust_core/src/core/lgbm.rs`

```rust
/// 1D partial dependence curve using a LightGBM RandomForest.
///
/// Trains on the full feature matrix. For each grid point v the target column
/// is fixed to v in every row; the average prediction gives the PDP value.
///
/// Returns `(grid, values, r_squared)` where `grid` spans the data range of
/// the target column and `values.len() == grid.len() == n_grid`.
pub fn compute_pdp_1d_lgbm(
    x_matrix: &[Vec<f64>],
    y: &[f64],
    param_idx: usize,
    n_grid: usize,
) -> Pdp1dResult {
    let n = y.len();
    if n < 2 || x_matrix.is_empty() || n_grid < 2 {
        return None;
    }
    let p = x_matrix[0].len();
    if param_idx >= p {
        return None;
    }

    let config = LgbmRfConfig {
        num_iterations: 100,
        ..Default::default()
    };
    let booster = train_lgbm_rf(x_matrix, y, &config)?;

    let col: Vec<f64> = x_matrix.iter().map(|r| r[param_idx]).collect();
    let min_j = col.iter().cloned().fold(f64::INFINITY, f64::min);
    let max_j = col.iter().cloned().fold(f64::NEG_INFINITY, f64::max);
    let grid = pdp_linspace(min_j, max_j, n_grid);

    let values: Vec<f64> = grid
        .iter()
        .map(|&v| {
            let rows: Vec<Vec<f64>> = x_matrix
                .iter()
                .map(|r| {
                    let mut row = r.clone();
                    row[param_idx] = v;
                    row
                })
                .collect();
            let preds = lgbm_predict(&booster, &rows);
            if preds.is_empty() {
                0.0
            } else {
                preds.iter().sum::<f64>() / preds.len() as f64
            }
        })
        .collect();

    let mse = lgbm_mse(&booster, x_matrix, y)?;
    let r_squared = mse_to_r_squared(mse, y);

    Some((grid, values, r_squared))
}
```

**設計上の注意点**:
- `rows.iter().map(|r| { let mut row = r.clone(); ... })` パターンを維持すること（`.into_iter()` では所有権問題が生じる）
- `num_iterations: 100` は 2D 実装と統一（設計ヒアリング Q3 確認済み）
- 入力ガード条件: `n < 2 || x_matrix.is_empty() || n_grid < 2 || param_idx >= p` → `None`

---

## 単体テスト要件

`lgbm.rs` の `#[cfg(test)] mod tests` 内に追加する。

### pdp_1d_lgbm_shape: 形状確認 🔵

**信頼性**: 🔵 *実装ガイド テストケース / REQ-001 より*

**テストファイル**: `rust_core/src/core/lgbm.rs` の `#[cfg(test)]` ブロック

```rust
#[test]
fn pdp_1d_lgbm_shape() {
    let (x, y) = synthetic_data(30);
    let (grid, values, r_squared) =
        compute_pdp_1d_lgbm(&x, &y, 0, 5).expect("pdp_1d_lgbm should return Some");
    assert_eq!(grid.len(), 5);
    assert_eq!(values.len(), 5);
    assert!(r_squared.is_finite());
}
```

**Given**: `synthetic_data(30)` で 30 行の合成データ, n_grid=5
**When**: `compute_pdp_1d_lgbm(&x, &y, 0, 5)` を呼ぶ
**Then**: `grid.len() == 5`, `values.len() == 5`, `r_squared.is_finite()`

### pdp_1d_lgbm_returns_none_for_invalid_input: 無効入力 None 🔵

**信頼性**: 🔵 *REQ-001 異常系 / 実装ガイド テストケースより*

```rust
#[test]
fn pdp_1d_lgbm_returns_none_for_invalid_input() {
    let (x, y) = synthetic_data(30);
    assert!(compute_pdp_1d_lgbm(&x, &y, 0, 0).is_none());   // n_grid < 2
    assert!(compute_pdp_1d_lgbm(&x, &y, 99, 5).is_none());  // param_idx 越境
    assert!(compute_pdp_1d_lgbm(&[], &[], 0, 5).is_none()); // 空データ
}
```

**Given/When/Then**: 無効入力 3 パターンすべてで `None` が返る

### pdp_1d_lgbm_monotone_for_linear_data: 線形データで単調増加 🔵

**信頼性**: 🔵 *TC-001-01 / 実装ガイドより*

```rust
#[test]
fn pdp_1d_lgbm_monotone_for_linear_data() {
    let n = 40;
    let x: Vec<Vec<f64>> = (0..n).map(|i| vec![i as f64, 0.0]).collect();
    let y: Vec<f64> = x.iter().map(|r| r[0] * 2.0).collect();
    let (_, values, _) = compute_pdp_1d_lgbm(&x, &y, 0, 10).unwrap();
    // 線形データなら PDP は単調増加
    for i in 0..values.len() - 1 {
        assert!(values[i] <= values[i + 1] + 1e-6, "PDP should be non-decreasing");
    }
}
```

**Given**: `y = x[0] * 2.0` の線形データ (n=40)
**When**: `compute_pdp_1d_lgbm(&x, &y, 0, 10)` を呼ぶ
**Then**: values が単調増加（許容誤差 1e-6）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2149` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 3 | 0 | 0 | 3 |

- **総項目数**: 5 項目
- 🔵 **青信号**: 5 項目 (100%)
- 🟡 **黄信号**: 0 項目 (0%)
- 🔴 **赤信号**: 0 項目 (0%)

**品質評価**: ✅ 高品質
