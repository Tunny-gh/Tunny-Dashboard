# TASK-2151: ModelType::RandomForest 追加・1D UI 更新（pdp_chart.rs）

**タスクID**: TASK-2151
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 1 - LightGBM PDP 実装
**信頼性レベル**: 🔵 *要件定義書 REQ-011〜013, REQ-021 / 実装ガイド Step 3 / ユーザヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/lightgbm-surface-plot/requirements.md) - REQ-011〜013, REQ-021
- **設計文書**: [📐 architecture.md](../../design/lightgbm-surface-plot/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/lightgbm-surface-plot/dataflow.md)
- **実装ガイド**: [🔧 implementation-guide.md](../../design/lightgbm-surface-plot/implementation-guide.md)

## タスク概要

`egui-app/src/ui/widgets/pdp_chart.rs` に `ModelType::RandomForest` バリアントを追加し、`label()` / `to_str()` を実装する。1D PDP の n_grid 分岐と ComboBox のモデル選択肢を更新する。

## 依存タスク

- **前提タスク**: [TASK-2150: "random_forest" ディスパッチ追加](TASK-2150.md)
- **後続タスク**: [TASK-2152: 2D ComboBox・n_grid 更新](TASK-2152.md)

## 完了条件

- [ ] `ModelType::RandomForest` が `ModelType` 列挙型に追加済み
- [ ] `ModelType::RandomForest.label()` が `"Random Forest (LightGBM)"` を返す
- [ ] `ModelType::RandomForest.to_str()` が `"random_forest"` を返す
- [ ] `show()` 内の n_grid 分岐に `ModelType::RandomForest => 30` が追加済み（REQ-021）
- [ ] 1D PDP の ComboBox に `ModelType::RandomForest` が追加済み（REQ-013）
- [ ] `ModelType` のデフォルト値が `Ridge` のまま変更されていない（NFR-011）
- [ ] `cargo test -p tunny-desktop` が通過
- [ ] `cargo clippy` 警告 0 件

---

## 実装詳細

### 1. ModelType 列挙型に RandomForest を追加 🔵

**信頼性**: 🔵 *REQ-011〜012 / ユーザヒアリング / 実装ガイド 3-1 より*

**実装ファイル**: `egui-app/src/ui/widgets/pdp_chart.rs`

`ModelType` 列挙型に `RandomForest` バリアントを追加し、`label()` と `to_str()` を実装する:

```rust
#[derive(Debug, Clone, PartialEq)]
pub enum ModelType {
    Ridge,
    Kriging,
    SparseKriging,
    RandomForest,   // ← 追加
}

impl ModelType {
    pub fn label(&self) -> &'static str {
        match self {
            ModelType::Ridge => "Ridge",
            ModelType::Kriging => "Kriging",
            ModelType::SparseKriging => "Sparse Kriging",
            ModelType::RandomForest => "Random Forest (LightGBM)",  // ← 追加
        }
    }

    pub fn to_str(&self) -> &'static str {
        match self {
            ModelType::Ridge => "ridge",
            ModelType::Kriging => "kriging",
            ModelType::SparseKriging => "sparse_kriging",
            ModelType::RandomForest => "random_forest",  // ← 追加
        }
    }
}
```

**注意**: `#[allow(dead_code)]` は不要（全バリアントが ComboBox で使われる）

### 2. 1D PDP n_grid 分岐に RandomForest を追加 🔵

**信頼性**: 🔵 *REQ-021（n_grid=30）/ 実装ガイド 3-2 より*

**実装ファイル**: `egui-app/src/ui/widgets/pdp_chart.rs`

`show()` 内の n_grid 計算ロジックを更新する:

```rust
// 変更前
let n_grid = match self.model_type {
    ModelType::Ridge => 50,
    _ => 30,
};

// 変更後
let n_grid = match self.model_type {
    ModelType::Ridge => 50,
    ModelType::RandomForest => 30,  // ← REQ-021: 明示的に 30 を指定
    _ => 30,                        // Kriging / SparseKriging
};
```

### 3. 1D PDP ComboBox に RandomForest を追加 🔵

**信頼性**: 🔵 *REQ-013 / ユーザヒアリング / 実装ガイド 3-3 より*

**実装ファイル**: `egui-app/src/ui/widgets/pdp_chart.rs`

モデル選択 ComboBox のイテレート配列に `ModelType::RandomForest` を追加する:

```rust
// 変更前
for model in [
    ModelType::Ridge,
    ModelType::Kriging,
    ModelType::SparseKriging,
] {

// 変更後
for model in [
    ModelType::Ridge,
    ModelType::Kriging,
    ModelType::SparseKriging,
    ModelType::RandomForest,   // ← 追加
] {
```

---

## 単体テスト要件

`pdp_chart.rs` の `#[cfg(test)]` ブロックに追加する（egui の `#[test]` として実装可能な範囲）。

### ModelType::RandomForest の label/to_str 確認 🔵

**信頼性**: 🔵 *TC-011-01・TC-011-02 より*

**Given**: `ModelType::RandomForest`
**When**: `label()` / `to_str()` を呼ぶ
**Then**: それぞれ `"Random Forest (LightGBM)"` / `"random_forest"` を返す

### ModelType デフォルト値確認（非回帰） 🔵

**信頼性**: 🔵 *TC-011-03 / NFR-011 より*

**Given**: `ModelType::default()` または `PdpChart::default().model_type`
**When**: デフォルト値を確認する
**Then**: `ModelType::Ridge` のまま変更されていない

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2151` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 2 | 0 | 0 | 2 |

- **総項目数**: 5 項目
- 🔵 **青信号**: 5 項目 (100%)
- 🟡 **黄信号**: 0 項目 (0%)
- 🔴 **赤信号**: 0 項目 (0%)

**品質評価**: ✅ 高品質
