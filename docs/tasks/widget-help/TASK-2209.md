# TASK-2209: 軽量 Markdown→egui レンダラ実装

**タスクID**: TASK-2209
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *設計文書 architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/widget-help/requirements.md) - REQ-015
- **設計文書**: [📐 architecture.md](../../design/widget-help/architecture.md)

## タスク概要

`egui-app/src/ui/help/md_renderer.rs` に軽量な Markdown→egui レンダラを実装する。見出し（H1〜H3）、太字、リスト、テーブル、コードブロック、プレーンテキスト数式に対応する。ScrollArea 内で描画する。

## 依存タスク

- **前提タスク**: [TASK-2208](TASK-2208.md)
- **後続タスク**: [TASK-2211](TASK-2211.md)

## 完了条件

- [x] `render_markdown(ui, markdown_str)` が実装されている
- [x] H1〜H3 見出しが egui heading/strong で表示される
- [x] `- item` / `* item` リストが "•" 付きで表示される
- [x] `**bold**` が strong テキストで表示される
- [x] `` `code` `` が monospace で表示される
- [x] テーブル行（`| col | col |`）が列ごとに表示される
- [x] コードブロック（``` ``` ```）が monospace セクションで表示される
- [x] 数式（`$...$`）がプレーンテキストで表示される
- [x] ScrollArea でスクロール可能
- [x] 単体テストが通る

---

## 実装詳細

### 1. パーサコア実装 🔵

**信頼性**: 🔵 *設計文書 architecture.md の「軽量 Markdown→egui レンダラ」より*

**実装ファイル**: `egui-app/src/ui/help/md_renderer.rs`

行単位でパースし、egui ウィジェットに変換:

```rust
pub fn render_markdown(ui: &mut egui::Ui, markdown: &str) {
    egui::ScrollArea::vertical()
        .max_height(ui.available_height())
        .show(ui, |ui| {
            let mut in_code_block = false;
            for line in markdown.lines() {
                if line.starts_with("```") {
                    in_code_block = !in_code_block;
                    continue;
                }
                if in_code_block {
                    ui.label(egui::RichText::new(line).monospace().size(12.0));
                    continue;
                }
                render_line(ui, line);
            }
        });
}
```

### 2. インラインフォーマット 🔵

**信頼性**: 🔵 *設計文書 architecture.md の対応要素表より*

`**bold**` と `` `code` `` のインラインパース:
- `**text**` → `RichText::new(text).strong()`
- `` `text` `` → `RichText::new(text).monospace()`
- 組み合わせ（`**bold with `code`**`）もサポート

### 3. テーブル描画 🟡

**信頼性**: 🟡 *設計文書から妥当な推測*

`|` で区切られた行をパースし、`ui.horizontal` で列表示:
- `|---|` セパレータ行はスキップ
- 各列は固定幅または可変幅で表示

---

## 単体テスト要件

### テストケース1: 見出しパース 🔵

**信頼性**: 🔵 *要件定義書 REQ-015 より*

- `# Title` → heading レベル1として識別
- `## Subtitle` → heading レベル2として識別
- `### Section` → heading レベル3として識別

### テストケース2: リストパース 🔵

**信頼性**: 🔵 *設計文書より*

- `- item` → リストアイテムとして識別
- `* item` → リストアイテムとして識別

### テストケース3: コードブロック 🔵

**信頼性**: 🔵 *設計文書より*

- ``` ``` ``` で囲まれたブロック → monospace セクションとして識別
- 開始・終了のトグルが正しく機能

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2209` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- **総項目数**: 5項目
- 🔵 **青信号**: 4項目 (80%)
- 🟡 **黄信号**: 1項目 (20%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
