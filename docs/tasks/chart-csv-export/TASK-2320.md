# TASK-2320: io/csv_export.rs 新規作成 - dispatch骨格・公開インターフェース

**タスクID**: TASK-2320
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤整備
**信頼性レベル**: 🔵 *architecture.md・design-interview.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/chart-csv-export/requirements.md) - REQ-010, REQ-012, REQ-201
- **設計文書**: [📐 architecture.md](../../design/chart-csv-export/architecture.md)
- **型定義**: [📐 types.rs](../../design/chart-csv-export/types.rs)
- **データフロー**: [📐 dataflow.md](../../design/chart-csv-export/dataflow.md)

## タスク概要

`egui-app/src/io/csv_export.rs` を新規作成し、以下の公開インターフェースを実装する。
各チャートの具体的なCSV生成ロジック（TASK-2321〜2325）は後続タスクで追加するため、このタスクでは dispatch骨格と `csv_export_filename()`・`has_csv_data()` の基本構造を実装する。
各チャートIDに対応するチャートの内部関数は `todo!()` または `None` を返す stub として実装する。

## 依存タスク

- **前提タスク**: [TASK-2319: io/export.rs に save_csv_to_file_named() を追加](TASK-2319.md)
- **後続タスク**: [TASK-2321: OptimizationHistory/HvHistory/SliceChart CSV生成](TASK-2321.md), [TASK-2322: trial系CSV生成](TASK-2322.md), [TASK-2323: Importance/Sensitivity CSV生成](TASK-2323.md), [TASK-2324: MCDM/AHP CSV生成](TASK-2324.md), [TASK-2325: PDP CSV生成](TASK-2325.md), [TASK-2326: UI統合](TASK-2326.md)

## 完了条件

- [ ] `egui-app/src/io/csv_export.rs` が存在する
- [ ] `pub fn build_chart_csv(chart_id: &ChartId, app_state: &AppState, widgets: &WidgetStates) -> Option<String>` が実装されている（各バリアントは `None` を返す stub）
- [ ] `pub fn has_csv_data(chart_id: &ChartId, app_state: &AppState, widgets: &WidgetStates) -> bool` が実装されている（`SurfacePlot` は常に `false`）
- [ ] `pub fn csv_export_filename(chart_id: &ChartId) -> String` が全バリアント対応で実装されている
- [ ] `cargo build -p tunny-desktop` がエラーなく通る
- [ ] `csv_export_filename()` の単体テストが通る

---

## 実装詳細

### 1. build_chart_csv() dispatch 骨格 🔵

**信頼性**: 🔵 *architecture.md・dataflow.md より*

**実装ファイル**: `egui-app/src/io/csv_export.rs`

```rust
use crate::state::{AppState, WidgetStates};
use crate::state::layout_state::ChartId;

pub fn build_chart_csv(
    chart_id: &ChartId,
    app_state: &AppState,
    widgets: &WidgetStates,
) -> Option<String> {
    match chart_id {
        ChartId::OptimizationHistory => build_optimization_history_csv(app_state, widgets),
        ChartId::HvHistory => build_hv_history_csv(app_state),
        ChartId::ImportanceChart => build_importance_csv(app_state, widgets),
        ChartId::PdpChart => build_pdp_csv(app_state, widgets),
        ChartId::PdpChart2D => build_pdp_2d_csv(app_state, widgets),
        ChartId::ParallelCoordinates => build_trial_based_csv(app_state),
        ChartId::ScatterMatrix => build_trial_based_csv(app_state),
        ChartId::ClusterScatter => build_cluster_csv(app_state),
        ChartId::SensitivityHeatmap => build_sensitivity_csv(app_state, widgets),
        ChartId::ParetoScatter2D => build_pareto_csv(app_state),
        ChartId::ParetoScatter3D => build_pareto_csv(app_state),
        ChartId::McdmRankChart => build_mcdm_rank_csv(app_state),
        ChartId::McdmScatterChart => build_mcdm_scatter_csv(app_state),
        ChartId::McdmTable => build_mcdm_table_csv(app_state),
        ChartId::AhpRankChart => build_ahp_rank_csv(app_state),
        ChartId::AhpTable => build_ahp_table_csv(app_state),
        ChartId::SliceChart => build_slice_csv(app_state, widgets),
        ChartId::SurfacePlot => None, // スキップ
    }
}
```

各内部関数は後続タスクで実装するため、このタスクでは `None` を返す stub として定義:
```rust
fn build_optimization_history_csv(_app_state: &AppState, _widgets: &WidgetStates) -> Option<String> { None }
// 同様に全チャート分のstub関数を定義
```

### 2. has_csv_data() 実装 🔵

**信頼性**: 🔵 *REQ-201・design-interview.md より*

```rust
pub fn has_csv_data(
    chart_id: &ChartId,
    app_state: &AppState,
    widgets: &WidgetStates,
) -> bool {
    match chart_id {
        ChartId::SurfacePlot => false,
        // 後続タスクで各チャートの実際の判定を実装するまでの暫定実装
        _ => build_chart_csv(chart_id, app_state, widgets).is_some(),
    }
}
```

### 3. csv_export_filename() 実装 🔵

**信頼性**: 🔵 *REQ-012・dataflow.md より*

```rust
pub fn csv_export_filename(chart_id: &ChartId) -> String {
    let name = match chart_id {
        ChartId::OptimizationHistory => "optimization_history",
        ChartId::HvHistory => "hv_history",
        ChartId::ImportanceChart => "importance_chart",
        ChartId::PdpChart => "pdp_chart",
        ChartId::PdpChart2D => "pdp_chart_2d",
        ChartId::ParallelCoordinates => "parallel_coordinates",
        ChartId::ScatterMatrix => "scatter_matrix",
        ChartId::ClusterScatter => "cluster_scatter",
        ChartId::SensitivityHeatmap => "sensitivity_heatmap",
        ChartId::ParetoScatter2D => "pareto_scatter_2d",
        ChartId::ParetoScatter3D => "pareto_scatter_3d",
        ChartId::McdmRankChart => "mcdm_rank_chart",
        ChartId::McdmScatterChart => "mcdm_scatter_chart",
        ChartId::McdmTable => "mcdm_table",
        ChartId::AhpRankChart => "ahp_rank_chart",
        ChartId::AhpTable => "ahp_table",
        ChartId::SliceChart => "slice_chart",
        ChartId::SurfacePlot => "surface_plot",
    };
    format!("{}.csv", name)
}
```

---

## 単体テスト要件

### テストケース1: csv_export_filename() の全バリアント確認 🔵

**信頼性**: 🔵 *REQ-012 より*

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::state::layout_state::ChartId;

    #[test]
    fn test_csv_export_filename_optimization_history() {
        assert_eq!(csv_export_filename(&ChartId::OptimizationHistory), "optimization_history.csv");
    }

    #[test]
    fn test_csv_export_filename_all_end_with_csv() {
        let chart_ids = vec![
            ChartId::OptimizationHistory,
            ChartId::HvHistory,
            ChartId::ImportanceChart,
            // ... 全バリアント
        ];
        for id in &chart_ids {
            assert!(csv_export_filename(id).ends_with(".csv"));
        }
    }
}
```

### テストケース2: SurfacePlot の has_csv_data が false を返す 🔵

**信頼性**: 🔵 *REQ-201・design-interview.md より*

- `has_csv_data(&ChartId::SurfacePlot, ...)` が `false` を返すことを確認
- ※ AppState/WidgetStates のデフォルト値が必要なため、実装時に確認

---

## 実装手順

### TDDプロセス

1. **Red**: `csv_export_filename()` のテストを先に書いて失敗することを確認
2. **Green**: `csv_export.rs` を作成し、`csv_export_filename()` を実装してテストを通す
3. **Green**: `build_chart_csv()` dispatch骨格を stub 実装（全バリアント `None`）
4. **Green**: `has_csv_data()` を実装（SurfacePlot は false、それ以外は `build_chart_csv().is_some()`）
5. **Refactor**: 関数の整理・ドキュメント整備
6. `cargo build -p tunny-desktop` でコンパイルを確認

---

## 信頼性レベルサマリー

- 🔵 青信号: 5件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質
