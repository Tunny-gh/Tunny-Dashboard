# TASK-2319: io/export.rs に save_csv_to_file_named() を追加

**タスクID**: TASK-2319
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 1 - 基盤整備
**信頼性レベル**: 🔵 *architecture.md・既存 save_csv_to_file() パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/chart-csv-export/requirements.md) - REQ-011, REQ-012
- **設計文書**: [📐 architecture.md](../../design/chart-csv-export/architecture.md)
- **型定義**: [📐 types.rs](../../design/chart-csv-export/types.rs)

## タスク概要

`egui-app/src/io/export.rs` に `save_csv_to_file_named(csv, default_name)` 関数を追加する。
既存の `save_csv_to_file()` はデフォルトファイル名を指定できないため、チャート名に対応したデフォルト名を設定できる新しいバリアントを追加する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-2320: io/csv_export.rs 新規作成](TASK-2320.md), [TASK-2326: UI統合](TASK-2326.md)

## 完了条件

- [ ] `save_csv_to_file_named(csv: &str, default_name: &str) -> Result<(), String>` が `export.rs` に存在する
- [ ] rfd の `.set_file_name(default_name)` でデフォルトファイル名が設定される
- [ ] ダイアログキャンセル時は `Ok(())` を返す
- [ ] `cargo build -p tunny-desktop` がエラーなく通る
- [ ] 既存の `save_csv_to_file()` の動作を破壊しない

---

## 実装詳細

### 1. save_csv_to_file_named() 追加 🟡

**信頼性**: 🟡 *既存 save_csv_to_file() の自然な拡張*

**実装ファイル**: `egui-app/src/io/export.rs`

既存 `save_csv_to_file()` の直下に追加する:

```rust
/// デフォルトファイル名を指定してCSVをファイルダイアログ経由で保存する。
/// ダイアログキャンセル時は Ok(()) を返す。
pub fn save_csv_to_file_named(csv: &str, default_name: &str) -> Result<(), String> {
    if let Some(path) = rfd::FileDialog::new()
        .add_filter("CSV", &["csv"])
        .set_file_name(default_name)
        .save_file()
    {
        write_csv_to_path(csv, &path)
    } else {
        Ok(())
    }
}
```

### 2. mod.rs への csv_export モジュール追加 🔵

**信頼性**: 🔵 *architecture.md より*

**実装ファイル**: `egui-app/src/io/mod.rs`

```rust
pub mod csv_export;  // 追加
```

---

## 単体テスト要件

### テストケース1: ダイアログキャンセル時の動作 🟡

**信頼性**: 🟡 *rfd の動作仕様から妥当な推測*

rfd のファイルダイアログはUIスレッドの呼び出しが必要なため、単体テストでのモックは困難。
`write_csv_to_path()` の正常動作は既存の関数で担保されているため、このタスクでは以下を確認する:

- [ ] `save_csv_to_file_named` がコンパイルできる（型チェック）
- [ ] 関数シグネチャが設計文書と一致する

### テストケース2: 既存関数への影響なし 🔵

**信頼性**: 🔵 *既存テストが引き続き通ることで確認*

- [ ] `cargo test -p tunny-desktop` で既存テストがすべて通る

---

## 統合テスト要件

TASK-2326（UI統合）完了後に手動テストで確認:

- [ ] チャートセルの⋯メニューから「Save as CSV」をクリックしたとき、指定ファイル名でダイアログが開く
- [ ] ダイアログをキャンセルしてもクラッシュしない

---

## 実装手順

1. `egui-app/src/io/export.rs` を開く
2. 既存 `save_csv_to_file()` の直下に `save_csv_to_file_named()` を追加
3. `egui-app/src/io/mod.rs` を開いて `pub mod csv_export;` を追加
4. `cargo build -p tunny-desktop` でコンパイルを確認
5. 既存テストが通ることを確認: `cargo test -p tunny-desktop`

---

## 信頼性レベルサマリー

- 🔵 青信号: 3件 (75%)
- 🟡 黄信号: 1件 (25%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質
