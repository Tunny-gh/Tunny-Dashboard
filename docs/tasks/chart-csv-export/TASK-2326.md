# TASK-2326: CellToolbarAction::SaveAsCsv 追加・handle_toolbar_action() 拡張・UI統合

**タスクID**: TASK-2326
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 3 - UI統合
**信頼性レベル**: 🔵 *architecture.md・design-interview.md 注意1・既存 SaveAsPng パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/chart-csv-export/requirements.md) - REQ-001, REQ-002, REQ-003, REQ-010
- **設計文書**: [📐 architecture.md](../../design/chart-csv-export/architecture.md)
- **型定義**: [📐 types.rs](../../design/chart-csv-export/types.rs)
- **データフロー**: [📐 dataflow.md](../../design/chart-csv-export/dataflow.md)

## タスク概要

`egui-app/src/ui/grid_canvas.rs` に以下の変更を加える:
1. `CellToolbarAction` enum に `SaveAsCsv(PanelItem)` バリアントを追加
2. `handle_toolbar_action()` に `app_state: &AppState` 引数を追加し `SaveAsCsv` ハンドリングを実装
3. `chart_cell_menu_items()` に "Save as CSV" を追加（テスト用）
4. 既存テスト `menu_contains_save_as_png_and_help` を更新して "Save as CSV" のアサートを追加

このタスクは TASK-2319（export.rs 拡張）と TASK-2320〜2325（csv_export.rs 実装）が完了していることを前提とする。

## 依存タスク

- **前提タスク**: [TASK-2319: save_csv_to_file_named()追加](TASK-2319.md), [TASK-2320: csv_export.rs骨格](TASK-2320.md), [TASK-2321](TASK-2321.md), [TASK-2322](TASK-2322.md), [TASK-2323](TASK-2323.md), [TASK-2324](TASK-2324.md), [TASK-2325](TASK-2325.md)
- **後続タスク**: [TASK-2327: ボタングレーアウト](TASK-2327.md)

## 完了条件

- [ ] `CellToolbarAction::SaveAsCsv(PanelItem)` がコンパイルできる
- [ ] ⋯メニューに「Save as CSV」ボタンが表示される
- [ ] `handle_toolbar_action()` が `SaveAsCsv` を受け取り、CSVを生成してファイルダイアログを表示する
- [ ] `chart_cell_menu_items()` に "Save as CSV" が含まれる
- [ ] 既存テスト `menu_contains_save_as_png_and_help` が通る（"Save as CSV" のアサートを追加した状態で）
- [ ] `cargo test -p tunny-desktop` が通る

---

## 実装詳細

### 1. CellToolbarAction に SaveAsCsv を追加 🔵

**信頼性**: 🔵 *architecture.md・既存パターンより*

**実装ファイル**: `egui-app/src/ui/grid_canvas.rs`

```rust
pub enum CellToolbarAction {
    None,
    Close,
    Help(PanelItem),
    SaveAsPng(PanelItem),
    SaveAsCsv(PanelItem),  // 追加
}
```

### 2. chart_cell_menu_items() に "Save as CSV" を追加 🔵

**信頼性**: 🔵 *architecture.md・受け入れ基準 TC-001-01 より*

```rust
fn chart_cell_menu_items() -> Vec<&'static str> {
    vec!["Save as PNG", "Save as CSV", "Help"]  // "Save as CSV" を追加
}
```

### 3. show_cell_toolbar() の⋯メニューに「Save as CSV」を追加 🔵

**信頼性**: 🔵 *architecture.md より*

既存の "Save as PNG" ボタンの直下に追加（グレーアウト制御は TASK-2327 で実装）:

```rust
// ⋯ポップアップメニュー内
if ui.button("Save as PNG").clicked() {
    menu_action = Some(CellToolbarAction::SaveAsPng(item.clone()));
    ui.close_menu();
}
// ↓ 追加
if ui.button("Save as CSV").clicked() {
    menu_action = Some(CellToolbarAction::SaveAsCsv(item.clone()));
    ui.close_menu();
}
// Help ボタンはその後
```

### 4. handle_toolbar_action() のシグネチャ変更と SaveAsCsv ハンドリング 🔵

**信頼性**: 🔵 *architecture.md・design-interview.md 注意1 より*

```rust
fn handle_toolbar_action(
    action: &CellToolbarAction,
    help_language: HelpLanguage,
    widgets: &mut WidgetStates,
    app_state: &AppState,   // 追加
    tx: &mpsc::SyncSender<AppMessage>,
) {
    match action {
        // 既存ハンドラーは変更なし
        CellToolbarAction::SaveAsPng(target) => { /* 既存 */ },
        // 新規追加
        CellToolbarAction::SaveAsCsv(target) => {
            if let PanelItem::Chart(chart_id) = target {
                let csv = crate::io::csv_export::build_chart_csv(chart_id, app_state, widgets);
                if let Some(csv_str) = csv {
                    let filename = crate::io::csv_export::csv_export_filename(chart_id);
                    if let Err(e) = crate::io::export::save_csv_to_file_named(&csv_str, &filename) {
                        let _ = tx.try_send(AppMessage::Error(e));
                    }
                }
            }
        },
        // ...
    }
}
```

### 5. render_cell_content() の呼び出しを更新 🔵

**信頼性**: 🔵 *architecture.md より（render_cell_content は既に app_state を受け取る）*

`handle_toolbar_action()` の呼び出し箇所に `app_state` を渡す:

```rust
handle_toolbar_action(&action, help_language, widgets, app_state, tx);
```

---

## 単体テスト要件

### テストケース1: chart_cell_menu_items に "Save as CSV" が含まれる 🔵

**信頼性**: 🔵 *受け入れ基準 TC-001-01 より*

```rust
#[test]
fn menu_contains_save_as_png_help_and_csv() {
    let items = chart_cell_menu_items();
    assert!(items.contains(&"Save as PNG"));
    assert!(items.contains(&"Save as CSV"));
    assert!(items.contains(&"Help"));
}
```

既存テスト `menu_contains_save_as_png_and_help` を上記に更新（またはアサートを追加）。

### テストケース2: CellToolbarAction::SaveAsCsv がコンパイルできる 🔵

**信頼性**: 🔵 *受け入れ基準 TC-001-02 より*

- `cargo build -p tunny-desktop` が通ることで確認

---

## 統合テスト（手動）

- [ ] 最適化履歴チャートの⋯メニューに「Save as CSV」が表示される
- [ ] 「Save as CSV」クリック → ファイルダイアログが表示される
- [ ] ダイアログでファイルを選択して保存 → CSVファイルが生成される
- [ ] ダイアログをキャンセル → クラッシュしない
- [ ] ファイル書き込み失敗時はUIにエラーが表示される

---

## 信頼性レベルサマリー

- 🔵 青信号: 6件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質
