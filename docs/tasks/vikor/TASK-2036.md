# TASK-2036: egui-app チャート統合（chart_registry.rs）

**タスクID**: TASK-2036
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 2 - 統合
**信頼性レベル**: 🔵 *設計文書architecture.md dataflow.md・既存chart_registry.rs MCDMセクションパターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/vikor/requirements.md) - REQ-001, REQ-202
- **設計文書**: [📐 architecture.md](../../design/vikor/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/vikor/dataflow.md)

## タスク概要

`egui-app/src/ui/chart_registry.rs` の MCDMRankChart セクションを以下のように変更する：
1. `pending_compute.take()` のパターンを `Option<McdmComputeRequest>` に対応させる
2. `match method` に `McdmMethod::Vikor` arm を追加
3. Vikor arm 内で `tunny_core::vikor::compute_vikor()` を呼び出す
4. `AppMessage::McdmDone(McdmResult::Vikor(VikorResult { ... }))` を送信する

## 依存タスク

- **前提タスク**: [TASK-2033](TASK-2033.md)（compute_vikor()が利用可能であること）, [TASK-2034](TASK-2034.md)（McdmResult::Vikorが定義されていること）, [TASK-2035](TASK-2035.md)（McdmComputeRequestが定義されていること）
- **後続タスク**: なし（VIKOR実装完了）

## 完了条件

- [x] `chart_registry.rs` の `pending_compute.take()` が `Option<McdmComputeRequest>` 対応になっている
- [x] `match method { McdmMethod::Vikor => { ... } }` が追加されている
- [x] `tunny_core::vikor::compute_vikor()` が正しい引数で呼び出されている（v パラメータ含む）
- [x] `AppMessage::McdmDone(McdmResult::Vikor(...))` が送信されている
- [x] `cargo build --package egui-app` がエラーなしで通る
- [x] VIKOR 選択 → Run ボタン → 結果表示 の E2E フローが動作する

---

## 実装詳細

### 1. pending_compute.take() パターン変更 🔵

**信頼性**: 🔵 *ユーザヒアリング（McdmComputeRequest採用）・既存chart_registry.rsパターンより*

**実装ファイル**: `egui-app/src/ui/chart_registry.rs`

```rust
// 変更前:
if let Some((method, weights)) = widgets.mcdm_chart.pending_compute.take() {
    widgets.mcdm_chart.computing = true;
    // ...
    match method {
        McdmMethod::Topsis => { ... }
    }
}

// 変更後:
if let Some(req) = widgets.mcdm_chart.pending_compute.take() {
    widgets.mcdm_chart.computing = true;
    let McdmComputeRequest { method, weights, v } = req;
    // ...
    match method {
        McdmMethod::Topsis => {
            // 既存コードそのまま（v は使用しない）
        }
        McdmMethod::Vikor => {
            // 新規追加
        }
    }
}
```

### 2. VIKOR dispatch arm の実装 🔵

**信頼性**: 🔵 *設計文書architecture.md Layer3・dataflow.md・既存TOPSISdispatchパターンより*

```rust
McdmMethod::Vikor => {
    match tunny_core::vikor::compute_vikor(
        &objectives,
        n_trials,
        n_objectives,
        &weights,
        &is_minimize,
        v,
    ) {
        Ok(r) => AppMessage::McdmDone(
            crate::state::results::McdmResult::Vikor(
                crate::state::results::VikorResult {
                    s_values: r.s_values,
                    r_values: r.r_values,
                    q_values: r.q_values,
                    display_scores: r.display_scores,
                    ranked_indices: r.ranked_indices,
                    best_values: r.best_values,
                    worst_values: r.worst_values,
                    duration_ms: start.elapsed().as_secs_f64() * 1000.0,
                },
            )
        ),
        Err(e) => AppMessage::Error(format!("VIKOR computation failed: {}", e)),
    }
}
```

### 3. use文の追加（必要な場合） 🔵

**信頼性**: 🔵 *既存chart_registry.rs use文パターンより*

```rust
use crate::state::results::McdmMethod;
// VikorResultは crate::state::results::VikorResult で参照するか、use文追加
use crate::ui::widgets::mcdm_chart::McdmComputeRequest;
```

---

## 単体テスト要件

### VIKOR dispatch のコンパイル確認 🔵

**信頼性**: 🔵 *既存chart_registry.rsテスト構成より*

`cargo build --package egui-app` がエラーなしで通ること。
exhastive match により、すべての `McdmMethod` バリアントが処理されることを確認。

---

## 統合テスト要件

### E2E: VIKOR計算フローの確認 🔵

**信頼性**: 🔵 *dataflow.md・acceptance-criteria.md TC-VIKOR-UI01〜03より*

以下の E2E フローが動作すること：
1. `McdmRankChart.method = McdmMethod::Vikor` に設定
2. `pending_compute = Some(McdmComputeRequest { method: Vikor, weights: [0.5, 0.5], v: 0.5 })`
3. chart_registry が `tunny_core::vikor::compute_vikor()` を呼び出す
4. `AppMessage::McdmDone(McdmResult::Vikor(...))` がメッセージキューに送信される
5. `app_state.mcdm_result = Some(McdmResult::Vikor(...))` に格納される

### TOPSIS の既存動作が壊れていないこと 🔵

**信頼性**: 🔵 *既存テスト構成（後退テスト）より*

TOPSIS 選択時の既存テストがすべてパスすること。

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2036` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成（コンパイルチェック中心）
3. `/tsumiki:tdd-red` - テスト実装（match arm不足によるコンパイルエラー）
4. `/tsumiki:tdd-green` - McdmComputeRequestデストラクチャ + Vikor arm追加
5. `/tsumiki:tdd-refactor` - 不要なコードの整理
6. `/tsumiki:tdd-verify-complete` - E2Eフロー確認・既存テスト確認

---

## 注意事項

- `tunny_core::vikor` は `tunny_core/src/lib.rs` で公開されていることを確認する（必要なら `pub use mcdm::vikor;` を追加）
- TOPSIS の既存 dispatch コードは変更しない（v は `_` で無視するか let でバインドするだけ）
- `start` 変数の計時は spawn_task クロージャの先頭でなく、compute_vikor 呼び出し直前に配置（TOPSIS と同じパターン）

---

## 信頼性レベルサマリー

- **総項目数**: 8項目
- 🔵 **青信号**: 8項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
