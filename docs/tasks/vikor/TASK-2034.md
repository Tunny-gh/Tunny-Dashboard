# TASK-2034: egui-app 状態型拡張（results.rs）

**タスクID**: TASK-2034
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 2 - egui-app型定義
**信頼性レベル**: 🔵 *REQ-004・REQ-005・設計文書interfaces.rs・既存McdmResult実装パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/vikor/requirements.md) - REQ-003, REQ-004, REQ-005
- **設計文書**: [📐 architecture.md](../../design/vikor/architecture.md)
- **型定義参照**: [📝 interfaces.rs](../../design/vikor/interfaces.rs)

## タスク概要

`egui-app/src/state/results.rs` に以下を追加する：
1. `VikorResult` 構造体（s_values, r_values, q_values, display_scores, ranked_indices, best_values, worst_values, duration_ms）
2. `McdmMethod` enum に `Vikor` バリアント追加
3. `McdmResult` enum に `Vikor(VikorResult)` バリアント追加
4. `McdmResult` の各 impl メソッドに `Vikor` arm を追加

## 依存タスク

- **前提タスク**: [TASK-2033](TASK-2033.md)（VikorResultの型定義参照のため）
- **後続タスク**: [TASK-2035](TASK-2035.md), [TASK-2036](TASK-2036.md)

## 完了条件

- [x] `egui-app/src/state/results.rs` に `VikorResult` 構造体が追加されている
- [x] `McdmMethod::Vikor` バリアントが追加されている（label="VIKOR"、all()に含まれる）
- [x] `McdmResult::Vikor(VikorResult)` バリアントが追加されている
- [x] `primary_scores()`, `ranked_indices()`, `duration_ms()`, `method()`, `method_label()` に Vikor arm が追加されている
- [x] `cargo test --package egui-app` の results.rs テストがすべてパスする

---

## 実装詳細

### 1. VikorResult 構造体の追加 🔵

**信頼性**: 🔵 *設計文書interfaces.rs・ユーザヒアリング（S/R/Q全出力）より*

**実装ファイル**: `egui-app/src/state/results.rs`

```rust
#[derive(Debug, Clone)]
pub struct VikorResult {
    pub s_values: Vec<f64>,
    pub r_values: Vec<f64>,
    pub q_values: Vec<f64>,
    pub display_scores: Vec<f64>,  // = 1.0 - q_values（バーチャート表示用）
    pub ranked_indices: Vec<u32>,
    pub best_values: Vec<f64>,
    pub worst_values: Vec<f64>,
    pub duration_ms: f64,
}
```

### 2. McdmMethod enum の拡張 🔵

**信頼性**: 🔵 *REQ-004・設計文書interfaces.rsより*

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum McdmMethod {
    Topsis,
    Vikor,  // 追加
}

impl McdmMethod {
    pub fn label(&self) -> &'static str {
        match self {
            McdmMethod::Topsis => "TOPSIS",
            McdmMethod::Vikor => "VIKOR",  // 追加
        }
    }

    pub fn all() -> &'static [McdmMethod] {
        &[McdmMethod::Topsis, McdmMethod::Vikor]  // 追加
    }
}
```

### 3. McdmResult enum の拡張 🔵

**信頼性**: 🔵 *REQ-005・設計文書interfaces.rsより*

```rust
#[derive(Debug, Clone)]
pub enum McdmResult {
    Topsis(TopsisResult),
    Vikor(VikorResult),  // 追加
}

impl McdmResult {
    pub fn primary_scores(&self) -> &[f64] {
        match self {
            McdmResult::Topsis(r) => &r.scores,
            McdmResult::Vikor(r) => &r.display_scores,  // 1.0 - Q
        }
    }

    pub fn ranked_indices(&self) -> &[u32] {
        match self {
            McdmResult::Topsis(r) => &r.ranked_indices,
            McdmResult::Vikor(r) => &r.ranked_indices,
        }
    }

    pub fn duration_ms(&self) -> f64 {
        match self {
            McdmResult::Topsis(r) => r.duration_ms,
            McdmResult::Vikor(r) => r.duration_ms,
        }
    }

    pub fn method(&self) -> McdmMethod {
        match self {
            McdmResult::Topsis(_) => McdmMethod::Topsis,
            McdmResult::Vikor(_) => McdmMethod::Vikor,
        }
    }

    pub fn method_label(&self) -> &'static str {
        self.method().label()
    }
}
```

---

## 単体テスト要件

### McdmMethod::Vikor の label テスト 🔵

**信頼性**: 🔵 *既存mcdm_method_label テストパターンより*

```rust
#[test]
fn mcdm_method_vikor_label() {
    assert_eq!(McdmMethod::Vikor.label(), "VIKOR");
}

#[test]
fn mcdm_method_all_includes_vikor() {
    assert!(McdmMethod::all().contains(&McdmMethod::Vikor));
    assert_eq!(McdmMethod::all().len(), 2);
}
```

### McdmResult::Vikor の各メソッド 🔵

**信頼性**: 🔵 *既存mcdm_result_topsis_* テストパターンより*

```rust
fn make_vikor_result() -> McdmResult {
    McdmResult::Vikor(VikorResult {
        s_values: vec![0.1, 0.5, 0.8],
        r_values: vec![0.2, 0.4, 0.7],
        q_values: vec![0.1, 0.5, 0.9],
        display_scores: vec![0.9, 0.5, 0.1],  // 1.0 - q
        ranked_indices: vec![0, 1, 2],
        best_values: vec![1.0, 2.0],
        worst_values: vec![5.0, 8.0],
        duration_ms: 5.0,
    })
}

#[test]
fn mcdm_result_vikor_primary_scores() {
    let r = make_vikor_result();
    assert_eq!(r.primary_scores(), &[0.9, 0.5, 0.1]);
}

#[test]
fn mcdm_result_vikor_method_label() {
    let r = make_vikor_result();
    assert_eq!(r.method_label(), "VIKOR");
}
```

### TC-VIKOR-PS01: primary_scores は 1.0 - Q 🔵

**信頼性**: 🔵 *REQ-003・acceptance-criteria.md TC-VIKOR-PS01より*

q_values=[0.0, 0.5, 1.0] → display_scores=[1.0, 0.5, 0.0]

---

## 統合テスト要件

### egui-app コンパイル確認 🔵

**信頼性**: 🔵 *既存実装より*

`cargo check --package egui-app` がエラーなしで通ること（すべてのmatch armが網羅されていること）。

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2034` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（コンパイルエラーも含む）
4. `/tsumiki:tdd-green` - 型追加・match arm追加
5. `/tsumiki:tdd-refactor` - 不要なコードがないか確認
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `TopsisResult` の既存テストは変更しない
- `McdmMethod::all()` の戻り値は `&'static [McdmMethod]` のため、配列リテラルのサイズを変更する必要がある
- コンパイラの exhaustive match 警告を活用して漏れを検出する

---

## 信頼性レベルサマリー

- **総項目数**: 10項目
- 🔵 **青信号**: 10項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
