# TASK-2035: egui-app UIウィジェット拡張（mcdm_chart.rs）

**タスクID**: TASK-2035
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - egui-app UI
**信頼性レベル**: 🔵 *REQ-006・REQ-007・REQ-201・設計文書architecture.md・ユーザヒアリング（McdmComputeRequest構造体・vスライダー）より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/vikor/requirements.md) - REQ-006, REQ-007, REQ-201, NFR-201
- **設計文書**: [📐 architecture.md](../../design/vikor/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/vikor/dataflow.md)

## タスク概要

`egui-app/src/ui/widgets/mcdm_chart.rs` に以下を追加・変更する：
1. `McdmComputeRequest` 構造体を新設（`method`, `weights`, `v` フィールド）
2. `McdmRankChart` に `v_param: f64` フィールド追加
3. `McdmRankChart.pending_compute` の型を `Option<McdmComputeRequest>` に変更
4. Weights セクションに v スライダーを VIKOR 選択時のみ表示

## 依存タスク

- **前提タスク**: [TASK-2034](TASK-2034.md)（McdmMethod::Vikor が定義されていること）
- **後続タスク**: [TASK-2036](TASK-2036.md)

## 完了条件

- [x] `McdmComputeRequest { method, weights, v }` 構造体が定義されている
- [x] `McdmRankChart` に `v_param: f64` フィールドが追加されている（デフォルト 0.5）
- [x] `pending_compute: Option<McdmComputeRequest>` に型が変更されている
- [x] VIKOR 選択時に v スライダーが Weights セクション内に表示される
- [x] TOPSIS 選択時は v スライダーが表示されない
- [x] Run ボタン押下時に `McdmComputeRequest { method, weights: normalized, v: self.v_param }` がセットされる
- [x] `cargo test --package egui-app` の mcdm_chart テストがすべてパスする

---

## 実装詳細

### 1. McdmComputeRequest 構造体の新設 🔵

**信頼性**: 🔵 *ユーザヒアリング（McdmComputeRequest構造体採用）・設計文書architecture.mdより*

**実装ファイル**: `egui-app/src/ui/widgets/mcdm_chart.rs`

```rust
/// MCDMコンピュートリクエスト（pending_compute の型）
pub struct McdmComputeRequest {
    pub method: McdmMethod,
    pub weights: Vec<f64>,
    pub v: f64,  // VIKOR戦略パラメータ（TOPSISでは無視）
}
```

### 2. McdmRankChart フィールド変更 🔵

**信頼性**: 🔵 *既存McdmRankChart実装・REQ-201より*

```rust
pub struct McdmRankChart {
    pub method: McdmMethod,
    pub weights: Vec<f64>,
    pub v_param: f64,                           // 追加: デフォルト 0.5
    pub computing: bool,
    pub pending_compute: Option<McdmComputeRequest>,  // 変更
    pub top_n: McdmTopN,
}

impl Default for McdmRankChart {
    fn default() -> Self {
        Self {
            method: McdmMethod::Topsis,
            weights: Vec::new(),
            v_param: 0.5,        // デフォルト
            computing: false,
            pending_compute: None,
            top_n: McdmTopN::Top10,
        }
    }
}
```

### 3. Run ボタンの変更 🔵

**信頼性**: 🔵 *既存Runボタン実装パターン・McdmComputeRequest採用より*

```rust
// 変更前:
self.pending_compute = Some((self.method, normalized));

// 変更後:
self.pending_compute = Some(McdmComputeRequest {
    method: self.method,
    weights: normalized,
    v: self.v_param,
});
```

### 4. v スライダーの追加（Weights collapsing 内） 🔵

**信頼性**: 🔵 *REQ-007・ユーザヒアリング（vスライダー・NFR-201）より*

```rust
// Weights collapsing セクション内に追加（重みスライダーの直後）
if self.method == McdmMethod::Vikor {
    ui.horizontal(|ui| {
        ui.label(egui::RichText::new("Strategy weight v").strong());
        ui.add(egui::Slider::new(&mut self.v_param, 0.0..=1.0).text("v"));
        ui.label("(0=min-regret, 1=max-consensus)");
    });
}
```

---

## 単体テスト要件

### McdmRankChart default に v_param=0.5 が含まれる 🔵

**信頼性**: 🔵 *NFR-201・REQ-201より*

```rust
#[test]
fn mcdm_rank_chart_default_has_v_param() {
    let chart = McdmRankChart::default();
    assert!((chart.v_param - 0.5).abs() < f64::EPSILON);
}
```

### pending_compute に McdmComputeRequest がセットされる 🔵

**信頼性**: 🔵 *既存mcdm_chart_run_button_sets_pending_computeパターンより*

```rust
#[test]
fn mcdm_compute_request_vikor_includes_v() {
    let req = McdmComputeRequest {
        method: McdmMethod::Vikor,
        weights: vec![0.5, 0.5],
        v: 0.3,
    };
    assert_eq!(req.method, McdmMethod::Vikor);
    assert!((req.v - 0.3).abs() < f64::EPSILON);
}
```

### TOPSIS では v が無視される（コンパイルテスト） 🔵

**信頼性**: 🔵 *設計方針より*

TOPSIS dispatchでは `let McdmComputeRequest { method, weights, v: _ } = req;` のように v を無視できることを確認。

---

## 統合テスト要件

### egui-app コンパイル確認 🔵

**信頼性**: 🔵 *既存テスト構成より*

`cargo check --package egui-app` がエラーなしで通ること。
chart_registry.rs が `Option<(McdmMethod, Vec<f64>)>` をまだ使っている場合はコンパイルエラーが出るが、TASK-2036 で修正する。

---

## UI/UX要件

### vスライダーの表示条件 🔵

**信頼性**: 🔵 *REQ-007・ユーザヒアリングより*

- VIKOR 選択時: v スライダー表示
- TOPSIS 選択時: v スライダー非表示
- v 変更はRunボタンを押すまで再計算しない（NFR-201）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2035` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（コンパイルエラーも含む）
4. `/tsumiki:tdd-green` - McdmComputeRequest追加 + フィールド変更 + UIスライダー追加
5. `/tsumiki:tdd-refactor` - コードクリーンアップ
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- chart_registry.rs は TASK-2036 で修正するため、TASK-2035 完了時点では `pending_compute.take()` の型が合わず一時的にコンパイルエラーになる可能性がある
- その場合は TASK-2035 と TASK-2036 を一緒に実装してもよい

---

## 信頼性レベルサマリー

- **総項目数**: 11項目
- 🔵 **青信号**: 11項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
