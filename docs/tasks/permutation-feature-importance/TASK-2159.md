# TASK-2159: egui-app UI実装（importance_chart.rs）

**タスクID**: TASK-2159
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - egui-app 実装
**信頼性レベル**: 🔵 *実装ガイド Step 6 + 既存 ImportanceMetric::Shap パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/permutation-feature-importance/requirements.md) - REQ-PFI-005-A〜G
- **設計文書**: [📐 architecture.md](../../design/permutation-feature-importance/architecture.md)
- **実装ガイド**: [📝 implementation-guide.md](../../design/permutation-feature-importance/implementation-guide.md)

## タスク概要

`egui-app/src/ui/widgets/importance_chart.rs` に `ImportanceMetric::Permutation` バリアントを追加し、
ドロップダウン UI・スコア取得・R² 表示の各箇所に対応ケースを追加する。
cache_id=7、label="Permutation"、Tree-based グループへの配置が要件。

## 依存タスク

- **前提タスク**: [TASK-2158: egui-app データ型追加](TASK-2158.md)
- **後続タスク**: [TASK-2160: egui-app ディスパッチ実装](TASK-2160.md)

## 完了条件

- [ ] `ImportanceMetric::Permutation` バリアントが追加されていること
- [ ] `cache_id()` が `7` を返すこと
- [ ] `label()` が `"Permutation"` を返すこと
- [ ] `is_sobol()` が `false` を返すこと
- [ ] ComboBox の Tree-based グループに `"Permutation"` が表示されること
- [ ] `compute_sorted_importance` の match 文に Permutation ケースが追加されていること
- [ ] R² 表示の match 文に Permutation ケースが追加されていること
- [ ] `cargo build -p egui-app` がコンパイルエラーなく通ること

---

## 実装詳細

### 1. ImportanceMetric に Permutation バリアントを追加 🔵

**信頼性**: 🔵 *実装ガイド Step 6-A + 受け入れ基準 TC-PFI-005-01 より*

**実装ファイル**: `egui-app/src/ui/widgets/importance_chart.rs`

```rust
pub enum ImportanceMetric {
    Spearman,
    Ridge,
    RfAnova,
    Mdi,
    SobolFirst,
    SobolTotal,
    Shap,
    Permutation,   // ← 追加
}
```

### 2. `label()` に Permutation ケースを追加 🔵

**信頼性**: 🔵 *実装ガイド Step 6-B + 受け入れ基準 TC-PFI-005-04 より*

```rust
ImportanceMetric::Permutation => "Permutation",
```

### 3. `cache_id()` に Permutation ケースを追加 🔵

**信頼性**: 🔵 *実装ガイド Step 6-C + 受け入れ基準 TC-PFI-005-02 より*

```rust
ImportanceMetric::Permutation => 7,
```

### 4. `is_sobol()` に Permutation ケースを追加 🔵

**信頼性**: 🔵 *実装ガイド Step 6-D + 受け入れ基準 TC-PFI-005-03 より*

既存の `_ => false` パターンが残っている場合は変更不要。
明示ケースが必要な場合：

```rust
ImportanceMetric::Permutation => false,
```

### 5. ComboBox の Tree-based グループに Permutation を追加 🔵

**信頼性**: 🔵 *実装ガイド Step 6-E + 受け入れ基準 TC-PFI-005-01 より*

既存の Shap の `selectable_value` の直後に追加：

```rust
ui.selectable_value(
    &mut self.metric,
    ImportanceMetric::Permutation,
    ImportanceMetric::Permutation.label(),
);
```

### 6. `compute_sorted_importance` に Permutation ケースを追加 🔵

**信頼性**: 🔵 *実装ガイド Step 6-F + データフロー図 機能 3 より*

```rust
ImportanceMetric::Permutation => {
    let Some(ref perm) = result.permutation else {
        return vec![];
    };
    perm.importances
        .iter()
        .map(|param_imp| param_imp.get(obj_idx).copied().unwrap_or(0.0).abs())
        .collect()
}
```

### 7. R² 表示の match 文に Permutation ケースを追加 🔵

**信頼性**: 🔵 *実装ガイド Step 6-G より*

```rust
ImportanceMetric::Permutation => sensitivity
    .and_then(|r| r.permutation.as_ref())
    .and_then(|p| p.r_squared.get(obj_idx))
    .copied(),
```

### 8. 既存 match 文のコンパイルエラー修正 🔵

**信頼性**: 🔵 *実装ガイド Step 6-H より*

`Permutation` バリアントを追加したため `_ => unreachable!()` が残っている match アームは
コンパイルエラーになる場合がある。その場合は `ImportanceMetric::Permutation` の明示ケースを追加する。

---

## 単体テスト要件

### テストケース 1: cache_id == 7 (TC-PFI-005-02) 🔵

**信頼性**: 🔵 *受け入れ基準 TC-PFI-005-02 より*

```rust
#[test]
fn test_permutation_cache_id() {
    assert_eq!(ImportanceMetric::Permutation.cache_id(), 7);
}
```

### テストケース 2: is_sobol == false (TC-PFI-005-03) 🔵

**信頼性**: 🔵 *受け入れ基準 TC-PFI-005-03 より*

```rust
#[test]
fn test_permutation_is_not_sobol() {
    assert!(!ImportanceMetric::Permutation.is_sobol());
}
```

### テストケース 3: label == "Permutation" (TC-PFI-005-04) 🔵

**信頼性**: 🔵 *受け入れ基準 TC-PFI-005-04 より*

```rust
#[test]
fn test_permutation_label() {
    assert_eq!(ImportanceMetric::Permutation.label(), "Permutation");
}
```

---

## 信頼性レベルサマリー

- 🔵 青信号: 10件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: ✅ 高品質
