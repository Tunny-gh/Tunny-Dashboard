# TASK-2203: io/html_report.rs に build_and_send_report 追加

**タスクID**: TASK-2203
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase C - REQ-003 HTML レポート io 層移動
**信頼性レベル**: 🔵 *requirements.md REQ-003・acceptance-criteria.md TC-003-02 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/egui-ui-refactor/requirements.md) - REQ-003
- **設計文書**: [📐 architecture.md](../../design/egui-ui-refactor/architecture.md)
- **関数シグネチャ**: [📝 interfaces.rs](../../design/egui-ui-refactor/interfaces.rs)

## タスク概要

`app.rs::apply_toolbar_actions` 内の `HtmlReportSnapshot` 構築ロジック（~30行）を
`io/html_report.rs` の新規関数 `build_and_send_report` に移植する。
この時点では `app.rs` の変更は行わない。

## 依存タスク

- **前提タスク**: [TASK-2202: tradeoff_navigator.rs 新規作成 + left_panel.rs 切り替え](TASK-2202.md)
- **後続タスク**: [TASK-2204: app.rs の GenerateHtmlReport 簡素化](TASK-2204.md)

## 完了条件

- [x] `egui-app/src/io/html_report.rs` に `pub fn build_and_send_report(...)` が追加されている
- [x] `HtmlReportSnapshot` の構築ロジックが html_report.rs 内に移植されている
- [x] `cargo build` がエラーなし

---

## 実装詳細

### 1. `build_and_send_report` 関数の追加 🔵

**信頼性**: 🔵 *requirements.md REQ-003・interfaces.rs より*

**実装ファイル**: `egui-app/src/io/html_report.rs`

シグネチャ:
```rust
pub fn build_and_send_report(
    ctx: &crate::state::types::StudyContext,
    selected_indices: &[u32],
    tx: std::sync::mpsc::SyncSender<crate::state::messages::AppMessage>,
)
```

内容:
- `app.rs` の `ToolbarAction::GenerateHtmlReport` ブロック内の `HtmlReportSnapshot { ... }` 構築ロジックをコピー
- `selected_trials` の構築: `selected_indices` でフィルタリングし `HtmlTrialRow` に変換
- `TrialStatistics` の構築（`objective_means`, `objective_variances`, `pareto_count`）
- `generate_html_report_async(snap, tx)` の呼び出し

参考 (`interfaces.rs` より):
```rust
pub fn build_and_send_report(
    ctx: &crate::state::types::StudyContext,
    selected_indices: &[u32],
    tx: std::sync::mpsc::SyncSender<crate::state::messages::AppMessage>,
) {
    let snap = HtmlReportSnapshot {
        study_name: ctx.meta.name.clone(),
        objective_names: ctx.meta.objective_names.clone(),
        param_names: ctx.meta.param_names.clone(),
        total_trials: ctx.trial_rows.len(),
        pareto_count: ctx.pareto_indices.len(),
        selected_trials: selected_indices
            .iter()
            .filter_map(|&id| ctx.trial_rows.iter().find(|r| r.trial_id == id))
            .map(|r| HtmlTrialRow {
                trial_id: r.trial_id,
                trial_number: r.trial_number,
                params: r.params.clone(),
                objectives: r.objectives.clone(),
                pareto_rank: r.pareto_rank,
            })
            .collect(),
        statistics: TrialStatistics {
            objective_means: vec![0.0; ctx.meta.objective_names.len()],
            objective_variances: vec![0.0; ctx.meta.objective_names.len()],
            pareto_count: ctx.pareto_indices.len(),
        },
    };
    generate_html_report_async(snap, tx);
}
```

---

## 単体テスト要件

### TC-2203-01: build_and_send_report が html_report.rs に存在する 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-003-02 より*

`grep -n "pub fn build_and_send_report" egui-app/src/io/html_report.rs` が 1 件ヒットすること

---

## 統合テスト要件

### cargo build エラーなし 🔵

**信頼性**: 🔵 *NFR-001 より*

`cargo build` がエラーなく完了すること
（この時点では app.rs はまだ変更していないため、HtmlReportSnapshot の構築コードが両方に存在するが問題ない）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2203` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `app.rs` を読み込んで `GenerateHtmlReport` ブロックの実際の実装を確認してからコピーすること
- `StudyContext`, `HtmlTrialRow`, `TrialStatistics`, `HtmlReportSnapshot` の型定義が html_report.rs 内か同じクレート内にあることを確認すること
- この時点では app.rs の変更は行わない（TASK-2204 で実施）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 0 | 0 | 1 |
| 単体テスト | 1 | 0 | 0 | 1 |
| 統合テスト | 1 | 0 | 0 | 1 |

- **総項目数**: 3 項目
- 🔵 **青信号**: 3 項目 (100%)

**品質評価**: ✅ 高品質
