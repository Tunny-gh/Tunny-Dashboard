# TASK-2198: rust_core/src/convergence.rs の新規作成

**タスクID**: TASK-2198
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase A - REQ-002 rust_core 計算ロジック移動
**信頼性レベル**: 🔵 *requirements.md REQ-002・interfaces.rs・left_panel.rs 既存テストより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/egui-ui-refactor/requirements.md) - REQ-002
- **設計文書**: [📐 architecture.md](../../design/egui-ui-refactor/architecture.md)
- **関数シグネチャ**: [📝 interfaces.rs](../../design/egui-ui-refactor/interfaces.rs)
- **データフロー**: [🔄 dataflow.md](../../design/egui-ui-refactor/dataflow.md)

## タスク概要

`egui-app/src/ui/left_panel.rs` に存在する収束診断計算関数 2 件を `rust_core/src/convergence.rs` に移植する。
`TrialRow` 型依存を排除したプリミティブ型スライスのシグネチャで実装する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-2200: lib.rs / mod.rs 更新 + left_panel.rs 呼び出し切り替え](TASK-2200.md)

## 完了条件

- [x] `rust_core/src/convergence.rs` が新規作成されている
- [x] `compute_improvement_rate` が実装されている
- [x] `build_best_trial_history` が実装されている（`&[TrialRow]` ではなく `&[u32]` + `&[f64]`）
- [x] `#[cfg(test)]` ブロックにテストが追加されている
- [x] `cargo test -p tunny-core` がグリーン（または `cargo test -p rust_core`）
- [x] `grep -rn "egui" rust_core/src/convergence.rs` が 0 件

---

## 実装詳細

### 1. ファイル新規作成: `rust_core/src/convergence.rs` 🔵

**信頼性**: 🔵 *architecture.md REQ-002・interfaces.rs より*

**実装ファイル**: `rust_core/src/convergence.rs`

```rust
/// 直近 last_n 試行で best 値が改善された割合を計算する。
pub fn compute_improvement_rate(history: &[(u32, f64)], last_n: usize) -> f64 {
    let window: Vec<_> = history.iter().rev().take(last_n).collect();
    if window.len() < 2 {
        return 0.0;
    }
    let mut best_so_far = f64::INFINITY;
    let mut improved_count = 0usize;
    for &(_, val) in window.iter().rev() {
        if *val < best_so_far {
            best_so_far = *val;
            improved_count += 1;
        }
    }
    (improved_count as f64) / (window.len() as f64)
}

/// 各試行時点でのベスト値の推移を計算する。
///
/// `trial_ids` と `objective_values` は同一長のスライスでなければならない。
/// `is_minimize = true` の場合、値が小さいほど改善と見なす。
pub fn build_best_trial_history(
    trial_ids: &[u32],
    objective_values: &[f64],
    is_minimize: bool,
) -> Vec<(u32, f64)> {
    let mut history = Vec::with_capacity(trial_ids.len());
    let mut best = if is_minimize { f64::INFINITY } else { f64::NEG_INFINITY };
    for (&id, &val) in trial_ids.iter().zip(objective_values.iter()) {
        let improved = if is_minimize { val < best } else { val > best };
        if improved {
            best = val;
        }
        history.push((id, best));
    }
    history
}
```

### 2. テストの移植 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-002-05, TC-002-06, TC-002-07 より*

`left_panel.rs` の既存テスト 3 件を `convergence.rs` の `#[cfg(test)]` ブロックに移植する。

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn improvement_rate_all_improving() {
        let history = vec![(0, 1.0), (1, 0.8), (2, 0.5)];
        let rate = compute_improvement_rate(&history, 100);
        assert!(rate > 0.0);
    }

    #[test]
    fn improvement_rate_empty_returns_zero() {
        let history: Vec<(u32, f64)> = vec![];
        let rate = compute_improvement_rate(&history, 100);
        assert_eq!(rate, 0.0);
    }

    #[test]
    fn build_best_trial_history_minimize() {
        let ids = vec![0u32, 1, 2];
        let vals = vec![1.0f64, 0.5, 0.8];
        let result = build_best_trial_history(&ids, &vals, true);
        assert_eq!(result, vec![(0, 1.0), (1, 0.5), (2, 0.5)]);
    }
}
```

---

## 単体テスト要件

### TC-2198-01: 単調改善ケース 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-002-05 より*

**Given**: `history = [(0, 1.0), (1, 0.8), (2, 0.5)]`
**When**: `compute_improvement_rate(&history, 100)` を呼び出す
**Then**: 戻り値が `0.0` より大きい

### TC-2198-02: 空履歴ケース 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-002-06 より*

**Given**: `history = []`
**When**: `compute_improvement_rate(&history, 100)` を呼び出す
**Then**: 戻り値が `0.0`

### TC-2198-03: 最小化履歴構築ケース 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-002-07 より*

**Given**: `trial_ids = [0, 1, 2]`, `obj_values = [1.0, 0.5, 0.8]`, `is_minimize = true`
**When**: `build_best_trial_history(...)` を呼び出す
**Then**: 戻り値が `[(0, 1.0), (1, 0.5), (2, 0.5)]`

---

## 統合テスト要件

### 層境界確認 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-NFR-003-02 より*

- `grep -rn "egui" rust_core/src/convergence.rs` が 0 件であること（egui 依存禁止）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2198` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `build_best_trial_history` の引数は `&[crate::state::types::TrialRow]` ではなく、プリミティブ型スライス `&[u32]` + `&[f64]` を使用する（rust_core と egui-app 間の型依存排除）
- この時点では `rust_core/src/lib.rs` への `pub mod convergence;` 追加は行わない（TASK-2200 で実施）
- egui-app 側の `left_panel.rs` はこのタスクでは変更しない（TASK-2200 で実施）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 3 | 0 | 0 | 3 |
| 統合テスト | 1 | 0 | 0 | 1 |

- **総項目数**: 6 項目
- 🔵 **青信号**: 6 項目 (100%)

**品質評価**: ✅ 高品質
