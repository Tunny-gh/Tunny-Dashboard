# TASK-2205: ui/render_chart.rs + ui/poll_chart.rs 新規作成（コード抽出）

**タスクID**: TASK-2205
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase D - REQ-001 chart_registry 分割
**信頼性レベル**: 🔵 *requirements.md REQ-001・acceptance-criteria.md TC-001-01, TC-001-02 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/egui-ui-refactor/requirements.md) - REQ-001
- **設計文書**: [📐 architecture.md](../../design/egui-ui-refactor/architecture.md)
- **関数シグネチャ**: [📝 interfaces.rs](../../design/egui-ui-refactor/interfaces.rs)
- **データフロー**: [🔄 dataflow.md](../../design/egui-ui-refactor/dataflow.md)

## タスク概要

`chart_registry.rs` から描画コードと dispatch コードをそれぞれ新規ファイルに抽出する。
この時点では `chart_registry.rs` は変更しない（元の private 関数が残る）。
`ui/mod.rs` に新規モジュールを登録し、`cargo build` が通ることを確認する。

## 依存タスク

- **前提タスク**: [TASK-2204: app.rs の GenerateHtmlReport 簡素化](TASK-2204.md)
- **後続タスク**: [TASK-2206: chart_registry.rs 薄いラッパー化](TASK-2206.md)

## 完了条件

- [x] `egui-app/src/ui/render_chart.rs` が作成されている
- [x] `pub(crate) fn render_chart(...)` が実装されている（`tx` 引数なし）
- [x] `egui-app/src/ui/poll_chart.rs` が作成されている
- [x] `pub(crate) fn poll_chart_work(...)` が実装されている（`egui::Ui` 引数なし）
- [x] `egui-app/src/ui/mod.rs` に `pub mod render_chart;` と `pub mod poll_chart;` が追加されている
- [x] `cargo build` がエラーなし

---

## 実装詳細

### 1. `egui-app/src/ui/render_chart.rs` の新規作成 🔵

**信頼性**: 🔵 *requirements.md REQ-001・interfaces.rs より*

**実装ファイル**: `egui-app/src/ui/render_chart.rs`

シグネチャ:
```rust
pub(crate) fn render_chart(
    ui: &mut egui::Ui,
    app_state: &mut crate::state::app_state::AppState,
    widgets: &mut crate::ui::widget_states::WidgetStates,
    chart_id: &crate::ui::chart_registry::ChartId,
)
```

内容:
- `chart_registry.rs` の `fn render_chart(...)` private 関数の実装全体をコピー
- visibility を `pub(crate)` に変更
- `tx` 引数を追加しない
- 必要な `use` 宣言をファイル先頭に追加

### 2. `egui-app/src/ui/poll_chart.rs` の新規作成 🔵

**信頼性**: 🔵 *requirements.md REQ-001・interfaces.rs より*

**実装ファイル**: `egui-app/src/ui/poll_chart.rs`

シグネチャ:
```rust
pub(crate) fn poll_chart_work(
    app_state: &mut crate::state::app_state::AppState,
    widgets: &mut crate::ui::widget_states::WidgetStates,
    chart_id: &crate::ui::chart_registry::ChartId,
    tx: &std::sync::mpsc::SyncSender<crate::state::messages::AppMessage>,
)
```

内容:
- `chart_registry.rs` の `fn poll_chart_work(...)` private 関数の実装全体をコピー
- visibility を `pub(crate)` に変更
- `ui: &mut egui::Ui` 引数を追加しない
- 必要な `use` 宣言をファイル先頭に追加

### 3. `egui-app/src/ui/mod.rs` への追加 🔵

**信頼性**: 🔵 *architecture.md REQ-001 より*

**実装ファイル**: `egui-app/src/ui/mod.rs`

```rust
// 既存の pub mod 宣言の後に追加
pub mod render_chart;
pub mod poll_chart;
```

---

## 単体テスト要件

### TC-2205-01: render_chart.rs に spawn_task / tx が含まれない 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-001-01 より*

`grep -n "spawn_task\|tx\.clone\|\.send(" egui-app/src/ui/render_chart.rs` が 0 件であること

### TC-2205-02: poll_chart.rs に egui::Ui 操作が含まれない 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-001-02 より*

`grep -n "egui::Ui\|\.add(\|\.label(\|\.button(" egui-app/src/ui/poll_chart.rs` が 0 件であること

---

## 統合テスト要件

### cargo build エラーなし 🔵

**信頼性**: 🔵 *NFR-001 より*

`cargo build` がエラーなく完了すること
（chart_registry.rs はまだ変更していないため、元のコードと新規ファイルが共存する。
Rust のモジュールシステムにより重複は問題なし）

---

## 実装手順

1. `/tsumiki:direct-setup` - chart_registry.rs の render_chart / poll_chart_work 関数を確認し、新規ファイルにコピー・調整
2. `/tsumiki:direct-verify` - grep 確認・cargo build 実行

---

## 注意事項

- `chart_registry.rs` の実際の `render_chart` / `poll_chart_work` 関数の引数型と内部実装を先に確認すること
- `ChartId` 型のパスを確認すること（`chart_registry` モジュール内にある可能性）
- 新ファイルは既存の `chart_registry.rs` と並存するため、この時点では `cargo build` は必ず通る
- TASK-2206 で `chart_registry.rs` を薄いラッパーに変更して初めて「分割完了」となる

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 2 | 0 | 0 | 2 |
| 統合テスト | 1 | 0 | 0 | 1 |

- **総項目数**: 6 項目
- 🔵 **青信号**: 6 項目 (100%)

**品質評価**: ✅ 高品質
