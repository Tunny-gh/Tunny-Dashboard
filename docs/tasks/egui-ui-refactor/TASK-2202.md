# TASK-2202: ui/widgets/tradeoff_navigator.rs 新規作成 + left_panel.rs 定義削除・切り替え

**タスクID**: TASK-2202
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase B - REQ-004 左パネル UI 分割
**信頼性レベル**: 🔵 *requirements.md REQ-004・acceptance-criteria.md TC-004-01, TC-004-03, TC-004-04 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/egui-ui-refactor/requirements.md) - REQ-004
- **設計文書**: [📐 architecture.md](../../design/egui-ui-refactor/architecture.md)
- **関数シグネチャ**: [📝 interfaces.rs](../../design/egui-ui-refactor/interfaces.rs)
- **データフロー**: [🔄 dataflow.md](../../design/egui-ui-refactor/dataflow.md)

## タスク概要

`left_panel.rs::show_tradeoff_navigator` の実装を独立したウィジェットファイルに移動する。
両ウィジェット完成後、`left_panel.rs` から両関数の定義を削除し、`ui::widgets` 経由の呼び出しに切り替える。

## 依存タスク

- **前提タスク**: [TASK-2201: convergence_card.rs 新規作成](TASK-2201.md)
- **後続タスク**: [TASK-2203: html_report.rs に build_and_send_report 追加](TASK-2203.md)

## 完了条件

- [x] `egui-app/src/ui/widgets/tradeoff_navigator.rs` が作成されている
- [x] `pub fn show_tradeoff_navigator(...)` が実装されている
- [x] `widgets/mod.rs` に `pub mod tradeoff_navigator;` が追加されている
- [x] `left_panel.rs` から `show_tradeoff_navigator` の定義が消えている
- [x] `left_panel.rs` から `show_convergence_card` の定義が消えている
- [x] `left_panel.rs` が `crate::ui::widgets` 経由で両関数を呼び出している
- [x] `cargo test --workspace` がグリーン

---

## 実装詳細

### 1. `egui-app/src/ui/widgets/tradeoff_navigator.rs` の新規作成 🔵

**信頼性**: 🔵 *requirements.md REQ-004・interfaces.rs より*

**実装ファイル**: `egui-app/src/ui/widgets/tradeoff_navigator.rs`

シグネチャ:
```rust
pub fn show_tradeoff_navigator(
    ui: &mut egui::Ui,
    app_state: &mut crate::state::app_state::AppState,
    objective_names: &[String],
    is_minimize: &[bool],
    tx: &std::sync::mpsc::SyncSender<crate::state::messages::AppMessage>,
)
```

内容:
- `left_panel.rs::show_tradeoff_navigator` の実装をそのままコピー
- `normalize_weights` の呼び出しを `tunny_core::multi_objective::weights::normalize_weights` に変更

### 2. `egui-app/src/ui/widgets/mod.rs` への追加 🔵

**信頼性**: 🔵 *requirements.md REQ-004 より*

**実装ファイル**: `egui-app/src/ui/widgets/mod.rs`

```rust
pub mod convergence_card;
pub mod tradeoff_navigator; // 追加
```

### 3. `left_panel.rs` からの定義削除と呼び出し切り替え 🔵

**信頼性**: 🔵 *requirements.md REQ-004・acceptance-criteria.md TC-004-03, TC-004-04 より*

**実装ファイル**: `egui-app/src/ui/left_panel.rs`

削除する関数定義:
- `pub fn show_tradeoff_navigator(...)` またはそれと同名の `fn`
- `pub fn show_convergence_card(...)` またはそれと同名の `fn`

追加する use 宣言:
```rust
use crate::ui::widgets::{convergence_card, tradeoff_navigator};
```

呼び出し箇所の変更:
```rust
// 多目的 Study の場合
tradeoff_navigator::show_tradeoff_navigator(ui, app_state, &obj_names, &is_minimize, tx);

// 単目的 Study の場合
convergence_card::show_convergence_card(ui, app_state);
```

---

## 単体テスト要件

### TC-2202-01: tradeoff_navigator.rs が存在する 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-004-01 より*

`egui-app/src/ui/widgets/tradeoff_navigator.rs` ファイルが存在すること

### TC-2202-02: left_panel.rs が show_tradeoff_navigator を定義しない 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-004-03 より*

`grep -n "^pub fn show_tradeoff_navigator\|^fn show_tradeoff_navigator" egui-app/src/ui/left_panel.rs` が 0 件

### TC-2202-03: left_panel.rs が show_convergence_card を定義しない 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-004-04 より*

`grep -n "^pub fn show_convergence_card\|^fn show_convergence_card" egui-app/src/ui/left_panel.rs` が 0 件

---

## 統合テスト要件

### cargo test --workspace グリーン 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-004-05 より*

`cargo test --workspace` がすべてパスすること

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2202` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `show_tradeoff_navigator` のシグネチャは変更しない（NFR-002）
- `show_convergence_card` のシグネチャは変更しない（NFR-002）
- `left_panel.rs` 内に `normalize_weights` の定義が残っている場合も、このタスクで削除する（TASK-2200 で削除していない場合のフォールバック）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 3 | 0 | 0 | 3 |
| 統合テスト | 1 | 0 | 0 | 1 |

- **総項目数**: 7 項目
- 🔵 **青信号**: 7 項目 (100%)

**品質評価**: ✅ 高品質
