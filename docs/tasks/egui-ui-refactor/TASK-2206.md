# TASK-2206: chart_registry.rs の薄いラッパー化 + cargo test 最終確認

**タスクID**: TASK-2206
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase D - REQ-001 chart_registry 分割
**信頼性レベル**: 🔵 *requirements.md REQ-001・acceptance-criteria.md TC-001-03 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/egui-ui-refactor/requirements.md) - REQ-001
- **設計文書**: [📐 architecture.md](../../design/egui-ui-refactor/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/egui-ui-refactor/dataflow.md)

## タスク概要

TASK-2205 で作成した `render_chart.rs` / `poll_chart.rs` を活用し、
`chart_registry.rs` を薄いラッパー（~20行）に変更する。
全 REQ の完了後に `cargo test --workspace` でグリーンを確認する。

## 依存タスク

- **前提タスク**: [TASK-2205: render_chart.rs + poll_chart.rs 新規作成](TASK-2205.md)
- **後続タスク**: なし（最終タスク）

## 完了条件

- [x] `chart_registry.rs` から `fn render_chart(...)` の実装が削除されている
- [x] `chart_registry.rs` から `fn poll_chart_work(...)` の実装が削除されている
- [x] `chart_registry.rs::show_chart` が `render_chart` と `poll_chart_work` を呼び出している
- [x] `chart_registry::show_chart` / `show_cell_chart` のシグネチャが変更前と一致する
- [x] `cargo test --workspace` がグリーン

---

## 実装詳細

### 1. `chart_registry.rs` の薄いラッパー化 🔵

**信頼性**: 🔵 *requirements.md REQ-001・dataflow.md より*

**実装ファイル**: `egui-app/src/ui/chart_registry.rs`

変更後の `show_chart` の実装:
```rust
pub fn show_chart(
    ui: &mut egui::Ui,
    app_state: &mut AppState,
    widgets: &mut WidgetStates,
    chart_id: &ChartId,
    tx: &std::sync::mpsc::SyncSender<AppMessage>,
) {
    crate::ui::render_chart::render_chart(ui, app_state, widgets, chart_id);
    crate::ui::poll_chart::poll_chart_work(app_state, widgets, chart_id, tx);
}
```

`show_cell_chart` は変更なし（シグネチャ不変・内部で `show_chart` を呼ぶだけ）。

削除する private 関数:
- `fn render_chart(...)` の実装全体（TASK-2205 で render_chart.rs に移動済み）
- `fn poll_chart_work(...)` の実装全体（TASK-2205 で poll_chart.rs に移動済み）

### 2. 不要な use 宣言の整理 🔵

**信頼性**: 🔵 *NFR-003（層境界の遵守）より*

`render_chart` / `poll_chart_work` の実装が chart_registry.rs から消えたことで
不要になった use 宣言を削除する。

---

## 単体テスト要件

### TC-2206-01: chart_registry.rs の外部公開 API が変わらない 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-001-03 より*

`show_chart` / `show_cell_chart` のシグネチャが変更前と一致すること（`cargo check` エラーなし）

### TC-2206-02: render_chart.rs に spawn_task / tx が含まれない 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-001-01 より*

`grep -n "spawn_task\|tx\.clone\|\.send(" egui-app/src/ui/render_chart.rs` が 0 件であること

### TC-2206-03: poll_chart.rs に egui::Ui 操作が含まれない 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-001-02 より*

`grep -n "egui::Ui\|\.add(\|\.label(\|\.button(" egui-app/src/ui/poll_chart.rs` が 0 件であること

---

## 統合テスト要件

### cargo test --workspace グリーン（全 REQ 完了後の最終確認）🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-001-04・TC-NFR-001-01 より*

`cargo test --workspace` がすべてパスすること。
これが全 9 タスク完了の最終確認となる。

### 層境界確認 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-NFR-003-01, TC-NFR-003-02 より*

- `grep -rn "use egui\|egui::" egui-app/src/state/` が 0 件であること
- `grep -rn "egui" rust_core/src/convergence.rs` が 0 件であること

---

## 実装手順

1. `/tsumiki:direct-setup` - chart_registry.rs の変更（private 関数削除・show_chart 更新）
2. `/tsumiki:direct-verify` - grep 確認・cargo test --workspace 実行・全受け入れ基準の最終チェック

---

## 注意事項

- `show_chart` / `show_cell_chart` のシグネチャは絶対に変更しない（NFR-002）
- `cargo check` でエラーが出ない状態を確認してから `cargo test` を実行する
- このタスクは全 REQ の最終タスクのため、このタスク完了後に全受け入れ基準（TC-001〜TC-NFR）を一括検証すること

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 3 | 0 | 0 | 3 |
| 統合テスト | 2 | 0 | 0 | 2 |

- **総項目数**: 7 項目
- 🔵 **青信号**: 7 項目 (100%)

**品質評価**: ✅ 高品質
