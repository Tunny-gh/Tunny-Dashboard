# TASK-2337: Cluster Scatter / MCDM 系ウィジェットの移行

**タスクID**: TASK-2337
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 3 - UI層（ウィジェット段階移行）
**信頼性レベル**: 🔵 *cluster_scatter.rs / mcdm_*.rs 調査より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/memory-efficiency/requirements.md) - REQ-001, REQ-401
- **設計文書**: [📐 architecture.md](../../design/memory-efficiency/architecture.md)

## タスク概要

`trial_rows` を参照する残りの主要ウィジェット — Cluster Scatter（`cluster_scatter.rs`、`compute_obj_axes_2d(trial_rows, ...)`）と MCDM 系（`mcdm_chart.rs` / `mcdm_scatter_chart.rs`、`make_trial_rows` 等で行参照）— を StudyView の列アクセス・並行配列（`cluster_id`）へ移行する。既存の結果キャッシュ（`cached_points`, `cached_topsis` 等）は維持しつつ、入力を行クローンから列借用へ置換する。

## 依存タスク

- **前提タスク**: [TASK-2333](TASK-2333.md)
- **後続タスク**: [TASK-2342](TASK-2342.md)

## 完了条件

- [ ] Cluster Scatter が `view.cluster_id`/列借用で動作（`cached_points` は維持）
- [ ] MCDM 系が列借用で入力を構築（結果キャッシュは維持）
- [ ] 描画・ランキング結果が現行と一致
- [ ] 単体テストグリーン

---

## 実装詳細

### 1. Cluster Scatter の移行 🔵

**信頼性**: 🔵 *cluster_scatter.rs:206-210（compute_obj_axes_2d）より*

`compute_obj_axes_2d(trial_rows, obj_names)` を、StudyView の目的列借用＋並行配列ベースへ置換。`cached_points: Option<Vec<[f32;2]>>` の最小キャッシュ方式は維持。

**実装ファイル**: `egui-app/src/ui/widgets/cluster_scatter.rs`

### 2. MCDM 系の移行 🔵

**信頼性**: 🔵 *mcdm_chart.rs / mcdm_scatter_chart.rs 行参照より*

MCDM 計算入力（目的値行列）を `view.numeric_column` 借用で構築。`cached_topsis`/`cached_vikor`/`display_rows_cache`（散布図の `Vec<(f64,f64,Color32)>`）は維持。

**実装ファイル**: `egui-app/src/ui/widgets/mcdm_chart.rs`, `egui-app/src/ui/widgets/mcdm_scatter_chart.rs`

---

## 単体テスト要件

### テストケース1: クラスタ散布図の等価性 🔵

**信頼性**: 🔵 *cluster_scatter.rs 既存テストより*

**Given**: 既知クラスタ結果
**When**: 2D 投影点を構築
**Then**: 現行と一致

### テストケース2: MCDM ランキングの等価性 🔵

**信頼性**: 🔵 *REQ-401・mcdm_chart.rs 既存テストより*

**Given**: 既知目的値
**When**: TOPSIS/VIKOR 計算
**Then**: 現行と一致

---

## 統合テスト要件

### 統合テスト1: クラスタ→色分け連携 🔵

**信頼性**: 🔵 *TASK-2333 連携より*

**テスト内容**: クラスタリング→cluster_id 並行配列→散布図色分け
**期待結果**: 現行と等価

---

## UI/UX要件

### 結果表示の維持 🔵

**信頼性**: 🔵 *REQ-401・README より*

- MCDM Table/Ranking、Cluster Scatter の表示・ソートを現行どおり維持。

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-2337` 〜 6. `/tsumiki:tdd-verify-complete`

---

## 注意事項

- MCDM 結果キャッシュ（`cached_*`）はメモリ最適化対象外（結果は小さい）。入力構築のみ列借用化。
- 本タスクで主要 trial_rows 参照ウィジェットの移行が完了する想定（残余があれば TASK-2342 前に洗い出す）。

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 2 | 0 | 0 | 2 |
| 統合テスト | 1 | 0 | 0 | 1 |
| UI/UX要件 | 1 | 0 | 0 | 1 |

- **総項目数**: 6項目
- 🔵 青信号: 6項目 (100%)

**品質評価**: 高品質
