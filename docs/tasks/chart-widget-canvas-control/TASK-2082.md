# TASK-2082: ドラッグハンドルによるサイズ変更の実装とテスト

**タスクID**: TASK-2082
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - UI機能実装
**信頼性レベル**: 🔵 *設計文書 architecture.md・ユーザヒアリング（端のみハンドル）より*

## 関連文書

- **概要**: [overview.md](overview.md)
- **設計文書**: [architecture.md](../../design/chart-widget-canvas-control/architecture.md)
- **データフロー**: [dataflow.md](../../design/chart-widget-canvas-control/dataflow.md)
- **型定義**: [interfaces.rs](../../design/chart-widget-canvas-control/interfaces.rs)

## タスク概要

コンテンツがあるセルの右端と下端にドラッグハンドル（6px幅）を配置し、クリックで1セル単位の拡大/縮小を可能にする。Phase 1 ではクリックベース（1クリックで1段階 expand/shrink）、Phase 2（将来）でドラッグベースの連続リサイズに対応予定。

## 依存タスク

- **前提タスク**: [TASK-2078](TASK-2078.md)（safe_expand_* メソッド）
- **後続タスク**: [TASK-2083](TASK-2083.md)

## 完了条件

- [ ] コンテンツありセルの右端に6px幅のハンドルが配置される
- [ ] コンテンツありセルの下端に6px幅のハンドルが配置される
- [ ] 右端ハンドルのクリックで safe_expand_right が呼ばれる
- [ ] 下端ハンドルのクリックで safe_expand_down が呼ばれる
- [ ] ハンドルホバー時にカーソルが変更される（水平/垂直リサイズ）
- [ ] ハンドルホバー時にハンドルが青くハイライトされる
- [ ] 空セルにハンドルが表示されない
- [ ] `cargo test` + `cargo clippy` が通る

---

## 実装詳細

### 1. ハンドル矩形の計算と ui.interact 登録 🔵

**信頼性**: 🔵 *設計文書 architecture.md・既存 calc_cell_width/height より*

**実装ファイル**: `egui-app/src/ui/grid_canvas.rs`

```rust
const HANDLE_THICKNESS: f32 = 6.0;

fn register_resize_handles(
    ui: &mut egui::Ui,
    cell_rect: egui::Rect,
    row: usize,
    col: usize,
    cell: &GridCell,
    cols: usize,
    rows: usize,
    pending_actions: &mut Vec<CellAction>,
) {
    if cell.content.is_none() { return; }

    // 右端ハンドル
    let can_expand_right = (col + cell.col_span as usize) < cols;
    if can_expand_right {
        let right_handle_rect = egui::Rect::from_min_size(
            egui::pos2(cell_rect.right() - HANDLE_THICKNESS, cell_rect.top()),
            egui::vec2(HANDLE_THICKNESS, cell_rect.height()),
        );
        let right_id = egui::Id::new("resize_right").with(row).with(col);
        let right_resp = ui.interact(right_handle_rect, right_id, egui::Sense::click());

        if right_resp.hovered() {
            ui.ctx().set_cursor_icon(egui::CursorIcon::ResizeHorizontal);
            ui.painter().rect_filled(
                right_handle_rect, 0.0,
                egui::Color32::from_rgba_unmultiplied(100, 150, 255, 80),
            );
        }

        if right_resp.clicked() {
            pending_actions.push(CellAction::ExpandRight(row, col));
        }
    }

    // 下端ハンドル（同様の構造）
    let can_expand_down = (row + cell.row_span as usize) < rows;
    if can_expand_down {
        let bottom_handle_rect = egui::Rect::from_min_size(
            egui::pos2(cell_rect.left(), cell_rect.bottom() - HANDLE_THICKNESS),
            egui::vec2(cell_rect.width(), HANDLE_THICKNESS),
        );
        let bottom_id = egui::Id::new("resize_bottom").with(row).with(col);
        let bottom_resp = ui.interact(bottom_handle_rect, bottom_id, egui::Sense::click());

        if bottom_resp.hovered() {
            ui.ctx().set_cursor_icon(egui::CursorIcon::ResizeVertical);
            ui.painter().rect_filled(
                bottom_handle_rect, 0.0,
                egui::Color32::from_rgba_unmultiplied(100, 150, 255, 80),
            );
        }

        if bottom_resp.clicked() {
            pending_actions.push(CellAction::ExpandDown(row, col));
        }
    }
}
```

### 2. pending_actions での safe_expand 使用 🔵

**信頼性**: 🔵 *設計文書より*

ハンドルからの expand アクションは safe_expand_* を使用:

```rust
CellAction::ExpandRight(r, c) => { layout.grid.safe_expand_right(r, c); }
CellAction::ExpandDown(r, c) => { layout.grid.safe_expand_down(r, c); }
```

既存のコンテキストメニューからの expand は引き続き `expand_right/down` を使用（ユーザーが意図的にコンテンツを上書きする場合を許容）。

### 3. グリッド描画ループへの統合 🔵

**信頼性**: 🔵 *既存 grid_canvas.rs 構造より*

`show_grid_canvas` のセル描画ループ内で、セル矩形計算直後に `register_resize_handles` を呼び出す。`dnd_drop_zone` の外側で実行。

---

## 単体テスト要件

### テストケース1: 右端ハンドルクリックで col_span が増加する 🔵

**信頼性**: 🔵 *設計文書より*

**Given**: 2x2 グリッド、[0][0] にコンテンツあり
**When**: 右端ハンドルをクリック
**Then**: col_span=2 になる

### テストケース2: 下端ハンドルクリックで row_span が増加する 🔵

**信頼性**: 🔵 *設計文書より*

### テストケース3: コンテンツありセルの隣には拡張できない 🔵

**信頼性**: 🔵 *safe_expand のテストより*

**Given**: [0][0] と [0][1] 両方にコンテンツあり
**When**: [0][0] の右端ハンドルをクリック
**Then**: col_span は変わらない

### テストケース4: 空セルにハンドルが表示されない 🔵

**信頼性**: 🔵 *設計文書より*

### テストケース5: グリッド境界ではハンドルが表示されない 🔵

**信頼性**: 🔵 *can_expand 条件より*

---

## UI/UX要件

### ハンドルの視覚仕様 🟡

**信頼性**: 🟡 *ユーザーヒアリング（端のみハンドル）から推測*

- 非ホバー時: 透明（見えない）
- ホバー時: 薄い青色でハイライト
- カーソル: 右端→ResizeHorizontal、下端→ResizeVertical
- 太さ: 6px

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2082`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- **総項目数**: 11項目
- 🔵 **青信号**: 10項目 (91%)
- 🟡 **黄信号**: 1項目 (9%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
