# TASK-2078: safe_expand_* メソッドの実装と単体テスト

**タスクID**: TASK-2078
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 1 - 型定義とデータモデル
**信頼性レベル**: 🔵 *設計文書 interfaces.rs・コード分析より*

## 関連文書

- **概要**: [overview.md](overview.md)
- **設計文書**: [architecture.md](../../design/chart-widget-canvas-control/architecture.md)
- **型定義**: [interfaces.rs](../../design/chart-widget-canvas-control/interfaces.rs)

## タスク概要

GridLayout に `safe_expand_right()` と `safe_expand_down()` を追加する。現在の `expand_right/down` は対象セルにコンテンツがあるかチェックしないため、結合後にコンテンツが見えなくなるバグがある。safe版では対象セルのコンテンツ有無をチェックし、安全に拡張する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-2082](TASK-2082.md)

## 完了条件

- [ ] `safe_expand_right()` が対象セルの content.is_some() をチェックする
- [ ] `safe_expand_down()` が対象セルの content.is_some() をチェックする
- [ ] コンテンツありセルへの拡張が false を返す
- [ ] グリッド境界超過が false を返す
- [ ] 既存 expand_right/down は変更しない（コンテキストメニュー用に残す）
- [ ] `cargo test` で全テストが通る

---

## 実装詳細

### 1. safe_expand_right の実装 🔵

**信頼性**: 🔵 *設計文書 interfaces.rs より*

**実装ファイル**: `egui-app/src/state/layout_state.rs`

```rust
/// 安全な右方向拡張（対象セルが空の場合のみ結合を許可）
pub fn safe_expand_right(&mut self, row: usize, col: usize) -> bool {
    let new_end_col = col + self.cells[row][col].col_span as usize;
    if new_end_col >= self.cols { return false; }
    let target = &self.cells[row][new_end_col];
    if target.merged_into.is_some() { return false; }
    if target.content.is_some() { return false; }
    self.cells[row][new_end_col].merged_into = Some((row, col));
    self.cells[row][col].col_span += 1;
    true
}
```

### 2. safe_expand_down の実装 🔵

**信頼性**: 🔵 *設計文書 interfaces.rs より*

同じパターンで下方向版を実装。

---

## 単体テスト要件

### テストケース1: 空セルへの右拡張が成功する 🔵

**信頼性**: 🔵 *設計文書より*

**Given**: 2x2 グリッド、[0][0] にコンテンツあり、[0][1] は空
**When**: safe_expand_right(0, 0)
**Then**: true を返し、col_span=2 になる

### テストケース2: コンテンツありセルへの右拡張が失敗する 🔵

**信頼性**: 🔵 *バグ修正要件より*

**Given**: 2x2 グリッド、[0][0] と [0][1] 両方にコンテンツあり
**When**: safe_expand_right(0, 0)
**Then**: false を返し、レイアウトは変更されない

### テストケース3: グリッド境界での右拡張が失敗する 🔵

**信頼性**: 🔵 *既存 expand_right のテストパターンより*

**Given**: セルが既に右端まで拡張済み
**When**: safe_expand_right(0, 0)
**Then**: false を返す

### テストケース4: 空セルへの下拡張が成功する 🔵

**信頼性**: 🔵 *safe_expand_right の対称性より*

### テストケース5: コンテンツありセルへの下拡張が失敗する 🔵

**信頼性**: 🔵 *バグ修正要件より*

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2078` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

- **総項目数**: 8項目
- 🔵 **青信号**: 8項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
