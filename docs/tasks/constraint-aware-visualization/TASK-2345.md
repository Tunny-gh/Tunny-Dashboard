# TASK-2345: compute_pareto_ranks() の feasibility フィルタ + 違反量ランキング実装

**タスクID**: TASK-2345
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 1 - rust_core 変更
**信頼性レベル**: 🔵 *要件定義書 REQ-CAV-030〜033 + ユーザヒアリング 2026-06-03 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/constraint-aware-visualization/requirements.md) - REQ-CAV-001, REQ-CAV-030〜033, EDGE-CAV-010
- **設計文書**: [📐 architecture.md](../../design/constraint-aware-visualization/architecture.md)
- **実装ガイド**: [🔧 implementation-guide.md](../../design/constraint-aware-visualization/implementation-guide.md)

## タスク概要

`rust_core/src/multi_objective/pareto/ranking.rs` の `compute_pareto_ranks()` 関数に、制約条件を考慮した Pareto ランク計算を実装する。

- 実行可能解（`is_feasible > 0.5`）のみで Pareto 非支配ソートを実行
- 実行不可能解を `constraint_sum` 昇順で `max_feasible_rank + 1, +2, ...` にランク付け
- `pareto_indices` には実行可能解の `rank == 0` のみを含める

既存の `is_feasible` 列と `constraint_sum` 列を活用するため、DataFrame の変更は不要。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-2346](TASK-2346.md), [TASK-2347](TASK-2347.md), [TASK-2348](TASK-2348.md), [TASK-2349](TASK-2349.md), [TASK-2350](TASK-2350.md)

## 完了条件

- [ ] `compute_pareto_ranks()` が `is_feasible` 列を読み取り、feasible 行のみで `nd_sort()` を実行する
- [ ] infeasible 行が `constraint_sum` 昇順で `max_feasible_rank + 1, +2, ...` にランク付けされる
- [ ] `pareto_indices` に infeasible trial のインデックスが含まれない
- [ ] 制約なし Study（`is_feasible` 列なし）では従来の動作と完全に同じ結果になる
- [ ] `cargo test -p tunny-core` が全件パス
- [ ] 以下のユニットテストが追加されパスする:
  - feasible のみの場合は従来と同結果
  - 全 trial が infeasible の場合は `pareto_indices` が空
  - infeasible が `constraint_sum` 昇順でランク付けされる
  - 混在ケースで infeasible が Pareto フロントに含まれない

---

## 実装詳細

### 1. `compute_pareto_ranks()` の変更 🔵

**信頼性**: 🔵 *ユーザヒアリング「rust_core 内部で処理」+ 実装ガイド implementation-guide.md 変更 2 より*

**実装ファイル**: `rust_core/src/multi_objective/pareto/ranking.rs`

変更内容:
1. 関数冒頭で `df.get_numeric_column("is_feasible")` と `df.get_numeric_column("constraint_sum")` を取得
2. `is_feasible` 列が `None`（制約なし）の場合は従来フローをそのまま実行
3. 制約あり場合:
   - feasible 行のみのインデックス・目的値を抽出
   - feasible 行のみで `nd_sort()` を実行し `max_feasible_rank` を取得
   - infeasible 行を `constraint_sum` 昇順でソートし `max_feasible_rank + 1, +2, ...` を割り当て
   - `pareto_indices` = feasible 行の `rank == 0` のインデックス

```rust
pub fn compute_pareto_ranks(is_minimize: &[bool]) -> ParetoResult {
    crate::dataframe::with_active_df(|df| {
        let obj_names = df.objective_col_names();
        let m = obj_names.len();
        let n = df.row_count();
        if n == 0 || m == 0 {
            return ParetoResult { ranks: vec![], pareto_indices: vec![], hypervolume: None };
        }

        let obj_cols: Vec<Option<&[f64]>> = obj_names
            .iter()
            .map(|name| df.get_numeric_column(name))
            .collect();

        // ★ 追加: feasibility 情報を取得
        let is_feasible_col = df.get_numeric_column("is_feasible");
        let constraint_sum_col = df.get_numeric_column("constraint_sum");
        let has_constraints = is_feasible_col.is_some();

        if !has_constraints {
            // 制約なし: 従来フロー（変更なし）
            let objectives: Vec<Vec<f64>> = (0..n)
                .map(|row| obj_cols.iter()
                    .map(|col| col.and_then(|c| c.get(row)).copied().unwrap_or(f64::NAN))
                    .collect())
                .collect();
            let ranks = nd_sort(&objectives, is_minimize);
            let pareto_indices: Vec<u32> = ranks.iter().enumerate()
                .filter(|(_, &r)| r == 0).map(|(i, _)| i as u32).collect();
            let hypervolume = compute_hypervolume(&pareto_indices, &objectives, is_minimize, m);
            return ParetoResult { ranks, pareto_indices, hypervolume };
        }

        // ★ 制約あり: feasible/infeasible 分離フロー
        let objectives: Vec<Vec<f64>> = (0..n)
            .map(|row| obj_cols.iter()
                .map(|col| col.and_then(|c| c.get(row)).copied().unwrap_or(f64::NAN))
                .collect())
            .collect();

        let is_feasible_vals = is_feasible_col.unwrap();

        let feasible_indices: Vec<usize> = (0..n)
            .filter(|&i| is_feasible_vals.get(i).copied().unwrap_or(1.0) > 0.5)
            .collect();
        let feasible_objectives: Vec<Vec<f64>> = feasible_indices.iter()
            .map(|&i| objectives[i].clone())
            .collect();

        let mut ranks = vec![0u32; n];

        let max_feasible_rank = if feasible_objectives.is_empty() {
            0u32
        } else {
            let feasible_ranks = nd_sort(&feasible_objectives, is_minimize);
            let max_r = feasible_ranks.iter().max().copied().unwrap_or(0);
            for (k, &orig_idx) in feasible_indices.iter().enumerate() {
                ranks[orig_idx] = feasible_ranks[k];
            }
            max_r
        };

        let mut infeasible_with_sum: Vec<(usize, f64)> = (0..n)
            .filter(|&i| is_feasible_vals.get(i).copied().unwrap_or(1.0) <= 0.5)
            .map(|i| {
                let sum = constraint_sum_col
                    .and_then(|col| col.get(i))
                    .copied()
                    .unwrap_or(0.0);
                (i, sum)
            })
            .collect();
        infeasible_with_sum.sort_by(|a, b| a.1.partial_cmp(&b.1).unwrap_or(std::cmp::Ordering::Equal));

        for (violation_rank, (orig_idx, _)) in infeasible_with_sum.iter().enumerate() {
            ranks[*orig_idx] = max_feasible_rank + 1 + violation_rank as u32;
        }

        let pareto_indices: Vec<u32> = feasible_indices.iter()
            .filter(|&&i| ranks[i] == 0)
            .map(|&i| i as u32)
            .collect();

        let hypervolume = compute_hypervolume(&pareto_indices, &objectives, is_minimize, m);

        ParetoResult { ranks, pareto_indices, hypervolume }
    })
    .unwrap_or(ParetoResult { ranks: vec![], pareto_indices: vec![], hypervolume: None })
}
```

注意事項:
- `with_active_df()` クロージャ内で完結させる（既存パターンを維持）
- Hypervolume 計算は feasible の `pareto_indices` のみで行う（既存 `compute_hypervolume` ヘルパーを流用）
- 全 feasible が NaN の場合の安全処理は `nd_sort()` 内の既存 `nan_mask` に委譲

### 2. ユニットテストの追加 🔵

**信頼性**: 🔵 *要件定義書 REQ-CAV-030〜033 + 受け入れ基準 TC-CAV-030-01〜B01 より*

**テストファイル**: `rust_core/src/multi_objective/pareto/tests.rs`

追加するテストケース:

#### TC-2345-01: infeasible が Pareto フロントから除外される 🔵

**信頼性**: 🔵 *REQ-CAV-033 より*

**Given**: feasible trial が 2 件（目的値 [1,2] と [2,1]）、infeasible trial が 1 件（目的値 [0,0]、constraint_sum=1.0）
**When**: `compute_pareto_ranks()` を呼び出す
**Then**: `pareto_indices` に infeasible trial のインデックスが含まれない

```rust
#[test]
fn feasible_only_pareto_excludes_infeasible() {
    // SharedStudyStore に制約あり trial をセットアップ
    // compute_pareto_ranks() 呼び出し後
    // assert!(!result.pareto_indices.contains(&infeasible_idx as u32))
}
```

#### TC-2345-02: infeasible が constraint_sum 昇順でランク付けされる 🔵

**信頼性**: 🔵 *REQ-CAV-032 より*

**Given**: infeasible trial が 3 件（constraint_sum それぞれ 3.0, 1.0, 2.0）、feasible trial なし
**When**: `compute_pareto_ranks()` を呼び出す
**Then**: constraint_sum=1.0 の trial が最小ランク（1）、constraint_sum=3.0 の trial が最大ランク（3）になる

```rust
#[test]
fn infeasible_ranked_by_constraint_sum_ascending() {
    // ranks[trial_with_sum_1.0] < ranks[trial_with_sum_2.0] < ranks[trial_with_sum_3.0]
}
```

#### TC-2345-03: 全 trial が infeasible のとき pareto_indices が空 🔵

**信頼性**: 🔵 *EDGE-CAV-010 より*

**Given**: 全 trial が infeasible（is_feasible=0.0）
**When**: `compute_pareto_ranks()` を呼び出す
**Then**: `result.pareto_indices` が空ベクタになる

```rust
#[test]
fn all_infeasible_gives_empty_pareto_indices() {
    // assert!(result.pareto_indices.is_empty())
}
```

#### TC-2345-04: 制約なし Study では従来と同結果 🔵

**信頼性**: 🔵 *REQ-CAV-031（has_constraints == true の場合のみフィルタ）より*

**Given**: `is_feasible` 列を持たない DataFrame（制約なし Study）
**When**: `compute_pareto_ranks()` を呼び出す
**Then**: 制約なしフローと同じランク・同じ `pareto_indices` が返る

```rust
#[test]
fn no_constraints_behaves_identically() {
    // 同一 trial セットを制約あり/なしで比較し、pareto_indices が一致することを確認
}
```

---

## 単体テスト要件

| テストID | 内容 | 信頼性 |
|----------|------|--------|
| TC-2345-01 | infeasible が pareto_indices から除外される | 🔵 |
| TC-2345-02 | infeasible が constraint_sum 昇順でランク付け | 🔵 |
| TC-2345-03 | 全 infeasible のとき pareto_indices が空 | 🔵 |
| TC-2345-04 | 制約なし Study では従来と同結果 | 🔵 |

---

## 実装手順

TDD の場合:
1. `/tsumiki:tdd-requirements TASK-2345` - 詳細要件定義（ranking.rs の既存コード・DataFrame アクセスパターン確認）
2. `/tsumiki:tdd-testcases` - TC-2345-01〜04 をテストコードに変換
3. `/tsumiki:tdd-red` - テスト実装（コンパイルエラー・失敗状態を確認）
4. `/tsumiki:tdd-green` - `compute_pareto_ranks()` に feasibility フィルタ・違反量ランキングを実装
5. `/tsumiki:tdd-refactor` - `compute_hypervolume` ヘルパーへの共通化・境界値処理の整理
6. `/tsumiki:tdd-verify-complete` - `cargo test -p tunny-core` 全件パス確認

---

## 注意事項

- `with_active_df()` クロージャ内で完結させること（既存の ranking.rs パターンを維持）
- `is_feasible` 列の閾値は `> 0.5`（model.rs の既存定義に合わせる）
- `constraint_sum` 列が存在しない場合は `unwrap_or(0.0)` で安全に処理する
- 全 feasible trial が NaN 目的値を持つ場合は `nd_sort()` 内の既存 `nan_mask` に委譲
- `infeasible_with_sum` のソートには `partial_cmp` を使用し NaN を `Equal` 扱いにする

---

## 関連要件

| 要件ID | 内容 |
|--------|------|
| REQ-CAV-001 | 実行可能性の判定（既実装の `is_feasible` 列を使用） |
| REQ-CAV-030 | Pareto ランク計算から実行不可能解を除外 |
| REQ-CAV-031 | `has_constraints == true` の場合のみフィルタ |
| REQ-CAV-032 | infeasible の pareto_rank = max_feasible_rank + 1 + violation_rank |
| REQ-CAV-033 | pareto_indices に infeasible を含めない |
| EDGE-CAV-010 | 全 trial が infeasible → pareto_indices が空 |

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 4 | 0 | 0 | 4 |

### 全体評価

- **総項目数**: 6項目
- 🔵 **青信号**: 6件 (100%)
- 🟡 **黄信号**: 0件 (0%)
- 🔴 **赤信号**: 0件 (0%)

**品質評価**: 高品質
