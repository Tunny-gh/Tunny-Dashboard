# TASK-2165: compute_pdp_1d_kriging_raw の正規化/R² をヘルパー呼び出しに変更

**タスクID**: TASK-2165
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 2 - kriging_core.rs リファクタリング
**信頼性レベル**: 🔵 *要件定義書 REQ-103/202 + 設計文書 architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/pdp-maintainability/requirements.md) - REQ-103, REQ-202
- **設計文書**: [📐 architecture.md](../../design/pdp-maintainability/architecture.md)

## タスク概要

`kriging_core.rs` の `compute_pdp_1d_kriging_raw` 関数で重複している
正規化ブロック（20行）と R² 計算ブロック（8行）を、
TASK-2162/2163 で追加した `normalize_x_minmax`, `normalize_y`, `r_squared` に置き換える。

## 依存タスク

- **前提タスク**: [TASK-2162: normalize_x_minmax / normalize_y](TASK-2162.md), [TASK-2163: r_squared](TASK-2163.md)
- **後続タスク**: [TASK-2166](TASK-2166.md)

## 完了条件

- [ ] `compute_pdp_1d_kriging_raw` が `normalize_x_minmax`, `normalize_y`, `r_squared` を呼んでいる
- [ ] 重複コードブロック（合計~28行）が削除されている
- [ ] 既存テスト `tc_1645_01_kriging_raw_grid_shape` が PASS すること
- [ ] `cargo test` が全件 PASS すること

---

## 実装詳細

### 1. 正規化ブロックの置き換え 🔵

**信頼性**: 🔵 *REQ-103 + 設計文書 architecture.md より*

**実装ファイル**: `rust_core/src/pdp/kriging_core.rs`

変更前（col_stats + y正規化 + x正規化の合計~25行のブロック）を
以下の2行に置き換える:

```rust
use super::utils::{col_mean_std, normalize_x_minmax, normalize_y, r_squared};

let (col_stats, x_norm) = normalize_x_minmax(x_matrix);
let (y_mean, y_std, y_norm) = normalize_y(y);
```

### 2. R² ブロックの置き換え 🔵

**信頼性**: 🔵 *REQ-202 + 設計文書 architecture.md より*

変更前（ss_tot / ss_res 計算の~8行）を以下に置き換える:

```rust
let y_pred: Vec<f64> = x_norm
    .iter()
    .map(|xi| gaussian_process::predict_mean(&model, xi) * y_std + y_mean)
    .collect();
let r_sq = r_squared(y, &y_pred);
```

---

## 単体テスト要件（既存テストで確認）

### tc_1645_01_kriging_raw_grid_shape 🔵

**信頼性**: 🔵 *既存テストコードより*

**Given**: N=30 の線形データ
**When**: `compute_pdp_2d_kriging_raw` を呼ぶ
**Then**: grid shape が n_grid と一致する

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2165`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)

**品質評価**: 高品質
