# TASK-2168: api.rs の extract_xy 適用

**タスクID**: TASK-2168
**タスクタイプ**: TDD
**推定工数**: 1.5時間
**フェーズ**: Phase 3 - api.rs / ridge_core.rs 修正 + 確認
**信頼性レベル**: 🔵 *要件定義書 REQ-302 + 設計文書 dataflow.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/pdp-maintainability/requirements.md) - REQ-302
- **設計文書**: [📐 architecture.md](../../design/pdp-maintainability/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/pdp-maintainability/dataflow.md)

## タスク概要

`api.rs` の `compute_pdp` と `compute_pdp_2d` で重複している
DataFrame → (x_matrix, y) 変換コード（各15行）を
TASK-2164 で追加した `extract_xy` 呼び出し（1行）に置き換える。

## 依存タスク

- **前提タスク**: [TASK-2164: extract_xy](TASK-2164.md), [TASK-2167](TASK-2167.md)
- **後続タスク**: [TASK-2170](TASK-2170.md)

## 完了条件

- [ ] `compute_pdp` 内の x_matrix / y 構築コードが `extract_xy` に置き換えられている
- [ ] `compute_pdp_2d` 内の x_matrix / y 構築コードが `extract_xy` に置き換えられている
- [ ] 既存の動作が変わらないこと（既存テストで確認）
- [ ] `cargo test` が全件 PASS すること

---

## 実装詳細

### 1. compute_pdp の extract_xy 適用 🔵

**信頼性**: 🔵 *REQ-302 + 設計文書 dataflow.md より*

**実装ファイル**: `rust_core/src/pdp/api.rs`

```rust
// 変更前（x_matrix 構築の ~10行 + y 構築の ~7行）

// 変更後
use super::utils::extract_xy;

crate::dataframe::with_active_df(|df| {
    let param_names = df.param_col_names().to_vec();
    let n = df.row_count();
    let target_idx = param_names.iter().position(|p| p == param_name)?;
    let _ = df.objective_col_names().iter().position(|o| o == objective_name)?;
    let _ = n; // 不要になる可能性あり

    let (x_matrix, y) = extract_xy(df, &param_names, objective_name); // ★

    Some(compute_pdp_from_matrix(&x_matrix, &y, &param_names, objective_name, target_idx, n_grid))
}).flatten()
```

### 2. compute_pdp_2d の extract_xy 適用 🔵

**信頼性**: 🔵 *REQ-302 + 設計文書 dataflow.md より*

同様のパターンで置き換える。
`extract_xy` が返す `x_matrix` から `p1_idx`, `p2_idx` でスライスする処理はそのまま維持。

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2168`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- **総項目数**: 2項目
- 🔵 **青信号**: 2項目 (100%)

**品質評価**: 高品質
