# TASK-2162: normalize_x_minmax / normalize_y を utils.rs に追加

**タスクID**: TASK-2162
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 1 - ベースライン確認 + utils.rs ヘルパー追加
**信頼性レベル**: 🔵 *要件定義書 REQ-101/102 + 設計文書 architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/pdp-maintainability/requirements.md) - REQ-101, REQ-102
- **設計文書**: [📐 architecture.md](../../design/pdp-maintainability/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/pdp-maintainability/dataflow.md)

## タスク概要

`rust_core/src/pdp/utils.rs` に `normalize_x_minmax` と `normalize_y` の2関数を追加する。
これらは `kriging_core.rs` の4関数で重複している正規化ブロック（合計~80行）を共通化する。

## 依存タスク

- **前提タスク**: [TASK-2161: cargo test ベースライン確認](TASK-2161.md)
- **後続タスク**: [TASK-2165](TASK-2165.md), [TASK-2166](TASK-2166.md)

## 完了条件

- [ ] `normalize_x_minmax` が `utils.rs` に実装されている
- [ ] `normalize_y` が `utils.rs` に実装されている
- [ ] 定数列（range=0）のクランプ（EPSILON）が実装されている
- [ ] 空スライスのエッジケースが処理されている
- [ ] `cargo test` が全件 PASS すること

---

## 実装詳細

### 1. normalize_x_minmax の実装 🔵

**信頼性**: 🔵 *REQ-101 + 設計文書 architecture.md + dataflow.md より*

**実装ファイル**: `rust_core/src/pdp/utils.rs`

```rust
/// 各列を min-max 正規化する。
///
/// 戻り値:
/// - col_stats: 各次元の (min, range) タプル。range = (max - min).max(EPSILON)
/// - x_norm: 正規化済み行列（各値が [0, 1] に収まる）
///
/// 定数列（range == 0）は EPSILON でクランプして NaN を防ぐ。
pub(super) fn normalize_x_minmax(
    x_matrix: &[Vec<f64>],
) -> (Vec<(f64, f64)>, Vec<Vec<f64>>) {
    let n_dims = x_matrix.first().map(|r| r.len()).unwrap_or(0);
    let col_stats: Vec<(f64, f64)> = (0..n_dims)
        .map(|d| {
            let min = x_matrix.iter().map(|r| r[d]).fold(f64::INFINITY, f64::min);
            let max = x_matrix.iter().map(|r| r[d]).fold(f64::NEG_INFINITY, f64::max);
            (min, (max - min).max(f64::EPSILON))
        })
        .collect();
    let x_norm = x_matrix
        .iter()
        .map(|row| {
            row.iter()
                .enumerate()
                .map(|(d, &v)| {
                    let (min, range) = col_stats[d];
                    (v - min) / range
                })
                .collect()
        })
        .collect();
    (col_stats, x_norm)
}
```

### 2. normalize_y の実装 🔵

**信頼性**: 🔵 *REQ-102 + 設計文書 dataflow.md より*

**実装ファイル**: `rust_core/src/pdp/utils.rs`

```rust
/// y を標準化（z-score 正規化）する。
///
/// 戻り値: (y_mean, y_std, y_norm)
/// y_std の最小値: f64::EPSILON（ゼロ除算ガード）
pub(super) fn normalize_y(y: &[f64]) -> (f64, f64, Vec<f64>) {
    let n = y.len();
    if n == 0 {
        return (0.0, f64::EPSILON, vec![]);
    }
    let y_mean = y.iter().sum::<f64>() / n as f64;
    let var = y.iter().map(|&v| (v - y_mean).powi(2)).sum::<f64>() / n as f64;
    let y_std = var.sqrt().max(f64::EPSILON);
    let y_norm = y.iter().map(|&v| (v - y_mean) / y_std).collect();
    (y_mean, y_std, y_norm)
}
```

---

## 単体テスト要件

### TC-101-01: 通常データの正規化 🔵

**信頼性**: 🔵 *受け入れ基準 TC-101-01 より*

**Given**: `x_matrix = [[0.0, 10.0], [0.5, 20.0], [1.0, 30.0]]`
**When**: `normalize_x_minmax(x_matrix)` を呼ぶ
**Then**: `x_norm[1] == [0.5, 0.5]` (±1e-10)

### TC-101-E01: 定数列のクランプ 🔵

**信頼性**: 🔵 *受け入れ基準 TC-101-E01 より*

**Given**: `x_matrix = [[5.0], [5.0], [5.0]]`
**When**: `normalize_x_minmax(x_matrix)` を呼ぶ
**Then**: パニックなし、`col_stats[0].1 == f64::EPSILON`

### TC-102-01: y 正規化の正確性 🔵

**信頼性**: 🔵 *受け入れ基準 TC-102-01 より*

**Given**: `y = [1.0, 2.0, 3.0]`
**When**: `normalize_y(y)` を呼ぶ
**Then**: `y_mean ≈ 2.0`, `y_std > 0`, `y_norm.iter().sum() ≈ 0.0`

### TC-102-E01: 空スライス 🟡

**信頼性**: 🟡 *受け入れ基準 TC-102-E01 から妥当な推測*

**Given**: `y = []`
**When**: `normalize_y(y)` を呼ぶ
**Then**: `(0.0, EPSILON, vec![])` が返る、パニックなし

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2162` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - normalize_x_minmax / normalize_y 実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 3 | 1 | 0 | 4 |

- **総項目数**: 6項目
- 🔵 **青信号**: 5項目 (83%)
- 🟡 **黄信号**: 1項目 (17%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
