# TASK-2171: Cargo.toml に rayon を追加

**タスクID**: TASK-2171
**タスクタイプ**: DIRECT
**推定工数**: 0.5時間
**フェーズ**: Phase 4 - rayon 並列化
**信頼性レベル**: 🔵 *要件定義書 REQ-501 + ユーザヒアリング（制限なし）より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/pdp-maintainability/requirements.md) - REQ-501
- **設計文書**: [📐 architecture.md](../../design/pdp-maintainability/architecture.md)

## タスク概要

`rust_core/Cargo.toml` の `[dependencies]` セクションに `rayon = "1"` を追加する。
ユーザーの確認で「rust_core は WASM と分離されているため制限なし」と確認済み。

## 依存タスク

- **前提タスク**: [TASK-2170: Phase 1-3 完了後の全テスト確認](TASK-2170.md)
- **後続タスク**: [TASK-2172](TASK-2172.md), [TASK-2173](TASK-2173.md)

## 完了条件

- [ ] `rust_core/Cargo.toml` に `rayon = "1"` が追加されている
- [ ] `cargo build` が成功すること
- [ ] `cargo test` が全件 PASS すること（rayon 未使用の段階でのコンパイル確認）

---

## 実装詳細

### 1. Cargo.toml への rayon 追加 🔵

**信頼性**: 🔵 *REQ-501 + ユーザヒアリング（制限なし）より*

**実装ファイル**: `rust_core/Cargo.toml`

```toml
[dependencies]
faer = "0.24.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rayon = "1"   # ← 追加
```

---

## 実装手順

1. `/tsumiki:direct-setup` - Cargo.toml を編集
2. `/tsumiki:direct-verify` - `cargo build` と `cargo test` で確認

---

## 注意事項

- WASM バインディングクレート側で rayon を使わないよう注意（本タスクのスコープ外）
- `rayon = "1"` は最新の 1.x 系を指定。2026年5月時点で 1.10 が安定版

---

## 信頼性レベルサマリー

- **総項目数**: 1項目
- 🔵 **青信号**: 1項目 (100%)

**品質評価**: 高品質
