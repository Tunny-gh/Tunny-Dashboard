# TASK-2057: ChartId::PdpChart2D + AppMessage::Pdp2dDone 追加

**タスクID**: TASK-2057
**タスクタイプ**: TDD
**推定工数**: 4h
**フェーズ**: Phase 3 - egui-app 接続
**信頼性レベル**: 🔵 *設計文書 architecture.md + 既存 ChartId パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/pdp-chart-2d/architecture.md)
- **データフロー図**: [🔄 dataflow.md](../../design/pdp-chart-2d/dataflow.md)

## タスク概要

`egui-app/src/state/layout_state.rs` に `ChartId::PdpChart2D` バリアントを追加し、`egui-app/src/state/messages.rs` に `AppMessage::Pdp2dDone(PdpResult2d)` を追加する。

## 依存タスク

- **前提タスク**: [TASK-2055](TASK-2055.md)（egui-app の PdpResult2d に uncertainties が追加済みであること）
- **後続タスク**: [TASK-2058](TASK-2058.md), [TASK-2059](TASK-2059.md)

## 完了条件

- [x] `ChartId::PdpChart2D` バリアントが追加されている
- [x] `ChartId::all()` が `PdpChart2D` を含む
- [x] `ChartId::label()` が `"PDP Chart 2D"` を返す
- [x] `AppMessage::Pdp2dDone(PdpResult2d)` バリアントが追加されている
- [x] 単体テスト全通過
- [x] `cargo build` でエラーなし

---

## 実装詳細

### 1. ChartId::PdpChart2D バリアント追加 🔵

**信頼性**: 🔵 *設計文書 architecture.md + 既存 ChartId パターンより*

**変更ファイル**: `egui-app/src/state/layout_state.rs`

```rust
pub enum ChartId {
    // 既存バリアント...
    PdpChart2D,  // 追加
}

pub fn all() -> &'static [ChartId] {
    &[
        // 既存...
        ChartId::PdpChart2D,  // 追加
    ]
}

pub fn label(&self) -> &'static str {
    match self {
        // 既存...
        ChartId::PdpChart2D => "PDP Chart 2D",  // 追加
    }
}
```

### 2. AppMessage::Pdp2dDone バリアント追加 🔵

**信頼性**: 🔵 *設計文書 architecture.md + 既存 AppMessage パターンより*

**変更ファイル**: `egui-app/src/state/messages.rs`

```rust
pub enum AppMessage {
    // 既存...
    Pdp2dDone(PdpResult2d),  // 追加
}
```

---

## 単体テスト要件

### テストケース1: chart_id_all_contains_pdp2d 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

**Given/When/Then**
- Given: `ChartId::all()` を呼び出す
- When: 返却されたスライスを確認する
- Then: `ChartId::PdpChart2D` が含まれている

**テストファイル**: `egui-app/src/state/layout_state.rs`

### テストケース2: chart_id_label_pdp2d 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

**Given/When/Then**
- Given: `ChartId::PdpChart2D`
- When: `.label()` を呼び出す
- Then: `"PDP Chart 2D"` が返る

**テストファイル**: `egui-app/src/state/layout_state.rs`

### テストケース3: pdp2d_done_variant_exists 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

**Given/When/Then**
- Given: `PdpResult2d` 型の値
- When: `AppMessage::Pdp2dDone(result)` を構築する
- Then: コンパイルが通る（コンパイル時テスト）

**テストファイル**: `egui-app/src/state/messages.rs`

---

## 実装手順

### TDDタスクの場合

1. `/tsumiki:tdd-requirements TASK-2057` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `ChartId` の `match` 式を使っている箇所（`grid_canvas.rs` 等）に `PdpChart2D` のアームを追加する必要があるが、それは TASK-2059 で行う
- `layout_state.rs` の既存の `all()` スライスに追加するだけでチャートピッカーに自動表示される

---

## 信頼性レベルサマリー

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
