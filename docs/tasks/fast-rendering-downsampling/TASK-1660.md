# TASK-1660: WASM バインディング追加（lib.rs・tunny_core.d.ts・wasmLoader.ts）

**タスクID**: TASK-1660
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - WASM Core
**信頼性レベル**: 🔵 *wasm-api.md / 既存 WasmLoader パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/fast-rendering-downsampling/architecture.md)
- **WASM API 仕様**: [📐 wasm-api.md](../../design/tunny-dashboard/wasm-api.md)
- **型定義**: [📝 interfaces.ts](../../design/fast-rendering-downsampling/interfaces.ts)

## タスク概要

TASK-1656〜1659 で実装した Rust 関数を WASM 経由で JavaScript から呼び出せるように、3ファイルを修正する。既存の `computeSensitivity`・`runPca` 等と同じパターンで実装する。

## 依存タスク

- **前提タスク**: [TASK-1656](TASK-1656.md), [TASK-1657](TASK-1657.md), [TASK-1658](TASK-1658.md), [TASK-1659](TASK-1659.md)
- **後続タスク**: [TASK-1661: downsampleStore 実装](TASK-1661.md)

## 完了条件

- [x] `rust_core/src/lib.rs` に 4 つの `#[wasm_bindgen]` 関数が追加されている
- [x] `wasm-pack build` が成功する
- [x] `frontend/src/wasm/pkg/tunny_core.d.ts` に型定義が追加されている
- [x] `frontend/src/wasm/wasmLoader.ts` に 4 プロパティとバインドが追加されている

---

## 実装詳細

### 1. lib.rs への WASM バインディング追加 🔵

**信頼性**: 🔵 *既存 wasm_bindgen パターン / wasm-api.md より*

**実装ファイル**: `rust_core/src/lib.rs`

```rust
#[wasm_bindgen]
pub fn downsample_smart(max_points: u32, include_pareto: bool) -> JsValue {
    // sampling::downsample_smart() を呼び出し、JsValue に変換
}

#[wasm_bindgen]
pub fn downsample_for_thumbnail(max_points: u32) -> JsValue { ... }

#[wasm_bindgen]
pub fn downsample_stratified_by_rank(max_points: u32, n_strata: u32) -> JsValue { ... }

#[wasm_bindgen]
pub fn downsample_by_cluster(max_points: u32) -> JsValue { ... }
```

シリアライズは既存関数と同じ `serde_wasm_bindgen::Serializer::json_compatible()` を使用。

### 2. tunny_core.d.ts 型定義追加 🔵

**信頼性**: 🔵 *既存 tunny_core.d.ts パターンより*

**実装ファイル**: `frontend/src/wasm/pkg/tunny_core.d.ts`

```typescript
export interface DownsampleResult {
  indices: number[];
  paretoCount: number;
  totalCount: number;
  durationMs: number;
}

export function downsampleSmart(maxPoints: number, includePareto: boolean): DownsampleResult;
export function downsampleForThumbnail(maxPoints: number): DownsampleResult;
export function downsampleStratifiedByRank(maxPoints: number, nStrata: number): DownsampleResult;
export function downsampleByCluster(maxPoints: number): DownsampleResult;
```

### 3. wasmLoader.ts へのメソッド追加 🔵

**信頼性**: 🔵 *既存 WasmLoader パターンより*

**実装ファイル**: `frontend/src/wasm/wasmLoader.ts`

既存の `parseJournal`・`computeSensitivity` と同じパターンで 4 プロパティを追加し、`_initialize()` 内でバインドする。

```typescript
downsampleSmart!: (maxPoints: number, includePareto: boolean) => DownsampleResult;
downsampleForThumbnail!: (maxPoints: number) => DownsampleResult;
downsampleStratifiedByRank!: (maxPoints: number, nStrata: number) => DownsampleResult;
downsampleByCluster!: (maxPoints: number) => DownsampleResult;
```

---

## 実装手順

1. `/tsumiki:direct-setup` - 3ファイルの修正と wasm-pack ビルド
2. `/tsumiki:direct-verify` - ビルド成功・型定義確認

---

## 注意事項

- wasm-pack ビルド後 `tunny_core.d.ts` が自動更新されるが、インターフェース定義（DownsampleResult）は手動追加が必要
- ビルドコマンド: `cd rust_core && wasm-pack build --target web --out-dir ../frontend/src/wasm/pkg`

---

## 信頼性レベルサマリー

- 🔵 青信号: 3項目 (100%)
- 🟡 黄信号: 0項目 (0%)
- 🔴 赤信号: 0項目 (0%)

**品質評価**: 高品質
