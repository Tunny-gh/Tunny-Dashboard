# TASK-1667: ScatterMatrix への thumbnail/hover インデックス統合

**タスクID**: TASK-1667
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - Chart Integration
**信頼性レベル**: 🔵 *REQ-063 / architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/tunny-dashboard-requirements.md) - REQ-061, REQ-063
- **設計文書**: [📐 architecture.md](../../design/fast-rendering-downsampling/architecture.md)

## タスク概要

`ScatterMatrix.tsx` に `useDownsampleStore` を統合する。サムネイル描画（80×80px）は `getIndices('thumbnail')` で 500 点、ホバー拡大（300×300px）は `getIndices('hover')` で 3,000 点を使用する（REQ-061・063 に準拠）。

## 依存タスク

- **前提タスク**: [TASK-1663](TASK-1663.md)
- **後続タスク**: [TASK-1672](TASK-1672.md)

## 完了条件

- [ ] サムネイル描画で `getIndices('thumbnail')` が使用されている
- [ ] ホバー描画で `getIndices('hover')` が使用されている
- [ ] サムネイル 500 点・ホバー 3,000 点の制限テストが通る

---

## 実装詳細

### サムネイル / ホバーの使い分け 🔵

**信頼性**: 🔵 *REQ-061（3段階レンダリング）/ DOWNSAMPLE_CONFIGS より*

**実装ファイル**: `frontend/src/components/charts/ScatterMatrix.tsx`

```typescript
const thumbnailIndices = getIndices('thumbnail'); // 500点、サムネイル用
const hoverIndices = getIndices('hover');          // 3,000点、ホバー用
```

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-1667`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- 🔵 青信号: 2項目 (100%)
- 品質評価: 高品質
