# TASK-1663: downsampleStore getIndices と DOWNSAMPLE_CONFIGS 定数

**タスクID**: TASK-1663
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - State Management
**信頼性レベル**: 🔵 *interfaces.ts DOWNSAMPLE_CONFIGS / architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **型定義**: [📝 interfaces.ts](../../design/fast-rendering-downsampling/interfaces.ts)
- **設計文書**: [📐 architecture.md](../../design/fast-rendering-downsampling/architecture.md)

## タスク概要

`interfaces.ts` の `DOWNSAMPLE_CONFIGS` 定数と `DownsampleKey` 型を `frontend/src/types/downsampling.ts` に実装し、`getIndices` のフォールバック動作（キャッシュなし時は全インデックスを返す）を実装・テストする。

## 依存タスク

- **前提タスク**: [TASK-1662: フィルタ変更対応](TASK-1662.md)
- **後続タスク**: [TASK-1664〜1671: 各チャート統合](TASK-1664.md)

## 完了条件

- [x] `frontend/src/types/downsampling.ts` に型定義と定数が実装されている
- [x] `getIndices` がキャッシュなし時に全インデックスを返すテストが通る
- [x] 各キーに対応する WASM 関数のルーティングが正しいテストが通る
- [x] `vitest` テストが通る

---

## 実装詳細

### 1. downsampling.ts の型定義と定数 🔵

**信頼性**: 🔵 *interfaces.ts より*

**実装ファイル**: `frontend/src/types/downsampling.ts`

`interfaces.ts` の `DOWNSAMPLE_CONFIGS`・`DownsampleKey`・`DownsampleConfig`・`DownsampleStrategy` を TypeScript として実装。

```typescript
export const DOWNSAMPLE_CONFIGS: Record<DownsampleKey, DownsampleConfig> = {
  scatter:      { maxPoints: 10_000, includePareto: true,  strategy: 'smart' },
  thumbnail:    { maxPoints: 500,    includePareto: true,  strategy: 'grid_thumbnail' },
  hover:        { maxPoints: 3_000,  includePareto: true,  strategy: 'grid_thumbnail' },
  pcp:          { maxPoints: 5_000,  includePareto: true,  strategy: 'stratified_by_rank' },
  data_points:  { maxPoints: 5_000,  includePareto: false, strategy: 'smart' },
  cluster:      { maxPoints: 10_000, includePareto: true,  strategy: 'by_cluster' },
};
```

### 2. getIndices のフォールバック 🔵

**信頼性**: 🔵 *architecture.md エラーハンドリングより*

キャッシュが空の場合（WASM 未初期化・エラー時）は全インデックスを返すことで、チャートがクラッシュせず描画を維持する。

---

## 単体テスト要件

### テストケース1: キャッシュなし時のフォールバック 🔵

**Given**: キャッシュが空の状態
**When**: `getIndices('scatter')` を呼び出す
**Then**: 全インデックスの Uint32Array が返される（空配列ではない）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-1663`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- 🔵 青信号: 3項目 (100%)
- 🟡 黄信号: 0項目 (0%)
- 🔴 赤信号: 0項目 (0%)

**品質評価**: 高品質
