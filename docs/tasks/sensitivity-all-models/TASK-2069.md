# TASK-2069: grid_canvas.rs メトリクス別 spawn_task + 統合確認

**タスクID**: TASK-2069
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 2 - egui-app 拡張
**信頼性レベル**: 🔵 *設計文書 architecture.md 2-C・PdpChart2D spawn_task パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/sensitivity-all-models/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/sensitivity-all-models/dataflow.md)

## タスク概要

`egui-app/src/ui/grid_canvas.rs` の `ChartId::ImportanceChart` ケースを更新し：
1. `show()` に `sobol_result` を渡す
2. `pending_compute.take()` でメトリクス別に `spawn_task` を分岐する
その後 `cargo check` + `cargo test` で統合確認を行う。

## 依存タスク

- **前提タスク**: [TASK-2068](TASK-2068.md)
- **後続タスク**: なし

## 完了条件

- [ ] `ChartId::ImportanceChart` ケースで `widgets.importance.show(ui, sensitivity.as_ref(), sobol.as_ref(), &obj_names)` が呼ばれる
- [ ] `pending_compute.take()` によりメトリクス別に `spawn_task` が分岐している
- [ ] Sobol → `AppMessage::SobolDone`、Spearman/Ridge/RfAnova → `AppMessage::SensitivityDone` で返す
- [ ] `cargo check -p tunny-desktop` がエラーなしで通る
- [ ] `cargo test` が全テスト通過する（既存テストへの回帰なし）

---

## 主要実装項目

### 1. `egui-app/src/ui/grid_canvas.rs` — ImportanceChart ケース更新 🔵

**信頼性**: 🔵 *architecture.md 2-C より*

```rust
ChartId::ImportanceChart => {
    let sobol = app_state.sobol_result.as_ref();
    widgets.importance.show(ui, sensitivity.as_ref(), sobol, &obj_names);
    if let Some(metric) = widgets.importance.pending_compute.take() {
        widgets.importance.computing = true;
        let tx = tx.clone();
        crate::app::spawn_task(tx, move || match metric {
            ImportanceMetric::Sobol => {
                tunny_core::sensitivity::compute_sobol(1024)
                    .map(AppMessage::SobolDone)
                    .unwrap_or(AppMessage::SensitivityError)
            }
            ImportanceMetric::Spearman => {
                tunny_core::sensitivity::compute_sensitivity_for(SensitivityMetric::Spearman)
                    .map(AppMessage::SensitivityDone)
                    .unwrap_or(AppMessage::SensitivityError)
            }
            ImportanceMetric::Ridge => {
                tunny_core::sensitivity::compute_sensitivity_for(SensitivityMetric::Ridge)
                    .map(AppMessage::SensitivityDone)
                    .unwrap_or(AppMessage::SensitivityError)
            }
            ImportanceMetric::RfAnova => {
                tunny_core::sensitivity::compute_sensitivity_for(SensitivityMetric::RfAnova)
                    .map(AppMessage::SensitivityDone)
                    .unwrap_or(AppMessage::SensitivityError)
            }
        });
    }
}
```

---

## 統合確認手順

1. `cargo check -p tunny-desktop` — コンパイルエラーがないことを確認
2. `cargo test` — 全テスト通過を確認

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2069`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 注意事項

- `sobol_result` が `app_state` に既に存在することを事前に確認する
- `SensitivityMetric` は `tunny_core::sensitivity::SensitivityMetric` を使用（TASK-2064 で追加済み）
- `AppMessage::SensitivityError` は TASK-2067 で追加済みが前提

---

## 信頼性レベルサマリー

- 🔵 青信号: 1件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: ✅ 高品質
