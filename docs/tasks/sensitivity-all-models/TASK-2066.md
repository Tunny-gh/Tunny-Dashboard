# TASK-2066: compute_sensitivity_for エントリーポイント追加

**タスクID**: TASK-2066
**タスクタイプ**: TDD
**推定工数**: 1時間
**フェーズ**: Phase 1 - rust_core リファクタリング
**信頼性レベル**: 🔵 *設計文書 architecture.md 1-C より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/sensitivity-all-models/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/sensitivity-all-models/dataflow.md)

## タスク概要

`rust_core/src/sensitivity/analysis.rs` に `compute_sensitivity_for(metric: SensitivityMetric)` を追加する。
`mod.rs` で公開し、`tunny_core::sensitivity::compute_sensitivity_for` としてクレート外から呼び出せるようにする。

## 依存タスク

- **前提タスク**: [TASK-2065](TASK-2065.md)
- **後続タスク**: [TASK-2069](TASK-2069.md)

## 完了条件

- [x] `compute_sensitivity_for(metric: SensitivityMetric) -> Option<SensitivityResult>` が `analysis.rs` に追加されている
- [x] `mod.rs` に `pub use analysis::compute_sensitivity_for;` が追加されている
- [x] `compute_sensitivity()` / `compute_sensitivity_all()` は変更なし（後方互換）
- [x] `cargo test -p tunny-core` が全テスト通過する

---

## 主要実装項目

### 1. `rust_core/src/sensitivity/analysis.rs` — compute_sensitivity_for 追加 🔵

**信頼性**: 🔵 *architecture.md 1-C より*

```rust
use super::{SensitivityMetric, SensitivityResult};

pub fn compute_sensitivity_for(metric: SensitivityMetric) -> Option<SensitivityResult> {
    dataframe::with_active_df(|df| match metric {
        SensitivityMetric::Spearman => full::compute_spearman_only(df),
        SensitivityMetric::Ridge    => full::compute_ridge_only(df),
        SensitivityMetric::RfAnova  => full::compute_rf_anova_only(df),
    })
}
```

### 2. `rust_core/src/sensitivity/mod.rs` — pub use 追加 🔵

**信頼性**: 🔵 *architecture.md 1-C より*

```rust
pub use analysis::compute_sensitivity_for;
```

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2066`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- 🔵 青信号: 2件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: ✅ 高品質
