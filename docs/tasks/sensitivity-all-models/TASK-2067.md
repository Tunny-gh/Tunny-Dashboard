# TASK-2067: SensitivityResult 拡張 + AppMessage::SensitivityError 追加

**タスクID**: TASK-2067
**タスクタイプ**: TDD
**推定工数**: 1時間
**フェーズ**: Phase 2 - egui-app 拡張
**信頼性レベル**: 🔵 *設計文書 architecture.md 2-A・2-D・2-E より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/sensitivity-all-models/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/sensitivity-all-models/dataflow.md)

## タスク概要

egui-app 版 `SensitivityResult` に `ridge` / `rf_anova` フィールドを追加する。
`AppMessage::SensitivityError` を `messages.rs` に追加し、`app.rs` の `poll_messages` でハンドリングする。

## 依存タスク

- **前提タスク**: [TASK-2066](TASK-2066.md)
- **後続タスク**: [TASK-2068](TASK-2068.md), [TASK-2069](TASK-2069.md)

## 完了条件

- [x] `egui-app/src/state/app_state.rs` の `SensitivityResult` に `ridge: Vec<RidgeResult>` と `rf_anova: Option<RfAnovaResult>` が追加されている
- [x] `RidgeResult` / `RfAnovaResult` 型が定義されている
- [x] `AppMessage::SensitivityError` が `messages.rs` に追加されている
- [x] `app.rs` の `poll_messages` に `SensitivityError` ハンドラが追加されている
- [x] `cargo check -p tunny-desktop` がエラーなしで通る
- [x] `cargo test -p tunny-desktop` が全テスト通過する

---

## 主要実装項目

### 1. `egui-app/src/state/app_state.rs` — SensitivityResult 拡張 🔵

**信頼性**: 🔵 *architecture.md 2-A より*

```rust
pub struct SensitivityResult {
    pub param_names: Vec<String>,
    pub objective_names: Vec<String>,
    pub spearman: Vec<Vec<f64>>,
    pub ridge: Vec<RidgeResult>,
    pub rf_anova: Option<RfAnovaResult>,
}

pub struct RidgeResult {
    pub beta: Vec<f64>,
    pub r_squared: f64,
}

pub struct RfAnovaResult {
    pub importances: Vec<Vec<f64>>,
}
```

### 2. `egui-app/src/state/messages.rs` — SensitivityError 追加 🟡

**信頼性**: 🟡 *architecture.md 2-D より*

```rust
AppMessage::SensitivityError
```

### 3. `egui-app/src/app.rs` — エラーハンドラ追加 🟡

**信頼性**: 🟡 *architecture.md 2-E より*

```rust
AppMessage::SensitivityError => {
    self.app_state.widgets.importance.computing = false;
}
```

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2067`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- 🔵 青信号: 1件 (33%)
- 🟡 黄信号: 2件 (67%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: ✅ 高品質
