# TASK-2068: ImportanceChart 全メトリクス対応 + Run ボタン追加

**タスクID**: TASK-2068
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 2 - egui-app 拡張
**信頼性レベル**: 🔵 *設計文書 architecture.md 2-B・PdpChart2D pending_compute パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/sensitivity-all-models/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/sensitivity-all-models/dataflow.md)

## タスク概要

`egui-app/src/ui/widgets/importance_chart.rs` を拡張する：
1. `ImportanceMetric::RfAnova` を追加
2. `ImportanceChart` に `pending_compute: Option<ImportanceMetric>` フィールドを追加
3. Run ボタンと spinner を追加
4. `show()` に `sobol: Option<&SobolResult>` 引数を追加
5. `compute_sorted_importance()` を Ridge / RfAnova / Sobol に対応

## 依存タスク

- **前提タスク**: [TASK-2067](TASK-2067.md)
- **後続タスク**: [TASK-2069](TASK-2069.md)

## 完了条件

- [x] `ImportanceMetric::RfAnova` が追加されている
- [x] `ImportanceChart` に `pub pending_compute: Option<ImportanceMetric>` が追加されている
- [x] `show()` に `sobol: Option<&SobolResult>` 引数が追加されている
- [x] コントロール行に Run ボタンと spinner が表示される
- [x] Ridge / RfAnova / Sobol の各スコア取得ロジックが実装されている
- [x] `importance_metric_labels_not_empty` テストに RfAnova ケースが追加されている
- [x] `cargo test -p tunny-desktop` が全テスト通過する

---

## 主要実装項目

### 1. `ImportanceMetric` に RfAnova 追加 🔵

**信頼性**: 🔵 *architecture.md 2-B・ヒアリング Q4 より*

```rust
pub enum ImportanceMetric {
    Spearman,
    Ridge,
    RfAnova,  // 新規追加
    Sobol,
}
```

### 2. `ImportanceChart` 構造体拡張 🔵

**信頼性**: 🔵 *architecture.md 2-B より*

```rust
pub struct ImportanceChart {
    pub selected_objective: usize,
    pub selected_metric: ImportanceMetric,
    pub computing: bool,
    pub pending_compute: Option<ImportanceMetric>,  // 新規追加
}
```

### 3. `show()` シグネチャ変更 + Run ボタン 🔵

**信頼性**: 🔵 *architecture.md 2-B より*

```rust
pub fn show(
    &mut self,
    ui: &mut egui::Ui,
    sensitivity: Option<&SensitivityResult>,
    sobol: Option<&SobolResult>,
    obj_names: &[String],
) { ... }
```

Run ボタン:
```rust
if ui.button("Run").clicked() {
    self.pending_compute = Some(self.selected_metric.clone());
}
if self.computing { ui.spinner(); }
```

### 4. `compute_sorted_importance` 全メトリクス対応 🔵

**信頼性**: 🔵 *dataflow.md メトリクス別データソース対応表より*

```rust
ImportanceMetric::Ridge   => sensitivity.ridge[obj_idx].beta を使用
ImportanceMetric::RfAnova => sensitivity.rf_anova.importances[param][obj_idx] を使用
ImportanceMetric::Sobol   => sobol.first_order[param][obj_idx] を使用
```

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2068`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- 🔵 青信号: 4件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: ✅ 高品質
