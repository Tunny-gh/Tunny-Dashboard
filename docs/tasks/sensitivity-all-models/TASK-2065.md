# TASK-2065: full.rs / selected.rs 個別計算関数追加

**タスクID**: TASK-2065
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 1 - rust_core リファクタリング
**信頼性レベル**: 🔵 *設計文書 architecture.md 1-B・既存 compute_sensitivity_all 実装より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/sensitivity-all-models/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/sensitivity-all-models/dataflow.md)

## タスク概要

`rust_core/src/sensitivity/analysis/full.rs` に `compute_spearman_only` / `compute_ridge_only` / `compute_rf_anova_only` を追加する。
各関数は既存の `compute_sensitivity_all` からロジックを抽出して実装する。
`selected.rs` にも同様のパターンで個別計算関数を追加する。

## 依存タスク

- **前提タスク**: [TASK-2064](TASK-2064.md)
- **後続タスク**: [TASK-2066](TASK-2066.md)

## 完了条件

- [x] `compute_spearman_only(df: &DataFrame) -> SensitivityResult` が `full.rs` に追加されている
- [x] `compute_ridge_only(df: &DataFrame) -> SensitivityResult` が `full.rs` に追加されている
- [x] `compute_rf_anova_only(df: &DataFrame) -> SensitivityResult` が `full.rs` に追加されている
- [x] `selected.rs` にも同様の個別計算関数が追加されている
- [x] `compute_sensitivity_all` は変更なし（後方互換）
- [x] `cargo test -p tunny-core` が全テスト通過する

---

## 主要実装項目

### 1. `rust_core/src/sensitivity/analysis/full.rs` — 個別計算関数 🔵

**信頼性**: 🔵 *既存 compute_sensitivity_all 実装より抽出*

```rust
/// Spearman 相関のみ計算
pub fn compute_spearman_only(df: &DataFrame) -> SensitivityResult {
    // spearman のみ計算。ridge = vec![], rf_anova = None
}

/// Ridge 回帰係数のみ計算
pub fn compute_ridge_only(df: &DataFrame) -> SensitivityResult {
    // spearman = vec![], ridge のみ計算。rf_anova = None
}

/// RF ANOVA 重要度のみ計算
pub fn compute_rf_anova_only(df: &DataFrame) -> SensitivityResult {
    // spearman = vec![], ridge = vec![], rf_anova のみ計算
}
```

### 2. `rust_core/src/sensitivity/analysis/selected.rs` — 個別計算関数 🟡

**信頼性**: 🟡 *full.rs パターンと同様（architecture.md 1-B 末尾より）*

full.rs と同様のパターンで `compute_spearman_selected` / `compute_ridge_selected` / `compute_rf_anova_selected` を追加。

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2065`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- 🔵 青信号: 1件 (50%)
- 🟡 黄信号: 1件 (50%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: ✅ 高品質
