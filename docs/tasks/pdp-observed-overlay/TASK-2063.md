# TASK-2063: grid_canvas.rs 更新 + 統合確認

**タスクID**: TASK-2063
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 1 - 実装
**信頼性レベル**: 🔵 *設計文書 architecture.md・dataflow.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/pdp-observed-overlay/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/pdp-observed-overlay/dataflow.md)

## タスク概要

`egui-app/src/ui/grid_canvas.rs` の `show_chart` 内で `ChartId::PdpChart` ケースを更新し、
`PdpChart::show()` の新シグネチャ（`trial_rows: &[TrialRow]` 追加）に対応させる。
その後 `cargo check` + `cargo test` で統合確認を行う。

## 依存タスク

- **前提タスク**: [TASK-2062: PdpChart 観測データオーバーレイ実装](TASK-2062.md)
- **後続タスク**: なし

## 完了条件

- [x] `ChartId::PdpChart` ケースで `widgets.pdp_chart.show(ui, &param_names, &obj_names, &trial_rows)` が呼ばれる
- [x] `cargo check` がエラーなしで通る
- [x] `cargo test` が全テスト通過する（既存テストへの回帰なし）

---

## 主要実装項目

### 1. `grid_canvas.rs` の呼び出し変更 🔵

**信頼性**: 🔵 *dataflow.md の grid_canvas.rs 呼び出し側変更より*

**実装ファイル**: `egui-app/src/ui/grid_canvas.rs`

```rust
// 変更前
ChartId::PdpChart => {
    widgets.pdp_chart.show(ui, &param_names, &obj_names);
}

// 変更後
ChartId::PdpChart => {
    widgets.pdp_chart.show(ui, &param_names, &obj_names, &trial_rows);
}
```

`trial_rows` は `show_chart` 関数内で既に `let trial_rows = ctx.trial_rows.clone()` として
確保されている（`grid_canvas.rs` L281 付近）。追加のクローンは不要。

---

## 統合確認手順

1. `cargo check -p egui-app` — コンパイルエラーがないことを確認
2. `cargo test -p egui-app` — 既存テスト + TASK-2062 追加テストが全通過することを確認

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2063` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `trial_rows` が既に `show_chart` スコープ内に存在することを事前に確認する
- 変更は呼び出し側（grid_canvas.rs）のみ。pdp_chart.rs は TASK-2062 完了済みが前提

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
