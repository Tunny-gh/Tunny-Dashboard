# TASK-1649: WASM rebuild + フロントエンド統合確認

**タスクID**: TASK-1649
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 2 - Web Workerオフロード
**信頼性レベル**: 🔵 *既存ビルドプロセス・REQ-001 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/kriging-performance-optimization/requirements.md) - REQ-001
- **設計文書**: [📐 architecture.md](../../design/kriging-performance-optimization/architecture.md)

## タスク概要

Phase 2 の実装（TASK-1645〜1648）が完了した後、WASM のリビルドとフロントエンド統合確認を行うマイルストーンタスク。`cargo test` と `npm test` の全テスト通過を確認し、実際のブラウザ動作を手動確認する。

## 依存タスク

- **前提タスク**: [TASK-1648: analysisStore Worker統合 + extractData実装](TASK-1648.md)
- **後続タスク**: [TASK-1650: K-means誘導点選択実装](TASK-1650.md)

## 完了条件

- [ ] `wasm-pack build --target web --out-dir ../frontend/src/wasm/pkg --release` が成功
- [ ] WASM パッケージに `computeKrigingRaw` がエクスポートされている
- [ ] `cargo test` が全て通過
- [ ] `npm test` が全て通過（既存テスト回帰なし）
- [ ] ブラウザで Kriging モデル選択時に Worker が起動し計算が完了する（手動確認）
- [ ] TC-001-01 〜 TC-001-03 が全て通過

---

## 実装詳細

### 1. WASM リビルド 🔵

**信頼性**: 🔵 *既存 CLAUDE.md ビルドプロセスより*

**実装ファイル**: `rust_core/` (コマンド実行)

```bash
cd rust_core
wasm-pack build --target web --out-dir ../frontend/src/wasm/pkg --release
```

ビルド後の確認事項:
- `frontend/src/wasm/pkg/tunny_core.js` に `computeKrigingRaw` が存在すること
- `frontend/src/wasm/pkg/tunny_core_bg.wasm` が更新されていること

### 2. 全テスト実行 🔵

**信頼性**: 🔵 *既存テスト実行パターンより*

```bash
# Rust 単体テスト
cd rust_core && cargo test

# フロントエンドテスト
cd frontend && npm test
```

### 3. ブラウザ手動確認 🟡

**信頼性**: 🟡 *既存動作確認パターンから妥当な推測*

以下を手動で確認:
1. アプリを起動（`npm run dev`）
2. Study を読み込み、Surface Plot タブを開く
3. モデルドロップダウンで "Kriging" を選択
4. スピナーが表示された後、3D サーフェスプロットが表示されること
5. UI がフリーズしないこと（Worker 経由の確認）

---

## 単体テスト要件

DIRECTタスクのため単体テスト実装はなし。既存テストの実行確認のみ。

---

## 統合テスト要件

### Phase 2 回帰テスト 🔵

**信頼性**: 🔵 *既存テストパターンより*

**テスト内容**: `npm test` で全フロントエンドテストが通過すること
**期待結果**: 全テスト PASS、回帰なし

---

## 実装手順

### DIRECTプロセス

1. `/tsumiki:direct-setup kriging-performance-optimization TASK-1649` - WASM ビルド実行
2. `/tsumiki:direct-verify` - テスト実行・ブラウザ確認

---

## 注意事項

- WASM ビルドは `--release` フラグを使用すること。`--dev` ビルドでは性能計測が不正確になる。
- ブラウザ手動確認は TC-001-01〜TC-001-03 の実質的な検証となる。

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 1 | 0 | 3 |
| 統合テスト | 1 | 0 | 0 | 1 |

- **総項目数**: 4項目
- 🔵 **青信号**: 3項目 (75%)
- 🟡 **黄信号**: 1項目 (25%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
