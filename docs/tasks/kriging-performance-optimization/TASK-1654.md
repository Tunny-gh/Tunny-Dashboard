# TASK-1654: TypeScript型拡張 + SurfacePlot3D UI更新

**タスクID**: TASK-1654
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - Sparse Kriging
**信頼性レベル**: 🔵 *REQ-005-01, REQ-005-05, REQ-005-06・設計文書 interfaces.ts より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/kriging-performance-optimization/requirements.md) - REQ-005-01, REQ-005-05, REQ-005-06
- **設計文書**: [📐 architecture.md](../../design/kriging-performance-optimization/architecture.md)
- **インターフェース定義**: [📝 interfaces.ts](../../design/kriging-performance-optimization/interfaces.ts)

## タスク概要

フロントエンドの型定義と UI コンポーネントを更新して、Sparse Kriging モデルをドロップダウンに追加する:
1. `types/index.ts` の `SurrogateModelType` に `'sparse_kriging'` を追加
2. `SurfacePlot3D.tsx` の `MODEL_OPTIONS` に "Sparse Kriging" を追加
3. `MODEL_COMPUTE_TIME` に `sparse_kriging: '< 5s'` を追加

## 依存タスク

- **前提タスク**: [TASK-1653: compute_pdp_2d_sparse_kriging + lib.rs dispatch](TASK-1653.md)
- **後続タスク**: [TASK-1655: 統合テスト + パフォーマンス計測](TASK-1655.md)

## 完了条件

- [ ] `SurrogateModelType` が `'ridge' | 'random_forest' | 'kriging' | 'sparse_kriging'` になっている
- [ ] `MODEL_OPTIONS` に `{ value: 'sparse_kriging', label: 'Sparse Kriging' }` が追加されている
- [ ] `MODEL_COMPUTE_TIME` に `sparse_kriging: '< 5s'` が追加されている
- [ ] TypeScript 型エラーがない（`npm run type-check` が通る）
- [ ] TC-005-01, TC-005-04 が通過する
- [ ] `npm test` が全て通過

---

## 実装詳細

### 1. SurrogateModelType 拡張 🔵

**信頼性**: 🔵 *interfaces.ts SurrogateModelType・REQ-005-01・既存 types/index.ts:307 より*

**実装ファイル**: `frontend/src/types/index.ts`

```typescript
// Before (line 307):
export type SurrogateModelType = 'ridge' | 'random_forest' | 'kriging'

// After:
export type SurrogateModelType = 'ridge' | 'random_forest' | 'kriging' | 'sparse_kriging'
```

### 2. MODEL_OPTIONS への追加 🔵

**信頼性**: 🔵 *REQ-005-05・既存 SurfacePlot3D.tsx MODEL_OPTIONS より*

**実装ファイル**: `frontend/src/components/charts/SurfacePlot3D.tsx`

```typescript
const MODEL_OPTIONS: ModelOption[] = [
  { value: 'ridge', label: 'Ridge' },
  { value: 'random_forest', label: 'Random Forest', disabled: true },
  { value: 'kriging', label: 'Kriging' },
  { value: 'sparse_kriging', label: 'Sparse Kriging' },  // 追加
]
```

### 3. MODEL_COMPUTE_TIME の更新 🔵

**信頼性**: 🔵 *REQ-005-06・REQ-006 より（kriging は Phase 1 最適化後 < 10s）*

**実装ファイル**: `frontend/src/components/charts/SurfacePlot3D.tsx`

```typescript
const MODEL_COMPUTE_TIME: ModelComputeTime = {
  ridge: '< 1s',
  random_forest: '< 2s',
  kriging: '< 10s',         // Phase 1 最適化後の目標値
  sparse_kriging: '< 5s',   // 追加
}
```

---

## 単体テスト要件

### TC-005-01: TypeScript 型エラーなし 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-005-01 より*

**Given**: `surrogateModelType = 'sparse_kriging'`
**When**: TypeScript コンパイラが型チェックを実行
**Then**: 型エラーなし

**テストファイル**: `npm run type-check` または `tsc --noEmit`

### TC-005-04: cacheKey 形式の確認 🔵

**信頼性**: 🔵 *acceptance-criteria.md TC-005-04 より*

**Given**: surrogateModelType='sparse_kriging', p1='x1', p2='x2', obj='obj0', nGrid=50
**When**: `computeSurface3d` 呼び出し時の cacheKey 生成
**Then**: cacheKey == 'sparse_kriging_x1_x2_obj0_50'

**テストファイル**: `frontend/src/stores/analysisStore.test.ts`

### Sparse Kriging 表示テスト 🔵

**信頼性**: 🔵 *既存 SurfacePlot3D.test.tsx のテストパターンより*

**Given**: SurfacePlot3D コンポーネントがレンダリングされている
**When**: モデル選択ドロップダウンを確認
**Then**: "Sparse Kriging" オプションが存在する

**テストファイル**: `frontend/src/components/charts/SurfacePlot3D.test.tsx`

---

## UI/UX要件

### ローディング状態 🔵

**信頼性**: 🔵 *既存スピナー実装・REQ-005-06 より*

- Sparse Kriging 計算中: スピナーに "Expected: < 5s" が表示される
- スピナー overlay が表示され、UI がブロックされない（Worker 経由のため）

### モデル選択 UI 🔵

**信頼性**: 🔵 *REQ-005-05 ユーザヒアリング（ドロップダウン追加）より*

- ドロップダウンに "Sparse Kriging" が `disabled: false` で表示される
- 選択後、`setSurrogateModelType('sparse_kriging')` が呼ばれる

---

## 統合テスト要件

### フロントエンド全テスト通過 🔵

**信頼性**: 🔵 *既存テストパターンより*

**テスト内容**: `npm test` で全フロントエンドテストが通過すること
**期待結果**: 型エラーなし、全テスト PASS

---

## 実装手順

### TDDプロセス

1. `/tsumiki:tdd-requirements kriging-performance-optimization TASK-1654`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 注意事項

- `types/index.ts` の `SurrogateModelType` 変更は型システム全体に影響するため、型エラーが他のファイルで出る可能性がある。`npm run type-check` で確認すること。
- `Random Forest` の `disabled: true` は既存の状態を維持する（REQ-005-05 では変更不要）。

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 3 | 0 | 0 | 3 |
| UI/UX要件 | 2 | 0 | 0 | 2 |
| 統合テスト | 1 | 0 | 0 | 1 |

- **総項目数**: 9項目
- 🔵 **青信号**: 9項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
