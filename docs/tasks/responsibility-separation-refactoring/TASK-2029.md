# TASK-2029: grid_canvas.rs チャートディスパッチの分離

**タスクID**: TASK-2029
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 2 - チャートディスパッチ分離
**信頼性レベル**: 🔵 *設計文書 architecture.md Phase 2・既存コード分析より*

## 関連文書

- **概要**: [overview.md](overview.md)
- **設計文書**: [architecture.md](../../design/responsibility-separation-refactoring/architecture.md) - Phase 2
- **データフロー**: [dataflow.md](../../design/responsibility-separation-refactoring/dataflow.md) - Phase 2
- **型定義**: [interfaces.rs](../../design/responsibility-separation-refactoring/interfaces.rs)
- **ヒアリング記録**: [design-interview.md](../../design/responsibility-separation-refactoring/design-interview.md)

## タスク概要

`egui-app/src/ui/grid_canvas.rs` (418行) に混在しているチャートディスパッチロジック（`show_chart()`, `show_cell_chart()`, `render_cell_content()`）を新規ファイル `ui/chart_registry.rs` に抽出する。`grid_canvas.rs` はグリッド描画に専任し、チャートの選択・描画はレジストリパターンで委譲する。

## 依存タスク

- **前提タスク**: [TASK-2028: 型・フィルター・結果の分離](TASK-2028.md)
- **後続タスク**: [TASK-2030: メッセージ処理の分離](TASK-2030.md)

## 完了条件

- [x] `ui/chart_registry.rs` が作成され、`show_chart()` と `show_cell_chart()` が移動されている
- [x] `grid_canvas.rs` の `render_cell_content()` が `chart_registry` を呼び出すように変更されている
- [x] `ui/mod.rs` に `pub mod chart_registry;` が追加されている
- [x] `cargo check` がエラーなしで通る
- [x] `cargo test` の既存テスト（8件）が全てグリーン

---

## 実装詳細

### 1. chart_registry.rs の作成 🔵

**信頼性**: 🔵 *既存コード grid_canvas.rs:257-371 より*

`grid_canvas.rs` から以下の関数を `ui/chart_registry.rs` に移動する:

- `fn show_chart()` — ChartId の match で各ウィジェットにディスパッチ
- `fn show_cell_chart()` — タイトル + 区切り線 + show_chart 呼び出し

**実装ファイル**: `egui-app/src/ui/chart_registry.rs`

```rust
use std::sync::mpsc;

use crate::state::app_state::AppState;
use crate::state::layout_state::ChartId;
use crate::state::messages::AppMessage;
use crate::ui::widget_states::WidgetStates;

/// ChartId に対応するチャートウィジェットを描画する
pub fn show_chart(
    ui: &mut egui::Ui,
    app_state: &mut AppState,
    widgets: &mut WidgetStates,
    chart_id: &ChartId,
    tx: &mpsc::SyncSender<AppMessage>,
) {
    let Some(ctx) = app_state.current_study.as_ref() else {
        return;
    };
    let trial_rows = ctx.trial_rows.clone();
    let obj_names = ctx.meta.objective_names.clone();
    let param_names = ctx.meta.param_names.clone();
    let is_minimize = ctx
        .meta
        .directions
        .first()
        .map(|d| matches!(d, crate::state::app_state::Direction::Minimize))
        .unwrap_or(true);
    let sensitivity = app_state.sensitivity_result.clone();
    let hv_history = app_state.hv_history.clone();

    match chart_id {
        ChartId::ParetoScatter2D => widgets.pareto_2d.show(ui, app_state),
        ChartId::OptimizationHistory => widgets.opt_history.show(ui, &trial_rows, is_minimize),
        ChartId::HvHistory => {
            widgets.hv_history.hv_history = hv_history;
            widgets.hv_history.show(ui);
        }
        ChartId::ImportanceChart => {
            let sobol = app_state.sobol_result.as_ref();
            widgets.importance.show(ui, sensitivity.as_ref(), sobol, &obj_names);
        }
        ChartId::PdpChart => widgets.pdp_chart.show(ui, &param_names, &obj_names, &trial_rows),
        ChartId::PdpChart2D => {
            widgets.pdp_2d.show(ui, &param_names, &obj_names);
            if let Some(req) = widgets.pdp_2d.pending_compute.take() {
                widgets.pdp_2d.computing = true;
                let tx = tx.clone();
                crate::app::spawn_task(tx, move || {
                    let result = tunny_core::pdp::compute_pdp_2d(
                        &req.param1, &req.param2, &req.objective,
                        req.n_grid, &req.model_type,
                    );
                    match result {
                        Some(r) => {
                            use crate::state::messages::PdpResult2d;
                            AppMessage::Pdp2dDone(PdpResult2d {
                                x_values: r.x_values,
                                y_values: r.y_values,
                                z_values: r.z_values,
                                param1_name: r.param1_name,
                                param2_name: r.param2_name,
                                objective_name: r.objective_name,
                                uncertainties: r.uncertainties,
                            })
                        }
                        None => AppMessage::Error("PDP 2D computation failed".into()),
                    }
                });
            }
        }
        ChartId::ParallelCoordinates => {
            widgets.parallel_coords.show(ui, &trial_rows, &param_names, &obj_names);
        }
        ChartId::ScatterMatrix => {
            widgets.scatter_matrix.show(ui, &trial_rows, &param_names, &obj_names);
        }
        ChartId::ParetoScatter3D => {
            ui.label("3D Pareto chart requires GPU rendering (not yet wired up).");
        }
        ChartId::SensitivityHeatmap => {
            widgets.sensitivity_heatmap.show(ui, sensitivity.as_ref());
        }
        ChartId::ClusterScatter => {
            widgets.cluster_scatter.show(ui, &trial_rows, app_state.cluster_result.as_ref(), &param_names);
        }
    }
}

/// タイトルと区切り線付きでチャートを描画する
pub fn show_cell_chart(
    ui: &mut egui::Ui,
    app_state: &mut AppState,
    widgets: &mut WidgetStates,
    chart_id: &ChartId,
    tx: &mpsc::SyncSender<AppMessage>,
) {
    ui.label(egui::RichText::new(chart_id.label()).strong());
    ui.separator();
    show_chart(ui, app_state, widgets, chart_id, tx);
}
```

### 2. grid_canvas.rs の変更 🔵

**信頼性**: 🔵 *既存コード grid_canvas.rs:230-255 より*

`render_cell_content` 関数を修正し、チャート描画を `chart_registry` に委譲する:

**変更前**:
```rust
fn render_cell_content(...) {
    match &cell.content {
        Some(PanelItem::Chart(id)) => {
            let id = id.clone();
            show_cell_chart(ui, app_state, widgets, &id, tx); // ローカル関数
        }
        // ...
    }
}
```

**変更後**:
```rust
fn render_cell_content(...) {
    match &cell.content {
        Some(PanelItem::Chart(id)) => {
            let id = id.clone();
            crate::ui::chart_registry::show_cell_chart(ui, app_state, widgets, &id, tx);
        }
        Some(PanelItem::TrialTable) => {
            TrialTableWidget::default().show(ui, app_state);
        }
        None => {
            let _ = (row, col);
            ui.centered_and_justified(|ui| {
                ui.label(egui::RichText::new("— No chart selected —").weak());
            });
        }
    }
}
```

**削除する関数**:
- `fn show_cell_chart()` (grid_canvas.rs 内のローカル関数)
- `fn show_chart()` (grid_canvas.rs 内のローカル関数)

### 3. ui/mod.rs の更新 🔵

**信頼性**: 🔵 *既存モジュール構成より*

`ui/mod.rs` に新しいモジュールを追加:

```rust
pub mod chart_registry; // 追加
pub mod grid_canvas;    // 既存
// ... 他の既存モジュール
```

---

## 単体テスト要件

### 既存テストの維持 🔵

**信頼性**: 🔵 *既存テスト grid_canvas.rs:373-418 より*

`grid_canvas.rs` の既存テスト（8件）は全てグリーンを維持する:
- `calc_cell_width_equal_division`
- `calc_cell_width_col_span_2`
- `calc_cell_width_3_cols`
- `calc_cell_height_equal_division`
- `calc_cell_height_row_span_2`
- `calc_cell_width_zero_cols_returns_zero`
- `calc_cell_height_zero_rows_returns_zero`

これらは純粋関数のテストであり、リファクタリングの影響を受けない。

### 新規テスト 🔵

**信頼性**: 🔵 *ヒアリング（既存テスト維持のみ）に基づく*

新規テストは追加しない。`chart_registry` は純粋なコード移動であり、既存のエンドツーエンドテスト（UI起動確認）でカバレッジされる。

---

## 統合テスト要件

### コンパイル・動作確認 🔵

**信頼性**: 🔵 *段階的リファクタリング方針より*

- `cargo check` がエラーなしで通ること
- `cargo test` の全テストがグリーンであること
- UI起動後、各チャートがグリッドに正しく表示されること（手動確認）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2029` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース確認（既存テストのまま）
3. `/tsumiki:tdd-red` - コンパイルエラー確認（移動前の状態）
4. `/tsumiki:tdd-green` - chart_registry.rs の作成と grid_canvas.rs の修正
5. `/tsumiki:tdd-refactor` - インポートの最適化
6. `/tsumiki:tdd-verify-complete` - cargo test 全グリーン確認

---

## 注意事項

- `PdpChart2D` ブランチの `spawn_task` 呼び出しは `crate::app::spawn_task` を使用するため、`chart_registry.rs` からのパスが正しいことを確認する
- `show_chart` 内で `app_state.current_study` からデータを clone するパターンは変更しない（egui の借用ルールのため必要）
- `grid_canvas.rs` に残る関数: `calc_cell_width`, `calc_cell_height`, `show_grid_canvas`, `render_cell_content`
- `CellAction` enum は `grid_canvas.rs` に残す

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 2 | 0 | 0 | 2 |
| 統合テスト | 1 | 0 | 0 | 1 |

### 全体評価

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質

**推測が多い項目**: なし（全て既存コードからの機械的移動）
