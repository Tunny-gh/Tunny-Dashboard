# TASK-2030: app.rs メッセージ処理の分離

**タスクID**: TASK-2030
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 3 - メッセージ処理分離
**信頼性レベル**: 🔵 *設計文書 architecture.md Phase 3・既存コード分析より*

## 関連文書

- **概要**: [overview.md](overview.md)
- **設計文書**: [architecture.md](../../design/responsibility-separation-refactoring/architecture.md) - Phase 3
- **データフロー**: [dataflow.md](../../design/responsibility-separation-refactoring/dataflow.md) - Phase 3
- **型定義**: [interfaces.rs](../../design/responsibility-separation-refactoring/interfaces.rs)
- **ヒアリング記録**: [design-interview.md](../../design/responsibility-separation-refactoring/design-interview.md)

## タスク概要

`egui-app/src/app.rs` (212行) の `poll_messages()` メソッドからメッセージ処理ロジックを新規ファイル `state/message_handler.rs` に抽出する。`MessageHandler` 構造体を作成し、`poll_messages()` は `MessageHandler::handle()` に委譲する。

これにより、`app.rs` は `TunnyApp` 構造体と `eframe::App` トレイト実装のみの薄いラッパーになる。

## 依存タスク

- **前提タスク**: [TASK-2029: チャートディスパッチの分離](TASK-2029.md)
- **後続タスク**: なし（最終フェーズ）

## 完了条件

- [x] `state/message_handler.rs` が作成され、`MessageHandler` 構造体が定義されている
- [x] `MessageHandler::handle()` に全メッセージの処理が実装されている
- [x] `app.rs` の `poll_messages()` が `MessageHandler::handle()` に委譲している
- [x] `app_state.rs` の `mod.rs` に `pub mod message_handler;` が追加されている
- [x] `cargo check` がエラーなしで通る
- [x] `cargo test` の既存テストが全てグリーン

---

## 実装詳細

### 1. message_handler.rs の作成 🔵

**信頼性**: 🔵 *既存コード app.rs:38-124 より*

`app.rs` の `poll_messages()` 内の match ブランチを `MessageHandler::handle()` に移動する:

**実装ファイル**: `egui-app/src/state/message_handler.rs`

```rust
use crate::state::app_state::{AppState, StudyContext};
use crate::state::messages::{AppMessage, DownsampleKey};
use crate::state::results::HvHistory;
use crate::ui::widget_states::WidgetStates;

/// バックグラウンドタスクからのメッセージを処理するハンドラー
pub struct MessageHandler;

impl MessageHandler {
    /// 単一メッセージを処理し、AppState と WidgetStates を更新する
    pub fn handle(
        msg: AppMessage,
        app_state: &mut AppState,
        widget_states: &mut WidgetStates,
        is_loading: &mut bool,
        load_error: &mut Option<String>,
    ) {
        match msg {
            AppMessage::JournalParsed { studies, path } => {
                app_state.all_studies = studies;
                app_state.journal_path = Some(path);
                *is_loading = false;
            }
            AppMessage::StudySelected {
                meta,
                trial_rows,
                gpu_data,
                pareto_indices,
            } => {
                app_state.clear();
                app_state.current_study = Some(StudyContext {
                    meta,
                    trial_rows,
                    gpu_data,
                    pareto_indices,
                });
                *is_loading = false;
            }
            AppMessage::SensitivityDone(result) => {
                app_state.sensitivity_result = Some(result);
            }
            AppMessage::SobolDone(result) => {
                app_state.sobol_result = Some(result);
            }
            AppMessage::ClusteringDone(result) => {
                app_state.cluster_result = Some(result);
            }
            AppMessage::TopsisDone(result) => {
                app_state.topsis_result = Some(result);
            }
            AppMessage::DownsampleDone { key, indices } => {
                match key {
                    DownsampleKey::Scatter => {
                        app_state.downsample_cache.scatter = Some(indices)
                    }
                    DownsampleKey::Pcp => {
                        app_state.downsample_cache.pcp = Some(indices)
                    }
                    DownsampleKey::Thumbnail => {
                        app_state.downsample_cache.thumbnail = Some(indices)
                    }
                    DownsampleKey::Hover => {
                        app_state.downsample_cache.hover = Some(indices)
                    }
                }
            }
            AppMessage::HvHistoryDone {
                trial_ids,
                hv_values,
            } => {
                app_state.hv_history = Some(HvHistory {
                    trial_ids,
                    hv_values,
                });
            }
            AppMessage::Pdp2dDone(result) => {
                widget_states.pdp_2d.result = Some(result);
                widget_states.pdp_2d.computing = false;
            }
            AppMessage::Error(e) => {
                *load_error = Some(e);
                *is_loading = false;
            }
            AppMessage::SensitivityError(_e) => {
                widget_states.importance.computing = false;
            }
            AppMessage::LiveUpdateDone { .. } | AppMessage::PdpDone { .. } => {
                // TODO: 今後のタスクで実装
            }
        }
    }
}
```

### 2. app.rs の変更 🔵

**信頼性**: 🔵 *設計文書 architecture.md Phase 3 より*

`poll_messages()` を `MessageHandler::handle()` に委譲するように変更:

**変更前**:
```rust
pub fn poll_messages(&mut self, ctx: &egui::Context) {
    while let Ok(msg) = self.rx.try_recv() {
        match msg {
            AppMessage::JournalParsed { ... } => { ... }
            AppMessage::StudySelected { ... } => { ... }
            // ... 全11バリアント
        }
        ctx.request_repaint();
    }
}
```

**変更後**:
```rust
use crate::state::message_handler::MessageHandler;

pub fn poll_messages(&mut self, ctx: &egui::Context) {
    while let Ok(msg) = self.rx.try_recv() {
        MessageHandler::handle(
            msg,
            &mut self.app_state,
            &mut self.widget_states,
            &mut self.is_loading,
            &mut self.load_error,
        );
        ctx.request_repaint();
    }
}
```

### 3. state/mod.rs の更新 🔵

**信頼性**: 🔵 *既存モジュール構成より*

`state/mod.rs` に新しいモジュールを追加:

```rust
pub mod message_handler; // 追加
pub mod app_state;       // 既存
pub mod layout_state;    // 既存
pub mod messages;        // 既存
```

---

## 単体テスト要件

### 既存テストの維持 🔵

**信頼性**: 🔵 *既存テスト app.rs:145-211 より*

`app.rs` の既存テスト（4件）はそのまま維持する:
- `channel_send_receive_journal_parsed`
- `channel_try_recv_empty_returns_error`
- `spawn_task_sends_message`
- `spawn_task_multiple_messages`

これらはチャネル通信のテストであり、メッセージ処理ロジックの変更に影響されない。

### テストの補足 🔵

**信頼性**: 🔵 *既存テスト方針より*

新規テストは追加しない。メッセージ処理のロジックは `app_state` のテストで間接的にカバーされている。

---

## 統合テスト要件

### コンパイル・動作確認 🔵

**信頼性**: 🔵 *段階的リファクタリング方針より*

- `cargo check` がエラーなしで通ること
- `cargo test` の全テストがグリーンであること
- UI起動後、ファイル読み込み→Study選択→各チャート表示のフローが正常に動作すること（手動確認）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2030` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース確認（既存テストのまま）
3. `/tsumiki:tdd-red` - コンパイルエラー確認（移動前の状態）
4. `/tsumiki:tdd-green` - message_handler.rs の作成と app.rs の修正
5. `/tsumiki:tdd-refactor` - インポートの最適化
6. `/tsumiki:tdd-verify-complete` - cargo test 全グリーン確認 + 全フェーズ完了確認

---

## 注意事項

- `MessageHandler` は状態を持たない（`pub struct MessageHandler;`）ため、`Default` 実装は不要
- `HvHistory` のインポートパスは Phase 1 で `results.rs` に移動済みであることを前提とする（`use crate::state::results::HvHistory`）
- `StudyContext` は Phase 1 で `types.rs` に移動済み、`pub use` で `app_state` から再エクスポートされているため `use crate::state::app_state::StudyContext` でアクセス可能
- `AppMessage::LiveUpdateDone` と `AppMessage::PdpDone` は TODO のままであり、将来的なタスクで実装される

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 2 | 0 | 0 | 2 |
| 統合テスト | 1 | 0 | 0 | 1 |

### 全体評価

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質

**推測が多い項目**: なし（全て既存コードからの機械的抽出）
