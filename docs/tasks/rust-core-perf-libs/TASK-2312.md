# TASK-2312: K-means の linfa-clustering バックエンド化

- **Task type**: TDD, 8h
- **Phase**: Phase 4 - linfa-clustering 導入
- **信頼性**: 🔵 *要件定義 REQ-401・設計文書より*
- **Deps**: TASK-2304, TASK-2309
- **Requirements**: REQ-401-01, REQ-401-03, REQ-401-04, REQ-401-05

## Summary

clustering/kmeans.rs の手作り K-means Lloyd 法を linfa-clustering の KMeans に置き換える。公開 API と結果型（KmeansResult）は維持する。faer::Mat → ndarray::Array2 の境界変換を使用。

## Implementation details

### 1. kmeans_clustering 関数の linfa バックエンド化 (🔵 設計文書 architecture.md より)

```rust
use linfa_clustering::KMeans;

pub fn kmeans_clustering(data: &faer::Mat<f64>, k: usize, max_iter: usize, seed: u64) -> KmeansResult {
    let ndarray_data = faer_to_ndarray(data);
    let model = KMeans::params(k)
        .max_n_iterations(max_iter)
        .rng_seed(seed)
        .fit(&ndarray_data)?;
    // 結果変換...
}
```

**Files**: `rust_core/src/clustering/kmeans.rs`

### 2. エルボー法の linfa 対応 (🔵 REQ-401-04 より)

- k=2..max_k のループで linfa KMeans を実行
- WCSS を計算してエルボー点を検出

### 3. xorshift64 PRNG 削除 (🔵)

- linfa-clustering が内部で RNG を管理するため不要

## Completion criteria

- [x] kmeans.rs が linfa-clustering バックエンドで動作する
- [x] エルボー法が動作する
- [x] KmeansResult 型が維持されている
- [x] xorshift64 PRNG が削除されている
- [x] 全テストが通過する

## Test requirements

- TC-401-01: K-means 結果が妥当 (🔵)
- TC-401-02: エルボー法が動作 (🔵)
- TC-401-B01: k=1 で全データ重心 (🔵)
- TC-401-E01: 空クラスタ時のフォールバック (🟡)

## Steps

TDD

## Notes

linfa-clustering の KMeans::params が rng_seed をサポートしていることを確認すること。

## Reliability

🔵 8 items, 🟡 1 item. ✅ 高品質
