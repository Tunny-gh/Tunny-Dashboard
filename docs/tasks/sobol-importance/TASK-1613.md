# TASK-1613: analysisStore.ts に sobolResult・computeSobol() を追加

**タスクID**: TASK-1613
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 2 - TypeScript Store
**信頼性レベル**: 🔵 *architecture.md Layer 3・implementation-guide.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/sobol-importance/architecture.md)
- **実装ガイド**: [📝 implementation-guide.md](../../design/sobol-importance/implementation-guide.md)
- **データフロー**: [🔄 dataflow.md](../../design/sobol-importance/dataflow.md)

## タスク概要

`frontend/src/stores/analysisStore.ts` に Sobol 状態管理を追加する。
`sobolResult`・`isComputingSobol`・`sobolError`・`computeSobol()` を既存 `computeSensitivity()` のパターンで追加する。

## 依存タスク

- **前提タスク**: [TASK-1612: wasmLoader.ts に computeSobol() を追加](TASK-1612.md)
- **後続タスク**: [TASK-1614: ImportanceChart.tsx に Sobol UI を追加](TASK-1614.md)

## 完了条件

- [ ] `AnalysisState` インターフェースに `sobolResult`・`isComputingSobol`・`sobolError`・`computeSobol` が追加されている
- [ ] `create()` の初期値に `sobolResult: null, isComputingSobol: false, sobolError: null` が設定されている
- [ ] `computeSobol(nSamples = 1024)` アクションが実装されている
- [ ] Study 変更時のリセット処理に `sobolResult`・`isComputingSobol`・`sobolError` が含まれている
- [ ] 既存の TypeScript ビルドエラーがない（`tsc --noEmit` 成功）
- [ ] 単体テストが合格する

---

## 実装詳細

### 1. SobolWasmResult のインポート追加 🔵

**信頼性**: 🔵 *architecture.md Layer 3・implementation-guide.md より*

**実装ファイル**: `frontend/src/stores/analysisStore.ts`

既存の `import { ..., SensitivityWasmResult } from '../wasm/wasmLoader'` の行を更新:

```typescript
import { ..., SensitivityWasmResult, SobolWasmResult } from '../wasm/wasmLoader'
```

### 2. AnalysisState インターフェースへの追加 🔵

**信頼性**: 🔵 *architecture.md Layer 3 より*

**実装ファイル**: `frontend/src/stores/analysisStore.ts`

既存の `sensitivityError: string | null` の後に追加:

```typescript
sobolResult: SobolWasmResult | null
isComputingSobol: boolean
sobolError: string | null
computeSobol: (nSamples?: number) => Promise<void>
```

### 3. create() 初期値への追加 🔵

**信頼性**: 🔵 *architecture.md Layer 3 より*

**実装ファイル**: `frontend/src/stores/analysisStore.ts`

既存の `sensitivityError: null,` の後に追加:

```typescript
sobolResult: null,
isComputingSobol: false,
sobolError: null,
```

### 4. computeSobol() アクションの追加 🔵

**信頼性**: 🔵 *architecture.md Layer 3・dataflow.md キャッシング戦略より*

**実装ファイル**: `frontend/src/stores/analysisStore.ts`

既存の `computeSensitivity` アクションの後に追加（同じパターン）:

```typescript
computeSobol: async (nSamples = 1024) => {
  const { sobolResult } = get()
  if (sobolResult !== null) return  // キャッシュ済みなら再計算しない

  set({ isComputingSobol: true, sobolError: null })
  try {
    const loader = WasmLoader.getInstance()
    const result = loader.computeSobol(nSamples)
    set({ sobolResult: result, isComputingSobol: false })
  } catch (e) {
    set({
      sobolError: e instanceof Error ? e.message : String(e),
      isComputingSobol: false,
    })
  }
},
```

### 5. Study 変更時のリセット処理への追加 🔵

**信頼性**: 🔵 *dataflow.md キャッシング戦略より*

**実装ファイル**: `frontend/src/stores/analysisStore.ts`

Study を切り替えた際のリセット処理（`subscribe` か `setStudy` などの既存パターン）に追加:

```typescript
sobolResult: null,
isComputingSobol: false,
sobolError: null,
```

---

## 単体テスト要件

### TC-1613-01: computeSobol がキャッシュを使用する 🔵

**信頼性**: 🔵 *dataflow.md キャッシング戦略より*

**Given**: `sobolResult` が既に計算済み（null でない）
**When**: `computeSobol()` を呼び出す
**Then**: `loader.computeSobol` は呼び出されない（再計算しない）

**テストファイル**: `frontend/src/stores/analysisStore.test.ts`

### TC-1613-02: computeSobol がローディング状態を管理する 🔵

**信頼性**: 🔵 *dataflow.md 状態管理フローより*

**Given**: `sobolResult` が null
**When**: `computeSobol()` を呼び出す（非同期）
**Then**: 開始時に `isComputingSobol: true`、完了後に `isComputingSobol: false, sobolResult: 非null`

### TC-1613-03: Study 変更時に sobolResult がリセットされる 🔵

**信頼性**: 🔵 *dataflow.md Study 変更時のリセット仕様より*

**Given**: `sobolResult` が計算済み
**When**: Study を切り替える
**Then**: `sobolResult: null, isComputingSobol: false, sobolError: null` にリセットされる

### TC-1613-04: computeSobol のエラーハンドリング 🔵

**信頼性**: 🔵 *dataflow.md エラーハンドリングフローより*

**Given**: `loader.computeSobol` が例外をスローする
**When**: `computeSobol()` を呼び出す
**Then**: `sobolError` にエラーメッセージが設定され、`isComputingSobol: false`

---

## 統合テスト要件

### 統合テスト1: computeSobol → sobolResult 更新 → ImportanceChart に反映 🔵

**信頼性**: 🔵 *dataflow.md 機能1 シーケンス図より*

**テスト内容**: `computeSobol()` 実行後に `sobolResult` が非 null になり、ImportanceChart コンポーネントが再レンダリングされる
**期待結果**: ImportanceChart が sobol_first のスコアを表示する

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-1613` - `analysisStore.ts` の既存実装（`computeSensitivity`・`subscribeWithSelector`・Study リセット処理）を確認
2. `/tsumiki:tdd-testcases` - TC-1613-01〜04 をテストコードに変換
3. `/tsumiki:tdd-red` - テスト実装
4. `/tsumiki:tdd-green` - `AnalysisState` 拡張・`computeSobol` アクション追加
5. `/tsumiki:tdd-refactor` - Study リセット処理の確認と追加
6. `/tsumiki:tdd-verify-complete` - `tsc --noEmit` + テスト合格確認

---

## 注意事項

- 既存の `computeSensitivity()` のパターンを参照して、同じ構造で `computeSobol()` を実装すること
- `sobolResult !== null` のキャッシュチェックを必ず実装すること（不要な再計算を防ぐ）
- Study のリセット処理がどこで行われているかを確認してから追加すること（`subscribe` コールバック内か `setStudy` アクション内か）

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 5 | 0 | 0 | 5 |
| 単体テスト | 4 | 0 | 0 | 4 |
| 統合テスト | 1 | 0 | 0 | 1 |

### 全体評価

- **総項目数**: 10項目
- 🔵 **青信号**: 10項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
