# TASK-2070: ColormapName列挙体とColorMap新規定義

**タスクID**: TASK-2070
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 1 - カラーマップ基盤
**信頼性レベル**: 🔵 *設計文書 architecture.md・ユーザヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/colormap-fix/architecture.md)
- **型定義**: [📝 interfaces.rs](../../design/colormap-fix/interfaces.rs)

## タスク概要

ColormapName 列挙体を types.rs に定義し、ColorMap 構造体に6つの新規カラーマップ定義（Jet, Turbo, Inferno, Coolwarm, Spectral, Cividis）を追加する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: TASK-2071

## 完了条件

- [x] ColormapName 列挙体が types.rs に定義されている（9バリアント: Viridis, Plasma, Jet, Turbo, Inferno, Coolwarm, Spectral, Cividis, BlueYellow）
- [x] ColormapName::label() が各バリアントのUI表示名を返す
- [x] ColormapName::all() が全選択肢を返す
- [x] ColorMap::jet() が定義されている（7停止点近似）
- [x] ColorMap::turbo() が定義されている（7停止点近似）
- [x] ColorMap::inferno() が定義されている（5停止点近似）
- [x] ColorMap::coolwarm() が定義されている（5停止点近似）
- [x] ColorMap::spectral() が定義されている（7停止点近似）
- [x] ColorMap::cividis() が定義されている（5停止点近似）
- [x] 全ての新規テストが通る

---

## 実装詳細

### 1. ColormapName 列挙体定義 🔵

**信頼性**: 🔵 *設計文書 architecture.md・interfaces.rs より*

**実装ファイル**: `egui-app/src/state/types.rs`

```rust
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ColormapName {
    Viridis,
    Plasma,
    Jet,
    Turbo,
    Inferno,
    Coolwarm,
    Spectral,
    Cividis,
    BlueYellow,
}

impl ColormapName {
    pub fn label(&self) -> &str {
        match self {
            Self::Viridis => "Viridis",
            Self::Plasma => "Plasma",
            Self::Jet => "Jet",
            Self::Turbo => "Turbo",
            Self::Inferno => "Inferno",
            Self::Coolwarm => "Coolwarm",
            Self::Spectral => "Spectral",
            Self::Cividis => "Cividis",
            Self::BlueYellow => "Blue-Yellow",
        }
    }

    pub fn all() -> &'static [ColormapName] {
        &[
            Self::Viridis, Self::Plasma, Self::Jet, Self::Turbo,
            Self::Inferno, Self::Coolwarm, Self::Spectral, Self::Cividis,
            Self::BlueYellow,
        ]
    }

    pub fn to_colormap(&self) -> crate::render::colormap::ColorMap {
        match self {
            Self::Viridis => ColorMap::viridis(),
            Self::Plasma => ColorMap::plasma(),
            Self::Jet => ColorMap::jet(),
            Self::Turbo => ColorMap::turbo(),
            Self::Inferno => ColorMap::inferno(),
            Self::Coolwarm => ColorMap::coolwarm(),
            Self::Spectral => ColorMap::spectral(),
            Self::Cividis => ColorMap::cividis(),
            Self::BlueYellow => ColorMap::blue_yellow(),
        }
    }
}
```

### 2. 新規カラーマップ定義 🔵

**信頼性**: 🔵 *設計文書 architecture.md・ユーザヒアリングより*

**実装ファイル**: `egui-app/src/render/colormap.rs`

各カラーマップの停止点定義（matplotlib/scientific color referenceベース）:

- **Jet** (7停止点): 青(0.0) → 水(0.17) → 緑(0.33) → 黄(0.5) → オレンジ(0.67) → 赤(0.83) → 暗赤(1.0)
- **Turbo** (7停止点): 暗青(0.0) → 青(0.17) → 緑(0.33) → 黄(0.5) → オレンジ(0.67) → 赤(0.83) → 暗赤(1.0)
- **Inferno** (5停止点): 黒(0.0) → 暗紫(0.25) → 赤(0.5) → オレンジ(0.75) → 黄(1.0)
- **Coolwarm** (5停止点): 青(0.0) → 水色(0.25) → 白(0.5) → オレンジ(0.75) → 赤(1.0)
- **Spectral** (7停止点): 暗赤(0.0) → オレンジ(0.17) → 黄(0.33) → 薄緑(0.5) → 水(0.67) → 青(0.83) → 暗紫(1.0)
- **Cividis** (5停止点): 暗青(0.0) → 青(0.25) → 水色(0.5) → 黄(0.75) → 明黄(1.0)

---

## 単体テスト要件

### テストケース1: ColormapName::all が9要素を返す 🔵

**信頼性**: 🔵 *設計文書 interfaces.rs より*

**テストファイル**: `egui-app/src/state/types.rs` (tests module)

- `ColormapName::all().len() == 9`

### テストケース2: ColormapName::label が空でない 🔵

**信頼性**: 🔵 *設計文書 interfaces.rs より*

- 全バリアントで `!label().is_empty()`

### テストケース3: 各カラーマップのinterpolate境界値 🔵

**信頼性**: 🔵 *既存テストパターンより*

- `interpolate(0.0)` が最初の停止点の色を返す
- `interpolate(1.0)` が最後の停止点の色を返す
- `interpolate(-0.1)` が最初の停止点にクランプ
- `interpolate(1.1)` が最後の停止点にクランプ

### テストケース4: Jet補間中間値 🔵

**信頼性**: 🔵 *既存テストパターンより*

- `interpolate(0.5)` が黄色系の中間色を返す

### テストケース5: to_colormap の往復 🔵

**信頼性**: 🔵 *設計文書 interfaces.rs より*

- `ColormapName::Jet.to_colormap().interpolate(0.0)` が期待する色を返す

---

## 実装手順

1. `/tsumiki:tdd-red TASK-2070` - テスト実装（失敗）
2. `/tsumiki:tdd-green` - 最小実装
3. `/tsumiki:tdd-refactor` - リファクタリング
4. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 既存の viridis(), plasma(), blue_yellow() のテストは維持すること
- 停止点のRGB値は matplotlib のカラーマップ定義を参考にすること
- ColorMap::interpolate() は既存のまま変更しないこと

---

## 信頼性レベルサマリー

- **総項目数**: 12項目
- 🔵 **青信号**: 12項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
