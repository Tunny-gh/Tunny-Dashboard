# TASK-2072: AppState拡張と初期色計算フロー

**タスクID**: TASK-2072
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 2 - 状態管理
**信頼性レベル**: 🔵 *設計文書 architecture.md・ユーザヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/colormap-fix/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/colormap-fix/dataflow.md)

## タスク概要

AppStateにselected_colormapとchart_colorsフィールドを追加し、StudySelected時の初期色計算フローとupdate_chart_colors()メソッドを実装する。

## 依存タスク

- **前提タスク**: [TASK-2071: 離散パレット・正規化関数・色計算関数](TASK-2071.md)
- **後続タスク**: TASK-2073, TASK-2074

## 完了条件

- [x] AppState に selected_colormap: ColormapName フィールドが追加されている
- [x] AppState に chart_colors: Vec<egui::Color32> フィールドが追加されている
- [x] AppState::new() で selected_colormap = ColormapName::Viridis に初期化
- [x] AppState::update_chart_colors() メソッドが実装されている
- [x] MessageHandler::StudySelected 処理後に update_chart_colors() が呼ばれる
- [x] AppState::clear() で chart_colors がクリアされる
- [x] 全てのテストが通る（既存テスト含む）

---

## 実装詳細

### 1. AppState フィールド追加 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

**実装ファイル**: `egui-app/src/state/app_state.rs`

```rust
pub struct AppState {
    // ... 既存フィールド ...
    pub selected_colormap: ColormapName,    // 新規
    pub chart_colors: Vec<egui::Color32>,   // 新規
}

impl AppState {
    pub fn new() -> Self {
        Self {
            // ... 既存初期化 ...
            selected_colormap: ColormapName::Viridis,
            chart_colors: Vec::new(),
        }
    }

    pub fn clear(&mut self) {
        // ... 既存クリア ...
        self.chart_colors.clear();
        // selected_colormap はクリアしない（ユーザー設定を維持）
    }
}
```

### 2. update_chart_colors メソッド 🔵

**信頼性**: 🔵 *設計文書 architecture.md・ユーザヒアリング（即時同期）より*

**実装ファイル**: `egui-app/src/state/app_state.rs`

```rust
impl AppState {
    /// ColorMode と ColormapName に基づいて chart_colors を再計算する
    /// 即時同期（5万点で1-3ms）
    pub fn update_chart_colors(&mut self) {
        if let Some(ctx) = &self.current_study {
            let color_mode = self.color_mode.clone();
            let colormap_name = self.selected_colormap.clone();
            let trial_rows = &ctx.trial_rows;
            let objective_names = &ctx.meta.objective_names;
            self.chart_colors = crate::render::colormap::compute_chart_colors(
                &color_mode,
                &colormap_name,
                trial_rows,
                objective_names,
            );
        } else {
            self.chart_colors.clear();
        }
    }
}
```

### 3. MessageHandler での初期色計算 🔵

**信頼性**: 🔵 *設計文書 dataflow.md より*

**実装ファイル**: `egui-app/src/state/message_handler.rs`

```rust
AppMessage::StudySelected { .. } => {
    // 既存処理 ...
    // 最後に色を更新
    // ※ app_state.update_chart_colors() を呼ぶ必要があるが、
    // MessageHandler::handle 内で直接呼び出し可能
}
```

**注意**: handle() のシグネチャは `handle(msg, app_state, widget_states, is_loading, load_error)` なので、app_state.update_chart_colors() をそのまま呼べる。

---

## 単体テスト要件

### テストケース1: AppState::new の初期値 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

- selected_colormap == ColormapName::Viridis
- chart_colors.is_empty()

### テストケース2: update_chart_colors がStudy選択後に色を生成 🔵

**信頼性**: 🔵 *設計文書 dataflow.md より*

- current_study がある場合、chart_colors.len() == trial_rows.len()

### テストケース3: update_chart_colors がStudy未選択時にクリア 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

- current_study が None の場合、chart_colors.is_empty()

### テストケース4: clear が chart_colors をクリア 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

- clear() 後に chart_colors.is_empty()

### テストケース5: ColormapName 変更で異なる色が生成される 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

- ColormapName::Viridis と ColormapName::Jet で異なる chart_colors が生成される

### テストケース6: 既存テストが引き続き通る 🔵

**信頼性**: 🔵 *既存テストの互換性より*

- app_state.rs, filter.rs の既存テストが全て通る

---

## 実装手順

1. `/tsumiki:tdd-red TASK-2072` - テスト実装（失敗）
2. `/tsumiki:tdd-green` - 最小実装
3. `/tsumiki:tdd-refactor` - リファクタリング
4. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- AppState::clear() で selected_colormap はリセットしない（ユーザーの選択を維持）
- chart_colors は Study 切り替え時にクリアして再計算
- 既存の AppState テストが壊れないように new() にフィールドを追加する際は Default 値を設定

---

## 信頼性レベルサマリー

- **総項目数**: 12項目
- 🔵 **青信号**: 12項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
