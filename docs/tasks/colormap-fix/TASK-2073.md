# TASK-2073: Left Panel ColormapNameセレクタUI追加

**タスクID**: TASK-2073
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 2 - 状態管理
**信頼性レベル**: 🔵 *設計文書 architecture.md・ユーザヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/colormap-fix/architecture.md)

## タスク概要

Left PanelにColormapNameセレクタ（ComboBox）を追加し、変更時にupdate_chart_colors()を呼び出す。

## 依存タスク

- **前提タスク**: [TASK-2072: AppState拡張と初期色計算フロー](TASK-2072.md)
- **後続タスク**: TASK-2074, TASK-2075, TASK-2076

## 完了条件

- [x] show_colormap_selector() 関数が left_panel.rs に追加されている
- [x] ColormapName ComboBox が ColorMode セレクタの下に配置されている
- [x] ColormapName 変更時に update_chart_colors() が呼ばれる
- [x] 全てのテストが通る（既存テスト含む）

---

## 実装詳細

### 1. show_colormap_selector 関数 🔵

**信頼性**: 🔵 *設計文書 architecture.md のUI構成より*

**実装ファイル**: `egui-app/src/ui/left_panel.rs`

```rust
/// カラーマップ選択セレクタ
fn show_colormap_selector(ui: &mut egui::Ui, app_state: &mut AppState) {
    ui.label("Colormap:");
    let current_label = app_state.selected_colormap.label().to_string();
    let mut changed = false;
    egui::ComboBox::from_label("")
        .selected_text(current_label)
        .show_ui(ui, |ui| {
            for cmap in ColormapName::all() {
                if ui.selectable_label(
                    app_state.selected_colormap == *cmap,
                    cmap.label(),
                ).clicked() {
                    app_state.selected_colormap = cmap.clone();
                    changed = true;
                }
            }
        });
    if changed {
        app_state.update_chart_colors();
    }
}
```

### 2. show_left_panel への組み込み 🔵

**信頼性**: 🔵 *既存 left_panel.rs 構造より*

**実装ファイル**: `egui-app/src/ui/left_panel.rs`

show_left_panel 内で show_color_mode の後に show_colormap_selector を呼び出す:

```rust
pub fn show_left_panel(ui: &mut egui::Ui, app_state: &mut AppState, _layout: &mut LayoutState) {
    egui::ScrollArea::vertical().show(ui, |ui| {
        show_study_info(ui, app_state);
        ui.separator();
        show_filter_sliders(ui, app_state);
        ui.separator();
        show_color_mode(ui, app_state);
        show_colormap_selector(ui, app_state);  // 新規追加
    });
}
```

---

## 単体テスト要件

### テストケース1: ColormapName::all の全要素が選択可能 🔵

**信頼性**: 🔵 *設計文書 architecture.md より*

- 全9バリアントがComboBoxに表示されることを確認（ロジックレベル）

### テストケース2: 既存テストが通る 🔵

**信頼性**: 🔵 *既存テストの互換性より*

- left_panel.rs の既存テストが全て通る

---

## 実装手順

1. `/tsumiki:direct-setup` - 直接実装
2. `/tsumiki:direct-verify` - 動作確認

---

## 注意事項

- ColormapName セレクタは ColorMode セレクタとは独立して動作する
- 変更時は即時に update_chart_colors() を呼び出す（非同期メッセージなし）
- ColormapName と ColorMode の両方が変更された場合、最終的な update_chart_colors() は1回だけでよい

---

## 信頼性レベルサマリー

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
