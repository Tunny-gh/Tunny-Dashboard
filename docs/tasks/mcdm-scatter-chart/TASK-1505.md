# TASK-1505: AppMessage::McdmScatterComputed メッセージ型定義

**タスクID**: TASK-1505  
**タスクタイプ**: DIRECT  
**推定工数**: 4時間  
**フェーズ**: Phase 2 - 状態管理  
**信頼性レベル**: 🔵 *architecture.md Section 8, interfaces.rs SECTION 4 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/mcdm-scatter-chart/architecture.md) - Section 8, 9
- **型定義**: [📝 interfaces.rs](../../design/mcdm-scatter-chart/interfaces.rs) - SECTION 4
- **データフロー**: [🔄 dataflow.md](../../design/mcdm-scatter-chart/dataflow.md) - Section 1

## タスク概要

バックグラウンド計算完了時に送信される AppMessage::McdmScatterComputed を定義します。

```rust
pub enum AppMessage {
    // ... existing variants
    McdmScatterComputed {
        points: Vec<(f64, f64, Color32)>,
        metadata: ScatterMetadata,
    },
    McdmScatterComputeFailed(String),
}
```

## 依存タスク

- **前提タスク**: [TASK-1504: WidgetStates追加](TASK-1504.md)
- **後続タスク**: [TASK-1506: メッセージハンドラ](TASK-1506.md)

## 完了条件

- [x] AppMessage enum に McdmScatterComputed バリアント追加
- [x] ScatterMetadata 型定義（total_trials, rendered_points, downsample_factor等）
- [x] エラーハンドリング用 McdmScatterComputeFailed バリアント追加
- [x] 既存 AppMessage バリアントに影響なし
- [x] コンパイル確認

---

## 実装詳細

### 1. AppMessage拡張 🔵

**信頼性**: 🔵 *architecture.md Section 9 メッセージ拡張より*

`egui-app/src/state/messages.rs`:

```rust
pub enum AppMessage {
    // ... existing variants
    
    McdmScatterComputed {
        points: Vec<(f64, f64, egui::Color32)>,
        metadata: ScatterMetadata,
    },
    
    McdmScatterComputeFailed(String),
}
```

### 2. ScatterMetadata型定義 🔵

**信頼性**: 🔵 *interfaces.rs - ScatterMetadata より*

```rust
pub struct ScatterMetadata {
    pub total_trials: usize,
    pub rendered_points: usize,
    pub downsample_factor: usize,
    pub compute_time_ms: u128,
    pub x_axis_label: String,
    pub y_axis_label: String,
}
```

---

## 単体テスト要件

### テストケース1: メッセージ型定義 🔵

**Given**: AppMessage enum  
**When**: McdmScatterComputed バリアントが存在  
**Then**: points と metadata フィールドを持つ

```rust
#[test]
fn test_mcdm_scatter_computed_variant() {
    let metadata = ScatterMetadata {
        total_trials: 100,
        rendered_points: 50,
        downsample_factor: 2,
        compute_time_ms: 100,
        x_axis_label: "Objective1".to_string(),
        y_axis_label: "Objective2".to_string(),
    };
    
    let msg = AppMessage::McdmScatterComputed {
        points: vec![(0.5, 0.5, Color32::RED)],
        metadata,
    };
    
    match msg {
        AppMessage::McdmScatterComputed { .. } => assert!(true),
        _ => panic!("Expected McdmScatterComputed"),
    }
}
```

---

## 信頼性レベルサマリー

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%)
- **品質評価**: ✅ **高品質**

