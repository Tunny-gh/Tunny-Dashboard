# TASK-1501: McdmScatterChart 構造体定義とウィジェット初期化

**タスクID**: TASK-1501  
**タスクタイプ**: TDD  
**推定工数**: 8時間  
**フェーズ**: Phase 1 - UI層基盤  
**信頼性レベル**: 🔵 *設計文書 interfaces.rs より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/mcdm-scatter-chart/requirements.md) - REQ-001, REQ-002
- **設計文書**: [📐 architecture.md](../../design/mcdm-scatter-chart/architecture.md) - Section 2, 3
- **型定義**: [📝 interfaces.rs](../../design/mcdm-scatter-chart/interfaces.rs) - SECTION 1, 2

## タスク概要

McdmScatterChart ウィジェットの基本構造を実装します。独立した状態管理を持つウィジェット として、以下を実装：
- ウィジェット構造体定義（x_axis, y_axis, color_threshold等）
- new()初期化関数
- キャッシュ管理ヘルパーメソッド
- UI フレームの基本スケルトン

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-1502: UI制御実装](TASK-1502.md)

## 完了条件

- [x] `egui-app/src/ui/widgets/mcdm_scatter_chart.rs` ファイル作成
- [x] McdmScatterChart 構造体定義（x_axis, y_axis, color_threshold, use_downsample, display_rows_cache, cache_key, pending_compute, computing, error_message, hover_info, selected_point）
- [x] new()初期化関数実装
- [x] invalidate_cache()メソッド実装
- [x] update_cache_key()メソッド実装
- [x] show(&mut self, ui: &mut egui::Ui, app_state: &mut AppState) メソッドの基本スケルトン作成
- [x] 単体テスト: 初期化・キャッシュ管理のテスト実装
- [x] 既存 ParetoScatter2D パターンとの整合性確認

---

## 実装詳細

### 1. 構造体定義 🔵

**信頼性**: 🔵 *interfaces.rs - SECTION 1 で型定義済み*

`egui-app/src/ui/widgets/mcdm_scatter_chart.rs` に以下を実装：

```rust
use egui_plot::Color32;

#[derive(Clone, Debug)]
pub struct McdmScatterChart {
    pub x_axis: String,
    pub y_axis: String,
    pub color_threshold: TopN,
    pub use_downsample: bool,
    pub display_rows_cache: Option<Vec<(f64, f64, Color32)>>,
    pub cache_key: (usize, u64),
    pub pending_compute: Option<()>,
    pub computing: bool,
    pub error_message: Option<String>,
    pub hover_info: Option<(usize, String)>,
    pub selected_point: Option<usize>,
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum TopN {
    Top5,
    Top10,
    Top20,
}
```

**参考**: `egui-app/src/ui/widgets/pareto_2d.rs` の ParetoScatter2D 構造体パターン  
**実装ガイドライン**:
- ParetoScatter2D との共通部分（x_axis, y_axis, use_downsample, display_rows_cache）を踏襲
- MCDM特有部分（color_threshold, ranked_indices参照）を追加

### 2. new()初期化関数 🔵

**信頼性**: 🔵 *interfaces.rs - impl McdmScatterChart に定義済み*

```rust
impl McdmScatterChart {
    pub fn new() -> Self {
        Self {
            x_axis: "Objective0".to_string(),
            y_axis: "Objective1".to_string(),
            color_threshold: TopN::Top5,
            use_downsample: true,
            display_rows_cache: None,
            cache_key: (0, 0),
            pending_compute: None,
            computing: false,
            error_message: None,
            hover_info: None,
            selected_point: None,
        }
    }
}
```

### 3. キャッシュ管理ヘルパー 🔵

**信頼性**: 🔵 *architecture.md - Section 3 で設計済み*

```rust
impl McdmScatterChart {
    pub fn invalidate_cache(&mut self) {
        self.display_rows_cache = None;
        self.pending_compute = Some(());
    }

    pub fn update_cache_key(&mut self, trial_count: usize) {
        let axis_hash = Self::hash_axes(&self.x_axis, &self.y_axis);
        self.cache_key = (trial_count, axis_hash);
    }

    fn hash_axes(x: &str, y: &str) -> u64 {
        use std::collections::hash_map::DefaultHasher;
        use std::hash::{Hash, Hasher};

        let mut hasher = DefaultHasher::new();
        x.hash(&mut hasher);
        y.hash(&mut hasher);
        hasher.finish()
    }
}
```

### 4. show()メソッドの基本スケルトン 🔵

**信頼性**: 🔵 *architecture.md - Section 2.2 UI レイアウトより*

```rust
impl McdmScatterChart {
    pub fn show(&mut self, ui: &mut egui::Ui, app_state: &mut AppState) {
        // TODO: Step 2 (TASK-1502) で実装
        // - UI制御行（ComboBox、チェックボックス）
        // - プロット描画エリア
        // - ステータスバー
    }
}
```

---

## 単体テスト要件

### テストケース1: 構造体初期化 🔵

**信頼性**: 🔵 *interfaces.rs より*

**Given**: McdmScatterChart::new()を呼び出し  
**When**: 新規インスタンスが生成される  
**Then**: すべてのフィールドが期待値で初期化される

**テストコード**:
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_mcdm_scatter_chart_new() {
        let chart = McdmScatterChart::new();
        
        assert_eq!(chart.x_axis, "Objective0");
        assert_eq!(chart.y_axis, "Objective1");
        assert_eq!(chart.color_threshold, TopN::Top5);
        assert_eq!(chart.use_downsample, true);
        assert!(chart.display_rows_cache.is_none());
        assert!(chart.pending_compute.is_none());
        assert_eq!(chart.computing, false);
        assert!(chart.error_message.is_none());
    }
}
```

### テストケース2: キャッシュ無効化 🔵

**信頼性**: 🔵 *architecture.md Section 3 キャッシング戦略より*

**Given**: キャッシュが設定されたMcdmScatterChart  
**When**: invalidate_cache()を呼び出す  
**Then**: キャッシュが None にリセットされ、pending_computeが Some(())になる

**テストコード**:
```rust
#[test]
fn test_invalidate_cache() {
    let mut chart = McdmScatterChart::new();
    chart.display_rows_cache = Some(vec![(0.5, 0.5, Color32::RED)]);
    chart.computing = false;
    
    chart.invalidate_cache();
    
    assert!(chart.display_rows_cache.is_none());
    assert!(chart.pending_compute.is_some());
}
```

### テストケース3: キャッシュキー更新 🔵

**信頼性**: 🔵 *architecture.md Section 3 より*

**Given**: trial_count = 100のアプリ状態  
**When**: update_cache_key(100)を呼び出す  
**Then**: cache_key.0 が 100、cache_key.1 が軸ハッシュになる

**テストコード**:
```rust
#[test]
fn test_update_cache_key() {
    let mut chart = McdmScatterChart::new();
    chart.update_cache_key(100);
    
    assert_eq!(chart.cache_key.0, 100);
    assert_ne!(chart.cache_key.1, 0);  // ハッシュが計算されている
}
```

---

## 統合テスト要件

### 統合テスト1: ウィジェット状態管理の独立性 🔵

**信頼性**: 🔵 *architecture.md Section 2.1 ウィジェット状態構造より*

**テスト内容**: McdmScatterChart の状態変更が他のウィジェットに影響しないことを確認

**期待結果**: 軸選択やキャッシュ状態の変更が McdmRankChart に反映されない

---

## UI/UX要件

### 基本UI構造 🔵

**信頼性**: 🔵 *architecture.md Section 2.2 UI レイアウトより*

- [x] ウィジェット全体を egui::Ui に描画可能
- [x] 将来の UI 制御（ComboBox、チェックボックス）のための枠組み作成

---

## 実装手順（TDDフロー）

1. `/tsumiki:tdd-requirements TASK-1501` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース実装
3. `/tsumiki:tdd-red` - テスト実装（失敗状態）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- ParetoScatter2D の既存実装を参考にしてください
- TopN enum の定義は設計で確定済み（Top5, Top10, Top20）
- 初期値は architecture.md の表に従ってください
- キャッシュキー計算ではハッシュ値を使用（FxHasher 推奨）

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 |
|---------|-------|-------|-------|
| 実装詳細 | 4 | 0 | 0 |
| 単体テスト | 3 | 0 | 0 |
| 統合テスト | 1 | 0 | 0 |
| UI/UX要件 | 1 | 0 | 0 |

### 全体評価

- **総項目数**: 9項目
- 🔵 **青信号**: 9項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ **高品質** - 設計文書から完全に導出されている
