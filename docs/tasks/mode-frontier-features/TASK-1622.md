# TASK-1622: TopsisRankingChart.tsx 新規作成・テスト ✅ **完了** (TDD開発完了 - 7テストケース全通過)

**タスクID**: TASK-1622
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - Zustand Store・UIコンポーネント（TOPSIS）
**信頼性レベル**: 🔵 *architecture.md・dataflow.md・既存EChartsコンポーネントパターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/mode-frontier-features/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/mode-frontier-features/dataflow.md)

## タスク概要

`frontend/src/components/charts/TopsisRankingChart.tsx` を新規作成する。ECharts 水平棒グラフで上位N件のTOPSISスコアを表示し、重みスライダーと上位N件セレクタをUI上に配置する。棒クリックで `selectionStore.setHighlight` を呼び出す。

## 依存タスク

- **前提タスク**: [TASK-1621: mcdmStore.ts作成](TASK-1621.md)
- **後続タスク**: [TASK-1623: FreeLayoutCanvas.tsx case追加](TASK-1623.md)

## 完了条件

- [x] `TopsisRankingChart.tsx` が作成されている
- [x] 重みスライダーがObjective数分表示される
- [x] ECharts 水平棒グラフで上位N件が表示される
- [x] 棒クリックで `selectionStore.setHighlight` が呼ばれる
- [x] Study未選択・計算エラー時に EmptyState が表示される
- [x] インラインスタイルのみ使用（Tailwind CSS禁止）
- [x] vitest テストがパスする（7件）

---

## 実装詳細

### 1. コンポーネント構成 🔵

**信頼性**: 🔵 *architecture.md・dataflow.md TopsisRankingChart設計より*

**実装ファイル**: `frontend/src/components/charts/TopsisRankingChart.tsx`

```typescript
// コンポーネント構成:
// <div style={{ display: 'flex', flexDirection: 'column', height: '100%' }}>
//   <div> // 重みセクション
//     {objectiveNames.map((name, i) => (
//       <div key={i}>
//         <label>{name}: {weights[i].toFixed(2)}</label>
//         <input type="range" min="0" max="1" step="0.01"
//           value={weights[i]}
//           onChange={e => updateWeight(i, +e.target.value)} />
//       </div>
//     ))}
//   </div>
//   <div> // TopN セレクタ
//     <select value={topN} onChange={e => setTopN(+e.target.value)}>
//       <option value={5}>上位5件</option>
//       <option value={10}>上位10件</option>
//       <option value={20}>上位20件</option>
//     </select>
//   </div>
//   <ReactECharts option={chartOption} onEvents={{ click: handleBarClick }} />
// </div>
```

### 2. ECharts オプション 🔵

**信頼性**: 🔵 *既存EChartsコンポーネントパターン・architecture.md より*

```typescript
const chartOption = {
  tooltip: { formatter: '{b}: {c}' },
  xAxis: { type: 'value', min: 0, max: 1 },
  yAxis: {
    type: 'category',
    data: topNTrialIds,
    inverse: true,
  },
  series: [{
    type: 'bar',
    data: topNScores,
    itemStyle: {
      color: (params: any) => params.dataIndex === 0 ? '#e74c3c' : '#3498db'
    },
  }],
}
```

### 3. ローディング・エラー状態 🔵

**信頼性**: 🔵 *既存EmptyStateパターンより*

- `currentStudy === null` → EmptyState "データを読み込んでください"
- `currentStudy.objectiveNames.length < 2` → EmptyState "目的関数が2つ以上必要です"
- `isComputing === true` → ローディングスピナー
- `topsisError !== null` → EmptyState "ランキング計算エラー"

### 4. スタイル制約 🔵

**信頼性**: 🔵 *NFR-201・既存コンポーネントパターンより*

インラインスタイルのみ使用。Tailwind CSSクラス禁止。

---

## 単体テスト要件

### テストケース1: 空データ時のEmptyState 🔵

**信頼性**: 🔵 *既存チャートテストパターンより*

**Given**: currentStudy=null
**When**: コンポーネントをレンダリング
**Then**: EmptyStateが表示される

### テストケース2: 重みスライダー操作 🟡

**信頼性**: 🟡 *UIパターンから妥当な推測*

**Given**: 2目的Study
**When**: スライダーを操作
**Then**: `mcdmStore.setTopsisWeights` が呼ばれる

---

## UI/UX要件

### ローディング状態 🔵

**信頼性**: 🔵 *既存コンポーネントパターンより*

`isComputing` が true の間はローディングスピナー表示（インラインスタイル）

### エラー表示 🔵

**信頼性**: 🔵 *既存EmptyStateパターンより*

`topsisError !== null` の場合は `<EmptyState message="ランキング計算エラー" />` を表示

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements TASK-1622`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 |
|---------|-------|-------|-------|
| 実装詳細 | 4 | 0 | 0 |
| 単体テスト | 1 | 1 | 0 |
| UI/UX要件 | 2 | 0 | 0 |

- **総項目数**: 7項目
- 🔵 **青信号**: 6項目 (86%)
- 🟡 **黄信号**: 1項目 (14%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
