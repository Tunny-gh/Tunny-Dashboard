# TASK-1626: `wasmLoader.ts` computePdp2dバインド追加・テスト ✅ **完了** (TDD開発完了 - 11テストケース全通過)

**タスクID**: TASK-1626
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 4 - 3D曲面プロット WASM層
**信頼性レベル**: 🔵 *wasmLoader.ts既存バインドパターン・design-interview.md Q2より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/mode-frontier-features/architecture.md)
- **型定義設計**: [📝 interfaces.ts](../../design/mode-frontier-features/interfaces.ts)

## タスク概要

`frontend/src/wasm/wasmLoader.ts` の `WasmModule` インターフェースと `wasmModuleInstance` に `computePdp2d` メソッドを追加する。TASK-1619（computeTopsisバインド）と同様のパターンで実装する。

## 依存タスク

- **前提タスク**: [TASK-1625: tunny_core.d.ts Pdp2dResult追加](TASK-1625.md)
- **後続タスク**: [TASK-1627: analysisStore.ts拡張](TASK-1627.md)

## 完了条件

- [x] `WasmLoader` クラスに `computePdp2d` メソッドが追加されている
- [x] `_initialize()` に `computePdp2d` バインドが追加されている
- [x] `frontend/src/wasm/wasmLoader.test.ts` にバインドテストが追加されている（TC-1626-01）
- [x] TypeScriptコンパイルエラーが発生しない
- [x] `npm test` でテストが通る（11件）

---

## 実装詳細

### 1. WasmModule インターフェース拡張 🔵

**信頼性**: 🔵 *wasmLoader.ts既存WasmModuleインターフェース・TASK-1619パターンより*

**実装ファイル**: `frontend/src/wasm/wasmLoader.ts`

```typescript
computePdp2d: (
  param1: string,
  param2: string,
  objective: string,
  nGrid: number
) => Pdp2dResult;
```

### 2. wasmModuleInstance への実装追加 🔵

**信頼性**: 🔵 *既存computeSobol・computeTopsisバインドパターンより*

```typescript
computePdp2d: (param1, param2, objective, nGrid) => {
  const wasm = getWasmModule();
  return wasm.computePdp2d(param1, param2, objective, nGrid);
},
```

### 3. バインドテスト追加 🔵

**信頼性**: 🔵 *既存wasmLoader.test.tsパターンより*

**テストファイル**: `frontend/src/wasm/wasmLoader.test.ts`

**テストケース1: computePdp2d呼び出し成功**
- Given: 2パラメータ・1目的関数が存在するStudy、n_grid=10
- When: `wasm.computePdp2d('x1', 'x2', 'value', 10)` 呼び出し
- Then: `{grid1: Float64Array(10), grid2: Float64Array(10), heatmap: Float64Array(100), durationMs: number}` が返る

**テストケース2: 未存在パラメータ名でのエラーハンドリング**
- Given: 存在しないパラメータ名
- When: `wasm.computePdp2d('nonexistent', 'x2', 'value', 10)` 呼び出し
- Then: エラーがキャッチされるか空の結果が返る（クラッシュしない）

---

## 単体テスト要件

### テストケース1: 正常系 🔵

**Given**: 有効なStudyが読み込まれている状態
**When**: `computePdp2d('x1', 'x2', 'value', 10)` を呼び出す
**Then**: `grid1.length === 10, grid2.length === 10, heatmap.length === 100` の `Pdp2dResult` が返る

### テストケース2: エラー系 🟡

**Given**: 存在しないパラメータ名
**When**: computePdp2d呼び出し
**Then**: 例外がスローされるかnullが返る（wasmクラッシュなし）

---

## 信頼性レベルサマリー

- 🔵 青信号: 4件 (80%)
- 🟡 黄信号: 1件 (20%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質
