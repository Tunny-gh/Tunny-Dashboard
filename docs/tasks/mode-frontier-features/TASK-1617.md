# TASK-1617: lib.rs wasm_compute_topsis wasm_bindgenエクスポート追加 ✅ **完了** (DIRECT実装完了 - wasm-packビルド成功・computeTopsisエクスポート確認)

**タスクID**: TASK-1617
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - WASM TOPSIS実装（Rust）
**信頼性レベル**: 🔵 *architecture.md・既存lib.rsパターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/mode-frontier-features/architecture.md)
- **WASM API仕様**: [📐 wasm-api.md](../../design/tunny-dashboard/wasm-api.md)

## タスク概要

`rust_core/src/lib.rs` に `wasm_compute_topsis` 関数を追加し、wasm-bindgen経由でJavaScript側に公開する。`serde_wasm_bindgen::Serializer::json_compatible()` を使用したシリアライズを行う（既存関数と同パターン）。

## 依存タスク

- **前提タスク**: [TASK-1616: topsis.rsテスト](TASK-1616.md)
- **後続タスク**: [TASK-1618: tunny_core.d.ts更新](TASK-1618.md)

## 完了条件

- [x] `lib.rs` に `wasm_compute_topsis` 関数が追加されている
- [x] `wasm-pack build --target web` が成功する
- [x] 生成された `tunny_core.d.ts` に `computeTopsis` が含まれる

---

## 実装詳細

### 1. lib.rs への関数追加 🔵

**信頼性**: 🔵 *既存lib.rsの`wasm_compute_hv_history`・`wasm_run_pca`パターンより*

**実装ファイル**: `rust_core/src/lib.rs`

```rust
#[wasm_bindgen(js_name = "computeTopsis")]
pub fn wasm_compute_topsis(
    values_js: js_sys::Float64Array,
    n_trials: u32,
    n_objectives: u32,
    weights_js: js_sys::Float64Array,
    is_minimize_js: js_sys::Array,
) -> Result<JsValue, JsValue> {
    let values: Vec<f64> = values_js.to_vec();
    let weights: Vec<f64> = weights_js.to_vec();
    let is_minimize: Vec<bool> = is_minimize_js
        .iter()
        .map(|v| v.as_bool().unwrap_or(true))
        .collect();

    topsis::compute_topsis(
        &values,
        n_trials as usize,
        n_objectives as usize,
        &weights,
        &is_minimize,
    )
    .map(|result| {
        let serializer = serde_wasm_bindgen::Serializer::json_compatible();
        result.serialize(&serializer).unwrap()
    })
    .map_err(|e| JsValue::from_str(&e))
}
```

### 2. wasm-pack ビルド確認 🔵

**信頼性**: 🔵 *既存wasm-pack手順（visualization-enablement/architecture.md）より*

```bash
cd rust_core
wasm-pack build --target web --out-dir ../frontend/src/wasm/pkg
```

---

## 実装手順（DIRECT）

1. `/tsumiki:direct-setup` - lib.rs追加・wasm-packビルド
2. `/tsumiki:direct-verify` - ビルド成功・エクスポート確認

---

## 信頼性レベルサマリー

- **総項目数**: 2項目
- 🔵 **青信号**: 2項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
