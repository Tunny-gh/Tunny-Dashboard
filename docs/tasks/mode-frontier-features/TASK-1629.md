# TASK-1629: `FreeLayoutCanvas.tsx` case 'surface3d'追加・ChartCatalog登録 ✅ **完了** (実装完了・全テスト通過 442/442)

**タスクID**: TASK-1629
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 5 - AnalysisStore拡張・SurfacePlot3D
**信頼性レベル**: 🔵 *architecture.md UI統合設計・TASK-1623パターンより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/mode-frontier-features/architecture.md)
- **型定義設計**: [📝 interfaces.ts](../../design/mode-frontier-features/interfaces.ts)

## タスク概要

TASK-1623（topsis-ranking統合）と同様のパターンで、`FreeLayoutCanvas.tsx` に `case 'surface3d'` を追加し、`ChartCatalogPanel` に `surface3d` エントリを登録する。

具体的には:
1. `frontend/src/types/index.ts` の `ChartId` union型に `'surface3d'` を追加（TASK-1620で実施済みの場合は確認のみ）
2. `frontend/src/components/FreeLayoutCanvas.tsx` の `ChartContent` switch文に `case 'surface3d'` を追加
3. `frontend/src/components/ChartCatalogPanel.tsx`（または相当ファイル）に `surface3d` エントリを追加

## 依存タスク

- **前提タスク**: [TASK-1628: SurfacePlot3D.tsx新規作成](TASK-1628.md), [TASK-1623: FreeLayoutCanvas topsis-ranking追加](TASK-1623.md)
- **後続タスク**: なし（最終タスク）

## 完了条件

- [x] `ChartId` union型に `'surface3d'` が含まれている
- [x] `FreeLayoutCanvas.tsx` の `ChartContent` に `if (chartId === 'surface3d') return <SurfacePlot3D />` が追加されている
- [x] `ChartCatalogPanel` に `surface3d` エントリが追加されている
- [x] TypeScriptコンパイルエラーが発生しない
- [x] `npm test` でテストが通る (442/442)

---

## 実装詳細

### 1. ChartId確認 🔵

**信頼性**: 🔵 *TASK-1620で実施済みのChartId拡張の確認*

`frontend/src/types/index.ts` の `ChartId` に `'surface3d'` が含まれていることを確認。TASK-1620で追加されていない場合は本タスクで追加:

```typescript
export type ChartId =
  | 'pareto-front'
  | 'parallel-coords'
  // ... 既存値 ...
  | 'topsis-ranking'
  | 'surface3d'   // ← 追加
```

### 2. FreeLayoutCanvas.tsx への case 追加 🔵

**信頼性**: 🔵 *architecture.md FreeLayoutCanvas統合・TASK-1623パターンより*

**実装ファイル**: `frontend/src/components/FreeLayoutCanvas.tsx`

```typescript
import { SurfacePlot3D } from './charts/SurfacePlot3D'

// ChartContent switch文に追加
case 'surface3d':
  return <SurfacePlot3D />
```

### 3. ChartCatalogPanel への surface3d エントリ追加 🔵

**信頼性**: 🔵 *chart-catalog-requirementsパターン・TASK-1623 topsis-rankingエントリ追加パターンより*

**実装ファイル**: `frontend/src/components/ChartCatalogPanel.tsx`（またはchart catalog定義ファイル）

```typescript
{
  id: 'surface3d' as ChartId,
  title: '3D応答曲面',
  description: 'サロゲートモデルによる3次元応答曲面プロット（Ridge回帰）',
  icon: '📊',  // または適切なアイコン
  category: 'surrogate',  // カテゴリが存在する場合
},
```

### 4. エンドツーエンドの動作確認 🔵

**信頼性**: 🔵 *完了条件・npm run buildより*

- `npm run build` でビルドエラーがないことを確認
- ChartCatalogから `surface3d` を選択するとSurfacePlot3Dコンポーネントが描画されることを確認

---

## 単体テスト要件

### テストケース1: case 'surface3d' レンダリング 🔵

**Given**: FreeLayoutCanvasにchartId='surface3d'のチャートが追加されている
**When**: コンポーネントをレンダリング
**Then**: `SurfacePlot3D` コンポーネントが表示される

### テストケース2: ChartCatalogへのエントリ確認 🔵

**Given**: ChartCatalogPanelをレンダリング
**When**: カタログ一覧を確認
**Then**: '3D応答曲面' エントリが表示される

---

## 信頼性レベルサマリー

- 🔵 青信号: 6件 (100%)
- 🟡 黄信号: 0件 (0%)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質
