# TASK-0001: theme.rs 新規作成（カラーパレット定数・Visuals構築）

**タスクID**: TASK-0001
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 1 - UIカラーテーマ実装
**信頼性レベル**: 🔵 *設計文書 architecture.md・interfaces.rs より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/ui-color-theming/architecture.md)
- **型定義**: [📝 interfaces.rs](../../design/ui-color-theming/interfaces.rs)
- **データフロー**: [🔄 dataflow.md](../../design/ui-color-theming/dataflow.md)

## タスク概要

`egui-app/src/theme.rs` を新規作成し、Tunny Dashboardのカラーパレット定数と
ライトテーマ用 `egui::Visuals` を返す `tunny_light_visuals()` 関数を実装する。
このファイルが他の全テーマ適用タスク（TASK-0002, TASK-0003）の基盤となる。

## 依存タスク

- **前提タスク**: なし（初手タスク）
- **後続タスク**: [TASK-0002: グローバルテーマ適用](TASK-0002.md), [TASK-0003: grid_canvas.rs カラー定数置き換え](TASK-0003.md)

## 完了条件

- [ ] `egui-app/src/theme.rs` が新規作成されている
- [ ] `TOOLBAR_BG`, `TOOLBAR_TEXT`, `PANEL_BG`, `CENTRAL_BG`, `ACCENT_BLUE` 等の全定数が定義されている
- [ ] `tunny_light_visuals() -> egui::Visuals` 関数が実装されている
- [ ] `cargo build` が成功する（コンパイルエラーなし）
- [ ] `main.rs` または `lib.rs` に `mod theme;` の宣言が追加されている

---

## 実装詳細

### 1. theme.rs ファイル作成 🔵

**信頼性**: 🔵 *設計文書 architecture.md カラーパレット表・interfaces.rs より*

**実装ファイル**: `egui-app/src/theme.rs`

設計文書 `docs/design/ui-color-theming/interfaces.rs` の内容をそのまま実装する。

```rust
use egui::{Color32, Stroke, Visuals};

// ツールバー背景色 — ダークネイビー (#1a2332)
pub const TOOLBAR_BG: Color32 = Color32::from_rgb(26, 35, 50);

// ツールバーテキスト色 — 明るいグレー (#dce6f5)
pub const TOOLBAR_TEXT: Color32 = Color32::from_rgb(220, 230, 245);

// サイドパネル背景色 — ライトグレー (#f5f7fa)
pub const PANEL_BG: Color32 = Color32::from_rgb(245, 247, 250);

// メインキャンバス背景色 — 白
pub const CENTRAL_BG: Color32 = Color32::WHITE;

// アクセントブルー (#2563eb)
pub const ACCENT_BLUE: Color32 = Color32::from_rgb(37, 99, 235);

// アクセントブルー（ホバー） (#1d4ed8)
pub const ACCENT_BLUE_HOVER: Color32 = Color32::from_rgb(29, 78, 216);

// アクセントブルー（選択状態、淡い） (#dbeafe)
pub const ACCENT_BLUE_MUTED: Color32 = Color32::from_rgb(219, 234, 254);

// 境界線色 (#cbd5e1)
pub const BORDER_COLOR: Color32 = Color32::from_rgb(203, 213, 225);

// メインテキスト (#1e293b)
pub const TEXT_PRIMARY: Color32 = Color32::from_rgb(30, 41, 59);

// サブテキスト (#64748b)
pub const TEXT_SECONDARY: Color32 = Color32::from_rgb(100, 116, 139);

// チャートセルツールバー背景 (#f8fafc)
pub const CELL_TOOLBAR_BG: Color32 = Color32::from_rgb(248, 250, 252);

// ウィジェット背景（非アクティブ）
pub const WIDGET_BG: Color32 = Color32::WHITE;

// ウィジェット背景（ホバー） (#f1f5f9)
pub const WIDGET_BG_HOVER: Color32 = Color32::from_rgb(241, 245, 249);

/// Tunny Dashboard のライトテーマ Visuals を返す
pub fn tunny_light_visuals() -> Visuals {
    let mut v = Visuals::light();

    v.panel_fill = PANEL_BG;
    v.window_fill = CENTRAL_BG;
    v.window_stroke = Stroke::new(1.0, BORDER_COLOR);
    v.override_text_color = Some(TEXT_PRIMARY);
    v.extreme_bg_color = Color32::WHITE;

    v.widgets.active.bg_fill = ACCENT_BLUE;
    v.widgets.active.fg_stroke = Stroke::new(1.5, Color32::WHITE);
    v.widgets.active.bg_stroke = Stroke::new(1.0, ACCENT_BLUE);

    v.widgets.hovered.bg_fill = WIDGET_BG_HOVER;
    v.widgets.hovered.bg_stroke = Stroke::new(1.0, ACCENT_BLUE);

    v.widgets.inactive.bg_fill = WIDGET_BG;
    v.widgets.inactive.bg_stroke = Stroke::new(1.0, BORDER_COLOR);

    v.widgets.noninteractive.bg_fill = PANEL_BG;
    v.widgets.noninteractive.bg_stroke = Stroke::new(1.0, BORDER_COLOR);

    v.selection.bg_fill = ACCENT_BLUE_MUTED;
    v.selection.stroke = Stroke::new(1.0, ACCENT_BLUE);

    v
}
```

### 2. main.rs への mod 宣言追加 🔵

**信頼性**: 🔵 *Rust モジュール宣言の慣例より*

**実装ファイル**: `egui-app/src/main.rs`

```rust
mod app;
mod io;
mod render;
mod state;
mod theme;  // ← 追加
mod ui;
```

---

## 実装手順（DIRECT）

1. `/tsumiki:direct-setup` で実装
2. `/tsumiki:direct-verify` で動作確認

具体的手順:
1. `egui-app/src/theme.rs` を新規作成（上記コード）
2. `egui-app/src/main.rs` に `mod theme;` を追加
3. `cargo build -p egui-app` でコンパイル確認
4. ビルドエラーがあれば修正

---

## 注意事項

- `egui::Color32::from_rgb()` は `const fn` なので `const` 宣言で使用可能
- `egui::Visuals::light()` はベースとして使い、必要フィールドのみ上書きする
- `ACCENT_BLUE_HOVER` は現時点で直接参照箇所がないが、将来拡張のために定義しておく

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |

- 🔵 **青信号**: 2項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
