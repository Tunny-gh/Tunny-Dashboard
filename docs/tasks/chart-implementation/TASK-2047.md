# TASK-2047: WidgetStates 拡張 + grid_canvas.rs の show_chart() 修正

**タスクID**: TASK-2047
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤整備
**信頼性レベル**: 🔵 *architecture.md・既存コード調査より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/chart-implementation/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/chart-implementation/dataflow.md)

## タスク概要

`egui-app/src/ui/widget_states.rs` に4つの新ウィジェットフィールドを追加し、`egui-app/src/ui/grid_canvas.rs` の `show_chart()` で "not yet implemented" ラベルを実際の `show()` 呼び出しに置換する。この時点では各 `show()` はスタブ（パニックまたは "coming soon" ラベル）でよい。

## 依存タスク

- **前提タスク**: [TASK-2046: Cargo.toml 修正](TASK-2046.md)
- **後続タスク**: [TASK-2048: SensitivityHeatmap 実装](TASK-2048.md), [TASK-2049: ScatterMatrix 実装](TASK-2049.md), [TASK-2050: ParallelCoordinates 実装](TASK-2050.md), [TASK-2051: ClusterScatter 実装](TASK-2051.md)

## 完了条件

- [x] `widget_states.rs` に `parallel_coords`, `scatter_matrix`, `sensitivity_heatmap`, `cluster_scatter` の4フィールドが追加されている
- [x] `WidgetStates` の `Default` 実装が更新されている
- [x] `grid_canvas.rs` の `show_chart()` で4チャートが `show()` を呼び出している（スタブ可）
- [x] `cargo test -p egui-app` がすべて通る

---

## 実装詳細

### WidgetStates への4フィールド追加 🔵

**信頼性**: 🔵 *architecture.md「WidgetStates 拡張」より*

`egui-app/src/ui/widget_states.rs` を修正:

```rust
use crate::ui::widgets::parallel_coords::ParallelCoordsChart;
use crate::ui::widgets::scatter_matrix::ScatterMatrix;
use crate::ui::widgets::sensitivity_heatmap::SensitivityHeatmap;
use crate::ui::widgets::cluster_scatter::ClusterScatter;

pub struct WidgetStates {
    // 既存フィールド...
    pub parallel_coords: ParallelCoordsChart,
    pub scatter_matrix: ScatterMatrix,
    pub sensitivity_heatmap: SensitivityHeatmap,
    pub cluster_scatter: ClusterScatter,
}

impl Default for WidgetStates {
    fn default() -> Self {
        Self {
            // 既存フィールド...
            parallel_coords: ParallelCoordsChart::default(),
            scatter_matrix: ScatterMatrix::default(),
            sensitivity_heatmap: SensitivityHeatmap::default(),
            cluster_scatter: ClusterScatter::default(),
        }
    }
}
```

**実装ファイル**: `egui-app/src/ui/widget_states.rs`

### grid_canvas.rs の show_chart() 修正 🔵

**信頼性**: 🔵 *architecture.md「grid_canvas.rs の修正箇所」より*

`egui-app/src/ui/grid_canvas.rs` の `show_chart()` 内で各チャートを接続:

```rust
ChartId::ParallelCoordinates => {
    widgets.parallel_coords.show(ui, &trial_rows, &param_names, &obj_names);
}
ChartId::ScatterMatrix => {
    widgets.scatter_matrix.show(ui, &trial_rows, &param_names, &obj_names);
}
ChartId::SensitivityHeatmap => {
    widgets.sensitivity_heatmap.show(ui, sensitivity.as_ref());
}
ChartId::ClusterScatter => {
    widgets.cluster_scatter.show(ui, &trial_rows, app_state.cluster_result.as_ref(), &param_names);
}
```

**実装ファイル**: `egui-app/src/ui/grid_canvas.rs`

---

## 単体テスト要件

### WidgetStates デフォルト初期化テスト 🔵

**信頼性**: 🔵 *既存 widget_states テストパターンより*

**Given**: WidgetStates::default() を呼び出す
**When**: コンパイル・初期化
**Then**: すべてのフィールドが初期化される（パニックしない）

### コンパイル通過確認 🔵

**信頼性**: 🔵 *標準確認項目*

`cargo test -p egui-app` でコンパイルエラーがないこと。

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2047`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
