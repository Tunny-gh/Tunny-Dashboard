# TASK-2046: Cargo.toml に linfa 系クレート追加・ビルド確認

**タスクID**: TASK-2046
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 1 - 基盤整備
**信頼性レベル**: 🔵 *ユーザヒアリング・architecture.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/chart-implementation/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/chart-implementation/dataflow.md)

## タスク概要

`egui-app/Cargo.toml` に ClusterScatter の PCA 計算に必要な linfa 系クレートを追加し、`cargo build` でコンパイルエラーがないことを確認する。

## 依存タスク

- **前提タスク**: なし
- **後続タスク**: [TASK-2047: WidgetStates 拡張 + grid_canvas.rs 修正](TASK-2047.md), [TASK-2051: ClusterScatter::show() 実装](TASK-2051.md)

## 完了条件

- [x] `egui-app/Cargo.toml` に `linfa = "0.7"`, `linfa-clustering = "0.7"`, `linfa-reduction = "0.7"`, `ndarray = "0.15"` が追加されている
- [x] `cargo build -p egui-app` がエラーなく通る
- [x] 追加クレートのバージョン互換性が確認されている

---

## 主要実装項目

### Cargo.toml への依存追加 🔵

**信頼性**: 🔵 *architecture.md「追加依存クレート」より*

`egui-app/Cargo.toml` の `[dependencies]` セクションに以下を追加:

```toml
linfa = "0.7"
linfa-clustering = "0.7"
linfa-reduction = "0.7"
ndarray = "0.15"
```

**実装ファイル**: `egui-app/Cargo.toml`

### ビルド確認 🔵

**信頼性**: 🔵 *標準的な DIRECT タスクパターンより*

```bash
cd egui-app && rtk cargo build
```

エラーが出た場合はバージョンを調整する（linfa 0.7 + ndarray 0.15 の組み合わせが正しいか確認）。

---

## 基本的なテスト要件

### ビルド成功確認 🔵

**信頼性**: 🔵 *DIRECT タスク標準確認項目より*

- `cargo build -p egui-app` がゼロエラーで完了すること

---

## 実装手順

1. `/tsumiki:direct-setup` - Cargo.toml 修正
2. `/tsumiki:direct-verify` - cargo build で確認

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
