# TASK-2051: ClusterScatter::show() + PCA キャッシュ実装

**タスクID**: TASK-2051
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - チャート実装
**信頼性レベル**: 🟡 *architecture.md・dataflow.md・ユーザヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/chart-implementation/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/chart-implementation/dataflow.md)

## タスク概要

`egui-app/src/ui/widgets/cluster_scatter.rs` に `show()` メソッドを実装する。linfa + ndarray を使ってパラメータ値を PCA 2D投影し、`egui_plot::Plot` でクラスタ別に色分けした散布図を描画する。PCA 結果はウィジェット内部でキャッシュし、データ変化時のみ再計算する。

## 依存タスク

- **前提タスク**: [TASK-2046: Cargo.toml 修正](TASK-2046.md), [TASK-2047: WidgetStates 拡張](TASK-2047.md)
- **後続タスク**: [TASK-2052: 統合確認](TASK-2052.md)

## 完了条件

- [x] `ClusterScatter::show(ui, trial_rows, cluster_result, param_names)` が実装されている
- [x] `cluster_result` が `None` のとき "No cluster data." ラベルを表示する
- [x] `trial_rows` のパラメータ値を PCA 2D投影する
- [x] PCA 投影結果をキャッシュする（`cached_pca` フィールド追加）
- [x] trial 数または n_clusters が変化したときキャッシュを無効化する
- [x] `egui_plot::Plot` でクラスタ別に色分けした散布図を描画する
- [x] `cargo test -p egui-app` がすべて通る

---

## 実装詳細

### ClusterScatter フィールド追加 🟡

**信頼性**: 🟡 *dataflow.md「ClusterScatter のキャッシュ管理」より*

`egui-app/src/ui/widgets/cluster_scatter.rs` の `ClusterScatter` 構造体にキャッシュフィールドを追加:

```rust
pub struct ClusterScatter {
    // 既存フィールド...
    cached_pca: Option<Vec<[f32; 2]>>,
    cache_key: (usize, usize),  // (trial_count, n_clusters)
}
```

### PCA 計算ロジック 🟡

**信頼性**: 🟡 *ユーザヒアリング「外部クレート使用」+architecture.md より*

```rust
use ndarray::Array2;
use linfa::traits::Fit;
use linfa_reduction::Pca;

fn compute_pca(trial_rows: &[TrialRow], param_names: &[String]) -> Vec<[f32; 2]> {
    let n = trial_rows.len();
    let p = param_names.len();
    let mut data = Array2::<f64>::zeros((n, p));
    for (i, trial) in trial_rows.iter().enumerate() {
        for (j, name) in param_names.iter().enumerate() {
            if let Some(&v) = trial.param_values.get(name.as_str()) {
                data[[i, j]] = v;
            }
        }
    }
    let dataset = linfa::Dataset::from(data);
    let pca = Pca::params(2).fit(&dataset).unwrap_or_default();
    let projected = pca.predict(&dataset);
    projected.targets().outer_iter()
        .map(|row| [row[0] as f32, row[1] as f32])
        .collect()
}
```

**実装ファイル**: `egui-app/src/ui/widgets/cluster_scatter.rs`

### show() メソッド実装 🔵

**信頼性**: 🔵 *dataflow.md「ClusterScatter データフロー」より*

```rust
use crate::state::app_state::{ClusterResult, TrialRow};

impl ClusterScatter {
    pub fn show(
        &mut self,
        ui: &mut egui::Ui,
        trial_rows: &[TrialRow],
        cluster_result: Option<&ClusterResult>,
        param_names: &[String],
    ) {
        let Some(cr) = cluster_result else {
            ui.centered_and_justified(|ui| {
                ui.label(egui::RichText::new("No cluster data.").weak());
            });
            return;
        };
        if trial_rows.is_empty() {
            ui.label("No trial data.");
            return;
        }

        // キャッシュ確認・更新
        let new_key = (trial_rows.len(), cr.n_clusters);
        if self.cached_pca.is_none() || self.cache_key != new_key {
            self.cached_pca = Some(compute_pca(trial_rows, param_names));
            self.cache_key = new_key;
        }
        let pca_points = self.cached_pca.as_ref().unwrap();

        // クラスタ色テーブル
        let cluster_colors = [
            egui::Color32::from_rgb(230, 80, 80),
            egui::Color32::from_rgb(80, 150, 230),
            egui::Color32::from_rgb(80, 200, 120),
            egui::Color32::from_rgb(230, 180, 60),
            egui::Color32::from_rgb(180, 80, 200),
        ];

        // クラスタ別に Points を集約
        let mut cluster_points: std::collections::HashMap<i32, Vec<[f64; 2]>> = std::collections::HashMap::new();
        for (i, &[x, y]) in pca_points.iter().enumerate() {
            let label = cr.labels.get(i).copied().unwrap_or(0);
            cluster_points.entry(label).or_default().push([x as f64, y as f64]);
        }

        egui_plot::Plot::new("cluster_scatter")
            .show(ui, |plot_ui| {
                for (label, pts) in &cluster_points {
                    let color = cluster_colors[(*label as usize) % cluster_colors.len()];
                    let points = egui_plot::Points::new(
                        pts.iter().map(|&[x, y]| [x, y]).collect::<Vec<_>>()
                    )
                    .color(color)
                    .radius(3.0)
                    .name(format!("Cluster {}", label));
                    plot_ui.points(points);
                }
            });
    }
}
```

---

## 単体テスト要件

### None クラスタ結果時のガイダンス表示 🔵

**Given**: cluster_result = None
**When**: show() を呼び出す
**Then**: "No cluster data." ラベルが表示される（パニックしない）

### キャッシュキーの更新検知 🟡

**信頼性**: 🟡 *dataflow.md のキャッシュ管理から妥当な推測*

**Given**: trial_rows.len() が変化した
**When**: show() を2回呼び出す
**Then**: 2回目に PCA が再計算される（cached_pca が更新される）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2051`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 注意事項

- `linfa_reduction::Pca` の API は linfa 0.7 の実際の API に合わせて調整が必要な場合がある。実装時に `cargo doc` で API を確認すること。
- PCA 計算に失敗した場合（パラメータが1種類しかない等）は graceful fallback を実装する

---

## 信頼性レベルサマリー

- **総項目数**: 7項目
- 🔵 **青信号**: 4項目 (57%)
- 🟡 **黄信号**: 3項目 (43%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質（外部クレート API の確認が必要）
