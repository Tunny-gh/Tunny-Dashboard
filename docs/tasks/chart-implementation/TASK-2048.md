# TASK-2048: SensitivityHeatmap::show() 実装

**タスクID**: TASK-2048
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - チャート実装
**信頼性レベル**: 🔵 *architecture.md・dataflow.md・既存コード調査より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **設計文書**: [📐 architecture.md](../../design/chart-implementation/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/chart-implementation/dataflow.md)

## タスク概要

`egui-app/src/ui/widgets/sensitivity_heatmap.rs` に `show()` メソッドを実装する。`SensitivityResult.spearman` 行列を色付きグリッドで表示し、各セルに値を数値テキストでオーバーレイする。既存の `diverging_colormap()` 関数を活用する。

## 依存タスク

- **前提タスク**: [TASK-2047: WidgetStates 拡張](TASK-2047.md)
- **後続タスク**: [TASK-2052: 統合確認](TASK-2052.md)

## 完了条件

- [x] `SensitivityHeatmap::show(ui, sensitivity)` が実装されている
- [x] `sensitivity` が `None` のとき "No sensitivity data." ラベルを表示する
- [x] `spearman` 行列の各セルが `diverging_colormap()` の色で塗られている
- [x] 行ヘッダ（param_names）・列ヘッダ（objective_names）が表示されている
- [x] 各セルに spearman 値が数値テキスト（小数点2桁）でオーバーレイされている
- [x] `cargo test -p egui-app` がすべて通る

---

## 実装詳細

### show() メソッド実装 🔵

**信頼性**: 🔵 *dataflow.md「SensitivityHeatmap データフロー」より*

`egui-app/src/ui/widgets/sensitivity_heatmap.rs` に追加:

```rust
use crate::state::app_state::SensitivityResult;

impl SensitivityHeatmap {
    pub fn show(&mut self, ui: &mut egui::Ui, sensitivity: Option<&SensitivityResult>) {
        let Some(sens) = sensitivity else {
            ui.centered_and_justified(|ui| {
                ui.label(egui::RichText::new("No sensitivity data.").weak());
            });
            return;
        };

        let n_params = sens.param_names.len();
        let n_objs = sens.objective_names.len();
        if n_params == 0 || n_objs == 0 {
            ui.label("No data.");
            return;
        }

        // ヘッダ行・列の幅を確保してグリッド描画
        let header_w = 80.0;
        let header_h = 20.0;
        let available = ui.available_rect_before_wrap();
        let cell_w = (available.width() - header_w) / n_objs as f32;
        let cell_h = (available.height() - header_h) / n_params as f32;

        let painter = ui.painter();

        // 列ヘッダ（目的関数名）
        for (j, obj_name) in sens.objective_names.iter().enumerate() {
            let x = available.min.x + header_w + j as f32 * cell_w;
            let rect = egui::Rect::from_min_size(
                egui::pos2(x, available.min.y),
                egui::vec2(cell_w, header_h),
            );
            painter.text(
                rect.center(),
                egui::Align2::CENTER_CENTER,
                obj_name,
                egui::FontId::proportional(10.0),
                ui.visuals().text_color(),
            );
        }

        // 行ヘッダ（パラメータ名）+ セルグリッド
        for (i, param_name) in sens.param_names.iter().enumerate() {
            let y = available.min.y + header_h + i as f32 * cell_h;

            // 行ヘッダ
            let row_header_rect = egui::Rect::from_min_size(
                egui::pos2(available.min.x, y),
                egui::vec2(header_w, cell_h),
            );
            painter.text(
                row_header_rect.center(),
                egui::Align2::CENTER_CENTER,
                param_name,
                egui::FontId::proportional(10.0),
                ui.visuals().text_color(),
            );

            // 各セル
            for (j, &val) in sens.spearman[i].iter().enumerate() {
                let x = available.min.x + header_w + j as f32 * cell_w;
                let cell_rect = egui::Rect::from_min_size(
                    egui::pos2(x, y),
                    egui::vec2(cell_w, cell_h),
                );
                let color = self.diverging_colormap(val as f32);
                painter.rect_filled(cell_rect, 0.0, color);
                painter.rect_stroke(cell_rect, 0.0, egui::Stroke::new(0.5, egui::Color32::GRAY));
                painter.text(
                    cell_rect.center(),
                    egui::Align2::CENTER_CENTER,
                    format!("{:.2}", val),
                    egui::FontId::proportional(9.0),
                    egui::Color32::BLACK,
                );
            }
        }

        ui.allocate_rect(available, egui::Sense::hover());
    }
}
```

**実装ファイル**: `egui-app/src/ui/widgets/sensitivity_heatmap.rs`

---

## 単体テスト要件

### None 入力時のガイダンス表示 🔵

**信頼性**: 🔵 *dataflow.md「None の場合は "No sensitivity data"」より*

**Given**: sensitivity = None
**When**: show() を呼び出す
**Then**: "No sensitivity data." ラベルが表示される（パニックしない）

### spearman 値が正のとき赤色系になる 🔵

**信頼性**: 🔵 *diverging_colormap の仕様（既実装）より*

**Given**: spearman[i][j] = 1.0
**When**: diverging_colormap(1.0) を呼び出す
**Then**: 赤色系の Color32 が返る

### spearman 値が負のとき青色系になる 🔵

**信頼性**: 🔵 *diverging_colormap の仕様（既実装）より*

**Given**: spearman[i][j] = -1.0
**When**: diverging_colormap(-1.0) を呼び出す
**Then**: 青色系の Color32 が返る

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2048`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
