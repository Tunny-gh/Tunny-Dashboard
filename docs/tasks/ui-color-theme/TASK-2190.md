# TASK-2190: theme/ ディレクトリ作成・theme.rs → theme/mod.rs 変換

**タスクID**: TASK-2190
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 1 - テーマモジュール基盤整備
**信頼性レベル**: 🔵 *要件定義REQ-003・実装ガイドより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/ui-color-theme/requirements.md) - REQ-001, REQ-003, REQ-011〜013
- **設計文書**: [📐 architecture.md](../../design/ui-color-theme/architecture.md)
- **実装ガイド**: [📝 implementation-guide.md](../../design/ui-color-theme/implementation-guide.md)

## タスク概要

`egui-app/src/theme/` ディレクトリを作成し、既存の `egui-app/src/theme.rs` の内容を `egui-app/src/theme/mod.rs` に移行する。あわせて `ERROR_COLOR` セマンティック定数を追加し、`pub mod colormap;` と `pub mod chart_colors;` のサブモジュール宣言を追加する。

## 依存タスク

- **前提タスク**: なし（最初のタスク）
- **後続タスク**: [TASK-2191: render/colormap.rs → theme/colormap.rs 移動](TASK-2191.md)

## 完了条件

- [ ] `egui-app/src/theme/` ディレクトリが存在する
- [ ] `egui-app/src/theme/mod.rs` が存在し、旧 `theme.rs` の全定数を含む
- [ ] `egui-app/src/theme/mod.rs` に `ERROR_COLOR` 定数が追加されている
- [ ] `egui-app/src/theme/mod.rs` に `pub mod colormap;` と `pub mod chart_colors;` が追加されている
- [ ] 旧 `egui-app/src/theme.rs` が削除されている
- [ ] `cargo check` が警告なしで通過する

---

## 実装詳細

### 1. theme/ ディレクトリ作成 🔵

**信頼性**: 🔵 *REQ-001・アーキテクチャ設計より*

`egui-app/src/theme/` ディレクトリを作成する。

### 2. theme/mod.rs 作成 🔵

**信頼性**: 🔵 *REQ-003・REQ-011〜013・実装ガイドより*

**実装ファイル**: `egui-app/src/theme/mod.rs`

以下の内容で作成する（旧 `theme.rs` の全内容 + ERROR_COLOR + サブモジュール宣言）:

```rust
use egui::{Color32, Stroke, Visuals};

pub const TOOLBAR_BG: Color32 = Color32::from_rgb(26, 35, 50);
pub const TOOLBAR_TEXT: Color32 = Color32::from_rgb(220, 230, 245);
pub const PANEL_BG: Color32 = Color32::from_rgb(225, 233, 248);
pub const CENTRAL_BG: Color32 = Color32::WHITE;
pub const ACCENT_BLUE: Color32 = Color32::from_rgb(37, 99, 235);
#[allow(dead_code)]
pub const ACCENT_BLUE_HOVER: Color32 = Color32::from_rgb(29, 78, 216);
pub const ACCENT_BLUE_MUTED: Color32 = Color32::from_rgb(219, 234, 254);
pub const BORDER_COLOR: Color32 = Color32::from_rgb(203, 213, 225);
pub const TEXT_PRIMARY: Color32 = Color32::from_rgb(30, 41, 59);
#[allow(dead_code)]
pub const TEXT_SECONDARY: Color32 = Color32::from_rgb(100, 116, 139);
pub const CELL_TOOLBAR_BG: Color32 = Color32::from_rgb(232, 239, 251);
pub const WIDGET_BG: Color32 = Color32::from_rgb(235, 241, 252);
pub const WIDGET_BG_HOVER: Color32 = Color32::from_rgb(220, 230, 247);
pub const TOOLBAR_BTN_HOVER: Color32 = Color32::from_rgb(55, 78, 120);
pub const TOOLBAR_BTN_ACTIVE: Color32 = Color32::from_rgb(37, 99, 235);
pub const TOOLBAR_INPUT_BG: Color32 = Color32::from_rgb(45, 62, 90);
pub const TOOLBAR_INPUT_STROKE: Color32 = Color32::from_rgb(100, 130, 180);

/// エラー表示に使用するセマンティック色
pub const ERROR_COLOR: Color32 = Color32::from_rgb(220, 50, 50);

pub fn tunny_light_visuals() -> Visuals { ... }

pub mod colormap;
pub mod chart_colors;
```

### 3. 旧 theme.rs の削除 🔵

**信頼性**: 🔵 *REQ-003より*

`egui-app/src/theme.rs` を削除する。Rust は `theme.rs` と `theme/mod.rs` のどちらか一方しか存在できないため、削除が必須。

---

## 単体テスト要件

### TC-001: cargo check 通過確認 🔵

**信頼性**: 🔵 *NFR-001より*

**Given**: `theme/mod.rs` が作成され `theme.rs` が削除されている
**When**: `cargo check` を実行する
**Then**: エラーなし、かつ警告なしで完了する

### TC-002: 定数値の不変確認 🔵

**信頼性**: 🔵 *REQ-051（外観変更なし）より*

**Given**: `theme/mod.rs` が作成されている
**When**: `TOOLBAR_BG` 等の定数値を確認する
**Then**: すべての定数が旧 `theme.rs` と同一の RGB 値を持つ

---

## 実装手順

1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- Rust は `egui-app/src/theme.rs` と `egui-app/src/theme/mod.rs` が同時に存在するとコンパイルエラーになる。旧ファイルを必ず削除すること
- `pub mod colormap;` と `pub mod chart_colors;` を追加しておくことで、TASK-2191・TASK-2192 の作業が完了後にコンパイルが通るようになる（ただし追加時点ではファイルが存在しないためエラーになる可能性がある。実際の追加は TASK-2192 完了後でもよい）
- `tunny_light_visuals()` 関数の本体は旧 `theme.rs` と完全に同一にする

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 2 | 0 | 0 | 2 |

- 🔵 **青信号**: 5項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
