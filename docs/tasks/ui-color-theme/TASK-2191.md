# TASK-2191: render/colormap.rs → theme/colormap.rs 移動・全呼び出し元パス更新

**タスクID**: TASK-2191
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - テーマモジュール基盤整備
**信頼性レベル**: 🔵 *要件定義REQ-004・設計文書より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/ui-color-theme/requirements.md) - REQ-004, REQ-021〜025, REQ-052
- **設計文書**: [📐 architecture.md](../../design/ui-color-theme/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/ui-color-theme/dataflow.md)
- **実装ガイド**: [📝 implementation-guide.md](../../design/ui-color-theme/implementation-guide.md)

## タスク概要

`egui-app/src/render/colormap.rs` を `egui-app/src/theme/colormap.rs` に移動する。その後、`crate::render::colormap` を参照している8箇所のファイルをすべて `crate::theme::colormap` パスに更新し、`render/colormap.rs` を削除する。

## 依存タスク

- **前提タスク**: [TASK-2190: theme/ ディレクトリ作成・theme.rs → theme/mod.rs 変換](TASK-2190.md)
- **後続タスク**: [TASK-2192: theme/chart_colors.rs 新規作成](TASK-2192.md)

## 完了条件

- [ ] `egui-app/src/theme/colormap.rs` が存在する
- [ ] `egui-app/src/render/colormap.rs` が存在しない
- [ ] `egui-app/src/render/mod.rs` から `pub mod colormap;` が削除されている
- [ ] `state/types.rs` の `render::colormap` 参照が `theme::colormap` に更新されている
- [ ] `state/app_state.rs` の `render::colormap` 参照が `theme::colormap` に更新されている
- [ ] 5つのウィジェットファイルの `render::colormap` 参照が `theme::colormap` に更新されている
- [ ] `cargo test` が全テスト通過で完了する

---

## 実装詳細

### 1. theme/colormap.rs への移動 🔵

**信頼性**: 🔵 *REQ-004・ユーザヒアリングより*

**実装ファイル**: `egui-app/src/theme/colormap.rs`

`render/colormap.rs` の内容を `theme/colormap.rs` にコピーする。内容の変更は不要。

### 2. render/mod.rs の更新 🔵

**信頼性**: 🔵 *ユーザヒアリング（render/colormap.rs を削除）より*

**実装ファイル**: `egui-app/src/render/mod.rs`

`pub mod colormap;` の行を削除する。変更後:
```rust
pub mod gpu_buffer;
```

### 3. state/types.rs の更新 🔵

**信頼性**: 🔵 *データフロー図の更新マップより*

**実装ファイル**: `egui-app/src/state/types.rs`

以下の参照をすべて更新する:
- `crate::render::colormap::ColorMap` → `crate::theme::colormap::ColorMap`
- `crate::render::colormap::ColorMap::viridis()` → `crate::theme::colormap::ColorMap::viridis()`
- （plasma / jet / turbo / inferno / coolwarm / spectral / cividis / blue_yellow も同様）
- テスト内の `use crate::render::colormap::ColorMap;` → `use crate::theme::colormap::ColorMap;`

### 4. state/app_state.rs の更新 🔵

**信頼性**: 🔵 *データフロー図の更新マップより*

**実装ファイル**: `egui-app/src/state/app_state.rs`

`crate::render::colormap::compute_chart_colors` → `crate::theme::colormap::compute_chart_colors`

### 5. 5つのウィジェットファイルの更新 🔵

**信頼性**: 🔵 *データフロー図の更新マップより*

以下のファイルを更新する:

| ファイル | 変更内容 |
|---------|---------|
| `egui-app/src/ui/widgets/cluster_scatter.rs` | `use crate::render::colormap::ColorMap;` → `use crate::theme::colormap::ColorMap;` |
| `egui-app/src/ui/widgets/pareto_2d.rs` | `use crate::render::colormap::compute_point_alpha;` → `use crate::theme::colormap::compute_point_alpha;` |
| `egui-app/src/ui/widgets/pareto_3d.rs` | `use crate::render::colormap::compute_point_alpha;` → `use crate::theme::colormap::compute_point_alpha;` |
| `egui-app/src/ui/widgets/pdp_2d.rs` | `use crate::render::colormap::ColorMap;` → `use crate::theme::colormap::ColorMap;` |
| `egui-app/src/ui/widgets/mcdm_chart.rs` | `crate::render::colormap::compute_chart_colors` → `crate::theme::colormap::compute_chart_colors` |

### 6. render/colormap.rs の削除 🔵

**信頼性**: 🔵 *ユーザヒアリングより*

`egui-app/src/render/colormap.rs` を削除する。

---

## 単体テスト要件

### TC-001: 既存テスト全通過 🔵

**信頼性**: 🔵 *REQ-025・NFR-011より*

**Given**: `theme/colormap.rs` が存在し、全参照が更新されている
**When**: `cargo test` を実行する
**Then**: `render/colormap.rs` にあった全テスト（`interpolate_at_zero_returns_first_stop` 等）がすべて通過する

### TC-002: import パス更新確認 🔵

**信頼性**: 🔵 *REQ-052より*

**Given**: 全ファイルの更新が完了している
**When**: `grep -r "render::colormap" egui-app/src` を実行する
**Then**: 結果が 0 件（参照が残っていない）

---

## 統合テスト要件

### 統合 TC-001: cargo check 通過 🔵

**信頼性**: 🔵 *NFR-001より*

**内容**: 全更新後に `cargo check` が警告なしで完了する
**期待結果**: コンパイルエラーなし、警告なし

---

## 実装手順

1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- コピー前に `egui-app/src/theme/` ディレクトリが存在することを確認する（TASK-2190 の完了が前提）
- `theme/colormap.rs` 内のテストブロック（`#[cfg(test)]`）も含めてそのままコピーする
- テスト内に `use crate::render::colormap::ColorMap;` があれば削除（`theme/colormap.rs` 内では `use super::ColorMap;` または省略で参照できる）
- コピー完了後に `render/colormap.rs` を削除し、その後に `cargo test` で確認する

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 6 | 0 | 0 | 6 |
| 単体テスト | 2 | 0 | 0 | 2 |
| 統合テスト | 1 | 0 | 0 | 1 |

- 🔵 **青信号**: 9項目 (100%)

**品質評価**: ✅ 高品質
