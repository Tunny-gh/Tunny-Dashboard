# TASK-2196: UI層・State層色置換

**タスクID**: TASK-2196
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - ウィジェット色置換
**信頼性レベル**: 🔵 *要件定義REQ-041〜042・データフロー図より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/ui-color-theme/requirements.md) - REQ-013, REQ-041, REQ-042
- **設計文書**: [📐 architecture.md](../../design/ui-color-theme/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/ui-color-theme/dataflow.md)

## タスク概要

UIレイヤー（toolbar, layout, grid_canvas, bottom_panel, comparison_panel）および State レイヤー（state/app_state.rs）のインライン色リテラルを `crate::theme` の定数に置換する。

対象ファイル:
- `egui-app/src/ui/toolbar.rs`
- `egui-app/src/ui/layout.rs`
- `egui-app/src/ui/grid_canvas.rs`
- `egui-app/src/ui/bottom_panel.rs`
- `egui-app/src/ui/comparison_panel.rs`
- `egui-app/src/state/app_state.rs`

## 依存タスク

- **前提タスク**: [TASK-2192: theme/chart_colors.rs 新規作成](TASK-2192.md)
- **後続タスク**: [TASK-2197: ビルド・テスト品質確認](TASK-2197.md)

## 完了条件

- [ ] `toolbar.rs` のインライン `Color32::RED` が `ERROR_COLOR` に置換されている
- [ ] `layout.rs` のインライン色が `theme` 定数に整理されている（TRANSPARENT/WHITE は任意）
- [ ] `grid_canvas.rs` の選択ハイライト色が `chart_colors` 定数に置換されている
- [ ] `bottom_panel.rs` のリンク色が `COLOR_LINK` に置換されている
- [ ] `comparison_panel.rs` のフォールバック灰色が整理されている
- [ ] `state/app_state.rs` の `Color32::RED` が `theme::ERROR_COLOR` に置換されている
- [ ] `cargo check` が警告なしで通過する

---

## 実装詳細

### 1. toolbar.rs の更新 🔵

**信頼性**: 🔵 *データフロー図の置換マップより*

**実装ファイル**: `egui-app/src/ui/toolbar.rs`

置換内容:
- `egui::Color32::RED` (エラー表示 `colored_label`) → `crate::theme::ERROR_COLOR`
- `egui::Color32::TRANSPARENT` → そのまま（組み込み定数、任意）
- `egui::Color32::WHITE` → そのまま

### 2. layout.rs の更新 🟡

**信頼性**: 🟡 *データフロー図の置換マップから妥当な判断*

**実装ファイル**: `egui-app/src/ui/layout.rs`

置換内容:
- `egui::Color32::TRANSPARENT` → そのまま（任意）
- `egui::Color32::WHITE` → そのまま（任意）
- 既に `crate::theme::*` を参照しているため、新規置換は最小限

### 3. grid_canvas.rs の更新 🔵

**信頼性**: 🔵 *データフロー図の置換マップより*

**実装ファイル**: `egui-app/src/ui/grid_canvas.rs`

置換内容:
- `egui::Color32::WHITE` (セル背景) → そのまま（任意）
- `from_rgba_unmultiplied(37, 99, 235, 40)` → `crate::theme::chart_colors::COLOR_SELECTION_HIGHLIGHT`
- `from_rgba_unmultiplied(37, 99, 235, 80)` → `crate::theme::chart_colors::COLOR_CELL_HIGHLIGHT`

```rust
use crate::theme::chart_colors::{COLOR_SELECTION_HIGHLIGHT, COLOR_CELL_HIGHLIGHT};
```

### 4. bottom_panel.rs の更新 🔵

**信頼性**: 🔵 *データフロー図の置換マップより*

**実装ファイル**: `egui-app/src/ui/bottom_panel.rs`

置換内容:
- `from_rgb(80, 120, 180)` → `crate::theme::chart_colors::COLOR_LINK`

### 5. comparison_panel.rs の更新 🟡

**信頼性**: 🟡 *データフロー図の置換マップから妥当な判断*

**実装ファイル**: `egui-app/src/ui/comparison_panel.rs`

置換内容:
- `Color32::GRAY` (フォールバック灰色) → そのまま（組み込み定数、任意）

### 6. state/app_state.rs の更新 🔵

**信頼性**: 🔵 *データフロー図の置換マップより*

**実装ファイル**: `egui-app/src/state/app_state.rs`

置換内容:
- `egui::Color32::RED` (chart_colors・comparison_colors の初期値) → `crate::theme::ERROR_COLOR`

---

## 単体テスト要件

### TC-001: インラインリテラル削減確認 🔵

**信頼性**: 🔵 *受け入れ基準 TC-042-01より*

**Given**: 6ファイルの更新が完了している
**When**: `grep -n "Color32::from_rgb" egui-app/src/ui/toolbar.rs egui-app/src/ui/grid_canvas.rs egui-app/src/ui/bottom_panel.rs egui-app/src/state/app_state.rs` を実行する
**Then**: 0 件

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-2196` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `layout.rs` と `comparison_panel.rs` は既に `crate::theme::*` を使用しているため、変更は最小限
- `Color32::TRANSPARENT`・`Color32::WHITE`・`Color32::BLACK`・`Color32::GRAY` はREQ-043により theme 化は任意

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 4 | 2 | 0 | 6 |
| 単体テスト | 1 | 0 | 0 | 1 |

- 🔵 **青信号**: 5項目 (71%)
- 🟡 **黄信号**: 2項目 (29%)

**品質評価**: ✅ 高品質
